# Full-Parity Backlog — "Skip Nothing"

Date: 2026-06-17. Owner doc for **what remains to reach full 1:1 parity**. Authoritative companion to `Port-Fidelity-Remediation-Plan.md` §0 (which records that Phases A–C are COMPLETE and Phase D is structurally ~complete). This doc enumerates *every* remaining gap — large and small — because for parity **nothing may be skipped**.

Branch `feature/object-spine-bigbang` @ `b9c908e25` · build 0 · golden 221/221 byte-exact (0 fidelity bugs) · GameServer 500/500 · Commons 55/55 · Login 122/122 · Chat 31/31 · bootstrap 9/9 · `check_fidelity` 0/0 · full DB-backed boot validated. **Backlog A–G done; ALL content ported (incl instance 78/78); config framework faithful. 2026-06-18 cross-tree deep-discovery (§I): no large gameplay subsystem missing; one real functional gap = the GS↔LS↔CS bridge admin/ops inbound flows (I1); rest are accepted idiomatic-infra divergences (I2) + minors (I3). Only F (live-client) is user-gated.**

## Standing reality (so the backlog isn't misread)

- **De-slop + structural port is DONE.** 126→0 reworked duplicate packets, `GameServerPacket` dropped, object store unified to single `World._allObjects`, all reworked `*Summary`/`*Table`/WorldNpc/Kisk/Rift/Housing slop retired, `GameServerConnection` god-class gone, guardrail baseline empty (0/0). ~92% of Java types (2269/2456) have an exact-name C# file. Content scripts: quests 1035/1035 · AI 462/462 · zone 3/3 · console 35/35 · player 16/16 · admin 103/103 · **instance 78/78** — **content parity COMPLETE** (instance set finished 2026-06-18 with the final 7-handler pvparenas arena chain; 5 tier-1 bases + 36 leaves ported, incl. full abyss barracks/chamber cluster, all dredgion/crucible/basicpvp leaves, and the TrainingGrounds→ArenaOf* chain + ArenaOfGlory) (corrected 2026-06-17; the prior "instance 37" was a stale miscount — the Java instance dir has 78, not 37). **§H DONE — see below.**
- **A faithful Java TODO is parity, not a gap.** Of 277 `TODO`/`FIXME` markers in C# src, the large majority are verbatim copies of TODOs in the Java source (e.g. "most retail npcs lose 364 hate. TODO: find formula"). The doctrine says mirror them; reproducing them *is* 1:1. Only the **C#-specific** subset (config framework, "port the rest") are real gaps — see §C.
- **The only thing that cannot be done here is the live-client test (§F)** — it needs the user's Aion 4.8 client. Everything else in this backlog is workable now.

---

## A. Runtime-parity VALIDATION depth — the #1 driver (bounded, provable without a client)

Structural 1:1 ≠ proven runtime parity. Absent the live client, the Java-oracle golden suite is how we *prove* parity. It is at 196 cases, **0 fidelity bugs across every probe** — strong, but not exhaustive.

- **A1. Un-golden'd `SM_*` packets: ~103 of 240.** 157 packet fixtures cover ~137 distinct faithful packets. The remaining ~103 read *live objects* (Player legion/group/store/flight graph, live World/WorldMapInstance, Summon/Pet/Skill graphs) and need a reusable **integration harness** (uninitialized-object Player/World/connection seams — precedents exist: `PacketHarnessCreature`, scalar/`HarnessPlayer`, DataManager-holder seam, real-`Npc` seam, item/`ItemInfoBlob` seam, GameCrypt Write seam). Build the harness increment, then golden ~1–3 packets/tick, byte-exact vs the mvn oracle. **Largest single remaining provable-parity vein.**
- **A2. Formula golden expansion.** 30 formula fixtures; remaining pure `StatFunctions`/`StatCapUtil`/calc methods that read only args are still capturable. Thin but non-zero.
- **A3. Behavioral/runtime golden gaps** the unit harness can't reach (Rnd-based combat, time-dependent) — document as not-golden-able; they fall to §F (live client) for ultimate proof.

### A1 per-packet triage (computed 2026-06-17, the authoritative un-golden'd list)

Ground truth from disk: **240** faithful `SM_*.cs` in `Network/Aion/ServerPackets` + **6** LoginServer-tree `SM_*.cs` (`SM_BAN`/`SM_HDDBAN_CONTROL`/`SM_PREMIUM_CONTROL`/`SM_PTRANSFER_CONTROL`/`SM_CHANGE_ALLOWED_HDD_SERIAL`/`SM_ACCOUNT_TOLL_INFO` under `Network/LoginServer/ServerPackets`). Distinct names covered by a golden fixture (`parity-artifacts/golden/packets/*.json`) or a dedicated faithful golden/byte test (`SmFindGroupTests`, `SmLegionDominionRankTests`, `SmLegionHistoryTests`, the `Golden*FixtureTests` families) or a `writeImpl audited 1:1` breadcrumb. NOTE: the earlier reconciliation note guessing SM_GAME_TIME/SM_AUTO_GROUP already had dedicated golden tests was WRONG — they do NOT; only SM_FIND_GROUP/SM_LEGION_DOMINION_RANK/SM_LEGION_HISTORY do (correctly excluded below). **Un-validated = 0 — A1 PACKET VALIDATION TRULY COMPLETE (batch 24b, 2026-06-17): whole-tree set-difference (all `src/.../SM_*.cs` MINUS {golden-fixture names ∪ `audited 1:1`-breadcrumb names}) is EMPTY.**

**Batch 24b** (2026-06-17) — closed the final 9 the whole-tree set-difference still listed (the earlier batch-24 sweep was scoped to the Aion tree only and missed the 6 LoginServer packets + 3 dedicated-test-only graph packets): AUDITED 1:1 T2 breadcrumbs, 0 golden delta, 0 fidelity bugs. (1) the 3 graph packets `SM_FIND_GROUP`/`SM_LEGION_DOMINION_RANK`/`SM_LEGION_HISTORY` (already covered by dedicated `Sm*Tests` byte tests; writeImpl re-audited line-by-line — live Player/team/Legion graph reads; `instanceof`→`is`, `getName(true)`→`GetName(true)`, `currentTimeMillis()/1000`→`DateTimeOffset`, `subList`→`GetRange`, `ranking.indexOf`→`IndexOf`, `LegionHistoryAction.Type{LEGION,REWARD,WAREHOUSE}` is plain-sequential so `ordinal()`==`(int)type`; byte-identical). (2) 5 LoginServer-tree scalar/control packets `SM_ACCOUNT_TOLL_INFO` (writeC(0x09)+writeD+writeQ), `SM_BAN` (writeC(0x06)+writeC(type)+writeD+writeS+writeD+writeD), `SM_CHANGE_ALLOWED_HDD_SERIAL` (super 15→writeC(0x0F)+writeD+writeS; ctor reads Account.getId/getAllowedHddSerial→T2), `SM_HDDBAN_CONTROL` (writeC(14)+writeC(BanAction.getId: UNBAN=0/BAN=1==ordinal)+writeS+writeQ), `SM_PREMIUM_CONTROL` (writeC(11)+writeD(req.accountId)+writeD(req.requestId)+writeQ(cost)+writeC(NetworkConfig.GAMESERVER_ID config singleton→T2)); all byte-identical incl. opcode-as-first-payload-byte matching Java `LsServerPacket.write` (len placeholder + `put((byte)opcode)` + writeImpl + back-patch len == C# `WritePayload` opcode-first + `ServerPacketFrameCodec` 2-byte LE len prefix). (3) `SM_PTRANSFER_CONTROL` — **NOW FULLY PORTED 1:1 (2026-06-17), all 6 `*_INFORMATION` branches written verbatim.** The earlier "blocked on unported deps" note was STALE — re-audit found every dep already faithful in C#: `InventoryDAO.LoadItems(int,StorageType)` + `ItemStoneListDAO.Load(ICollection<Item>)` (both exist in `src/.../Dao`), `Player.GetEmotions()`/`GetMotions()`/`GetTitleList()` (present on `Player.Part2/Part4` partials, returning `EmotionList`/`MotionList`/`TitleList` with `GetEmotions()`/`GetMotions()`/`GetTitles()`), full `PlayerAppearance` (~55 getters) + `PlayerCommonData` + `Item` (all 29 getters across partials) + `ManaStone`/`ItemStone` (`GetItemId`/`GetSlot`) + `Emotion`/`Title` (`secondsUntilExpiration` via `((IExpirable)x).SecondsUntilExpiration()` default-interface-method cast) + `Motion` + `NpcFaction`+`NpcFactions` + `Macros.Macro(Id,Xml)` + `PetCommonData`+`PetList` + `PlayerSettings` + `PlayerSkillEntry`/`PlayerSkillList` (`GetAllSkills`/`IsStigmaSkill`) + `RecipeList.GetRecipeList()` + `QuestState`/`QuestStateList`. Faithful notes: Java `getTime()` (epoch millis) → `new DateTimeOffset(dt.ToUniversalTime()).ToUnixTimeMilliseconds()` on `pet.getBirthdayTimestamp()`/`qs.getLastCompleteTime()`/`qs.getNextRepeatTime()`; `getStatus().toString()`/`getState().toString()` → enum `.ToString()`; null-color → -1; `getName()` → `GetName(false)`. **Reachability:** sent only during cross-server character transfer (no golden fixture exists for this LS-tree packet); not golden'd but writeImpl is now line-for-line with Java. NO Player.cs change was needed (accessors pre-existed). Build 0, golden 221/221, suite 500/500. The original `Un-validated = 0` for the 240-Aion-tree scope was already correct (batch 24); 24b makes the **whole-tree** set-difference empty and adds belt-and-suspenders breadcrumbs to the 3 dedicated-test graph packets. Build 0, golden 221/221, suite green. A full re-derivation sweep (covered-name set = `parity-artifacts/golden/packets/*.json` ∪ every `SM_*` token in `tests/.../Golden*.cs`+`Sm*Tests.cs`, plus every `SM_*.cs` carrying an `audited 1:1` breadcrumb, diffed against the 240 scoped `find src -path '*ServerPackets/SM_*.cs'`) confirms **all 240 scoped SM_* packets are now either runtime-golden'd or 1:1 audit-breadcrumbed; 0 remain.** The earlier "16" was a stale header never decremented after batches 16–23 added the breadcrumbs. Build 0, golden 221/221. Batch 24 was verification-only: no source change, no new golden case, 0 fidelity bugs. Original batch-23 note** (batch 23 golden'd SM_LOGIN_QUEUE (T1: private no-arg ctor pins fixed 5/60/50, con never read) + SM_SYSTEM_MESSAGE (T1: (int,Object...) ctor -> GOLDEN_YELLOW(25)+null sender; wire-format path validated with scalar params, 28k-line msgId catalog is verified-path data not per-entry) + SM_CREATE_CHARACTER (T1 non-OK branch writeD(responseCode)+return; OK branch delegates to validated writePlayerInfo) — golden 218->221 cases, suite 481->484, 0 fidelity bugs; AND AUDITED 1:1 T2 breadcrumbs SM_CHARACTER_LIST (con.getAccount() live + per-char writePlayerInfo) + SM_L2AUTH_LOGIN_CHECK (writeD(ok) + hex-literal standardData byte-identical + DataManager.WORLD_MAPS_DATA live-holder iteration + writeS(accountName)); all byte-identical, 0 fidelity bugs. batch 22 AUDITED 1:1 T2 breadcrumbs, no golden delta — 6 builder/item-blob/live-graph/DataManager-enum-static-init readers: SM_CUSTOM_PACKET (admin //fsc builder, no plain ctor), SM_PRIVATE_STORE (live PrivateStore/seller Player/Inventory + getFullBlob), SM_UPDATE_PLAYER_APPEARANCE (writeEquippedItems live Item graph: equipmentSlot mask w/ TwoHanded SUB_HAND removal), SM_MAIL_SERVICE (con.getActivePlayer().getMailbox() + Letter/Item/ItemInfoBlob; letters.size()*-1, totalCount+unreadCount*0x10000, (int)(time/1000)), SM_BROKER_SERVICE (live BrokerItem/Item/blob + currentTimeMillis + PlayerService.getPlayerName singleton + settleTime; duplicate-id BrokerPacketType via reference-identity if-chain == Java identity switch; toDays==ms/86400000, settleTime/60000; all 8 writers byte-identical), SM_AUTO_GROUP (DataManager.AUTO_GROUP via AutoGroupType enum static-init; writeImpl scalars + name only, windowId switch case-groupings 0,7/1,3,8/2,4,5/6); all byte-identical field set/order/encoding vs Java, 0 fidelity bugs. batch 21 AUDITED 1:1 T2 breadcrumbs, no golden delta — 5 live-graph/singleton/con readers: SM_KISK_UPDATE (live Kisk graph — 8x writeD scalar getters; Kisk ctor needs NpcController/SpawnTemplate/kiskStatsTemplate, not bounded-constructible), SM_LOOT_ITEMLIST (con.getActivePlayer null-guard + live DropNpc/DropItem/Player graph + DataManager.ITEM_DATA per-drop isTradeable + isOnlyPossibleLooter loot-confirmation logic; (int) cast on count), SM_TRADELIST (ctor: live Player.getLegion + Npc + DataManager.GOODSLIST_DATA legion-level filter + LimitedItemTradeService singleton; TradeNpcType.index() explicit 1-5 == Java), SM_INSTANCE_SCORE (live InstanceScoreWriter graph + InstanceProgressionType.getId() explicit 1/2/3*1024*1024 + writeMe(buf) delegation), SM_ABYSS_ARTIFACT_INFO3 (Collection ctor DTO + int-loc ctor SiegeService singleton; writeImpl locationId*10+1 + ArtifactStatus.getValue() explicit 0-3); all byte-identical field set/order/encoding vs Java, 0 fidelity bugs. batch 20 golden'd SM_HOUSE_SCRIPTS (T1: PlayerScript record + fixed compressedBytes, fully deterministic, 4 cases byte-exact, golden 217->218 / suite 480->481, 0 fidelity bugs) + AUDITED 1:1 T2 breadcrumbs 6 live-graph/singleton readers: SM_HOUSE_RENDER + SM_HOUSE_UPDATE (AbstractHouseInfoPacket.writeCommonInfo: LegionService singleton + live House/registry/emblem graph; PartType.values()/getRooms() order+count match), SM_HOUSE_OBJECTS (live HouseObject graph), SM_SUMMON_PANEL + SM_SUMMON_UPDATE (live Summon/lifeStats/gameStats Stat2 graph; getMode().getId() explicit-value), SM_PET_EMOTE (live Pet/MoveController graph; PetEmote enum value==Java emoteId so (int)emote==getEmoteId()); all byte-identical field set/order/encoding, 0 fidelity bugs. batch 19 AUDITED 1:1 T2 breadcrumbs, no golden delta — 10 con/live-singleton/live-graph readers: SM_IN_GAME_SHOP_CATEGORY_LIST (InGameShopEn singleton), SM_IN_GAME_SHOP_ITEM (InGameShopEn singleton), SM_IN_GAME_SHOP_LIST (live Player.inGameShop + InGameShopEn), SM_HOUSE_OBJECT (con.getActivePlayer + live HouseObject graph), SM_HOUSE_REGISTRY (con.getActivePlayer + live HouseRegistry graph), SM_HOUSE_BIDS (con.getActivePlayer + HousingBidService/HousingService/AuctionEndTask singletons), SM_HOUSE_EDIT (con.getActivePlayer + live House registry), SM_OBJECT_USE_UPDATE (live HouseObject graph), SM_HOUSE_OWNER_INFO (live Player/House graph + HousingService + ServerTime.now time-based), SM_CASTSPELL_RESULT (live Skill/Effect/Creature graph + setLastCounterSkill side-effect); all confirmed byte-identical writeImpl field set/order/encoding vs Java (incl. enum .getId()/.getValue() explicit-value getters, Duration.between(now,nextPay).toDays() == (long)(nextPay-now).TotalDays toward-zero truncation, byte vs int comparison widening), 0 fidelity bugs. batch 18 AUDITED 1:1 T2 breadcrumbs, no golden delta — 8 con/live-singleton/time/live-graph readers: SM_ACCOUNT_PROPERTIES (con.getAccount), SM_KEY (con.enableCryptKey), SM_INSTANCE_INFO (con.getActivePlayer + currentTimeMillis), SM_SIEGE_LOCATION_INFO (con.getActivePlayer + live SiegeService/LegionService), SM_PLAYER_SPAWN (live World singleton), SM_DIE (live Player/WorldMapInstance/Kisk graph), SM_ITEM_COOLDOWN (currentTimeMillis), SM_RECIPE_COOLDOWN (live Player.getCraftCooldowns); all confirmed byte-identical writeImpl field set/order/encoding vs Java (incl. (int)(reuse-now)/1000 cast precedence), 0 fidelity bugs. batch 17 AUDITED 1:1 T2 breadcrumbs, no golden delta — 9 full live-Player/Legion/Group/Alliance graph readers: SM_LEGION_INFO, SM_LEGION_MEMBERLIST, SM_LEGION_ADD_MEMBER, SM_LEGION_UPDATE_MEMBER, SM_GM_SHOW_LEGION_INFO, SM_GROUP_MEMBER_INFO, SM_ALLIANCE_MEMBER_INFO, SM_PRIVATE_STORE_NAME, SM_CHAT_WINDOW; all confirmed byte-identical writeImpl field set/order/encoding vs Java (incl. SkillTargetSlot.ordinal()=Array.IndexOf(Enum.GetValues), PlayerClass.values().length=Enum.GetValues.Length, Legion.Announcement time/1000), 0 fidelity bugs. batch 16 AUDITED 1:1 T2 breadcrumbs, no golden delta — all 8 are audit-only con/live-singleton/time readers: SM_GAME_TIME, SM_TIME_CHECK, SM_VERSION_CHECK, SM_PRICES, SM_DIALOG_WINDOW, SM_FRIEND_UPDATE, SM_CHALLENGE_LIST, SM_PLAY_MOVIE; all confirmed byte-identical writeImpl field set/order/encoding vs Java, 0 fidelity bugs. batch 15 golden'd SM_GATHERABLE_INFO + SM_QUEST_COMPLETED_LIST; golden suite 215->217 test methods, +3 cases byte-exact, 0 fidelity bugs; batch 14 golden'd SM_NPC_ASSEMBLER + SM_GATHER_UPDATE; golden suite 213->215 test methods, +7 cases byte-exact, 0 fidelity bugs; batch 13 golden'd SM_TUNE_RESULT/SM_SKILL_LIST/SM_WAREHOUSE_ADD_ITEM/SM_INVENTORY_INFO). Batch 14 also AUDITED 1:1 (T2 breadcrumbs): SM_INFLUENCE_RATIO, SM_FORTRESS_STATUS, SM_LEGION_DOMINION_LOC_INFO. SM_QUEST_COMPLETED_LIST is now DONE (batch 15) via the QUEST_DATA DataManager-holder seam: `qs.canRepeat()` reads `DataManager.QUEST_DATA.getQuestById(questId)` per entry -> two NON-time-based quests (repeatCycle null -> isTimeBased() false, no clock) reflect-set with id+maxRepeatCount cover both canRepeat branches (255 -> true/writeC 0; completeCount>=max!=255 -> false/writeC 1). SM_FIRST_SHOW_DECOMPOSABLE/SM_SECONDARY_SHOW_DECOMPOSABLE ARE done (fixtures + C# cases exist; the §A "next batch" note below is stale).

Triage key: **T1** = TIER-1 runtime-golden reachable now (writeImpl is `con`-null-safe under `CaptureWriteImplPayload` and reads only scalars / simple DTOs / an existing-or-small-bounded seam); **T1-seam** = reachable via an existing heavy seam (item/ItemInfoBlob, real-Npc, DataManager-holder) — bounded but more work; **T2** = TIER-2 audit-only (writeImpl reads `con.getActivePlayer()`/`con.getAccount()`/`con.enableCryptKey()`, the Java static-final `World` singleton, a live `SiegeService`/`Influence`/`GameTimeService`/`LoginServer`/`GameServer` singleton, `System.currentTimeMillis`/JVM-uptime, or a full live Player/Legion/Group/Alliance graph the unit harness cannot bounded-build) — validate by line-by-line writeImpl audit + breadcrumb.

**T1 — pure scalar / simple-DTO, con-null-safe (golden these first):**
- `SM_CHARACTER_SELECT` — scalar type/messageType/wrongCount + `SecurityConfig.PASSKEY_WRONG_MAXCOUNT` const. **[DONE 2026-06-17 batch 10]**
- `SM_AFTER_SIEGE_LOCINFO_475` — pure const writeImpl (writeH 0 / writeC 0), default ctor. **[DONE batch 10]**
- `SM_NEARBY_QUESTS` — `Map<Integer,Integer>`; `-size&0xFFFF` + per-entry `questId|bit`. **[DONE batch 10]**
- `SM_MACRO_LIST` — playerObjectId + `List<Macros.Macro>` (public record) + clearList; `writeH(-size)`. **[DONE batch 10]**
- `SM_QUEST_COMPLETED_LIST` — updateMode + `List<QuestState>` (constructible ctor). **[DONE batch 10]**
- `SM_FIRST_SHOW_DECOMPOSABLE` / `SM_SECONDARY_SHOW_DECOMPOSABLE` — objectId + `Collection<ResultedItem>` (XML DTO; reflect-set itemId/minCount both sides). **[DONE — fixtures + C# cases exist (generator lines 901-918 + GoldenPacketFixtureTests).]**
- `SM_QUEST_COMPLETED_LIST` — updateMode + `List<QuestState>`. **[DONE 2026-06-17 batch 15]** via the **QUEST_DATA DataManager-holder seam**: `qs.canRepeat()` reads `DataManager.QUEST_DATA.getQuestById(questId)` per entry. Two NON-time-based quest templates (repeatCycle null -> isTimeBased() false, no clock) reflect-set with id+maxRepeatCount cover both branches (max 255 -> canRepeat true/writeC 0; completeCount>=max!=255 -> false/writeC 1); rewriteAll + insert updateModes. First capture byte-exact, 0 fidelity bugs.
- `SM_ABYSS_RANKING_LEGIONS` / `SM_ABYSS_RANKING_PLAYERS` — `List<RankingListLegion/Player>` DTO ctor. **[DONE 2026-06-17 batch 11]** (records con-null-safe; byte-exact first capture.)
- `SM_GM_SHOW_PLAYER_SKILLS` — `List<PlayerSkillEntry>` DTO. **[DONE batch 11]** (PlayerSkillEntry(skillId,skillLvl,skillType,state) ctor; only non-normal entries used so getFlag()'s System-time path never fires -> deterministic.)
- `SM_GM_SHOW_LEGION_MEMBERLIST` — `List<LegionMember>` DTO. **[AUDITED 2026-06-17 batch 11 -> demoted T2]** writeLegionMember calls `PlayerService.getOrLoadPlayerCommonData` (live singleton/DB) -> audit-only breadcrumb, 1:1 confirmed.
- `SM_TOWNS_LIST` — `Map<Integer,Town>`; Town DTO. **[AUDITED 2026-06-17 batch 11 -> demoted T2]** Town ctor (faithful 1:1) runs `spawnNewObjects()` (DataManager+SpawnEngine) + `GeoService.updateTown` -> not bounded-constructible -> audit-only breadcrumb, 1:1 confirmed.
- `SM_MACRO_LIST` (paged) covered above.

**T1-seam — reachable via an existing heavy seam (item/ItemInfoBlob, real-Npc, DataManager-holder), bounded:**
- item/ItemInfoBlob seam: `SM_TUNE_RESULT`, `SM_INVENTORY_INFO`, `SM_WAREHOUSE_ADD_ITEM`, `SM_PRIVATE_STORE`, `SM_MAIL_SERVICE`, `SM_BROKER_SERVICE`, `SM_UPDATE_PLAYER_APPEARANCE` (several also touch `con.getActivePlayer` -> verify per-packet, may demote to T2). **[DONE 2026-06-17 batch 12]** `SM_EXCHANGE_ADD_ITEM`, `SM_WAREHOUSE_INFO`, `SM_WAREHOUSE_UPDATE_ITEM`, `SM_REPURCHASE` runtime-golden'd via the GENERAL_INFO simple-item seam (player null = blob owner only; `SM_REPURCHASE` allocated uninitialized to dodge the `RepurchaseService` singleton in its ctor, fields reflectively pinned); byte-exact first capture, 0 fidelity bugs. **[verified -> T2]** `SM_LOOT_ITEMLIST` reads `con.getActivePlayer()` -> moved to T2 audit (below). **[DONE 2026-06-17 batch 13]** `SM_TUNE_RESULT` (BuildSimpleItem + PendingTuneResult DTO -> the already-validated EnchantInfoBlobEntry.WriteInfo ENCHANT_INFO writer; con-null-safe, no player), `SM_WAREHOUSE_ADD_ITEM` (item seam, player==blob owner only -> null; writeImpl reads only addType.GetMask() scalar), `SM_INVENTORY_INFO` (NEW player-scalar seam: uninitialized Player + pinned playerAccountData->PlayerCommonData carrying npc/quest/itemExpands; GetFullBlob player==blob owner) all runtime-golden'd byte-exact first capture, 0 fidelity bugs.
- real-VisibleObject seam: `SM_GATHERABLE_INFO` **[DONE 2026-06-17 batch 15]** — reads getX/Y/Z (WorldPosition) + getObjectId() + getSpawn().getStaticId()/getHeading() (SpawnTemplate) + getObjectTemplate().getTemplateId()/getL10nId() (GatherableTemplate); object NOT a StaticDoor -> writeH(1); con never read. Built via a minimal concrete VisibleObject (harness precedent — no DataManager/IDFactory/KnownList cascade) with reflect-set GatherableTemplate id+nameId. First capture byte-exact, 0 fidelity bugs. `SM_TRADELIST` **[AUDITED 1:1 2026-06-17 batch 21 -> T2]** (TRADELIST ctor reads getLegion/GOODSLIST_DATA legion-level filter + LimitedItemTradeService singleton -> heavier; writeImpl byte-identical, TradeNpcType.index() explicit 1-5). `SM_NPC_ASSEMBLER` **[DONE 2026-06-17 batch 14]** — turned out NOT to need the real-Npc seam: `AssembledNpc`/`AssembledNpcPart` are plain DTOs (reflect-set template npcId/staticId), writeImpl reads only scalars; `timeOnMap` (System.currentTimeMillis-derived, cached at ctor) reflect-pinned to a fixed value both sides -> byte-exact; null-despawn case writes only `writeD(0)`. First capture byte-exact, 0 fidelity bugs.
- pure-scalar DTO (no seam): `SM_GATHER_UPDATE` **[DONE 2026-06-17 batch 14]** — ctor caches `GatherableTemplate.getHarvestSkill()` + `Material.getItemId()/getL10n()` + scalars; writeImpl reads only those + the `SM_SYSTEM_MESSAGE` `STR_EXTRACT_GATHER_*` catalog ids (static constants) + `ChatUtil.l10n()` (pure: `"$"`+2-char encode of `nameid<<1|1`, no DataManager/time). GatherableTemplate.harvestSkill/Material.nameid+itemid reflect-set; 5 action cases (0/1/6/7/8 cover both msgId!=0 writeS(l10n) and msgId==0 writeS(null)). First capture byte-exact, 0 fidelity bugs.
- DataManager-holder seam: `SM_SKILL_LIST` **[DONE 2026-06-17 batch 13]** (silent ctor is DataManager-FREE — only the (skill,messageId) ctor reads SKILL_DATA; stigma entries skillType>0 -> IsNormalSkill() false -> GetFlag()==0 no clock -> deterministic; SKILL_DATA holder seam not even needed).
- new bounded Pet/Summon seam: `SM_PET_EMOTE` **[AUDITED 1:1 2026-06-17 batch 20 -> T2]** (live Pet/MoveController graph), `SM_SUMMON_PANEL` **[AUDITED 1:1 2026-06-17 batch 20 -> T2]** (live Summon/lifeStats/gameStats), `SM_SUMMON_UPDATE` **[AUDITED 1:1 2026-06-17 batch 20 -> T2]** (live Summon Stat2 graph).
- model-graph (Kisk/House): `SM_KISK_UPDATE` **[AUDITED 1:1 2026-06-17 batch 21 -> T2]** (live Kisk graph, 8x scalar writeD; Kisk ctor not bounded-constructible), `SM_HOUSE_SCRIPTS` **[DONE 2026-06-17 batch 20]** (T1: PlayerScript is a plain record (id,compressedBytes,uncompressedSize) — built directly with fixed bytes, no CompressUtil -> deterministic; con never read; 4 cases oneWithData/oneNoData/mixed/emptyNullCtor byte-exact first capture, 0 fidelity bugs), `SM_HOUSE_RENDER` **[AUDITED 1:1 2026-06-17 batch 20 -> T2]** + `SM_HOUSE_UPDATE` **[AUDITED 1:1 2026-06-17 batch 20 -> T2]** (AbstractHouseInfoPacket.writeCommonInfo reads LegionService singleton + live House/registry/emblem graph; PartType order+rooms match), `SM_HOUSE_OBJECTS` **[AUDITED 1:1 2026-06-17 batch 20 -> T2]** (live HouseObject graph), `SM_HOUSE_OBJECT` reads con -> T2.

**T2 — audit-only (writeImpl needs con / live World / live singleton / full live-Player graph / time):**
- reads `con.getActivePlayer()`: `SM_PRICES` **[AUDITED 1:1 2026-06-17 batch 16]**, `SM_PLAY_MOVIE` **[AUDITED 1:1 2026-06-17 batch 16]**, `SM_DIALOG_WINDOW` **[AUDITED 1:1 2026-06-17 batch 16]**, `SM_FRIEND_UPDATE` **[AUDITED 1:1 2026-06-17 batch 16]**, `SM_CHALLENGE_LIST` **[AUDITED 1:1 2026-06-17 batch 16]**, `SM_GROUP_INFO` **[AUDITED 1:1 2026-06-17]**, `SM_ALLIANCE_INFO` **[AUDITED 1:1 2026-06-17]**, `SM_LOOT_ITEMLIST` **[AUDITED 1:1 2026-06-17 batch 21]** (writeImpl `con.getActivePlayer()` null-guard + per-drop loot-confirmation read — full live DropNpc/Player graph + DataManager.ITEM_DATA), `SM_HOUSE_OBJECT` **[AUDITED 1:1 2026-06-17 batch 19]**, `SM_HOUSE_REGISTRY` **[AUDITED 1:1 2026-06-17 batch 19]**, `SM_HOUSE_BIDS` **[AUDITED 1:1 2026-06-17 batch 19]**, `SM_HOUSE_EDIT` **[AUDITED 1:1 2026-06-17 batch 19]**, `SM_INSTANCE_INFO` **[AUDITED 1:1 2026-06-17 batch 18]** (con.getActivePlayer + System.currentTimeMillis + portal-cooldown graph), `SM_SIEGE_LOCATION_INFO` **[AUDITED 1:1 2026-06-17 batch 18]** (con.getActivePlayer + live SiegeService/LegionService + emblem graph).
- reads `con.getAccount()` / `con.enableCryptKey()`: `SM_ACCOUNT_PROPERTIES` **[AUDITED 1:1 2026-06-17 batch 18]**, `SM_KEY` **[AUDITED 1:1 2026-06-17 batch 18]**.
- live World singleton (`World.getInstance()` / static-final): `SM_PLAYER_SPAWN` **[AUDITED 1:1 2026-06-17 batch 18]** (World.getWorldMap().getTemplate().getBeginnerTwinCount()), `SM_DIE` **[AUDITED 1:1 2026-06-17 batch 18]** (ctor: live Player/WorldMapInstance/InstanceHandler/Kisk graph).
- live `SiegeService`/`Influence`/`GameTimeService`/`TownService`/`GameServer`/`LoginServer` singleton or `System.currentTimeMillis`/JVM-uptime: `SM_GAME_TIME` **[AUDITED 1:1 2026-06-17 batch 16]** (live GameTimeService), `SM_TIME_CHECK` **[AUDITED 1:1 2026-06-17 batch 16]** (ManagementFactory uptime -> Process.StartTime), `SM_VERSION_CHECK` **[AUDITED 1:1 2026-06-17 batch 16]** (GSConfig/MembershipConfig/GameServer/LoginServer/ChatServer/AtreianPassportService + currentTimeMillis; full config blob byte-identical), `SM_INFLUENCE_RATIO` **[AUDITED 1:1 2026-06-17 batch 14]** (live Influence+SiegeService), `SM_FORTRESS_STATUS` **[AUDITED 1:1 2026-06-17 batch 14]** (live SiegeService.getFortresses()+Influence), `SM_LEGION_DOMINION_LOC_INFO` **[AUDITED 1:1 2026-06-17 batch 14]** (live LegionDominionService+LegionService+legion/emblem graph), `SM_IN_GAME_SHOP_CATEGORY_LIST` **[AUDITED 1:1 2026-06-17 batch 19]**, `SM_IN_GAME_SHOP_ITEM` **[AUDITED 1:1 2026-06-17 batch 19]**, `SM_IN_GAME_SHOP_LIST` **[AUDITED 1:1 2026-06-17 batch 19]**, `SM_ITEM_COOLDOWN` **[AUDITED 1:1 2026-06-17 batch 18]** (System.currentTimeMillis reuse calc), `SM_RECIPE_COOLDOWN` **[AUDITED 1:1 2026-06-17 batch 18]** (ctor: live Player.getCraftCooldowns time-based remainingSeconds).
- full live-Player/Legion/Group/Alliance graph: `SM_GM_SHOW_PLAYER_STATUS` **[AUDITED 1:1 2026-06-17]**, `SM_CHAT_WINDOW` **[AUDITED 1:1 2026-06-17 batch 17]**, `SM_GROUP_MEMBER_INFO` **[AUDITED 1:1 2026-06-17 batch 17]**, `SM_ALLIANCE_MEMBER_INFO` **[AUDITED 1:1 2026-06-17 batch 17]**, `SM_LEGION_INFO` **[AUDITED 1:1 2026-06-17 batch 17]**, `SM_LEGION_MEMBERLIST` **[AUDITED 1:1 2026-06-17 batch 17]**, `SM_LEGION_ADD_MEMBER` **[AUDITED 1:1 2026-06-17 batch 17]**, `SM_LEGION_UPDATE_MEMBER` **[AUDITED 1:1 2026-06-17 batch 17]**, `SM_GM_SHOW_LEGION_INFO` **[AUDITED 1:1 2026-06-17 batch 17]**, `SM_PRIVATE_STORE_NAME` **[AUDITED 1:1 2026-06-17 batch 17]**, `SM_CASTSPELL_RESULT` **[AUDITED 1:1 2026-06-17 batch 19]** (Skill/Effect graph), `SM_GATHER_UPDATE`, `SM_OBJECT_USE_UPDATE` **[AUDITED 1:1 2026-06-17 batch 19]** (HouseObject), `SM_HOUSE_OWNER_INFO` **[AUDITED 1:1 2026-06-17 batch 19]**.
- login/account/character graph: `SM_CHARACTER_LIST` **[AUDITED 1:1 2026-06-17 batch 23]** (con.getAccount() live + writeD(playOk2)/writeC(account.size())/per-char writePlayerInfo), `SM_CHARACTER_SELECT`(done T1), `SM_CREATE_CHARACTER` **[DONE 2026-06-17 batch 23]** (T1 non-OK branch writeD(responseCode)+return golden byte-exact 3 cases; OK branch delegates to validated writePlayerInfo), `SM_L2AUTH_LOGIN_CHECK` **[AUDITED 1:1 2026-06-17 batch 23]** (writeD(ok?0:1) + hex-literal standardData byte-identical + DataManager.WORLD_MAPS_DATA live-holder per-map writeD(mapId)+writeH(isInstance?0:twinCount) + writeS(accountName)), `SM_VERSION_CHECK`.
- builder / no reachable plain-value public ctor: `SM_CUSTOM_PACKET` **[AUDITED 1:1 2026-06-17 batch 22]** (admin //fsc builder; writeImpl iterates elements, each PacketElementType.write byte-identical: d/h/c Integer.decode, b Integer.valueOf-decimal, q Long.decode, f/e Float/Double, s; con never read), `SM_LOGIN_QUEUE` **[DONE 2026-06-17 batch 23]** (T1: private no-arg ctor pins fixed 5/60/50, writeD x3, con never read; byte-exact).
- item/ItemInfoBlob + live-graph readers: `SM_PRIVATE_STORE` **[AUDITED 1:1 2026-06-17 batch 22]** (live PrivateStore/seller Player/Inventory + getFullBlob; null-store guard; writeD seller objId/writeH size then per-item writeD objId/itemId, writeH((int)count), writeQ(price), blob.writeMe), `SM_UPDATE_PLAYER_APPEARANCE` **[AUDITED 1:1 2026-06-17 batch 22]** (writeD(playerId) + AbstractPlayerInfoPacket.writeEquippedItems live Item graph: equipmentSlot mask w/ TwoHanded SUB_HAND removal, per-item skin/godStone/dye/enchant), `SM_MAIL_SERVICE` **[AUDITED 1:1 2026-06-17 batch 22]** (con.getActivePlayer().getMailbox() live graph + Letter/Item/ItemInfoBlob; serviceId switch 0/1/2/3/5/6, letters.size()*-1 isLastPacket, totalCount+unreadCount*0x10000, (int)(time/1000), letterType.getId()), `SM_BROKER_SERVICE` **[AUDITED 1:1 2026-06-17 batch 22]** (live BrokerItem/Item/blob + currentTimeMillis + PlayerService.getPlayerName singleton + settleTime; duplicate-id BrokerPacketType SHOW_SETTLED_ICON=5/SETTLED_ITEMS=5 via reference-identity if-chain == Java identity switch; toDays(ms)==ms/86400000 trunc-toward-zero, settleTime/60000; all 8 writers byte-identical), `SM_AUTO_GROUP` **[AUDITED 1:1 2026-06-17 batch 22]** (ctor reads DataManager.AUTO_GROUP via AutoGroupType enum static-init -> not bounded; writeImpl scalars + name only, windowId switch case-groupings 0,7/1,3,8/2,4,5/6 byte-identical, con never read).
- `SM_SYSTEM_MESSAGE` **[DONE 2026-06-17 batch 23]** — generated 28k-line catalog + ChatType/sender graph. Wire-format path golden'd via the (int msgId, Object...) ctor (GOLDEN_YELLOW id 25 + null sender objId 0): writeC(chatType) writeC(0) writeD(senderObjId) writeD(msgId) writeC(params.length) per-param writeS(param.toString()) writeC(specialParams.length) per writeS; Integer.toString==int.ToString; 3 cases (noParams / int+string / two-strings+int) byte-exact. The msgId catalog itself is DATA (verified the wire-format path, not every entry).
- abyss/fortress/instance status singleton readers: `SM_ABYSS_ARTIFACT_INFO3` **[AUDITED 1:1 2026-06-17 batch 21 -> T2]** (writeImpl reads ArtifactLocation.getLocationId()/getStatus().getValue() — ArtifactStatus explicit 0-3; getStatus() consults SiegeService + ArtifactLocation ctor needs SiegeLocationTemplate -> not bounded-constructible, demoted to audit), `SM_FORTRESS_STATUS`, `SM_INSTANCE_SCORE` **[AUDITED 1:1 2026-06-17 batch 21 -> T2]** (writeImpl delegates to live InstanceScoreWriter graph + InstanceProgressionType.getId() explicit 1/2/3*1024*1024), `SM_ABYSS_RANKING_*` writeImpl is DTO-only (T1) — keep in T1.

(The above is a living split; packets move T2->T1 as bounded seams are built and T1->done as golden'd. Re-derive the un-golden'd set with: build the covered-name set from `parity-artifacts/golden/packets/*.json` + every `SM_*` token in `tests/.../Golden*.cs` + `Sm*Tests.cs`, diff against `find src -path '*ServerPackets/SM_*.cs'`.)

Recipe (proven): add `capture(...)` to the Java `Golden*FixtureGeneratorTest`, run `mvn -pl game-server -am test -Dtest=<Gen> -Dmaven.test.skip=false -Dsurefire.failIfNoSpecifiedTests=false`, add C# `[InlineData]`+Reconstruct+`CaptureWriteImplPayload`; **Java oracle bytes are truth, never tune to C#**; any mismatch = a real fidelity bug to fix in the C# packet.

---

## B. Explicit `NotSupportedException` / `Unsupported*` throw sites — TRIAGE COMPLETE (2026-06-17)

**CORRECTION:** there are ZERO `NotImplementedException` in src. The real stubs are `NotSupportedException` (C# idiom for Java's `UnsupportedOperationException`) + the custom `UnsupportedCollisionException` type. 14 throw-site files. **All 14 verified FAITHFUL GUARDS against their Java source — 0 real porting gaps, 0 fidelity bugs.** Each C# throw matches a Java `UnsupportedOperationException`/`CloneNotSupportedException` with identical message + condition. Breadcrumbed in place (comment-only); build 0, golden 221/221.

Per-file verdict (all FAITHFUL-GUARD, [Java path]::method throws UnsupportedOperationException unless noted):
- `Model/Team/Legion/LegionWarehouse.cs` — `model/team/legion/LegionWarehouse.java`: ~22 player-storage mutators throw "LWH should be used behind proxy" / SetOwner "LWH doesnt have owner" / SetLimit "Slot limit is controlled by the expansion level…". The proxy IS the impl (STRONG PRIOR confirmed).
- `Model/Items/Storage/LegionStorageProxy.cs` — `model/items/storage/LegionStorageProxy.java`: quest-status decrease overloads throw "Quests should not update LWH!"; SetOwner "LWH doesnt have owner".
- `GeoEngine/Collision/UnsupportedCollisionException.cs` — `geoEngine/collision/UnsupportedCollisionException.java` (jMonkeyEngine/Kirill): IS the Java exception type (extends UnsupportedOperationException → C# NotSupportedException).
- `GeoEngine/Math/FastMath.cs` — `geoEngine/math/FastMath.java::convertFloatToHalf` throws "NaN to half conversion not supported!".
- `GeoEngine/Scene/Node.cs` — `geoEngine/scene/Node.java::clone` throws CloneNotSupportedException (non-Geometry/Node child).
- `GeoEngine/Scene/DespawnableNode.cs` — `geoEngine/scene/DespawnableNode.java` copy-ctor + clone throw CloneNotSupportedException (2 sites).
- `GeoEngine/Bounding/BoundingBox.cs` — `geoEngine/bounding/BoundingBox.java::collideWith` throws UnsupportedCollisionException("With: "+simpleName).
- `GeoEngine/Collision/Bih/BIHTree.cs` — `geoEngine/collision/bih/BIHTree.java::collideWith` throws UnsupportedCollisionException() (no-arg).
- `GeoEngine/Math/Ray.cs` — `geoEngine/math/Ray.java::collideWith` throws UnsupportedCollisionException() (no-arg).
- `Commons/Network/AcceptDispatcherImpl.cs` — `commons/.../network/AcceptDispatcherImpl.java::closeConnection` throws "This method should never be called!".
- `Instance/Handlers/GeneralInstanceHandler.cs` — `instance/handlers/GeneralInstanceHandler.java::portToStartPosition` throws UnsupportedOperationException (default base impl).
- `Services/Instance/InstanceService.cs` — `services/instance/InstanceService.java::getNextAvailableInstance` throws "Invalid call for next available instance  of …" (double-space preserved 1:1).
- `Services/SiegeService.cs` — `services/SiegeService.java` cron transform: "…Preparation over midnight not supported." + catch(NumberFormatException→FormatException) rethrow.
- `Network/Aion/Iteminfo/ItemInfoBlob.cs` — `network/aion/iteminfo/ItemInfoBlob.java::newBlobEntry` throws UnsupportedOperationException for STAT_BONUSES.
- `SkillEngine/Effect/EffectTemplate.cs` — `skillengine/effect/EffectTemplate.java::calculateHate` default-arm throws "Unhandled effect type … for hate calculation".

Note: `Taskmanager/Tasks/MoveTaskManager.cs` (listed earlier as a candidate) is NOT a throw site — it passes `new NotSupportedException()` as a log argument to LogWarning, a faithful mirror of the Java log call. No action.

**§B remaining to triage: 0.** B is a documentation/breadcrumb pass — no real gaps existed (the backlog's "Legion warehouse" suspicion was wrong; it's the canonical proxy-guard pattern).

---

## C. C#-specific deferrals (the *real* subset of the 277 TODOs)

These are gaps because Java does NOT have them — they're C#-side "wire later" notes. (Hard contract: `.properties` keys + override precedence must be preserved — so the config one matters for parity.)

- **C1. Config framework (systemic).** Configs historically loaded from `@Property` default values baked into C# field initializers, NOT the faithful Java path = `.properties` file load + `ConfigurableProcessor` reflection + active-event overrides. Hard contract: `.properties` keys, default values, type-coercion semantics, and override precedence must match Java exactly.

  **Java sources of truth:** `commons/configuration/ConfigurableProcessor.java` (reflect over `@Property` static fields, walk superclass+interfaces, getValue = default-fallback + unused-key tracking + literal-`""` -> empty + `${...}` placeholder substitution, coerce via transformer, DEFAULT_VALUE sentinel = don't overwrite init value), `commons/configuration/Property.java` (`key()` + `defaultValue()` default = sentinel `DO_NOT_OVERWRITE_INITIALIAZION_VALUE`), `commons/configuration/transformers/*` (Number via Integer/Long.decode, Boolean true/false/1/0-only, Char, String identity, Enum case-sensitive valueOf, Array/CSV split-on-unquoted-comma, File, InetSocketAddress, Pattern, Class, Collection, TimeZone, ZoneId), `commons/utils/PropertiesUtils.java` (load file / loadFromDirectory cascading) over `java.util.Properties` semantics.

  **STATUS: CORE PORTED + PROVEN (2026-06-17).** Implemented in `Aion.Commons` 1:1 with Java:
    - `Configuration/PropertyAttribute.cs` — `[Property(key:, defaultValue:)]` with `DEFAULT_VALUE` sentinel; usable on static field OR static property.
    - `Configuration/ConfigurableProcessor.cs` — `Process(JavaProperties, params object[])` reflects [Property] members (fields+properties), base-type walk, `GetValue` (default fallback + unused-key set + `""`->empty + `${...}` substitution + sentinel skip), coercion via transformers, returns unused-key set. Faithful to Java getValue/transform/processField.
    - `Configuration/Transformers/*` — `PropertyTransformer` base + registry (`PropertyTransformers`) + Number (Java `*.decode` radix prefixes), Boolean, Char, String, Enum, Array (CSV split verbatim incl. quote-stripping), Pattern. **Ported this slice = every type the migrated holders need.**
    - `Configuration/JavaProperties.cs` — faithful `java.util.Properties` subset (`#`/`!` comments, `=`/`:`/whitespace separators, `\` escapes incl. `\uXXXX`, line continuation via trailing odd backslash) + chained defaults + `PropertiesUtils` load/loadFromDirectory cascading.
    - Migrated `Aion.Commons/Configs/CommonsConfig.cs` to `[Property]` (both Java fields: `commons.runnablestats.enable`, `commons.script_compiler.caching.enable`).
    - Parity proof: `tests/Aion.Commons.Tests/ConfigurableProcessorParityTests.cs` (6 tests, green) — **a `.properties` override now actually overrides the [Property] default** (bool/int/long-hex/float/string/enum/array), sentinel preserves init value, literal-`""`+`${}` substitution, unused-key reporting, JavaProperties comment/separator/escape/continuation parsing.

  **BATCH 1 GAME-SERVER CORE MIGRATED + BOOT-WIRED (2026-06-17).** 5 game-server core holders migrated to `[Property(key:, defaultValue:)]` (Java keys/defaults verbatim), plus the boot seam wired:
    - `Configs/Main/GSConfig.cs`, `Configs/Main/ThreadConfig.cs`, `Configs/Main/RatesConfig.cs` (30 `float[]` rate fields via ArrayTransformer), `Configs/Main/WorldConfig.cs` (SCREAMING_SNAKE `[Property]` members forward to the PascalCase backing storage), `Configs/Network/NetworkConfig.cs`. Each field keeps its Java-default field-initializer (so behavior is unchanged when `Config.Load` is not invoked, e.g. unit tests) AND the `[Property]` binding (overwritten when the processor runs).
    - **New transformer (depth-first):** `Configuration/Transformers/ZoneIdTransformer.cs` — Java `ZoneId` → `TimeZoneInfo` (empty → `TimeZoneInfo.Local`, else `FindSystemTimeZoneById`); registered in `PropertyTransformers`. Used by `GSConfig.TIME_ZONE_ID`.
    - **Boot wired:** `Configs/Config.cs` `Load(params Type[])` now mirrors Java `Config.load`: `LoadProperties()` cascades `config/{administration,main,network}/*` then merges `config/mygs.properties` (faithful relative-path order), validates `allowedConfigs` against the full Java `CONFIGS` list, and `ConfigurableProcessor.Process`es the migrated subset (in Java CONFIGS order: Commons, GS, Rates, Thread, World, Network); unmigrated holders keep initializer defaults. Added `LoadFrom(JavaProperties, Type[])` host/test entrypoint. `ChatProcessor.Reload`'s `Config.Load(typeof(CommandsConfig))` still validates (CommandsConfig is in CONFIGS) and is a no-op for processing until CommandsConfig migrates.
    - **Override proof:** `tests/Aion.GameServer.Tests/GameServerConfigPropertyOverrideTests.cs` (2 tests, green) — loads the REAL `game-server/config/main/*` and asserts `gameserver.character.reentry.time = 10` (in `gameserver.properties`) overrides the `[Property]` default `20` on `GSConfig.CHARACTER_REENTRY_TIME`; ZoneId binds `TIME_ZONE_ID` to system default; a `RatesConfig` `float[]` override via `Config.LoadFrom` flows through ArrayTransformer.

  **BATCH 2 GAME-SERVER SCALAR/ARRAY/PATTERN HOLDERS MIGRATED + BOOT-WIRED (2026-06-17).** 9 more game-server holders migrated to `[Property(key:, defaultValue:)]` (Java keys/defaults verbatim) — all needing ONLY already-registered transformers (Number/Boolean/Array/Pattern/String), no new transformer:
    - `Configs/Main/AIConfig.cs` (bool/int + `HANDLER_DIRECTORY` Java `File`→string via StringTransformer, same pattern as GSConfig.QUEST_HANDLER_DIRECTORY), `CleaningConfig.cs`, `CraftConfig.cs`, `FallDamageConfig.cs` (incl. `float` via NumberTransformer), `GeoDataConfig.cs`, `GroupConfig.cs`, `MembershipConfig.cs` (`byte` + `string[]` MEMBERSHIP_TYPES via ArrayTransformer, default "Premium"), `NameConfig.cs` (`bool`/`int` + `Regex` via PatternTransformer + `string[]` FORBIDDEN_WORDS; the two `forbidden_*` fields carry `[Property(key:)]` with NO defaultValue = Java DO_NOT_OVERWRITE sentinel, keep their initializer unless the key is present), `PeriodicSaveConfig.cs`. Each field keeps its Java-default initializer AND the `[Property]` binding.
    - **Boot wired:** added all 9 to `Config.MigratedConfigs` in Java CONFIGS declaration order (AIConfig, …, CommonsConfig, CleaningConfig, CraftConfig, FallDamageConfig, GSConfig, GeoDataConfig, GroupConfig, …, MembershipConfig, NameConfig, PeriodicSaveConfig, RatesConfig, ThreadConfig, WorldConfig, NetworkConfig).
    - **Override proof (extended):** `GameServerConfigPropertyOverrideTests.cs` +2 tests (now 4, green) — `RealPropertiesFile_OverridesNewlyMigratedHolders` processes the 9 holders against the REAL `game-server/config/main/*.properties`; `ExplicitOverride_DiffersFromAnnotatedDefault_AcrossBatchCHolders` asserts an explicit `.properties` override differs from + replaces the `[Property]` default for AI/Cleaning/Craft/Group/Membership/Name (incl. `string[]` MEMBERSHIP_TYPES and FORBIDDEN_WORDS via ArrayTransformer). 0 fidelity concerns.

  **BATCH 3 COLLECTION TRANSFORMER + AdminConfig/SecurityConfig MIGRATED + BOOT-WIRED (2026-06-17).** Ported the Collection transformer 1:1 and migrated the two holders whose only blocker was it:
    - **New transformer (depth-first):** `Configuration/Transformers/CollectionTransformer.cs` — Java `transformers/CollectionTransformer` (Neon): `CommaSeparatedValueTransformer` subclass, `Matches` = generic single-arg `IEnumerable`; resolves the closed-generic member type to a concrete collection (`List<T>`/`IList<T>`/`ICollection<T>`/`IEnumerable<T>`/`IReadOnly*<T>` → `List<T>`; `ISet<T>`/concrete-set → `HashSet<T>`; other concrete `ICollection<T>` via parameterless ctor), coerces each element with the element-type transformer through closed `ICollection<T>.Add` (so `HashSet<T>` de-duplicates like Java `HashSet`), empty input → empty collection (never null). Registered in `PropertyTransformers` right after `ArrayTransformer` (Java order).
    - **Holders migrated:** `Configs/Administration/AdminConfig.cs` (all 24 fields: scalar `sbyte`/`bool`, `string[] NAME_TAGS` via ArrayTransformer with the verbatim `customtags` default incl. private-use unicode `»`/`«`/``, and `List<string> LOGIN_EXECUTE_COMMANDS`/`ANNOUNCE_LEVELS` via the new CollectionTransformer); `Configs/Main/SecurityConfig.cs` (all 26 fields: scalar `bool`/`int`, `MultiClientingRestrictionMode` enum via EnumTransformer, `ISet<string> MULTI_CLIENTING_IGNORED_MAC_ADDRESSES` via CollectionTransformer). Java keys/defaults verbatim; field initializers kept.
    - **Boot wired:** added `AdminConfig` (first, matching Java CONFIGS) and `SecurityConfig` (after RatesConfig) to `Config.MigratedConfigs`.
    - **Override proof:** Commons `ConfigurableProcessorParityTests.CollectionTransformer_BindsListAndSet_FromDefaultsAndOverrides` (defaults + List order + quoted-comma token + Set de-dup + empty→empty); GameServer `GameServerConfigPropertyOverrideTests` +2 tests (`RealPropertiesFile_BindsCollectionFields_AdminAndSecurity` loads the REAL `config/administration` + `config/main` and asserts `LOGIN_EXECUTE_COMMANDS`/`ANNOUNCE_LEVELS`/`NAME_TAGS`/the empty MAC `ISet`/restriction enum; `ExplicitOverride_CollectionTransformer_ListAndSet` proves CSV-quoted List + de-dup Set overrides). The class joined the `GoldenDataManager` non-parallel collection because `Config.Load()` (driven by GameServerBootstrapTests) now also binds these shared static holders — they must not race. 0 fidelity concerns. Build 0, golden 221/221, GameServer suite 490/490 (+2), bootstrap 9/9, Commons config tests green (only the PRE-EXISTING 3306-vs-3307 `ConfigLoaderParityTests.DatabaseOptions` failure remains).

  **BATCH 4 — 8 NO-NEW-TRANSFORMER HOLDERS MIGRATED + BOOT-WIRED (2026-06-17).** Migrated every remaining holder whose `@Property` field types are already covered (Number/Boolean/String/Pattern/Collection); **no new transformer needed**:
    - **Holders migrated (Java keys/defaults verbatim, field initializers kept):** `HTMLConfig` (5: bool/string), `InGameShopConfig` (3: bool), `InstanceConfig` (6: int/`ISet<int>` via CollectionTransformer/bool/`File HANDLER_DIRECTORY`→string via StringTransformer — matching the established AIConfig/GSConfig/WorldConfig File-handler-directory convention; consumers take `string`/`new FileInfo(string)`), `LegionConfig` (40: `Pattern`→Regex via PatternTransformer ×3, int, bool), `LoggingConfig` (17: bool), `PlayerTransferConfig` (18: long/string/bool/int), `PricesConfig` (5: int), `PunishmentConfig` (3: bool/int).
    - **Boot wired:** added all 8 to `Config.MigratedConfigs` in Java CONFIGS declaration order (after GroupConfig: HTML, InGameShop, Instance, Legion, Logging; after PeriodicSave: PlayerTransfer, Prices, Punishment). MigratedConfigs now 25 classes.
    - **Override proof:** `GameServerConfigPropertyOverrideTests` +2 tests (now 8, green) — `ExplicitOverride_DiffersFromAnnotatedDefault_AcrossBatchC2Holders` proves an explicit `.properties` override differs from + replaces the `[Property]` default across all 8 (incl. `long` MAX_KINAH > int range, `ISet<int>` excluded-maps CSV, Regex compile); `RealPropertiesFile_BindsNewlyMigratedHolders_BatchC2` loads the REAL `config/main/*.properties` and asserts the fields bind (HTML root, Legion disband/warehouse/max-members/name-pattern, Prices, Punishment type, empty excluded-maps `ISet`). 0 fidelity concerns. Build 0, golden 221/221, GameServer suite 492/492 (+2), bootstrap 9/9, Commons config tests green (only the PRE-EXISTING 3306-vs-3307 `ConfigLoaderParityTests.DatabaseOptions` failure remains).

  **BATCH 5 — REAL-QUARTZ CRON TRANSFORMER PORTED + 6 CRON/SIMPLE HOLDERS MIGRATED + BOOT-WIRED (2026-06-17).** Ported the blocking Cron dep 1:1 and migrated every remaining holder that needs *only* Cron (or no new transformer):
    - **Dep ported (1:1, real Quartz.NET — NOT a stub):** `Services/Cron/CronExpressionTransformer.cs` mirrors Java `game-server services/cron/CronExpressionTransformer` (`matches(CronExpression)`; `parseObject` = `value.isEmpty() ? null : CronExpressions.getOrCreate(value)`) — coerces to the REAL `Quartz.CronExpression` via the existing `CronExpressions.GetOrCreate` intern cache. Like Java (registered in `GameServer.main`, lives in game-server because it depends on the Quartz type), it is NOT in the commons `PropertyTransformers` default registry; `CronExpressionTransformer.EnsureRegistered()` idempotently adds it and `Config`'s static ctor calls it before any holder is processed (test fixtures that bypass full boot call `EnsureRegistered()` directly). `ArrayTransformer` resolves `CronExpression[]` element coercion through it (AutoGroup's quoted single-cron `@Property` defaults → 1-element arrays).
    - **Holders migrated (Java keys/defaults verbatim, field initializers kept):** `AutoGroupConfig` (bool/long + 6× `CronExpression[]`), `CustomConfig` (60 fields: bool/int/long/float + `ISet<int>` CP_WORLDS + 4× `CronExpression`), `DropConfig` (nullable `ItemQuality?` enum + `ISet<int>`), `EventsConfig` (bool/int + `ISet<string>`/`ISet<int>`), `ShutdownConfig` (int + nullable `CronExpression RESTART_SCHEDULE` no-default → null), `SiegeConfig` (bool/int/float/double + 2× `CronExpression`).
    - **Boot wired:** added all 6 to `Config.MigratedConfigs` in Java CONFIGS declaration order (after AIConfig: AutoGroup; after CraftConfig: Custom, Drop, Events; after SecurityConfig: Shutdown, Siege). MigratedConfigs now 31 classes.
    - **Override proof:** `GameServerConfigPropertyOverrideTests` +2 tests (now 10, green) — `RealPropertiesFile_BindsNewlyMigratedHolders_BatchC3_IncludingCron` loads the REAL `config/main/*.properties` and asserts scalar overrides (`VORTEX_DURATION=2`, `BALAUR_AUTO_ASSAULT=true`), the nullable enum override (`MIN_ANNOUNCE_QUALITY=MYTHIC`), empty `ISet`s, AND that the **real Quartz CronExpression** fields compile/bind (`VORTEX_BRUSTHONIN_SCHEDULE`/`MOLTENUS_SPAWN_SCHEDULE`/`DREDGION_TIMES[0]` via `.CronExpressionString`; shutdown empty-schedule → `null`); `ExplicitOverride_BindsCron_AndScalars_BatchC3` proves an explicit `gameserver.shutdown.restart_schedule` override binds a non-default CronExpression + scalar/Set/enum overrides. 0 fidelity concerns. Build 0, golden 221/221, GameServer suite 494/494 (+2), bootstrap 9/9, Commons config tests green (only the PRE-EXISTING 3306-vs-3307 `ConfigLoaderParityTests.DatabaseOptions` failure remains).

  **BATCH 6 — `@Properties` MAP ANNOTATION PORTED + LAST 4 GAME-SERVER HOLDERS MIGRATED → game-server holder migration 100% (2026-06-17).** Ported the final blocking dep 1:1 and migrated every remaining game-server holder:
    - **Dep ported (1:1):** `Aion.Commons/Configuration/PropertiesAttribute.cs` mirrors Java `commons/configuration/Properties` (the plural annotation, separate from `@Property`; `keyPattern` default `.+`). `ConfigurableProcessor` now handles a member carrying `[Property]` **XOR** `[Properties]` (throws if both, like Java): for `[Properties]` it compiles the pattern, runs `FilterProperties` (Java `filterProperties` — `Matcher.find()`; map key = first capturing group when present, else whole key; value via `getValue` with `""` default + placeholder substitution + unused-key tracking), then coerces into the map via the new `Transformers/MapTransformer.cs` (Java `MapTransformer` — resolves `IDictionary<K,V>`/`Dictionary<K,V>` to `Dictionary<K,V>`, coerces K and V through the registry, empty→empty never-null). Note: .NET `Match.Groups.Count` always includes group[0], so Java `groupCount() > 0` ⇔ C# `Groups.Count > 1`. Also added `Transformers/FileTransformer.cs` (Java `FileTransformer`; `java.io.File`→`FileInfo`, no existence check) + registered it, unblocking `CommandsConfig.HANDLER_DIRECTORIES` (`File[]`→`FileInfo[]` via ArrayTransformer).
    - **Holders migrated (Java keys/defaults/patterns verbatim, field initializers kept):** **CommandsConfig** (`[Properties("^[a-zA-Z0-9_]+$")] ACCESS_LEVELS` Map<string,sbyte> — Java `Byte` is signed; no capturing group → whole key is map key; `[Property] HANDLER_DIRECTORIES` FileInfo[]), **RankingConfig** (2× Cron + int + nullable-into-enum `XFORM_MIN_RANK` + 2× `[Properties]` Map<AbyssRankEnum,int> quota/gp_loss), **HousingConfig** (all scalar/float `[Property]`, 3× Cron, `int[] HOUSE_AUCTION_REGISTER_DAYS` with NO defaultValue → DO_NOT_OVERWRITE keeps `{1,5}`, `[Properties]` Map<HouseType,int> auto_fill_limits), **PffConfig** (`[Property] PFF_MODE` + `[Properties]` Map<int,int> hex-opcode→ms; NumberTransformer decodes `0x...` keys).
    - **Boot wired:** added all 4 to `Config.MigratedConfigs` in Java CONFIGS declaration order (Commands after Admin; Housing after Group; Ranking after Punishment; Pff after Network — last). **MigratedConfigs now == CONFIGS (35 classes): every game-server config holder is `[Property]`/`[Properties]`-migrated.**
    - **Override proof:** new `GameServerConfigPropertiesMapTests` (4 tests, joins the `GoldenDataManager` non-parallel collection) — `RealPropertiesFile_BindsCommandsAccessLevelMap` loads the REAL `config/administration` and asserts the command→access-level map binds (incl. underscore key `Bookmark_add`, and that the dotted `handler_directories` key is excluded by the pattern) + `HANDLER_DIRECTORIES` 3-dir `FileInfo[]`; `RealPropertiesFile_BindsRankingEnumMaps_AndCron` asserts the AbyssRankEnum quota/gp_loss maps (capturing group strips prefix) + the Cron field via `.CronExpressionString`; `RealPropertiesFile_BindsHousingHouseTypeMap_AndCron` asserts the HouseType→int map + Cron + `int[]{1,5}`; `ExplicitOverride_BindsPffHexOpcodeMap` proves the shipped (commented-out) map → empty never-null, then a hex-opcode override decodes `0x0C4`/`0xAB` into int keys and excludes a non-hex key. 0 fidelity concerns. Build 0, golden 221/221, GameServer suite 498/498 (+4), bootstrap 9/9, Commons config tests green (only the PRE-EXISTING 3306-vs-3307 `ConfigLoaderParityTests.DatabaseOptions` failure remains).

  - **Batch C4 (2026-06-17) — commons `DatabaseConfig` faithful holder + the 3306/3307 red-test fix.** Ported `commons/configs/DatabaseConfig.java` 1:1 to `src/Aion.Commons/Configs/DatabaseConfig.cs` as a `[Property]`-annotated static holder (keys/defaults copied VERBATIM: `database.url`/`database.user`/`database.password` no-default; `database.connectionpool.connections.max`=5; `database.connectionpool.timeout`=5000). It binds via `ConfigurableProcessor.Process(JavaProperties, typeof(DatabaseConfig))` — the same faithful path the game-server holders use — so a my{ls,cs}.properties override (loaded last) wins exactly per Java PropertiesUtils precedence. **Root-caused the pre-existing 3306-vs-3307 failure:** the checked-in `login-server/config/network/database.properties` defaults to 3306, but `login-server/config/myls.properties` (highest precedence) overrides `database.url` to `localhost:3307`; the cascade applies it last, so the faithful loaded port IS 3307. Fixed `ConfigLoaderParityTests.DatabaseOptions_LoadFromJavaConfig` to assert the faithful 3307 (data-driven, not the network/ default), and fixed the identical sibling `ChatServerOptionsTests.LoadDatabaseOptionsFromJavaConfig` (mycs.properties → 3307) the same way. **Override-proof:** new `ConfigLoaderParityTests.DatabaseConfig_PropertyHolder_BindsPerInstanceOverride` builds a network/ defaults + my* override cascade in a temp dir, processes the holder, and asserts the override wins for url (3307) + max-connections (9) while non-overridden keys keep the default. **ConfigLoader/DatabaseOptions status:** NOT retired this tick (still consumed by `DatabaseFactory`, all three `Program.cs`, `GameServerOptions`, `ChatServerOptions` — a larger depth-first cascade); they are NOT lossy on DB values (they faithfully read the my* override → 3307), so this is bounded-correct. Full retirement = redirect those 5+ consumers onto the `DatabaseConfig` holder, then delete `ConfigLoader.cs`/`DatabaseOptions.LoadFromJavaConfig` — see remaining (5). 0 fidelity concerns. Build 0, golden 221/221, GameServer suite 498/498, bootstrap 9/9, Commons config tests 55/55 (the 3306/3307 test now PASSES + the new override-proof test), Login 121/121, Chat 29/29.
  - **Batch C5 (2026-06-17) — active-event override merge (Java `Config.load` line 49) + login-server `Config` `[Property]` holder.** Two slices, both 1:1, both override-proof-tested:
    - **Active-event override merge (was REMAINING (2)) — DONE.** Java `Config.load()` does `properties.putAll(EventService.getInstance().getActiveEventConfigProperties())` immediately after `loadProperties()` so an active event's inline `<config_properties><property>key=value</property></config_properties>` overrides the loaded base config. Mirrored 1:1 in `Aion.GameServer/Configs/Config.cs.Load()`: after `LoadProperties()`, `properties.PutAll(EventService.GetInstance().GetActiveEventConfigProperties())`. Added `JavaProperties.PutAll(IEnumerable<KeyValuePair<string,string>>)` (faithful `Hashtable.putAll` — writes own map, overrides). The C# `EventService.GetActiveEventConfigProperties()` (nullsFirst-by-startDate sort, later event wins on collision) + `EventTemplate.LoadConfigProperties()` (StringReader key=value parse) were already faithful — only the `Config.Load` merge was missing. Override-proof tests (`GameServerConfigPropertyOverrideTests`): `ActiveEventConfigProperties_OverrideBaseConfig_AtProcessTime` proves base props (reentry.time=10) → PutAll(event props reentry.time=42) → `ConfigurableProcessor.Process(GSConfig)` binds 42; `GetActiveEventConfigProperties_EmptyWhenNoActiveEvents_AndLaterEventWins` proves empty with no active events + later-startDate event wins on key collision (putAll ordering).
    - **Login-server `Config` holder (was part of REMAINING (1)) — DONE.** Ported `login-server/configs/Config.java` 1:1 to `Aion.LoginServer/Configuration/Config.cs` as a `[Property]`-annotated static holder (all 9 keys/defaults copied VERBATIM: client/gameserver `socket_address` 0.0.0.0:2106 / 0.0.0.0:9014 held as `host:port` string per the game-server NetworkConfig convention; `logintrybeforeban`=5; `bantimeforbruteforcing`=15; `nio.threads`=0; `accounts.autocreate`=true; `external_auth.url`=""; `bruteforceprotector`=true; `log.logins`=false; `useExternalAuth()` mirrored). `Config.Load(startDir)` cascades config/main + config/network defaults then myls.properties (exact Java load order) and `ConfigurableProcessor.Process(Config, CommonsConfig, DatabaseConfig)` — same triple Java's `Config.load()` processes. `LoginServerOptions.LoadFromJavaConfig` now calls `Config.Load()` and reads off the bound holder (consumers/DI surface unchanged; removed the orphaned `ConfigLoader`/`FindRepoRoot` path in that class). Override-proof test (`LoginServerOptionsTests.Config_PropertyHolder_BindsDefaults_AndHonorsOverrides`): asserts annotated defaults on empty props, then overrides all 9 keys via `Config.LoadFrom` and proves flow-through. 0 fidelity concerns. Build 0, golden 221/221, GameServer suite 500/500, Commons 55/55, Login 122/122, Chat 29/29.

  - **Batch C6 (2026-06-17) — chat-server `Config` `[Property]` holder + boot-wire — CONFIG FRAMEWORK MIGRATION COMPLETE.** Ported `chat-server/configs/Config.java` + `main/LoggingConfig.java` + `network/NetworkConfig.java` 1:1 to `Aion.ChatServer/Configuration/Config.cs` as a `[Property]`-annotated static holder (all 8 keys/defaults copied VERBATIM: `chatserver.log.channel.request`/`channel.invalid`/`chat`/`chat_to_db`=false; `chatserver.network.client.connect_address`=0.0.0.0:10241 + `client.socket_address`=0.0.0.0:10241 + `gameserver.socket_address`=0.0.0.0:9021 held as `host:port` string per the game/login NetworkConfig convention; `gameserver.password`=""; `nio.threads`=1). `Config.Load(startDir)` cascades config/main + config/network defaults then `mycs.properties` (exact Java `Config.loadProperties()` load order) and `ConfigurableProcessor.Process(CommonsConfig, Config, DatabaseConfig)` — mirroring Java `Config.load()`'s `process(props, CommonsConfig, LoggingConfig, DatabaseConfig, NetworkConfig)`. The `connect_address = ${...socket_address}` reference now resolves through `ConfigurableProcessor`'s faithful `${}` placeholder substitution (replacing the ad-hoc `ChatServerOptions.ResolvePropertyReference`). `ChatServerOptions.LoadFromJavaConfig` now calls `Config.Load()` and reads off the bound holder; the old lossy `ConfigLoader`+env-var+`${}` path is gone from the network/logging fields (DI/consumer surface unchanged). Override-proof tests (`ChatServerOptionsTests`): `Load_BindsRealCheckedInChatConfig_ResolvingConnectAddressReference` loads the REAL checked-in `chat-server/config/*.properties` and asserts the network fields bind incl. the resolved `${}` connect_address; `Load_HighestPrecedenceOverrideWins_OnMigratedField` builds the real defaults cascade then applies a per-instance override last and proves nio.threads/log.chat/gameserver.password flow through. **ConfigLoader NOT retired** (intentional): still consumed by `DatabaseOptions.LoadFromJavaConfig`, `GameServerOptions`, `ChatServerOptions.LoadDatabaseOptionsFromJavaConfig`, and its own `ConfigLoaderParityTests` — it is NOT lossy on DB values (faithfully applies the my* override). The chat DB path keeps the `DatabaseOptions`-style `ConfigLoader` cascade exactly as login-server does (parity). The non-Java `FilteredKeywords`/`ClientConnectEndPoint` C# extensions remain (Java has no keyword filter; `FilteredKeywords` binds empty → never alters parity). 0 fidelity concerns. Build 0, golden 221/221, GameServer suite 500/500, Commons 55/55, Login 122/122, Chat 31/31 (+2).

  **C1 CONFIG FRAMEWORK MIGRATION — COMPLETE.** All servers' holders are `[Property]`-migrated + boot-wired over the exact Java cascade (config/main + config/network defaults → my{ls,cs,gs}.properties override, processed through `ConfigurableProcessor`): all 35 game-server holders (+ active-event override merge, C5), commons `CommonsConfig`/`DatabaseConfig` (C4), login-server `Config` (C5), chat-server `Config` (C6). Keys/defaults/precedence + `${}` substitution + `""`-literal + active-event override are byte-faithful to Java. **Sanctioned infra deferrals (NOT parity gaps):** (a) `Configuration/ConfigLoader.cs`/`DatabaseOptions.LoadFromJavaConfig` not fully retired — still the DB-cascade path for all three servers' `Program.cs`/`DatabaseFactory` + `GameServerOptions`; it faithfully applies the my* override (not lossy), full retirement = redirect those consumers onto the `DatabaseConfig` holder then delete it. (b) InetSocketAddress `CLIENT_CONNECT_ADDRESS` local-IPv4 discovery (Java `Config.load` `isAnyLocalAddress()` + `NetworkUtils.findLocalIPv4()`) — address held as `host:port` string, local-IP autodiscovery is a network-bind concern. (c) Java unknown-key warning + `removePropertiesUsedInLogbackXml` filtering — logging-only. (d) Remaining transformers (Class, TimeZone) only when a migrated holder needs them.
- **C2. The ~13 "TODO … Java" / "port from Java the rest" markers.** Enumerate (`grep -rIn "TODO" … | grep -i java`) and resolve each as a 1:1 port; these are explicit C#-deferred porting, not Java mirrors.
- **C3. Triage the rest of the 277.** Mechanically separate faithful-Java-mirror TODOs (keep verbatim — they are parity) from any remaining C#-specific deferrals; only the latter are backlog. Produce the split once so the count is honest.

---

## D. Editor / admin-save & DAO write-path deferrals (small, real)

- **D1. `SpawnsData` admin-save helpers — DONE (2026-06-17). Ported 1:1, all deps present.** The 5 helper TODOs (`saveSpawn(VisibleObject,bool)` / `getRelativePath` / `loadSpawnsFromTemplateFiles` / `findSpawnTemplate` / `positionMatches`) at `dotnetConversion/src/Aion.GameServer/Dataholders/SpawnsData.cs` were verified-buildable and ported verbatim from `game-server/.../dataholders/SpawnsData.java:205-325`. **Dep audit (all faithful in C#):** `VisibleObject` (GetSpawn/GetX/Y/Z/GetHeading/GetWorldId/GetPosition), `WorldMapInstance.GetParent().GetName()`/`.IsInstanceType()`, `Gatherable` (`is Gatherable`), `SpawnMap(int)`/`SpawnMap.GetSpawns()`, `Spawn(int,int,SpawnHandlerType)`/`SetCustom`/`GetSpawnSpotTemplates`, `SpawnSpotTemplate` ctor, `SpawnHandlerType.RIFT`, `AddRegularSpawns(SpawnMap)`. **The only missing dep was `JAXBUtil.serialize`** — added a bounded faithful shim (`Serialize(obj)`/`Serialize(obj,schema)` via `XmlSerializer`+`XmlWriterSettings{Indent,UTF8}` mirroring JAXB_FORMATTED_OUTPUT+JAXB_ENCODING) plus a generic `Deserialize<T>(FileInfo,schema)` overload. Faithful notes: Java `synchronized` → `lock`; `Files.walkFileTree` → `Directory.EnumerateFiles(folder,"*",AllDirectories)` with `.xml` filter + first-match `TERMINATE`; `getParentFile().mkdir()` → `xml.Directory?.Create()`. **Reachability:** currently UNREACHABLE in C# — the 3 calling admin handlers (`Delete.java`/`SpawnNpc.java`/`SpawnUpdate.java`, all in `game-server/data/handlers/admincommands/`) are NOT yet ported, so the method is dead until they land; it is now a complete, orphan-free target ready for them (not a stub). Build 0, golden 221/221, suite 500/500, fidelity clean.
- **D2. Any DAO/editor `// TODO` save/persist paths** surfaced in §C3 triage (e.g. admin-tool persistence) — port 1:1.
- **D3. Unported admincommand content handlers — DONE (2026-06-17). All 11 now ported; the final 2 (Info, FixPath) landed this tick. admincommands now 103/103 — CONTENT PARITY COMPLETE (no unported content handler remains anywhere).** **PORTED FINAL TICK (2 + 1 dep):** **`Info.cs`** (`//info`; full target-info display 1:1). Dep audit confirmed every getter faithful: `PlayerGameStats`/`CreatureGameStats` carry all ~60 stat getters (HP/MP/FP/DP, base attributes, cast-time boost, attack/movement speed, M.Boost/Accuracy/Critical, main/off-hand M./P. attack via `GetStat(...,CalculationType.DISPLAY)` + min/max via `GetMinDamageRatio`, accuracy/critical, M./P. defence, magic resist, crit spell/strike resist + fortitude via `GetStat(StatEnum.MAGICAL/PHYSICAL_CRITICAL_DAMAGE_REDUCE,0)`, all 6 `GetElementalDefenseFor(SkillElement.*)`, all 6 PvP-ratio `GetStat(StatEnum.PVP_*_RATIO*,0)`); `AggroList.Stream()`+`AggroInfo.GetAttacker/GetDamage/GetHate`+`GetMaster().GetRace()` per-race A/E/N tallies; `SiegeService.FindFortress`+`FortressLocation.GetLocationId`; `TownService.GetTownIdByPosition`/`GetTownResidence`; `NpcFaction`/`ENpcFactionQuestState.COMPLETE`+`NpcFactions.GetActiveNpcFaction`; `Npc.GetAi().GetName/GetState/GetSubState`, `GetRating/GetRank/GetNpcTemplateType/GetAbyssNpcType`, `WalkerGroup.GetClusterData`+`ClusteredNpc.GetXDelta/GetYDelta/GetWalkerIndex`, `StatFunctions.CalculateExperienceReward`, `SiegeNpc.GetSiegeId/GetSiegeRace`, `PlayerRestrictions.CanAttack`, `Npc.GetType_(Creature)` hostility (renamed from Java `getType` to avoid `Object.GetType` clash), `creature.GetPosition().ToCoordString()`. **The only missing dep — `PositionUtil.getDirectionalBound(VisibleObject,VisibleObject,boolean)` + `calculateAngleTowards(VisibleObject,VisibleObject)` — was ported 1:1** into `Utils/PositionUtil.cs` (verbatim trig: `Math.toRadians`→`*(Math.PI/180.0)`, bound-radius side/front projection, retiring the 5 stale F2 TODO placeholders). Faithful notes: `getClass().getSimpleName()`→`target.GetType().Name`; `AtomicInteger` tallies→`int`+`Interlocked.Add`; `ENpcFactionQuestState.name()`→`.ToString()`; model `Pet` fully-qualified (the `Handlers.AdminCommands.Pet` admin-command shadows it). **`FixPath.cs`** (`//fixpath`; `[z-offset]` / `<route id> [z-offset]` / `<cancel>` — client-geo Z-poll route fixer 1:1). All data deps verified present: `WALKER_DATA.GetWalkerTemplate`, `WalkerTemplate.GetRouteStep(s)/GetLoopType/LoopType.WALK_BACK/GetRouteId/SetRouteSteps/SetLoopType/SetPool/SetType/GetType_/SetRows/GetRows`, `RouteStep.SetZ`+ctor, `WalkerData.AddTemplate/SaveData`, `TeleportService.TeleportTo(Player,WorldPosition)`, `MovementMask.IMMEDIATE`, `CustomPlayerState.INVULNERABLE`, `Player.IsInvulnerable/Set/UnsetCustomState/IsOnline/IsSpawned/IsInState(CreatureState.DEAD)/GetMoveController().GetMovementMask()`. **The blocking-poll-with-timed-get seam (the prior blocker) is now resolved faithfully:** Java `scheduleAtFixedRate(...).get(5,SECONDS)` distinguishing `TimeoutException` (abort+message) from `CancellationException` (the poll self-cancels once it captured the new client Z → success+apply) maps onto `ThreadPoolManager.ScheduleAtFixedRateTask` (returns a `ScheduledTask` with a self-cancellable token + a `.Completion` task that finishes when the poll cancels itself); `GetZ()` awaits `waitTask.Completion.Wait(TimeSpan.FromSeconds(5))` — `false`=timeout (cancel+abort+return 0), `true`=poll self-cancelled (return captured `lastZ`) — preserving the exact observable behavior (gameplay-faithful-infra-idiomatic: only the await/cancel plumbing is idiomatic; the 500/250ms poll, 5s timeout, abort-vs-apply outcomes are 1:1). Faithful notes: `static volatile Player runner`/`static boolean oldInvul` kept as static fields; `Float.parseFloat`→`float.TryParse(...,InvariantCulture)`; run body on `ThreadPoolManager.Execute(Action)` as in Java's `execute(() -> ...)`. **Build 0, golden 221/221, suite 500/500, bootstrap 9/9, `check_fidelity` clean.**
  <!-- HISTORICAL (kept for trace): the 11 unported admincommands and their per-tick ports —
  - **D3 (IN PROGRESS 2026-06-17 — superseded).** The 11 unported `game-server/data/handlers/admincommands/*.java` were: Configure, Debug, Delete, FixPath, Grant, Info, Reload, Send, SpawnNpc, SpawnUpdate, Sys. **PORTED LATEST TICK (3 + deps):** **`Reload.cs`** (`//reload`; all 13 Java subcommands — quests/skills/npcskills/items/customdrops/events/arcade/decomposables reload static data, ai/commands/config/gameshop reload handlers/config — same names + messages 1:1). The DataManager holders are read-only accessors delegating to `StaticData`; added a **minimal settable reload surface** (infra contract): `StaticData` now captures `StaticDataDirectory` at boot and exposes `ReloadItemData/ReloadSkillData/ReloadQuestData/ReloadXmlQuests/ReloadCustomDrop/ReloadUpgradeArcadeData/ReloadDecomposableItemsData/ReloadNpcSkillData/ReloadEventData` (each re-runs the *same* leaf `TryLoadHolder`/`TryLoadMergedHolder` loader against the captured tree and reassigns the `*Dh` slot), surfaced as static `DataManager.ReloadXxx` so the existing `DataManager.*_DATA` accessors reflect the reload immediately. `AIEngine.Reload()`/`ChatProcessor.Reload()`/`Config.Load()`/`InGameShopEn.Reload()`/`QuestEngine.Reload()`+`GetQuestHandlerCount()`/`EventService.Stop()`+`Start()` all already faithful. NPC_SKILL/EVENT mirror Java's setter-on-existing-holder shape via slot reassignment (merged loader already runs AfterUnmarshal → identical observable result). **`Sys.cs`** (`//sys`; info/memory[gc]/threadpool/shutdown/restart). info/memory render new faithful-infra `commons/utils/info/SystemInfo` (GC.GetGCMemoryInfo + RuntimeInformation/Environment — JVM Runtime/System-props → .NET equivalents preserving the line labels/shapes), shutdown/restart drive `GameServer.InitShutdown(ExitCode.NORMAL/RESTART, delay)` via the existing ShutdownHook path; added `commons/utils/ExitCode` (NORMAL=0/ERROR=1/RESTART=2, verbatim from Java) + `VersionInfo.commons` static field. **threadpool branch (idiomatic-infra, documented metric divergence):** the faithful C# `ThreadPoolManager` is a single async `ValueTask`/`Schedule` scheduler with NO Java `ThreadPoolExecutor` per-pool internals (Java reads three pools' `getActiveCount`/`getCorePoolSize`/`getLargestPoolSize`/`getCompletedTaskCount`/`getQueue().size()` etc.) — that exact per-pool breakdown is gated on an unported substrate, so the branch emits the .NET `ThreadPool` global counters (ThreadCount/PendingWorkItemCount/CompletedWorkItemCount/min-avail-max worker+IOCP) as the closest observable equivalent rather than stubbing or omitting it. **`Debug.cs`** (`//debug`; connections/connectedPlayers/dcBuggedPlayers — full 1:1 incl. coord/spawned/in-world details, ChatUtil.Color RED "not in world", and the dcBuggedPlayers cancel-tasks/set-offline/StorePlayer/close-connection sequence). Java reaches the live connections by reflecting the static `GameServer.nioServer` field + walking selector keys; the C# reactor (held privately in `GameServerHostedService`) now exposes the same observable connection set via a **NioServer singleton bridge** (`RegisterInstance`/`GetRegisteredInstance` registered at boot) + public `GetAllConnections()` (= the existing private `FindAllConnections` selector-key-attachment walk) — gameplay-faithful-infra-idiomatic. All deps faithful: `PlayerService.StorePlayer`, `AionConnection.GetActivePlayer/SetActivePlayer/Close`, `Player.GetController().CancelAllTasks()`/`GetCommonData().SetOnline(false)`/`Get/SetClientConnection`. **PORTED PRIOR TICK (2 + deps):** **`Configure.cs`** (`//configure`; list/get/set config values via reflection over the migrated `[Property]`/`[Properties]` static config-holder fields — dep added: `Config.GetClasses()` returning the full `CONFIGS` list; uses existing `ConfigurableProcessor.Transform(string,Type)` for scalars and `MapTransformer.Transform(IDictionary,Type)` for `[Properties]` fields; faithful `toMap`/`getFieldValue` array-formatting; `getSimpleName().toLowerCase().replace("config","")`→`Type.Name.ToLower().Replace("config","")`; `field.set/get(null)`→`FieldInfo.SetValue/GetValue(null)` over `Public|NonPublic|Static|DeclaredOnly`; `TransformationException.getCause().getMessage()`→`.InnerException?.Message`), **`Grant.cs`** (`//grant`; a/m access/membership level grant via `LoginServer.GetInstance().SendLsControlPacket(type,level,player,admin)` — deps added: faithful `SM_LS_CONTROL.cs` LoginServerPacket (opcode 0x05, writeC type/param + writeD accountId/adminId, mirroring the existing `SM_BAN` opcode-as-first-payload-byte convention) + `LoginServer.SendLsControlPacket(int,int,Player,Player)`; `World.GetInstance().GetPlayer(name)`, `admin.GetTarget() is Player`, `GetAccount().GetAccessLevel()`, `SM_SYSTEM_MESSAGE.STR_NO_SUCH_USER/STR_INVALID_TARGET` all faithful). **PORTED PRIOR TICK** (→ `dotnetConversion/src/Aion.GameServer/Handlers/AdminCommands/`): **`Delete.cs`** (`//delete`; target/range delete via `KnownList.ForEachObject`+`PositionUtil.IsInRange`, `SpawnTemplate.HasPool()`, `GetType()==typeof(SpawnTemplate)` special-spawn guard, `Controller.Delete()`, `SPAWNS_DATA.SaveSpawn(obj,true)`), **`SpawnNpc.cs`** (`//spawn`; npc/gatherable temp+respawn spawn via `SpawnEngine.NewSpawn`/`SpawnObject`/`SaveSpawn`, plus house-object branch with faithful nested `DummyHouseObject : HouseObject<PlaceableHouseObject>` using `IDFactory.NextId()` + `World.CreatePosition` + `BringIntoWorld`), **`SpawnUpdate.cs`** (`//spawnu`; x/y/z/h/xyz/xyzh coord edit + `w` walker-id assign with `WALKER_DATA` pool-limit check via `GetSpawnsByWorldId`.SelectMany; sends `SM_NPC_INFO`/`SM_GATHERABLE_INFO`/`SM_DELETE`), **`Send.cs`** (`//send`; custom-packet XML player via `JAXBUtil.Deserialize<Packets>` + `SM_CUSTOM_PACKET`/`PacketElementType.GetByCode`/`AddElement` + `ThreadPoolManager.Schedule(Action,long)`; faithful nested `Packets:IEnumerable<Packet>`/`Packet`/`Part` XML model; `Integer.decode`→inlined `Decode` for opcode hex/oct/dec; `Part.getType()`→`GetTypeChar()` because `object.GetType()` is reserved). All auto-register via the reflection ChatCommandsLoader (public `ChatCommand` subclasses). **DEFERRED (2, each on a genuinely-missing pillar — not half-ported, no stub):** **Info** — extremely deep display surface (full `PlayerGameStats` ~60 stat getters incl. CalculationType.DISPLAY main/off-hand attack, elemental defenses, PvP ratios, cast-time boost; `AggroList` iteration with per-race damage tallies; `SiegeService.findFortress`; `TownService.getTownIdByPosition`/`getTownResidence`; `PositionUtil.getDirectionalBound`; `NpcFaction`/`ENpcFactionQuestState`; `Npc.getAi().getState()/getSubState()`, `WalkerGroup.getClusterData`); high fidelity risk, gated on the stat/siege/town/AI-state display pillars — needs a per-getter audit before a faithful port. **FixPath** — core is a client-geo polling loop using Java `scheduleAtFixedRate(...).get(5,SECONDS)` blocking-with-timeout, distinguishing `TimeoutException` (abort) from `CancellationException` (Z change detected → success); the C# `ScheduledTask.Get()` has no timeout overload and `ScheduleAtFixedRate` returns a plain `Task` (no self-cancellable `ScheduledFuture` handle), so the timeout-vs-cancel correctness needs a small await-with-CTS-timeout harness on the thread-pool substrate (`WalkerData.SaveData`/`RouteStep.SetZ`/`WALKER_DATA`/`TeleportService.TeleportTo`/`MovementMask` all already exist — only the blocking-poll-with-timed-get seam blocks it). **Remaining unported admincommand count: 2 of the original 11 (Info, FixPath). admincommands 101/103.** Build 0, golden 221/221, suite 500/500, bootstrap 9/9, `check_fidelity` clean.
  -->
- **D4. Unported playercommand content handlers — DONE (2026-06-17). playercommands now 16/16; the last 2 (`Decompose` + `Preview`) ported 1:1 this tick, all deps faithful, 0 fidelity concerns.** (→ `dotnetConversion/src/Aion.GameServer/Handlers/PlayerCommands/`, auto-registered via the reflection ChatCommandsLoader.) **`Decompose.cs`** (`.decompose <item> [count]`; finds first inventory item by id, rejects non-`DecomposeAction` / selectable-decomposable items, then schedules a repeating `ItemUseObserver`-guarded decompose loop). Deps all faithful: `ChatUtil.GetItemId`/`Item`, `Inventory.GetFirstItemByItemId`/`GetItemCountByItemId`, `ItemActions.GetItemActions().OfType<DecomposeAction>()`, `DECOMPOSABLE_ITEMS_DATA.GetSelectableItems`, `DecomposeAction.CanAct`/`Act`/`USAGE_DELAY`, `SM_SYSTEM_MESSAGE.STR_DECOMPOSE_ITEM_NO_TARGET_ITEM`/`STR_DECOMPOSE_ITEM_IT_CAN_NOT_BE_DECOMPOSED`, `Controller.AddTask`/`CancelTask(TaskId.SKILL_USE)`, `ObserveController.AddObserver`/`RemoveObserver`/`NotifyItemuseObservers`, `ItemUseObserver`. Faithful-infra notes: Java anonymous `Runnable` + captured-state `ItemUseObserver` → a private `DecomposeTask` class holding `remainingCount`/`totalCount`/`observer` with a nested `DecomposeObserver:ItemUseObserver`; `ThreadPoolManager.scheduleAtFixedRate(r,10,USAGE_DELAY+100)` → `ScheduleAtFixedRateTask(_=>{Run();...}, 10ms, (USAGE_DELAY+100)ms)`; `Long.MAX_VALUE` → `long.MaxValue`. **`Preview.cs`** (`.preview <emotion card|color|item(s) [color]>`; emotion-card preview via `SM_EMOTION`, else equipment/dye preview via fake `Item(0,...)` list + `SM_CUSTOM_SETTINGS`/`SM_UPDATE_PLAYER_APPEARANCE`/`SM_RIDE_ROBOT`, with a scheduled appearance-reset after `PREVIEW_TIME_SECONDS`). Deps all faithful: `EmotionLearnAction.GetEmotionId`, `CreatureState.POWERSHARD/ACTIVE` (bitmask via `(int)`), `player.GetTarget()`/`GetState()`/`GetOppositeRace()`/`GetGender()`/`IsInRobotMode()`/`GetRobotId()`/`GetEquipment().GetEquippedForAppearance()`/`GetPlayerSettings().GetDisplay()/GetDeny()`, `ItemTemplate.GetActions/GetEquipmentType/GetRace/GetUseLimits().GetGenderPermitted/GetItemSlot/IsItemSet/GetItemSet/IsItemDyePermitted/IsTwoHandWeapon/IsOneHandWeapon/GetItemGroup/GetRobotId/GetTemplateId/GetL10n`, `ItemSlotExtensions.IsVisible`/`GetSlotIdMask` + `ItemSlot.HELMET/PLUME/MAIN_HAND`, `EquipType.NONE`, `ItemGroup.KEYBLADE`, `ItemSetTemplate.GetItempart()`/`ItemPart.GetItemId`, `ItemActions.GetDyeAction().GetColor()`, `Item(0,template,1,true,slotMask)` ctor + `SetItemColor(int?)`/`GetEquipmentSlot`, all 6 `SM_SYSTEM_MESSAGE.STR_*` preview/dye/skin strings. Faithful notes: Java `Color.class.getField(NAME).getRGB()` reflection → `typeof(System.Drawing.Color).GetProperty(TitleCase(name)).GetValue(null)).ToArgb()` (java.awt named-color static fields → System.Drawing named-color static properties; `getRGB()`/`ToArgb()` both ARGB-int); `Integer.valueOf(s,16)` → `Convert.ToInt32(s,16)`; `Long.lowestOneBit(x)` → `x & -x`; `String.format("%06X", c & 0xFFFFFF)` → `(c & 0xFFFFFF).ToString("X6")`; the `param.split(",|…[…]…")` item-tag regex ported verbatim via `Regex.Split`; `ConcurrentHashMap.compute` reset-task map → `ConcurrentDictionary.AddOrUpdate` of `ScheduledTask`; static `record ItemParam` → private sealed class. **No deferrals — playercommands content surface is now complete (16/16).** Build 0, golden 221/221, suite 500/500, bootstrap 9/9, `check_fidelity` clean. **Overall remaining unported content = NONE — Info + FixPath landed (per D3); content parity is COMPLETE across quests, AI, and all player/console/admin chat commands.**

---

## E. Hygiene / tooling (small, keeps the audit honest)

- **E1. Delete stray slop-named Java-tree files — DONE (2026-06-17).** The 2 named seeds (`FindGroupMutationPostTraceCaptureHooks`, `PetFeedUnusualStorageArtifactCapture`) turned out to be the heads of an invented "parity-artifact / trace-capture" scaffold that has NO real-aionemu counterpart — confirmed tree contamination, not faithful source. Deleted 3 `src/` slop files (`services/findgroup/FindGroupMutationPostTraceCaptureHooks.java`, `services/toypet/PetFeedUnusualStorageArtifactCapture.java`, `configs/main/PetFeedUnusualStorageArtifactCaptureConfig.java`) + the entire 8-file `test/.../findgroup/FindGroupMutationPostTraceCapture*.java` family (ArtifactValidator/ArtifactWriter/HookPlacementPreflight/InMemoryArtifactBridge/Instrumentation/ScenarioBuilder/Serializer/Test) + all compiled `target/**/*.class` artifacts. The slop had injected hook calls into 5 FAITHFUL files; excised them to restore real-aionemu state: `FindGroupService.java` (4 `record*` hook calls), `GameServer.java` (import + `installIfEnabled()` seam), `ShutdownHook.java` (import + `shutdown()` call), `ItemPacketService.java` (import + `registerStorageUpdate()` call), `Config.java` (removed `PetFeedUnusualStorageArtifactCaptureConfig.class` from the `CONFIGS` list). **C# side was already clean** — no counterpart was ever ported (`FindGroupService.cs` carried an explicit "Golden-capture …Hooks calls (no-op instrumentation, not Aion gameplay) omitted" breadcrumb; trimmed the now-dangling class name from that comment). Grep confirms **0 remaining refs** in `game-server/**/*.java` and `dotnetConversion/src`. C# build 0, golden 221/221, suite 500/500, `check_fidelity` clean.
- **E2. Audit-tool hygiene — DONE (2026-06-17).** Taught `scripts/parity/check_fidelity.py` + `structural_audit.py` to ALSO index `game-server/data/handlers/` as a Java truth-root (`JAVA_ROOTS = [JAVA_ROOT, JAVA_HANDLERS_ROOT]`): `java_type_norms`/`java_oversized_norms` (fidelity) and `collect_java` (audit) now walk both trees. Content-script class names (`OphidanBridgeInstance`, `_4217TheImprisonedExecutor`, `_21217NewResearchPlan`, `ScaldingExecutorAI`, `WaveEventExecutorAI`, …) are now recognized as faithful 1:1 ports. **check_fidelity went from FAILING (6 false-positive banned-vocab "new slop" flags) → CLEAN (0/0, no baseline change needed).** Handler classes are bucketed under a `handlers/<sub>` area in the scorecard so they do NOT pollute the engine-gap (`ai`/`services`/…) buckets. The slop heuristic for the real `src/` tree is UNCHANGED (src/ still indexed exactly as before; only the faithful-name set is widened). Regenerated `docs/Structural-Audit-Scorecard.md`: Java types indexed 2456→**4178**, exact-name matches →**3935**; the orphan table no longer lists any content script (remaining orphans are genuine C# infra — login `Sm*Auth` packets, `Atomic*` shims, extension methods, repositories).
- **E3. "Missing" engine-class false positives — DONE (2026-06-17). 27/27 FALSE POSITIVE, 0 real gaps, 0 ported.** Triaged every one of the 27 audit-flagged "missing" Java engine/service classes (services 13, skillengine 7, ai 4, controllers 3). **All 27 are present in C#** — the audit was systematically over-flagging because `collect_cs` indexed only the C# file *stem* while `collect_java` indexes every inner `class/enum/interface/record`. Two root causes: **(1) case-only renames** (the port PascalCases Java acronyms: `AIState`→`AiState`, `AISubState`→`AiSubState`, `AIEventType`→`AiEventType`) and **(2) nested/secondary types folded into their parent file** (the C# port faithfully keeps Java's one-file-many-types layout, so a Java inner enum/Runnable lands inside its parent `.cs`, not a same-name file). Verdict table (Java class → status[C# location]):

  | area | Java class (Java file) | verdict | C# location |
  | --- | --- | --- | --- |
  | ai | `AIState` (ai/AIState.java) | FALSE-POSITIVE (case rename) | `Ai/AiState.cs` |
  | ai | `AISubState` (ai/AISubState.java) | FALSE-POSITIVE (case rename) | `Ai/AiSubState.cs` |
  | ai | `AIEventType` (ai/event/AIEventType.java) | FALSE-POSITIVE (case rename) | `Ai/Event/AiEventType.cs` |
  | ai | `PhaseHandler` (ai/HpPhases.java) | FALSE-POSITIVE (nested interface) | `Ai/HpPhases.cs` (`interface PhaseHandler`) |
  | controllers | `CheckType` (controllers/observer/AbstractCollisionObserver.java) | FALSE-POSITIVE (nested enum) | `Controllers/Observer/AbstractCollisionObserver.cs` |
  | controllers | `MovementModifierDirection` (controllers/movement/PlayableMoveController.java) | FALSE-POSITIVE (nested enum) | `Controllers/Movement/PlayableMoveController.cs` |
  | controllers | `PetUpdateTask` (controllers/PetController.java) | FALSE-POSITIVE (nested class) | `Controllers/PetController.cs` |
  | services | `AbyssSkills` (services/abyss/AbyssSkillService.java) | FALSE-POSITIVE (secondary class) | `Services/Abyss/AbyssSkillService.cs` |
  | services | `BrokerOpSaveTask` (services/BrokerService.java) | FALSE-POSITIVE (nested class) | `Services/BrokerService.cs` |
  | services | `BrokerPeriodicTaskManager` (services/BrokerService.java) | FALSE-POSITIVE (nested class) | `Services/BrokerService.cs` |
  | services | `GeneralUpdateTask` (services/player/PlayerEnterWorldService.java) | FALSE-POSITIVE (secondary class) | `Services/Player/PlayerEnterWorldService.cs` |
  | services | `ItemAddType` (services/item/ItemPacketService.java) | FALSE-POSITIVE (nested enum) | `Services/Item/ItemPacketService.cs` |
  | services | `ItemDeleteType` (services/item/ItemPacketService.java) | FALSE-POSITIVE (nested enum) | `Services/Item/ItemPacketService.cs` |
  | services | `ItemUpdatePredicate` (services/item/ItemService.java) | FALSE-POSITIVE (nested class) | `Services/Item/ItemService.cs` |
  | services | `ItemUpdateTask` (services/player/PlayerEnterWorldService.java) | FALSE-POSITIVE (secondary class) | `Services/Player/PlayerEnterWorldService.cs` |
  | services | `ItemUpdateType` (services/item/ItemPacketService.java) | FALSE-POSITIVE (nested enum) | `Services/Item/ItemPacketService.cs` |
  | services | `MaxLevelReward` (services/reward/WebRewardService.java) | FALSE-POSITIVE (nested class) | `Services/Reward/WebRewardService.cs` |
  | services | `PunishmentType` (services/PunishmentService.java) | FALSE-POSITIVE (nested enum) | `Services/PunishmentService.cs` |
  | services | `RespawnTask` (services/RespawnService.java) | FALSE-POSITIVE (nested class) | `Services/RespawnService.cs` |
  | services | `TaskUpdate` (services/SurveyService.java) | FALSE-POSITIVE (nested class) | `Services/SurveyService.cs` |
  | skillengine | `CastState` (skillengine/properties/Properties.java) | FALSE-POSITIVE (nested enum) | `SkillEngine/Properties/Properties.cs` |
  | skillengine | `ValidationResult` (skillengine/properties/Properties.java) | FALSE-POSITIVE (nested class) | `SkillEngine/Properties/Properties.cs` |
  | skillengine | `ConfuseTask` (skillengine/effect/ConfuseEffect.java) | FALSE-POSITIVE (nested class) | `SkillEngine/Effect/ConfuseEffect.cs` |
  | skillengine | `FearTask` (skillengine/effect/FearEffect.java) | FALSE-POSITIVE (nested class) | `SkillEngine/Effect/FearEffect.cs` |
  | skillengine | `ForceType` (skillengine/model/Effect.java) | FALSE-POSITIVE (nested class) | `SkillEngine/Model/Effect.cs` |
  | skillengine | `ResourceType` (skillengine/model/EffectReserved.java) | FALSE-POSITIVE (nested enum) | `SkillEngine/Model/EffectReserved.cs` |
  | skillengine | `SkillMethod` (skillengine/model/Skill.java) | FALSE-POSITIVE (nested enum) | `SkillEngine/Model/Skill.cs` |

  **Audit-tool refined (NOT weakening slop detection):** added `CS_TYPE_RE` + made `collect_cs` index nested C# type names per file, and changed ONLY the `missing` (gap-table) computation to match case-insensitively against the union of C# file-stems + nested type names. The orphan-stem and explosion-cluster slop heuristics are UNCHANGED (they still key off file stems exactly as before — verified: orphan table byte-identical, explosion `cs_files`/`java_classes`/`ratio` numbers unchanged; only the illustrative `Java match` sample column reordered). Regenerated `docs/Structural-Audit-Scorecard.md`: **missing 27 → 0.** Build 0, golden 221/221, suite 500/500, bootstrap 9/9, `check_fidelity` clean.

---

## F. [USER-GATED] Live-client enter-world test — the ultimate parity proof

The one test that exercises the *real* end-to-end loop (client → login → enter-world → gameplay). **Environment-gated: needs the user's actual Aion 4.8 client.** The 3-server stack boots (LS/GS/CS authed, GS↔LS bridge stable); the only missing step is the real client connection. This is the gate on declaring *live* parity (vs structural+golden parity). Not autonomously doable.

---

## G. Deferred-data / runtime-load completeness — verify

Reconcile `datamanager-data-placeholders` (noted QUEST_DATA + 4 holders once wired EMPTY for compile-convergence) against `hollow-holder-census` (later: all 96 accessors delegate to loaded `SD.*Dh`). Confirm by boot-assert that every DataManager holder a live consumer reads is non-empty at boot; wire any still-empty placeholder. Believed DONE (RealStaticDataLoad + bootstrap tests assert real loads), but verify exhaustively since "skip nothing."

- **G re-check — DONE (2026-06-17). ZERO hollow holders remain; 0 silently-dead features from this axis; nothing to wire this tick.** Re-scanned `Dataholders/DataManager.cs`: all **93** static `*_DATA`/`*_TABLE` accessors are `=> SD.<Holder>` delegations (none is an inline `{ get; } = new()` placeholder). Traced every delegation target on `Dataholders/StaticData.cs` to its loader: **83** are populated by `LoadLeafHoldersFromFiles` via the generic `TryLoad(Merged)Holder`; **4** via dedicated `TryLoad*` helpers also called inside `LoadLeafHoldersFromFiles` (`AiDataDh`←`TryLoadAiData`, `DecomposableItemsDataDh`←`TryLoadDecomposableItems` (depends on `ItemDataDh`), `PlayerInitialDataDh`←`TryLoadPlayerInitialData` (depends on `ItemDataDh`), `WorldMaps2`←`TryLoadWorldMaps`); **6** are constructor-injected from the main `static_data.xml` parse in `XmlDataLoader` (`PlayerExperienceTable`, `ItemRandomBonuses`, `ItemSets`, `EnchantTemplates`, `WalkerVersions`, `PetSkills`). Set-difference proof: every `{ get; private set; } = new()` holder field on `StaticData` is reassigned by a loader (the only 4 the generic-pattern grep missed — `AiDataDh`/`DecomposableItemsDataDh`/`PlayerInitialDataDh`/`WorldMaps2` — use the named `TryLoad*` helpers above), and the 6 ctor-injected ones are `get;`-only (no `new()` placeholder). The 13 originally-hollow holders from `hollow-holder-census` are all now wired (GLOBAL_DROP/GLOBAL_EXCLUSION/INSTANCE_EXIT/STATICDOOR/NPC/ITEM/SKILL + the SKILL_TREE/Portal2/ItemRandomBonus/Tempering/Challenge/CustomDrop/Housing batch). Boot-asserted by `RealStaticDataLoadIntegrationTests` + `GameServerBootstrapTests` (9/9). **Structural-hollowness axis: CLOSED.** (Note: a holder whose source XML *file* is absent at boot would fall back to its empty `new()` via `TryLoadHolder`'s file-exists guard — but that is a deployment-data concern, not a code wiring gap; with the real `game-server/data/static_data` tree present, all load.)

---

## H. Instance handlers — 78/78 ✅ DONE (2026-06-18) — content parity complete

The instance-handler set was long recorded as "37/37 complete," but a Java-vs-C# basename set-diff shows the Java `game-server/data/handlers/instance/` dir has **78 `.java`, not 37**. 37 PvE dungeon handlers are ported & green (Haramel … ShugoImperialTomb).

**2026-06-18 — FINAL ARENA CHAIN PORTED (7 leaves) → INSTANCE 78/78 COMPLETE.** Commit `6b25b98f6`. Ported the full pvparenas/ chain 1:1 (bases-first, depth-first): 3 intermediate TrainingGrounds bases `ChaosTrainingGroundsInstance` (300420000), `DisciplineTrainingGroundsInstance` (300430000), `HarmonyTrainingGroundsInstance` (300570000) all `: PvPArenaInstance`; then the 3 leaves `ArenaOfChaosInstance` (300350000 `: ChaosTrainingGroundsInstance`), `ArenaOfDisciplineInstance` (300360000 `: DisciplineTrainingGroundsInstance`), `ArenaOfHarmonyInstance` (300450000 `: HarmonyTrainingGroundsInstance`); plus standalone `ArenaOfGloryInstance` (300550000 `: PvPArenaInstance`). All 1:1, build 0, golden 221/221, GameServer 500/0, bootstrap 9/9, fidelity clean. **No new deps or SM strings needed** — every dependency already existed faithfully: ArenaScoreWriter, HarmonyScoreWriter, HarmonyArenaScore (`GetGroupReward`/`GetHarmonyGroupInside`), HarmonyGroupReward, FlyRing/FlyRingTemplate/Point3D (double-arg ctor), SM_INSTANCE_SCORE (3 overloads incl. `(mapId, InstanceScoreWriter, int time)`), Rates.Get(player, float[])+RatesConfig.PVP_ARENA_{CHAOS,DISCIPLINE,HARMONY,GLORY}_REWARD_RATES, RewardItem(int,long), PvPArenaPlayerReward.SetRewardItem1/2, Gatherable, Npc.GetController().DeleteAndScheduleRespawn()/IsSpawned()/GetNpcId(), instance.GetMapId()/GetInstanceId()/ForEachPlayer. Idiom notes: Java enhanced-switch `case 0,1 ->` → C# `switch` *expression* `0 or 1 => …` for the int/float-returning overrides (`GetBoostMoraleEffectDuration`/`GetRunnerUpScoreMod`/`GetRewardRate`/`GetBaseRewards*`); nested item-reward switches → C# `switch` *statement* with explicit `break`; `PvPArenaInstance.SendPacket(Player, InstanceScoreType?)` nullable-enum signature carried through every leaf override (Harmony passes `scoreType.Value` since it always supplies a value); `FlyRingTemplate` ctor radius 6 (Chaos) / 3 (Harmony) preserved; Discipline `setScoreCaps` had no Java `@Override` but does override the virtual base → `override` in C#.

**Per-area final set-diff (basename `comm -23`, run 2026-06-18):** quest = EMPTY · ai = `PadmarashkaAI` (basename collision: `ai/instance/padmarashkasCave/PadmarashkaAI.java` ↔ ported `PadmarashkaCaveAI.cs`/`PadmarashkaEggAI.cs`, PLUS a *second* distinct `ai/worlds/gelkmaros/PadmarashkaAI.java` — out-of-scope AI residual, not an instance gap) · instance = `TheShugoEmperorsVault` (covered by `TheShugoEmperorsVaultInstance.cs`, same InstanceID 301400000 — `Instance`-suffix naming diff only, genuinely 78/78) · zone = EMPTY · consolecommands = EMPTY (`Teleportto` ported 2026-06-18 — see below) · playercommands = EMPTY · admincommands = EMPTY. **Re-run 2026-06-18 after the Teleportto port: every area is EMPTY modulo the two known naming-suffix non-gaps — `instance = TheShugoEmperorsVault` (→ `TheShugoEmperorsVaultInstance.cs`) and `ai = PadmarashkaAI` (→ `GelkmarosPadmarashkaAI.cs`, the `ai/worlds/gelkmaros` variant). CONTENT PARITY IS NOW TRULY COMPLETE ACROSS ALL AREAS.**

**Teleportto — DONE (2026-06-18). The LAST unported content handler anywhere. consolecommands now 35/35 truly.** (→ `dotnetConversion/src/Aion.GameServer/Handlers/ConsoleCommands/Teleportto.cs`.) `//`-less GM-console wrapper for the "Transport" button; delegates 1:1 to the `//goto` admincommand — `GetSyntaxInfo()` returns GoTo's syntax with the alias `.Replace`d, `Execute` calls `ChatProcessor.GetInstance().GetCommand<GoTo>().Execute(admin, paramsArr)`. **Two base-API points handled faithfully (minimal change):** (1) `ChatCommand.GetSyntaxInfo()` made `virtual` (Java's is overridable; the field-based `SetSyntaxInfo` path is unaffected). (2) Java widens `GoTo.execute` from the `protected abstract` base to `public` so the cross-package wrapper can call it; C# cannot widen visibility on an override, so `ChatCommand.Execute` was promoted from `protected abstract` to `public abstract` and the 153 `protected override void Execute` handler overrides were mechanically updated to `public override` (same method, only the C#-vs-Java visibility-widening mechanism differs — no behavior change; the Cron `ExecuteTask` overrides are untouched and remain `protected`). Build 0, golden 221/221, suite 500/500, bootstrap 9/9, `check_fidelity` clean.

**2026-06-18 — TIER-1 BASES PORTED (all 5):** `BasicPvpInstance` (pvp/), `PvPArenaInstance` (pvparenas/, abstract), `DredgionInstance` (dredgion/), `CrucibleInstance` (crucible/), `AbstractInnerUpperAbyssInstance` (abyss/, abstract) — all 1:1, build 0, golden 221/221, GameServer 500/0, fidelity clean. **No new deps needed** — every dependency (PvpInstanceScore/PvPArenaScore/CruciblePlayerReward/PvpInstancePlayerReward/PvPArenaPlayerReward/HarmonyGroupReward/ArenaRewardItem, all score writers, DamageList/DamageInfo/TeamDamageList, DredgionRoom, WalkManager/NpcAI, SkillEngine, services) already existed faithfully. Idiom notes for the leaf batches: service namespaces are **plural** (`Services.Items.ItemService`, `Services.Players.PlayerReviveService`) despite singular folders; `Rates.AP_DREDGION.CalcResult(player,int)` has an int overload; `AbyssRank.Rank` int property replaces Java `getRank().getId()`; `TemporaryPlayerTeam<?>` wildcard → non-generic `TemporaryPlayerTeam` base (has `GetLeaderObject()`/`GetOnlineMembers()`); `DamageInfo<?>` collection can't be a single var (creature `DamageInfo<Creature>` vs team `DamageInfo<AionObject>` are invariant) → branch the loop on `shouldMergeGroupDamage()` and factor a per-damage helper taking `AionObject attacker`; `scheduleAtFixedRate` → `ScheduleAtFixedRateTask(_=>{…;return ValueTask.CompletedTask;}, TimeSpan, TimeSpan)`; `PvPArenaInstance.sendPacket(Player, InstanceScoreType)` made the enum param **`InstanceScoreType?`** so `SendPacket(null, null)` compiles (leaf overrides must match the nullable signature).

**2026-06-18 — STANDALONE LEAF BATCH 1 PORTED (8 leaves, all `: GeneralInstanceHandler`):** `AsteriaInstance` (300050000), `RoahInstance` (300070000), `RightWingChamberInstance` (300090000), `SulfurTreeNestInstance` (300060000), `LeftWingChamberInstance` (300080000), `SteelRakeInstance` (300100000), `SteelRakeCabineInstance` (300460000), `EsoterraceInstance` (300250000) — all 1:1, build 0, golden 221/221, GameServer 500/0, bootstrap 9/9, fidelity clean. **No new deps or SM strings needed** — FlyRing/FlyRingTemplate/Point3D, SM_QUEST_ACTION(questId,timer), Rnd.Chance()/Get(int,int)/Get(int[])/NextBoolean(), TeleportService.MoveToInstanceExit/TeleportTo, PlayerReviveService.Revive, instance.SetDoorState/GetNpcs, PacketSendUtility.BroadcastMessage(npc,msgId), ZoneName.Get, and SM strings (STR_MSG_INSTANCE_START_IDABRE / STR_REBIRTH_MASSAGE_ME / STR_MSG_IDF4Re_Drana_03/04/05/07/08 / SM_SYSTEM_MESSAGE(1400919)) all already existed. Idiom notes: Java `AtomicLong startTime.compareAndSet(0, now)` → `long startTime` field + `Interlocked.CompareExchange(ref startTime, DateTimeOffset.UtcNow.ToUnixTimeMilliseconds(), 0) == 0`, with `Volatile.Read(ref startTime)` for the getter; `ThreadPoolManager.getInstance().schedule(()->…, 900000)` → `.Schedule(_ => { …; return ValueTask.CompletedTask; }, 900000L)` (long-delay overload); `stream().allMatch(Creature::isDead)` → `.All(c => c.IsDead())`; `getObjectTemplate().getTemplateId()` 1:1.

**2026-06-18 — ABYSS BARRACKS/CHAMBER + SPLINTER BATCH PORTED (12 leaves):** `KrotanBarracks` (301300000), `KysisBarracks` (301280000), `MirenBarracks` (301290000), `KrotanChamber` (300140000), `KysisChamber` (300120000), `MirenChamber` (300130000) — all `: AbstractInnerUpperAbyssInstance`; `LegionsKrotanBarracks` (301260000, `: KrotanBarracks`), `LegionsKysisBarracks` (301240000, `: KysisBarracks`), `LegionsMirenBarracks` (301250000, `: MirenBarracks`) — Legion variants override only `GetChestId()`; `AbyssalSplinterInstance` (300220000), `UnstableSplinterpathInstance` (300600000), `TheHexwayInstance` (300700000) — all `: GeneralInstanceHandler`. All 1:1, build 0, golden 221/221, GameServer 500/0, bootstrap 9/9, fidelity clean. **`TheShugoEmperorsVault` confirmed already ported** (existing `TheShugoEmperorsVaultInstance.cs`, InstanceID 301400000, full 641-line 1:1) — skipped to avoid dup. **No new deps or SM strings needed** — all 8 `STR_MSG_IDAbRe_Core_*` strings (NmdDH/NmdD_Wakeup, NmdC_Light/Dark_Die, NmdC_BoxSpawn, Artifact_Die_01/02/03), ChatType.BRIGHT_YELLOW_CENTER, SM_MESSAGE(int,string,string,ChatType), PositionUtil.IsInRange, EffectController.RemoveEffect, LifeStats.GetHpPercentage/GetMaxHp/IsAboutToDie, ItemService.AddItem (Services.Items), SkillEngine.ApplyEffectDirectly, Spawn 6-arg (staticId overload), Die()/Delete() controllers all already existed. Idiom notes: Java `AtomicLongArray` → `long[]` with per-element `Interlocked.CompareExchange`/`Volatile.Read`; `ConcurrentHashMap<Integer,Integer>` → `ConcurrentDictionary<int,int>` (iterate over `new List<int>(Keys)` snapshot for safe removal); `Future<?>[]` → `ScheduledTask[]` (`.IsDone()` method, `.Cancel(true)`); Java `getController().onAttack(c, dmg, null)` (3-arg, null AttackStatus reference) → C# AttackStatus is non-nullable enum so call the full 8-arg overload directly with `null` status (`OnAttack(c, null, TYPE.REGULAR, dmg, true, LOG.REGULAR, (AttackStatus?)null, HopType.DAMAGE)`), exactly mirroring Java's 3-arg expansion; `Rnd.chance() < 12` → `Rnd.Chance() < 12` (float); `switch` expression for the artifact bossId map preserved 1:1.

**2026-06-18 — DREDGION/CRUCIBLE/BASICPVP LEAF BATCH PORTED (9 leaves):** `ChantraDredgionInstance` (300210000), `BaranathDredgionInstance` (300110000), `TerathDredgionInstance` (300440000) — all `: DredgionInstance`; `CrucibleChallengeInstance` (300320000), `EmpyreanCrucibleInstance` (300300000) — both `: CrucibleInstance`; `EngulfedOphidanBridgeInstance` (301210000), `IdgelDomeInstance` (301310000), `IronWallWarfrontInstance` (301220000), `KamarBattlefieldInstance` (301120000) — all `: BasicPvpInstance`. All 1:1, build 0, golden 221/221, GameServer 500/0, fidelity clean. **No new deps or SM strings needed** — every SM string verbatim-present (all `STR_MSG_IDArena_*`/`STR_MSG_IDKamar_*`/`STR_MSG_IDLDF5_*`/`STR_MSG_FORTRESS_RE_*`/`STR_MSG_GuardDarkHero*`/`STR_MSG_GUARDLIGHTHERO*`/`STR_MSG_S2_Saam1_01`/`STR_MSG_S3_*`/`STR_MSG_S7_BookBox_01`/`STR_MSG_INSTANCE_MOVE_BIRTHAREA_*_IDARENA`), all `StageType` enum members, CrucibleScoreWriter, SM_INSTANCE_STAGE_INFO, SM_CUSTOM_SETTINGS, CreatureType.PEACE.GetId(), DropRegistrationService.GetCurrentDropMap/RegDropItem, SkillEngine.GetSkill+UseSkill/UseNoAnimationSkill, SpawnEngine.NewSingleTimeSpawn/SpawnObject, InstanceService.DestroyInstance, Player.IsInAlliance/GetPlayerAllianceGroup().GetObjectId(), reward setters (SetReward1-4/SetBaseGp/GetMythicKunaxEquipment/SetInsignia/SetSpawnPosition/SetPlayerDefeated/SetPlayerLeave), all already existed. Idiom notes: `synchronized(npcs)`→`lock(npcs)`; `Future<?> bonusTimer`→`ScheduledTask` (`.IsCancelled` property, `.Cancel()`); `scheduleAtFixedRate(()->…,init,period)`→`ScheduleAtFixedRateTask(_=>{…;return ValueTask.CompletedTask;}, TimeSpan.FromMilliseconds(init), TimeSpan.FromMilliseconds(period))` (the Func overload is TimeSpan-only, not long); **SkillEngine/SpawnEngine must be fully-qualified inline** (`Aion.GameServer.SkillEngine.SkillEngine.GetInstance()` / `Aion.GameServer.SpawnEngine.SpawnEngine.NewSingleTimeSpawn`) — a `using X = ...SkillEngine.SkillEngine;` alias is shadowed by the same-named namespace and resolves to the namespace (CS0234); `static {}` initializer→`static` ctor; `Rnd.get(List)`→`Rnd.Get(IReadOnlyList)`; `byte timeInMin=-1`→`unchecked((byte)-1)`; `getCurrentDropMap().get(id)`→`.GetCurrentDropMap().GetValueOrDefault(id)` (returns `ISet<DropItem>`); `stageType.getType()`→`.GetType_()` (Object.GetType clash); Java enhanced-switch `case A,B ->`→C# `case A: case B:` fallthrough with explicit `break`; Empyrean inner class `EmpyreanStage.containsNpcs()` takes `instance` param (no implicit outer-instance access in C# nested class).

**0 leaf handlers remain unported — instance set COMPLETE (78/78).** Full breakdown:

- **Dredgion:** ✅ ALL ported — Chantra / Baranath / Terath (`DredgionInstance` base)
- **Crucible:** ✅ ALL ported — Crucible / CrucibleChallenge / EmpyreanCrucible (`CrucibleInstance` base)
- **Arena:** ✅ ALL ported (2026-06-18) — ArenaOfChaos / ArenaOfDiscipline / ArenaOfGlory / ArenaOfHarmony + Chaos/Discipline/Harmony TrainingGrounds (`PvPArenaInstance` base + TrainingGrounds→ArenaOf* chain)
- **Barracks/Chamber:** ✅ ALL ported — Krotan/Kysis/Miren Barracks + Legions{Krotan,Kysis,Miren}Barracks + Krotan/Kysis/Miren Chamber (`AbstractInnerUpperAbyssInstance` base) + Left/RightWingChamber (batch 1)
- **PvP (4) :** ✅ ALL ported — Kamar / IronWall / IdgelDome / EngulfedOphidanBridge (`BasicPvpInstance` base). (✅ also ported: AbyssalSplinter / UnstableSplinterpath / TheHexway / TheShugoEmperorsVault / SulfurTreeNest / SteelRake / SteelRakeCabine / Asteria / Roah / Esoterrace)

**Why this hid so long (tooling blind spot to fix):** `structural_audit.py`'s "Missing High-Value Java" scan is scoped to `['ai','controllers','questEngine','services','skillengine']` only — it indexes `data/handlers/` Java as *faithful-when-a-C#-file-exists* (the E2 fix) but never reports `data/handlers/` Java that has **no** C# counterpart. So unported content handlers are invisible to the audit. **Reliable detection = per-handler-dir basename set-diff:** `comm -23 <(find game-server/data/handlers/<area> -name '*.java' -exec basename {} .java \;|sort -u) <(find dotnetConversion/src/Aion.GameServer/Handlers/<Area> -name '*.cs' -exec basename {} .cs \;|sort -u)`. (Small tooling item: extend the audit to flag missing `data/handlers` counterparts so this can't recur.)

**Portable now (substrate exists):** `GeneralInstanceHandler` base + `[InstanceID]`/`InstanceHandlerClassListener` registration, `InstanceScore`/`PvpInstanceScore`/`InstanceScoreType`, `PvpInstancePlayerReward`, `InstanceScoreWriter`/`PvpInstanceScoreWriter` are all present. Port recipe = same as the 37 done (extend base, `[InstanceID(n)]`, override `On*`/`Handle*`, auto-register), in **layered batches**: port the intermediate/abstract bases first (`BasicPvpInstance`→`PvPArenaInstance`/`DredgionInstance`/`CrucibleInstance`/`AbstractInnerUpperAbyssInstance`) to unblock the leaves, then the leaf handlers ~6-10/batch with all-green-or-revert. Watch for any PvP-score/reward or Dredgion/Arena-specific writer subtype that needs porting first (depth-first).

---

## I. Cross-tree completeness deep-discovery (2026-06-18) — what's *actually* still missing

A full name-based set-diff of **every Java type** (commons + login/chat/game-server `src` + `data/handlers`, ~6,400 types) against the whole C# tree (underscore- and case-insensitive, nested types included). **Verdict: no hidden large gameplay subsystem is missing.** Gameplay engine/model/services/packets/content are structurally complete (DAO **57/57**, content **100%**, gameplay types all present). The residuals fall into these buckets:

**I1. One real functional gap — GS↔LS↔CS bridge admin/ops inbound flows.** The C# GS-side bridge (`Aion.GameServer/Network/LoginServer/LoginServer.cs`) dispatches inbound login-server messages **inline** via an opcode switch (idiomatic vs Java's discrete `CM_*` classes — fine by the infra doctrine), but it only handles **4 opcodes**: `0x00` GS-register-response, `0x01` account-auth-response (`CM_ACOUNT_AUTH_RESPONSE`), `0x08` character-count, `0x0B` `CM_LS_PING`→`SM_LS_PONG`. That's enough to boot, register, keep-alive, and log players in (why the 3-server stack authenticates). **Everything else falls through to a "Unknown login-server packet" warning — unhandled:** `CM_BAN_RESPONSE`, `CM_HDD_BANLIST`, `CM_MACBAN_LIST`, `CM_PREMIUM_RESPONSE`, `CM_PTRANSFER_RESPONSE`, `CM_LS_CONTROL_RESPONSE`, `CM_GS_CHARACTER_RESPONSE`, `CM_ACCOUNT_RECONNECT_KEY`, `CM_REQUEST_KICK_ACCOUNT`. Correspondingly: the **LS-side HDD/MAC-ban subsystem** (`BannedHdd*`/`BannedMac*` controllers+DAOs) and the LS-side login **crypto** classes (`CryptEngine`/`EncryptedRSAKeyPair`/`KeyGen` — by-name) return NONE in `Aion.LoginServer` (auth works, so crypto exists under other names — verify), and `SM_ACCOUNT_LIST` + the GS↔CS bridge `CsClientPacket`/`CsServerPacket` bases + several CS inbound packets are absent. **This is the one concrete area worth a port/verify pass** — it's the cross-server account-management plane (ban/kick/hdd-mac-ban/premium/account-transfer sync), and it ties to the admincommands that are functionally thin (Grant sends `SM_LS_CONTROL` but the GS never processes the `CM_LS_CONTROL_RESPONSE`). Scoped, infra-adjacent, but real for operator/account parity. **Recommend: enumerate the Java LS↔GS↔CS opcode protocol end-to-end and either handle each inbound opcode inline 1:1 or port the discrete packet classes; port the LS HDD/MAC-ban subsystem.**

**I2. Accepted idiomatic-infra divergences (NOT gaps — document so the audit/future-me stops re-flagging).** Per the gameplay-faithful / infra-idiomatic doctrine, these Java infra classes are intentionally replaced by idiomatic C#/.NET equivalents, not ported 1:1: the **runtime Java script-compiler subsystem** (`ScriptCompiler`/`ScriptClassLoader`/`ScriptContext`/`JavaSourceFromString`/`ClassFileManager`/`CompilationResult`/`VirtualClassURL*`) — C# compiles `data/handlers` at build time + auto-registers via reflection, so the entire in-memory-compile machinery is unneeded; **Netty/NIO networking + pipeline** (`NettyServer`/`*PacketDecoder`/`*Encoder`/`*PipelineFactory`/`PacketFrameDecoder`/`AionPacketHandlerFactory`/`NetConnector`) — C# uses its own socket/pipeline; **JAXB/XML infra** (`XmlUtil`/`XmlValidationHandler`/`StaticDataListener`/`StringSchemaOutputResolver`) — replaced by `XmlSerializer` + the C# static-data loader; **threading/executor internals** (`PriorityThreadFactory`/`AionRejectedExecutionHandler`/`RunnableWrapper`/`UncaughtExceptionHandler`/`PacketProcessorTask`/`CheckerTask`/`DeadLockDetector`) — C# `ThreadPoolManager` is a single async scheduler; **logback appenders** (`DiscordChannelAppender`/`OnConsoleWarningStatusListener`/`Logging`) — C# logging; and the login/chat servers' **Controller+DAO → Repository reorg** (`AccountController`/`AccountDAO`→`AccountRepository`-style; `PlayerTransferRepository`/`PremiumRepository` confirmed present). The `Sys` admincommand's threadpool branch already documents this same divergence.

**I3. Minor / low-value items to verify or close:** **Localization `L10n` — VERIFIED COMPLETE (2026-06-18), NOT a gap.** Java `L10n` is an *interface* (templates with a client name/desc ID); the set-diff flagged it only as the same I-prefix rename false-positive (`L10n`→`IL10n`). Fully ported: `IL10n` interface (`GetL10nId()` + default `GetL10n()=>ChatUtil.L10n(id)`); `ChatUtil.L10n(int)` byte-exact (`id==0`→null, `id<<1|1`, 2-char pack via low-16 then unsigned `>>>16`, `"$"` prefix); name-token helpers `Item`/`ItemName`/`Recipe`/`Quest` (`[item:%d]`/`[item_ex:%d]`/…) all 1:1; 115 Java consumers → 151 C# refs; unit-tested (`ChatUtilTests.L10n_ProducesExpectedEncoding`/`_ZeroReturnsNull`). The server sends the encoded client-string token; the client renders the localized name — wire-identical to Java. **`Chance`** (8 Java refs — small enum/util; verify folded). **`Polyline2D`** (1 ref — zone geometry; likely folded into the C# geo polygon types). **Config transformers `InetSocketAddressTransformer`/`TimeZoneTransformer`** (deferred-because-no-holder-needs-them per §C; `ZoneIdTransformer` was ported, these two weren't). **`DDSConverter`** (client-texture tool — not server-needed, skip).

**Other phases?** No new large phase. After I1 (bridge admin/ops) + I2 (document divergences) + I3 (verify/close minors), the only substantive remaining work is the perpetual **runtime-validation depth** (golden/formula expansion — A1 already done: all `SM_*` golden'd or audited) and **F (live-client enter-world test, USER-GATED)**.

---

## Recommended order (no client; skip nothing)

0. ~~**H — port the unported instance handlers**~~ **✅ DONE 2026-06-18 (instance 78/78, content parity complete).** Was the single largest content-parity gap (surfaced 2026-06-17); closed via layered bases→leaves batches.
0b. **I1 — GS↔LS↔CS bridge admin/ops inbound completeness** (new, found 2026-06-18): handle the unhandled inbound LS opcodes (ban/kick/hdd-macban/premium/ptransfer/control-response) + port the LS HDD/MAC-ban subsystem. The one real functional gap from the deep-discovery. **I2 — document the accepted idiomatic-infra divergences** (no code). **I3 — verify L10n completeness + close Chance/Polyline2D/transformers.**
1. **A1 integration harness + golden the ~103 live-object packets** — the main provable-parity vein; build the harness once, then grind packets (also re-confirms every live-object writer byte-exact, likely surfacing any remaining latent bugs as the 3 already found did). *(A1 itself is now DONE — every SM_* golden'd or audited; see A1 status below.)*
2. ~~B triage~~ **DONE (2026-06-17): all 14 NotSupported throw sites verified FAITHFUL GUARDS vs Java — 0 real gaps, 0 bugs, breadcrumbed.**
3. **C1 config framework** — systemic; restores `.properties` override fidelity (a hard contract).
4. **G verify holders; D editor-save TODOs; ~~E hygiene + audit-tool~~ E1/E2 DONE (2026-06-17): slop-scaffold deleted + audit tools index data/handlers, guardrail clean; E3 missing-class triage remains** — small, interleave.
5. **C2/C3 TODO triage** — produce the honest faithful-vs-real split, resolve the reals.
6. **A2 formula golden** — thin top-up.
7. **F live-client test** — when the user can run their client. Ultimate parity sign-off.

When A–E + G are clear and F passes, the §10 "project done" definition is met: every Java file has one faithful golden-validated-or-deferred C# counterpart, and the live client proves the runtime loop.
