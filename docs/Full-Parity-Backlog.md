# Full-Parity Backlog — "Skip Nothing"

Date: 2026-06-17. Owner doc for **what remains to reach full 1:1 parity**. Authoritative companion to `Port-Fidelity-Remediation-Plan.md` §0 (which records that Phases A–C are COMPLETE and Phase D is structurally ~complete). This doc enumerates *every* remaining gap — large and small — because for parity **nothing may be skipped**.

Branch `feature/object-spine-bigbang` @ `5c3b84ffa`+batch14 · build 0 · golden 215 test methods byte-exact (0 fidelity bugs) · suite 478/0 · bootstrap 9/9 · full DB-backed boot validated.

## Standing reality (so the backlog isn't misread)

- **De-slop + structural port is DONE.** 126→0 reworked duplicate packets, `GameServerPacket` dropped, object store unified to single `World._allObjects`, all reworked `*Summary`/`*Table`/WorldNpc/Kisk/Rift/Housing slop retired, `GameServerConnection` god-class gone, guardrail baseline empty (0/0). ~92% of Java types (2269/2456) have an exact-name C# file; content scripts 100% (quests 1035 · AI 462 · instance 37 · zone 3 · chat).
- **A faithful Java TODO is parity, not a gap.** Of 277 `TODO`/`FIXME` markers in C# src, the large majority are verbatim copies of TODOs in the Java source (e.g. "most retail npcs lose 364 hate. TODO: find formula"). The doctrine says mirror them; reproducing them *is* 1:1. Only the **C#-specific** subset (config framework, "port the rest") are real gaps — see §C.
- **The only thing that cannot be done here is the live-client test (§F)** — it needs the user's Aion 4.8 client. Everything else in this backlog is workable now.

---

## A. Runtime-parity VALIDATION depth — the #1 driver (bounded, provable without a client)

Structural 1:1 ≠ proven runtime parity. Absent the live client, the Java-oracle golden suite is how we *prove* parity. It is at 196 cases, **0 fidelity bugs across every probe** — strong, but not exhaustive.

- **A1. Un-golden'd `SM_*` packets: ~103 of 240.** 157 packet fixtures cover ~137 distinct faithful packets. The remaining ~103 read *live objects* (Player legion/group/store/flight graph, live World/WorldMapInstance, Summon/Pet/Skill graphs) and need a reusable **integration harness** (uninitialized-object Player/World/connection seams — precedents exist: `PacketHarnessCreature`, scalar/`HarnessPlayer`, DataManager-holder seam, real-`Npc` seam, item/`ItemInfoBlob` seam, GameCrypt Write seam). Build the harness increment, then golden ~1–3 packets/tick, byte-exact vs the mvn oracle. **Largest single remaining provable-parity vein.**
- **A2. Formula golden expansion.** 30 formula fixtures; remaining pure `StatFunctions`/`StatCapUtil`/calc methods that read only args are still capturable. Thin but non-zero.
- **A3. Behavioral/runtime golden gaps** the unit harness can't reach (Rnd-based combat, time-dependent) — document as not-golden-able; they fall to §F (live client) for ultimate proof.

### A1 per-packet triage (computed 2026-06-17, the authoritative un-golden'd list)

Ground truth from disk: **240** faithful `SM_*.cs` in `Network/Aion/ServerPackets` + **6** LoginServer-tree `SM_*.cs` (`SM_BAN`/`SM_HDDBAN_CONTROL`/`SM_PREMIUM_CONTROL`/`SM_PTRANSFER_CONTROL`/`SM_CHANGE_ALLOWED_HDD_SERIAL`/`SM_ACCOUNT_TOLL_INFO` under `Network/LoginServer/ServerPackets`). Distinct names covered by a golden fixture (`parity-artifacts/golden/packets/*.json`) or a dedicated faithful golden/byte test (`SmFindGroupTests`, `SmLegionDominionRankTests`, `SmLegionHistoryTests`, the `Golden*FixtureTests` families) or a `writeImpl audited 1:1` breadcrumb. NOTE: the earlier reconciliation note guessing SM_GAME_TIME/SM_AUTO_GROUP already had dedicated golden tests was WRONG — they do NOT; only SM_FIND_GROUP/SM_LEGION_DOMINION_RANK/SM_LEGION_HISTORY do (correctly excluded below). **Un-validated = 0 — A1 PACKET VALIDATION TRULY COMPLETE (batch 24b, 2026-06-17): whole-tree set-difference (all `src/.../SM_*.cs` MINUS {golden-fixture names ∪ `audited 1:1`-breadcrumb names}) is EMPTY.**

**Batch 24b** (2026-06-17) — closed the final 9 the whole-tree set-difference still listed (the earlier batch-24 sweep was scoped to the Aion tree only and missed the 6 LoginServer packets + 3 dedicated-test-only graph packets): AUDITED 1:1 T2 breadcrumbs, 0 golden delta, 0 fidelity bugs. (1) the 3 graph packets `SM_FIND_GROUP`/`SM_LEGION_DOMINION_RANK`/`SM_LEGION_HISTORY` (already covered by dedicated `Sm*Tests` byte tests; writeImpl re-audited line-by-line — live Player/team/Legion graph reads; `instanceof`→`is`, `getName(true)`→`GetName(true)`, `currentTimeMillis()/1000`→`DateTimeOffset`, `subList`→`GetRange`, `ranking.indexOf`→`IndexOf`, `LegionHistoryAction.Type{LEGION,REWARD,WAREHOUSE}` is plain-sequential so `ordinal()`==`(int)type`; byte-identical). (2) 5 LoginServer-tree scalar/control packets `SM_ACCOUNT_TOLL_INFO` (writeC(0x09)+writeD+writeQ), `SM_BAN` (writeC(0x06)+writeC(type)+writeD+writeS+writeD+writeD), `SM_CHANGE_ALLOWED_HDD_SERIAL` (super 15→writeC(0x0F)+writeD+writeS; ctor reads Account.getId/getAllowedHddSerial→T2), `SM_HDDBAN_CONTROL` (writeC(14)+writeC(BanAction.getId: UNBAN=0/BAN=1==ordinal)+writeS+writeQ), `SM_PREMIUM_CONTROL` (writeC(11)+writeD(req.accountId)+writeD(req.requestId)+writeQ(cost)+writeC(NetworkConfig.GAMESERVER_ID config singleton→T2)); all byte-identical incl. opcode-as-first-payload-byte matching Java `LsServerPacket.write` (len placeholder + `put((byte)opcode)` + writeImpl + back-patch len == C# `WritePayload` opcode-first + `ServerPacketFrameCodec` 2-byte LE len prefix). (3) `SM_PTRANSFER_CONTROL` — PARTIAL/TRACKED DEFERRAL (not a bug): control path OK/ERROR/TASK_STOP byte-identical to Java; the 6 `*_INFORMATION` branches (CHARACTER/ITEMS/DATA/SKILL/RECIPE/QUEST) write only `writeC(type)+writeD(taskId)` because their full bodies read live DB (`InventoryDAO`/`ItemStoneListDAO`) plus Player emotion/motion/title subsystems NOT wired onto the faithful Player (`Player.cs` has no `GetEmotions`/`GetMotions`/`GetTitleList`); completing them is blocked on those unported deps → tracked in §C/§D. The original `Un-validated = 0` for the 240-Aion-tree scope was already correct (batch 24); 24b makes the **whole-tree** set-difference empty and adds belt-and-suspenders breadcrumbs to the 3 dedicated-test graph packets. Build 0, golden 221/221, suite green. A full re-derivation sweep (covered-name set = `parity-artifacts/golden/packets/*.json` ∪ every `SM_*` token in `tests/.../Golden*.cs`+`Sm*Tests.cs`, plus every `SM_*.cs` carrying an `audited 1:1` breadcrumb, diffed against the 240 scoped `find src -path '*ServerPackets/SM_*.cs'`) confirms **all 240 scoped SM_* packets are now either runtime-golden'd or 1:1 audit-breadcrumbed; 0 remain.** The earlier "16" was a stale header never decremented after batches 16–23 added the breadcrumbs. Build 0, golden 221/221. Batch 24 was verification-only: no source change, no new golden case, 0 fidelity bugs. Original batch-23 note** (batch 23 golden'd SM_LOGIN_QUEUE (T1: private no-arg ctor pins fixed 5/60/50, con never read) + SM_SYSTEM_MESSAGE (T1: (int,Object...) ctor -> GOLDEN_YELLOW(25)+null sender; wire-format path validated with scalar params, 28k-line msgId catalog is verified-path data not per-entry) + SM_CREATE_CHARACTER (T1 non-OK branch writeD(responseCode)+return; OK branch delegates to validated writePlayerInfo) — golden 218->221 cases, suite 481->484, 0 fidelity bugs; AND AUDITED 1:1 T2 breadcrumbs SM_CHARACTER_LIST (con.getAccount() live + per-char writePlayerInfo) + SM_L2AUTH_LOGIN_CHECK (writeD(ok) + hex-literal standardData byte-identical + DataManager.WORLD_MAPS_DATA live-holder iteration + writeS(accountName)); all byte-identical, 0 fidelity bugs. batch 22 AUDITED 1:1 T2 breadcrumbs, no golden delta — 6 builder/item-blob/live-graph/DataManager-enum-static-init readers: SM_CUSTOM_PACKET (admin //fsc builder, no plain ctor), SM_PRIVATE_STORE (live PrivateStore/seller Player/Inventory + getFullBlob), SM_UPDATE_PLAYER_APPEARANCE (writeEquippedItems live Item graph: equipmentSlot mask w/ TwoHanded SUB_HAND removal), SM_MAIL_SERVICE (con.getActivePlayer().getMailbox() + Letter/Item/ItemInfoBlob; letters.size()*-1, totalCount+unreadCount*0x10000, (int)(time/1000)), SM_BROKER_SERVICE (live BrokerItem/Item/blob + currentTimeMillis + PlayerService.getPlayerName singleton + settleTime; duplicate-id BrokerPacketType via reference-identity if-chain == Java identity switch; toDays==ms/86400000, settleTime/60000; all 8 writers byte-identical), SM_AUTO_GROUP (DataManager.AUTO_GROUP via AutoGroupType enum static-init; writeImpl scalars + name only, windowId switch case-groupings 0,7/1,3,8/2,4,5/6); all byte-identical field set/order/encoding vs Java, 0 fidelity bugs. batch 21 AUDITED 1:1 T2 breadcrumbs, no golden delta — 5 live-graph/singleton/con readers: SM_KISK_UPDATE (live Kisk graph — 8x writeD scalar getters; Kisk ctor needs NpcController/SpawnTemplate/kiskStatsTemplate, not bounded-constructible), SM_LOOT_ITEMLIST (con.getActivePlayer null-guard + live DropNpc/DropItem/Player graph + DataManager.ITEM_DATA per-drop isTradeable + isOnlyPossibleLooter loot-confirmation logic; (int) cast on count), SM_TRADELIST (ctor: live Player.getLegion + Npc + DataManager.GOODSLIST_DATA legion-level filter + LimitedItemTradeService singleton; TradeNpcType.index() explicit 1-5 == Java), SM_INSTANCE_SCORE (live InstanceScoreWriter graph + InstanceProgressionType.getId() explicit 1/2/3*1024*1024 + writeMe(buf) delegation), SM_ABYSS_ARTIFACT_INFO3 (Collection ctor DTO + int-loc ctor SiegeService singleton; writeImpl locationId*10+1 + ArtifactStatus.getValue() explicit 0-3); all byte-identical field set/order/encoding vs Java, 0 fidelity bugs. batch 20 golden'd SM_HOUSE_SCRIPTS (T1: PlayerScript record + fixed compressedBytes, fully deterministic, 4 cases byte-exact, golden 217->218 / suite 480->481, 0 fidelity bugs) + AUDITED 1:1 T2 breadcrumbs 6 live-graph/singleton readers: SM_HOUSE_RENDER + SM_HOUSE_UPDATE (AbstractHouseInfoPacket.writeCommonInfo: LegionService singleton + live House/registry/emblem graph; PartType.values()/getRooms() order+count match), SM_HOUSE_OBJECTS (live HouseObject graph), SM_SUMMON_PANEL + SM_SUMMON_UPDATE (live Summon/lifeStats/gameStats Stat2 graph; getMode().getId() explicit-value), SM_PET_EMOTE (live Pet/MoveController graph; PetEmote enum value==Java emoteId so (int)emote==getEmoteId()); all byte-identical field set/order/encoding, 0 fidelity bugs. batch 19 AUDITED 1:1 T2 breadcrumbs, no golden delta — 10 con/live-singleton/live-graph readers: SM_IN_GAME_SHOP_CATEGORY_LIST (InGameShopEn singleton), SM_IN_GAME_SHOP_ITEM (InGameShopEn singleton), SM_IN_GAME_SHOP_LIST (live Player.inGameShop + InGameShopEn), SM_HOUSE_OBJECT (con.getActivePlayer + live HouseObject graph), SM_HOUSE_REGISTRY (con.getActivePlayer + live HouseRegistry graph), SM_HOUSE_BIDS (con.getActivePlayer + HousingBidService/HousingService/AuctionEndTask singletons), SM_HOUSE_EDIT (con.getActivePlayer + live House registry), SM_OBJECT_USE_UPDATE (live HouseObject graph), SM_HOUSE_OWNER_INFO (live Player/House graph + HousingService + ServerTime.now time-based), SM_CASTSPELL_RESULT (live Skill/Effect/Creature graph + setLastCounterSkill side-effect); all confirmed byte-identical writeImpl field set/order/encoding vs Java (incl. enum .getId()/.getValue() explicit-value getters, Duration.between(now,nextPay).toDays() == (long)(nextPay-now).TotalDays toward-zero truncation, byte vs int comparison widening), 0 fidelity bugs. batch 18 AUDITED 1:1 T2 breadcrumbs, no golden delta — 8 con/live-singleton/time/live-graph readers: SM_ACCOUNT_PROPERTIES (con.getAccount), SM_KEY (con.enableCryptKey), SM_INSTANCE_INFO (con.getActivePlayer + currentTimeMillis), SM_SIEGE_LOCATION_INFO (con.getActivePlayer + live SiegeService/LegionService), SM_PLAYER_SPAWN (live World singleton), SM_DIE (live Player/WorldMapInstance/Kisk graph), SM_ITEM_COOLDOWN (currentTimeMillis), SM_RECIPE_COOLDOWN (live Player.getCraftCooldowns); all confirmed byte-identical writeImpl field set/order/encoding vs Java (incl. (int)(reuse-now)/1000 cast precedence), 0 fidelity bugs. batch 17 AUDITED 1:1 T2 breadcrumbs, no golden delta — 9 full live-Player/Legion/Group/Alliance graph readers: SM_LEGION_INFO, SM_LEGION_MEMBERLIST, SM_LEGION_ADD_MEMBER, SM_LEGION_UPDATE_MEMBER, SM_GM_SHOW_LEGION_INFO, SM_GROUP_MEMBER_INFO, SM_ALLIANCE_MEMBER_INFO, SM_PRIVATE_STORE_NAME, SM_CHAT_WINDOW; all confirmed byte-identical writeImpl field set/order/encoding vs Java (incl. SkillTargetSlot.ordinal()=Array.IndexOf(Enum.GetValues), PlayerClass.values().length=Enum.GetValues.Length, Legion.Announcement time/1000), 0 fidelity bugs. batch 16 AUDITED 1:1 T2 breadcrumbs, no golden delta — all 8 are audit-only con/live-singleton/time readers: SM_GAME_TIME, SM_TIME_CHECK, SM_VERSION_CHECK, SM_PRICES, SM_DIALOG_WINDOW, SM_FRIEND_UPDATE, SM_CHALLENGE_LIST, SM_PLAY_MOVIE; all confirmed byte-identical writeImpl field set/order/encoding vs Java, 0 fidelity bugs. batch 15 golden'd SM_GATHERABLE_INFO + SM_QUEST_COMPLETED_LIST; golden suite 215->217 test methods, +3 cases byte-exact, 0 fidelity bugs; batch 14 golden'd SM_NPC_ASSEMBLER + SM_GATHER_UPDATE; golden suite 213->215 test methods, +7 cases byte-exact, 0 fidelity bugs; batch 13 golden'd SM_TUNE_RESULT/SM_SKILL_LIST/SM_WAREHOUSE_ADD_ITEM/SM_INVENTORY_INFO). Batch 14 also AUDITED 1:1 (T2 breadcrumbs): SM_INFLUENCE_RATIO, SM_FORTRESS_STATUS, SM_LEGION_DOMINION_LOC_INFO. SM_QUEST_COMPLETED_LIST is now DONE (batch 15) via the QUEST_DATA DataManager-holder seam: `qs.canRepeat()` reads `DataManager.QUEST_DATA.getQuestById(questId)` per entry -> two NON-time-based quests (repeatCycle null -> isTimeBased() false, no clock) reflect-set with id+maxRepeatCount cover both canRepeat branches (255 -> true/writeC 0; completeCount>=max!=255 -> false/writeC 1). SM_FIRST_SHOW_DECOMPOSABLE/SM_SECONDARY_SHOW_DECOMPOSABLE ARE done (fixtures + C# cases exist; the §A "next batch" note below is stale).

Triage key: **T1** = TIER-1 runtime-golden reachable now (writeImpl is `con`-null-safe under `CaptureWriteImplPayload` and reads only scalars / simple DTOs / an existing-or-small-bounded seam); **T1-seam** = reachable via an existing heavy seam (item/ItemInfoBlob, real-Npc, DataManager-holder) — bounded but more work; **T2** = TIER-2 audit-only (writeImpl reads `con.getActivePlayer()`/`con.getAccount()`/`con.enableCryptKey()`, the Java static-final `World` singleton, a live `SiegeService`/`Influence`/`GameTimeService`/`LoginServer`/`GameServer` singleton, `System.currentTimeMillis`/JVM-uptime, or a full live Player/Legion/Group/Alliance graph the unit harness cannot bounded-build) — validate by line-by-line writeImpl audit + breadcrumb.

**T1 — pure scalar / simple-DTO, con-null-safe (golden these first):**
- `SM_CHARACTER_SELECT` — scalar type/messageType/wrongCount + `SecurityConfig.PASSKEY_WRONG_MAXCOUNT` const. **[DONE 2026-06-17 batch 10]**
- `SM_AFTER_SIEGE_LOCINFO_475` — pure const writeImpl (writeH 0 / writeC 0), default ctor. **[DONE batch 10]**
- `SM_NEARBY_QUESTS` — `Map<Integer,Integer>`; `-size&0xFFFF` + per-entry `questId|bit`. **[DONE batch 10]**
- `SM_MACRO_LIST` — playerObjectId + `List<Macros.Macro>` (public record) + clearList; `writeH(-size)`. **[DONE batch 10]**
- `SM_QUEST_COMPLETED_LIST` — updateMode + `List<QuestState>` (constructible ctor). **[DONE batch 10]**
- `SM_FIRST_SHOW_DECOMPOSABLE` / `SM_SECONDARY_SHOW_DECOMPOSABLE` — objectId + `Collection<ResultedItem>` (XML DTO; reflect-set itemId/minCount both sides). **[DONE — fixtures + C# cases exist (generator lines 901-918 + GoldenPacketFixtureTests).]**
- `SM_QUEST_COMPLETED_LIST` — updateMode + `List<QuestState>`. **[DONE 2026-06-17 batch 15]** via the **QUEST_DATA DataManager-holder seam**: `qs.canRepeat()` reads `DataManager.QUEST_DATA.getQuestById(questId)` per entry. Two NON-time-based quest templates (repeatCycle null -> isTimeBased() false, no clock) reflect-set with id+maxRepeatCount cover both branches (max 255 -> canRepeat true/writeC 0; completeCount>=max!=255 -> false/writeC 1); rewriteAll + insert updateModes. First capture byte-exact, 0 fidelity bugs.
- `SM_ABYSS_RANKING_LEGIONS` / `SM_ABYSS_RANKING_PLAYERS` — `List<RankingListLegion/Player>` DTO ctor. **[DONE 2026-06-17 batch 11]** (records con-null-safe; byte-exact first capture.)
- `SM_GM_SHOW_PLAYER_SKILLS` — `List<PlayerSkillEntry>` DTO. **[DONE batch 11]** (PlayerSkillEntry(skillId,skillLvl,skillType,state) ctor; only non-normal entries used so getFlag()'s System-time path never fires -> deterministic.)
- `SM_GM_SHOW_LEGION_MEMBERLIST` — `List<LegionMember>` DTO. **[AUDITED 2026-06-17 batch 11 -> demoted T2]** writeLegionMember calls `PlayerService.getOrLoadPlayerCommonData` (live singleton/DB) -> audit-only breadcrumb, 1:1 confirmed.
- `SM_TOWNS_LIST` — `Map<Integer,Town>`; Town DTO. **[AUDITED 2026-06-17 batch 11 -> demoted T2]** Town ctor (faithful 1:1) runs `spawnNewObjects()` (DataManager+SpawnEngine) + `GeoService.updateTown` -> not bounded-constructible -> audit-only breadcrumb, 1:1 confirmed.
- `SM_MACRO_LIST` (paged) covered above.

**T1-seam — reachable via an existing heavy seam (item/ItemInfoBlob, real-Npc, DataManager-holder), bounded:**
- item/ItemInfoBlob seam: `SM_TUNE_RESULT`, `SM_INVENTORY_INFO`, `SM_WAREHOUSE_ADD_ITEM`, `SM_PRIVATE_STORE`, `SM_MAIL_SERVICE`, `SM_BROKER_SERVICE`, `SM_UPDATE_PLAYER_APPEARANCE` (several also touch `con.getActivePlayer` -> verify per-packet, may demote to T2). **[DONE 2026-06-17 batch 12]** `SM_EXCHANGE_ADD_ITEM`, `SM_WAREHOUSE_INFO`, `SM_WAREHOUSE_UPDATE_ITEM`, `SM_REPURCHASE` runtime-golden'd via the GENERAL_INFO simple-item seam (player null = blob owner only; `SM_REPURCHASE` allocated uninitialized to dodge the `RepurchaseService` singleton in its ctor, fields reflectively pinned); byte-exact first capture, 0 fidelity bugs. **[verified -> T2]** `SM_LOOT_ITEMLIST` reads `con.getActivePlayer()` -> moved to T2 audit (below). **[DONE 2026-06-17 batch 13]** `SM_TUNE_RESULT` (BuildSimpleItem + PendingTuneResult DTO -> the already-validated EnchantInfoBlobEntry.WriteInfo ENCHANT_INFO writer; con-null-safe, no player), `SM_WAREHOUSE_ADD_ITEM` (item seam, player==blob owner only -> null; writeImpl reads only addType.GetMask() scalar), `SM_INVENTORY_INFO` (NEW player-scalar seam: uninitialized Player + pinned playerAccountData->PlayerCommonData carrying npc/quest/itemExpands; GetFullBlob player==blob owner) all runtime-golden'd byte-exact first capture, 0 fidelity bugs.
- real-VisibleObject seam: `SM_GATHERABLE_INFO` **[DONE 2026-06-17 batch 15]** — reads getX/Y/Z (WorldPosition) + getObjectId() + getSpawn().getStaticId()/getHeading() (SpawnTemplate) + getObjectTemplate().getTemplateId()/getL10nId() (GatherableTemplate); object NOT a StaticDoor -> writeH(1); con never read. Built via a minimal concrete VisibleObject (harness precedent — no DataManager/IDFactory/KnownList cascade) with reflect-set GatherableTemplate id+nameId. First capture byte-exact, 0 fidelity bugs. `SM_TRADELIST` **[AUDITED 1:1 2026-06-17 batch 21 -> T2]** (TRADELIST ctor reads getLegion/GOODSLIST_DATA legion-level filter + LimitedItemTradeService singleton -> heavier; writeImpl byte-identical, TradeNpcType.index() explicit 1-5). `SM_NPC_ASSEMBLER` **[DONE 2026-06-17 batch 14]** — turned out NOT to need the real-Npc seam: `AssembledNpc`/`AssembledNpcPart` are plain DTOs (reflect-set template npcId/staticId), writeImpl reads only scalars; `timeOnMap` (System.currentTimeMillis-derived, cached at ctor) reflect-pinned to a fixed value both sides -> byte-exact; null-despawn case writes only `writeD(0)`. First capture byte-exact, 0 fidelity bugs.
- pure-scalar DTO (no seam): `SM_GATHER_UPDATE` **[DONE 2026-06-17 batch 14]** — ctor caches `GatherableTemplate.getHarvestSkill()` + `Material.getItemId()/getL10n()` + scalars; writeImpl reads only those + the `SM_SYSTEM_MESSAGE` `STR_EXTRACT_GATHER_*` catalog ids (static constants) + `ChatUtil.l10n()` (pure: `"$"`+2-char encode of `nameid<<1|1`, no DataManager/time). GatherableTemplate.harvestSkill/Material.nameid+itemid reflect-set; 5 action cases (0/1/6/7/8 cover both msgId!=0 writeS(l10n) and msgId==0 writeS(null)). First capture byte-exact, 0 fidelity bugs.
- DataManager-holder seam: `SM_SKILL_LIST` **[DONE 2026-06-17 batch 13]** (silent ctor is DataManager-FREE — only the (skill,messageId) ctor reads SKILL_DATA; stigma entries skillType>0 -> IsNormalSkill() false -> GetFlag()==0 no clock -> deterministic; SKILL_DATA holder seam not even needed).
- new bounded Pet/Summon seam: `SM_PET_EMOTE` **[AUDITED 1:1 2026-06-17 batch 20 -> T2]** (live Pet/MoveController graph), `SM_SUMMON_PANEL` **[AUDITED 1:1 2026-06-17 batch 20 -> T2]** (live Summon/lifeStats/gameStats), `SM_SUMMON_UPDATE` **[AUDITED 1:1 2026-06-17 batch 20 -> T2]** (live Summon Stat2 graph).
- model-graph (Kisk/House): `SM_KISK_UPDATE` **[AUDITED 1:1 2026-06-17 batch 21 -> T2]** (live Kisk graph, 8x scalar writeD; Kisk ctor not bounded-constructible), `SM_HOUSE_SCRIPTS` **[DONE 2026-06-17 batch 20]** (T1: PlayerScript is a plain record (id,compressedBytes,uncompressedSize) — built directly with fixed bytes, no CompressUtil -> deterministic; con never read; 4 cases oneWithData/oneNoData/mixed/emptyNullCtor byte-exact first capture, 0 fidelity bugs), `SM_HOUSE_RENDER` **[AUDITED 1:1 2026-06-17 batch 20 -> T2]** + `SM_HOUSE_UPDATE` **[AUDITED 1:1 2026-06-17 batch 20 -> T2]** (AbstractHouseInfoPacket.writeCommonInfo reads LegionService singleton + live House/registry/emblem graph; PartType order+rooms match), `SM_HOUSE_OBJECTS` **[AUDITED 1:1 2026-06-17 batch 20 -> T2]** (live HouseObject graph), `SM_HOUSE_OBJECT` reads con -> T2.

**T2 — audit-only (writeImpl needs con / live World / live singleton / full live-Player graph / time):**
- reads `con.getActivePlayer()`: `SM_PRICES` **[AUDITED 1:1 2026-06-17 batch 16]**, `SM_PLAY_MOVIE` **[AUDITED 1:1 2026-06-17 batch 16]**, `SM_DIALOG_WINDOW` **[AUDITED 1:1 2026-06-17 batch 16]**, `SM_FRIEND_UPDATE` **[AUDITED 1:1 2026-06-17 batch 16]**, `SM_CHALLENGE_LIST` **[AUDITED 1:1 2026-06-17 batch 16]**, `SM_GROUP_INFO` **[AUDITED 1:1 2026-06-17]**, `SM_ALLIANCE_INFO` **[AUDITED 1:1 2026-06-17]**, `SM_LOOT_ITEMLIST` **[AUDITED 1:1 2026-06-17 batch 21]** (writeImpl `con.getActivePlayer()` null-guard + per-drop loot-confirmation read — full live DropNpc/Player graph + DataManager.ITEM_DATA), `SM_HOUSE_OBJECT` **[AUDITED 1:1 2026-06-17 batch 19]**, `SM_HOUSE_REGISTRY` **[AUDITED 1:1 2026-06-17 batch 19]**, `SM_HOUSE_BIDS` **[AUDITED 1:1 2026-06-17 batch 19]**, `SM_HOUSE_EDIT` **[AUDITED 1:1 2026-06-17 batch 19]**, `SM_INSTANCE_INFO` **[AUDITED 1:1 2026-06-17 batch 18]** (con.getActivePlayer + System.currentTimeMillis + portal-cooldown graph), `SM_SIEGE_LOCATION_INFO` **[AUDITED 1:1 2026-06-17 batch 18]** (con.getActivePlayer + live SiegeService/LegionService + emblem graph).
- reads `con.getAccount()` / `con.enableCryptKey()`: `SM_ACCOUNT_PROPERTIES` **[AUDITED 1:1 2026-06-17 batch 18]**, `SM_KEY` **[AUDITED 1:1 2026-06-17 batch 18]**.
- live World singleton (`World.getInstance()` / static-final): `SM_PLAYER_SPAWN` **[AUDITED 1:1 2026-06-17 batch 18]** (World.getWorldMap().getTemplate().getBeginnerTwinCount()), `SM_DIE` **[AUDITED 1:1 2026-06-17 batch 18]** (ctor: live Player/WorldMapInstance/InstanceHandler/Kisk graph).
- live `SiegeService`/`Influence`/`GameTimeService`/`TownService`/`GameServer`/`LoginServer` singleton or `System.currentTimeMillis`/JVM-uptime: `SM_GAME_TIME` **[AUDITED 1:1 2026-06-17 batch 16]** (live GameTimeService), `SM_TIME_CHECK` **[AUDITED 1:1 2026-06-17 batch 16]** (ManagementFactory uptime -> Process.StartTime), `SM_VERSION_CHECK` **[AUDITED 1:1 2026-06-17 batch 16]** (GSConfig/MembershipConfig/GameServer/LoginServer/ChatServer/AtreianPassportService + currentTimeMillis; full config blob byte-identical), `SM_INFLUENCE_RATIO` **[AUDITED 1:1 2026-06-17 batch 14]** (live Influence+SiegeService), `SM_FORTRESS_STATUS` **[AUDITED 1:1 2026-06-17 batch 14]** (live SiegeService.getFortresses()+Influence), `SM_LEGION_DOMINION_LOC_INFO` **[AUDITED 1:1 2026-06-17 batch 14]** (live LegionDominionService+LegionService+legion/emblem graph), `SM_IN_GAME_SHOP_CATEGORY_LIST` **[AUDITED 1:1 2026-06-17 batch 19]**, `SM_IN_GAME_SHOP_ITEM` **[AUDITED 1:1 2026-06-17 batch 19]**, `SM_IN_GAME_SHOP_LIST` **[AUDITED 1:1 2026-06-17 batch 19]**, `SM_ITEM_COOLDOWN` **[AUDITED 1:1 2026-06-17 batch 18]** (System.currentTimeMillis reuse calc), `SM_RECIPE_COOLDOWN` **[AUDITED 1:1 2026-06-17 batch 18]** (ctor: live Player.getCraftCooldowns time-based remainingSeconds).
- full live-Player/Legion/Group/Alliance graph: `SM_GM_SHOW_PLAYER_STATUS` **[AUDITED 1:1 2026-06-17]**, `SM_CHAT_WINDOW` **[AUDITED 1:1 2026-06-17 batch 17]**, `SM_GROUP_MEMBER_INFO` **[AUDITED 1:1 2026-06-17 batch 17]**, `SM_ALLIANCE_MEMBER_INFO` **[AUDITED 1:1 2026-06-17 batch 17]**, `SM_LEGION_INFO` **[AUDITED 1:1 2026-06-17 batch 17]**, `SM_LEGION_MEMBERLIST` **[AUDITED 1:1 2026-06-17 batch 17]**, `SM_LEGION_ADD_MEMBER` **[AUDITED 1:1 2026-06-17 batch 17]**, `SM_LEGION_UPDATE_MEMBER` **[AUDITED 1:1 2026-06-17 batch 17]**, `SM_GM_SHOW_LEGION_INFO` **[AUDITED 1:1 2026-06-17 batch 17]**, `SM_PRIVATE_STORE_NAME` **[AUDITED 1:1 2026-06-17 batch 17]**, `SM_CASTSPELL_RESULT` **[AUDITED 1:1 2026-06-17 batch 19]** (Skill/Effect graph), `SM_GATHER_UPDATE`, `SM_OBJECT_USE_UPDATE` **[AUDITED 1:1 2026-06-17 batch 19]** (HouseObject), `SM_HOUSE_OWNER_INFO` **[AUDITED 1:1 2026-06-17 batch 19]**.
- login/account/character graph: `SM_CHARACTER_LIST` **[AUDITED 1:1 2026-06-17 batch 23]** (con.getAccount() live + writeD(playOk2)/writeC(account.size())/per-char writePlayerInfo), `SM_CHARACTER_SELECT`(done T1), `SM_CREATE_CHARACTER` **[DONE 2026-06-17 batch 23]** (T1 non-OK branch writeD(responseCode)+return golden byte-exact 3 cases; OK branch delegates to validated writePlayerInfo), `SM_L2AUTH_LOGIN_CHECK` **[AUDITED 1:1 2026-06-17 batch 23]** (writeD(ok?0:1) + hex-literal standardData byte-identical + DataManager.WORLD_MAPS_DATA live-holder per-map writeD(mapId)+writeH(isInstance?0:twinCount) + writeS(accountName)), `SM_VERSION_CHECK`.
- builder / no reachable plain-value public ctor: `SM_CUSTOM_PACKET` **[AUDITED 1:1 2026-06-17 batch 22]** (admin //fsc builder; writeImpl iterates elements, each PacketElementType.write byte-identical: d/h/c Integer.decode, b Integer.valueOf-decimal, q Long.decode, f/e Float/Double, s; con never read), `SM_LOGIN_QUEUE` **[DONE 2026-06-17 batch 23]** (T1: private no-arg ctor pins fixed 5/60/50, writeD x3, con never read; byte-exact).
- item/ItemInfoBlob + live-graph readers: `SM_PRIVATE_STORE` **[AUDITED 1:1 2026-06-17 batch 22]** (live PrivateStore/seller Player/Inventory + getFullBlob; null-store guard; writeD seller objId/writeH size then per-item writeD objId/itemId, writeH((int)count), writeQ(price), blob.writeMe), `SM_UPDATE_PLAYER_APPEARANCE` **[AUDITED 1:1 2026-06-17 batch 22]** (writeD(playerId) + AbstractPlayerInfoPacket.writeEquippedItems live Item graph: equipmentSlot mask w/ TwoHanded SUB_HAND removal, per-item skin/godStone/dye/enchant), `SM_MAIL_SERVICE` **[AUDITED 1:1 2026-06-17 batch 22]** (con.getActivePlayer().getMailbox() live graph + Letter/Item/ItemInfoBlob; serviceId switch 0/1/2/3/5/6, letters.size()*-1 isLastPacket, totalCount+unreadCount*0x10000, (int)(time/1000), letterType.getId()), `SM_BROKER_SERVICE` **[AUDITED 1:1 2026-06-17 batch 22]** (live BrokerItem/Item/blob + currentTimeMillis + PlayerService.getPlayerName singleton + settleTime; duplicate-id BrokerPacketType SHOW_SETTLED_ICON=5/SETTLED_ITEMS=5 via reference-identity if-chain == Java identity switch; toDays(ms)==ms/86400000 trunc-toward-zero, settleTime/60000; all 8 writers byte-identical), `SM_AUTO_GROUP` **[AUDITED 1:1 2026-06-17 batch 22]** (ctor reads DataManager.AUTO_GROUP via AutoGroupType enum static-init -> not bounded; writeImpl scalars + name only, windowId switch case-groupings 0,7/1,3,8/2,4,5/6 byte-identical, con never read).
- `SM_SYSTEM_MESSAGE` **[DONE 2026-06-17 batch 23]** — generated 28k-line catalog + ChatType/sender graph. Wire-format path golden'd via the (int msgId, Object...) ctor (GOLDEN_YELLOW id 25 + null sender objId 0): writeC(chatType) writeC(0) writeD(senderObjId) writeD(msgId) writeC(params.length) per-param writeS(param.toString()) writeC(specialParams.length) per writeS; Integer.toString==int.ToString; 3 cases (noParams / int+string / two-strings+int) byte-exact. The msgId catalog itself is DATA (verified the wire-format path, not every entry).
- abyss/fortress/instance status singleton readers: `SM_ABYSS_ARTIFACT_INFO3` **[AUDITED 1:1 2026-06-17 batch 21 -> T2]** (writeImpl reads ArtifactLocation.getLocationId()/getStatus().getValue() — ArtifactStatus explicit 0-3; getStatus() consults SiegeService + ArtifactLocation ctor needs SiegeLocationTemplate -> not bounded-constructible, demoted to audit), `SM_FORTRESS_STATUS`, `SM_INSTANCE_SCORE` **[AUDITED 1:1 2026-06-17 batch 21 -> T2]** (writeImpl delegates to live InstanceScoreWriter graph + InstanceProgressionType.getId() explicit 1/2/3*1024*1024), `SM_ABYSS_RANKING_*` writeImpl is DTO-only (T1) — keep in T1.

(The above is a living split; packets move T2->T1 as bounded seams are built and T1->done as golden'd. Re-derive the un-golden'd set with: build the covered-name set from `parity-artifacts/golden/packets/*.json` + every `SM_*` token in `tests/.../Golden*.cs` + `Sm*Tests.cs`, diff against `find src -path '*ServerPackets/SM_*.cs'`.)

Recipe (proven): add `capture(...)` to the Java `Golden*FixtureGeneratorTest`, run `mvn -pl game-server -am test -Dtest=<Gen> -Dmaven.test.skip=false -Dsurefire.failIfNoSpecifiedTests=false`, add C# `[InlineData]`+Reconstruct+`CaptureWriteImplPayload`; **Java oracle bytes are truth, never tune to C#**; any mismatch = a real fidelity bug to fix in the C# packet.

---

## B. Explicit `NotSupportedException` / `Unsupported*` throw sites — TRIAGE COMPLETE (2026-06-17)

**CORRECTION:** there are ZERO `NotImplementedException` in src. The real stubs are `NotSupportedException` (C# idiom for Java's `UnsupportedOperationException`) + the custom `UnsupportedCollisionException` type. 14 throw-site files. **All 14 verified FAITHFUL GUARDS against their Java source — 0 real porting gaps, 0 fidelity bugs.** Each C# throw matches a Java `UnsupportedOperationException`/`CloneNotSupportedException` with identical message + condition. Breadcrumbed in place (comment-only); build 0, golden 221/221.

Per-file verdict (all FAITHFUL-GUARD, [Java path]::method throws UnsupportedOperationException unless noted):
- `Model/Team/Legion/LegionWarehouse.cs` — `model/team/legion/LegionWarehouse.java`: ~22 player-storage mutators throw "LWH should be used behind proxy" / SetOwner "LWH doesnt have owner" / SetLimit "Slot limit is controlled by the expansion level…". The proxy IS the impl (STRONG PRIOR confirmed).
- `Model/Items/Storage/LegionStorageProxy.cs` — `model/items/storage/LegionStorageProxy.java`: quest-status decrease overloads throw "Quests should not update LWH!"; SetOwner "LWH doesnt have owner".
- `GeoEngine/Collision/UnsupportedCollisionException.cs` — `geoEngine/collision/UnsupportedCollisionException.java` (jMonkeyEngine/Kirill): IS the Java exception type (extends UnsupportedOperationException → C# NotSupportedException).
- `GeoEngine/Math/FastMath.cs` — `geoEngine/math/FastMath.java::convertFloatToHalf` throws "NaN to half conversion not supported!".
- `GeoEngine/Scene/Node.cs` — `geoEngine/scene/Node.java::clone` throws CloneNotSupportedException (non-Geometry/Node child).
- `GeoEngine/Scene/DespawnableNode.cs` — `geoEngine/scene/DespawnableNode.java` copy-ctor + clone throw CloneNotSupportedException (2 sites).
- `GeoEngine/Bounding/BoundingBox.cs` — `geoEngine/bounding/BoundingBox.java::collideWith` throws UnsupportedCollisionException("With: "+simpleName).
- `GeoEngine/Collision/Bih/BIHTree.cs` — `geoEngine/collision/bih/BIHTree.java::collideWith` throws UnsupportedCollisionException() (no-arg).
- `GeoEngine/Math/Ray.cs` — `geoEngine/math/Ray.java::collideWith` throws UnsupportedCollisionException() (no-arg).
- `Commons/Network/AcceptDispatcherImpl.cs` — `commons/.../network/AcceptDispatcherImpl.java::closeConnection` throws "This method should never be called!".
- `Instance/Handlers/GeneralInstanceHandler.cs` — `instance/handlers/GeneralInstanceHandler.java::portToStartPosition` throws UnsupportedOperationException (default base impl).
- `Services/Instance/InstanceService.cs` — `services/instance/InstanceService.java::getNextAvailableInstance` throws "Invalid call for next available instance  of …" (double-space preserved 1:1).
- `Services/SiegeService.cs` — `services/SiegeService.java` cron transform: "…Preparation over midnight not supported." + catch(NumberFormatException→FormatException) rethrow.
- `Network/Aion/Iteminfo/ItemInfoBlob.cs` — `network/aion/iteminfo/ItemInfoBlob.java::newBlobEntry` throws UnsupportedOperationException for STAT_BONUSES.
- `SkillEngine/Effect/EffectTemplate.cs` — `skillengine/effect/EffectTemplate.java::calculateHate` default-arm throws "Unhandled effect type … for hate calculation".

Note: `Taskmanager/Tasks/MoveTaskManager.cs` (listed earlier as a candidate) is NOT a throw site — it passes `new NotSupportedException()` as a log argument to LogWarning, a faithful mirror of the Java log call. No action.

**§B remaining to triage: 0.** B is a documentation/breadcrumb pass — no real gaps existed (the backlog's "Legion warehouse" suspicion was wrong; it's the canonical proxy-guard pattern).

---

## C. C#-specific deferrals (the *real* subset of the 277 TODOs)

These are gaps because Java does NOT have them — they're C#-side "wire later" notes. (Hard contract: `.properties` keys + override precedence must be preserved — so the config one matters for parity.)

- **C1. Config framework (systemic).** Configs historically loaded from `@Property` default values baked into C# field initializers, NOT the faithful Java path = `.properties` file load + `ConfigurableProcessor` reflection + active-event overrides. Hard contract: `.properties` keys, default values, type-coercion semantics, and override precedence must match Java exactly.

  **Java sources of truth:** `commons/configuration/ConfigurableProcessor.java` (reflect over `@Property` static fields, walk superclass+interfaces, getValue = default-fallback + unused-key tracking + literal-`""` -> empty + `${...}` placeholder substitution, coerce via transformer, DEFAULT_VALUE sentinel = don't overwrite init value), `commons/configuration/Property.java` (`key()` + `defaultValue()` default = sentinel `DO_NOT_OVERWRITE_INITIALIAZION_VALUE`), `commons/configuration/transformers/*` (Number via Integer/Long.decode, Boolean true/false/1/0-only, Char, String identity, Enum case-sensitive valueOf, Array/CSV split-on-unquoted-comma, File, InetSocketAddress, Pattern, Class, Collection, TimeZone, ZoneId), `commons/utils/PropertiesUtils.java` (load file / loadFromDirectory cascading) over `java.util.Properties` semantics.

  **STATUS: CORE PORTED + PROVEN (2026-06-17).** Implemented in `Aion.Commons` 1:1 with Java:
    - `Configuration/PropertyAttribute.cs` — `[Property(key:, defaultValue:)]` with `DEFAULT_VALUE` sentinel; usable on static field OR static property.
    - `Configuration/ConfigurableProcessor.cs` — `Process(JavaProperties, params object[])` reflects [Property] members (fields+properties), base-type walk, `GetValue` (default fallback + unused-key set + `""`->empty + `${...}` substitution + sentinel skip), coercion via transformers, returns unused-key set. Faithful to Java getValue/transform/processField.
    - `Configuration/Transformers/*` — `PropertyTransformer` base + registry (`PropertyTransformers`) + Number (Java `*.decode` radix prefixes), Boolean, Char, String, Enum, Array (CSV split verbatim incl. quote-stripping), Pattern. **Ported this slice = every type the migrated holders need.**
    - `Configuration/JavaProperties.cs` — faithful `java.util.Properties` subset (`#`/`!` comments, `=`/`:`/whitespace separators, `\` escapes incl. `\uXXXX`, line continuation via trailing odd backslash) + chained defaults + `PropertiesUtils` load/loadFromDirectory cascading.
    - Migrated `Aion.Commons/Configs/CommonsConfig.cs` to `[Property]` (both Java fields: `commons.runnablestats.enable`, `commons.script_compiler.caching.enable`).
    - Parity proof: `tests/Aion.Commons.Tests/ConfigurableProcessorParityTests.cs` (6 tests, green) — **a `.properties` override now actually overrides the [Property] default** (bool/int/long-hex/float/string/enum/array), sentinel preserves init value, literal-`""`+`${}` substitution, unused-key reporting, JavaProperties comment/separator/escape/continuation parsing.

  **REMAINING for C1 (later ticks):** (1) migrate the **40 game-server `Configs/**` holder classes** (+ login/chat-server config) from C# field initializers to `[Property(key:, defaultValue:)]`, copying Java keys/defaults verbatim — mechanical, one class at a time, golden per-class. (2) Wire `ConfigurableProcessor.Process` into each server's boot (`Configs/Config.cs:27` TODO — the GameServer `Config.Load(params Type[])` should `JavaProperties.Load`+`loadFromDirectory` the same `config/**` order Java uses, then `Process` the CONFIGS set; also `Configs/Main/WorldConfig.cs:5`). (3) Active-event config overrides (Java `Event.start/stop` re-`process` over event-specific properties). (4) Remaining transformers as holders need them: File, InetSocketAddress, Class, Collection (List/Set), TimeZone, ZoneId, and the `@Properties` key-pattern Map annotation (none of the currently-migrated holders use these). (5) Note: existing `Configuration/ConfigLoader.cs` is a separate lossy/idiomatic helper used only by `ChatServerOptions`/`DatabaseOptions` — superseded by `JavaProperties` for faithful loads; retire/redirect once those options classes migrate.
- **C2. The ~13 "TODO … Java" / "port from Java the rest" markers.** Enumerate (`grep -rIn "TODO" … | grep -i java`) and resolve each as a 1:1 port; these are explicit C#-deferred porting, not Java mirrors.
- **C3. Triage the rest of the 277.** Mechanically separate faithful-Java-mirror TODOs (keep verbatim — they are parity) from any remaining C#-specific deferrals; only the latter are backlog. Produce the split once so the count is honest.

---

## D. Editor / admin-save & DAO write-path deferrals (small, real)

- **D1. `SpawnsData` 4 editor/admin-save helper TODOs** (non-load-bearing spawn-save; faithful read path is complete).
- **D2. Any DAO/editor `// TODO` save/persist paths** surfaced in §C3 triage (e.g. admin-tool persistence) — port 1:1.

---

## E. Hygiene / tooling (small, keeps the audit honest)

- **E1. Delete 2 stray slop-named Java-tree files** if confirmed dead: `FindGroupMutationPostTraceCaptureHooks`, `PetFeedUnusualStorageArtifactCapture` (slop-vocabulary names that shouldn't be in `game-server/src`).
- **E2. Audit-tool hygiene.** Teach `scripts/parity/structural_audit.py` + `check_fidelity.py` to index `game-server/data/handlers/` as Java, so the ~1,534 faithfully-ported content scripts stop showing as "orphan" and the banned-vocab heuristic stops false-flagging legit Java names (`OphidanBridgeInstance`, `_4217TheImprisonedExecutor`, `_21217NewResearchPlan`, `ScaldingExecutorAI`, `WaveEventExecutorAI`). Until then those 6 guardrail "failures" are known false positives.
- **E3. Resolve the 6 name-mismatch "missing" engine classes** (all currently false positives): confirm `AIState`/`AISubState`/`AIEventType` are faithful nested enums, `CronExpressionTransformer`→`CronExpressions` is a faithful rename, and the 2 §E1 files. Net engine/service missing-surface = 0 after this.

---

## F. [USER-GATED] Live-client enter-world test — the ultimate parity proof

The one test that exercises the *real* end-to-end loop (client → login → enter-world → gameplay). **Environment-gated: needs the user's actual Aion 4.8 client.** The 3-server stack boots (LS/GS/CS authed, GS↔LS bridge stable); the only missing step is the real client connection. This is the gate on declaring *live* parity (vs structural+golden parity). Not autonomously doable.

---

## G. Deferred-data / runtime-load completeness — verify

Reconcile `datamanager-data-placeholders` (noted QUEST_DATA + 4 holders once wired EMPTY for compile-convergence) against `hollow-holder-census` (later: all 96 accessors delegate to loaded `SD.*Dh`). Confirm by boot-assert that every DataManager holder a live consumer reads is non-empty at boot; wire any still-empty placeholder. Believed DONE (RealStaticDataLoad + bootstrap tests assert real loads), but verify exhaustively since "skip nothing."

---

## Recommended order (no client; skip nothing)

1. **A1 integration harness + golden the ~103 live-object packets** — the main provable-parity vein; build the harness once, then grind packets (also re-confirms every live-object writer byte-exact, likely surfacing any remaining latent bugs as the 3 already found did).
2. ~~B triage~~ **DONE (2026-06-17): all 14 NotSupported throw sites verified FAITHFUL GUARDS vs Java — 0 real gaps, 0 bugs, breadcrumbed.**
3. **C1 config framework** — systemic; restores `.properties` override fidelity (a hard contract).
4. **G verify holders; D editor-save TODOs; E hygiene + audit-tool** — small, interleave.
5. **C2/C3 TODO triage** — produce the honest faithful-vs-real split, resolve the reals.
6. **A2 formula golden** — thin top-up.
7. **F live-client test** — when the user can run their client. Ultimate parity sign-off.

When A–E + G are clear and F passes, the §10 "project done" definition is met: every Java file has one faithful golden-validated-or-deferred C# counterpart, and the live client proves the runtime loop.
