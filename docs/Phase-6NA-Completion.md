# Phase 6NA Completion Handoff - Selectable Decompose First Show

Date: May 24, 2026
Unit of Work: UOW-853
Branch: `4.8`
Commit: pending at handoff creation (`[Phase 6][UOW-853] Test selectable decompose first show`)

## Status

Phase 6 is still in progress. This unit added a narrow runtime regression for the selectable decompose first-show branch through `GameServerConnection`.

The test verifies C# observable first-show behavior. It does not execute selectable reward selection, source consume, reward mutation, Java runtime comparison, or live-client validation.

## Files Changed

- `dotnetConversion/tests/Aion.GameServer.Tests/GameServerConnectionInventoryExpansionUseItemTests.cs`
- `docs/PHASE-6-PROGRESS.md`
- `docs/Phase-6NA-Completion.md`

## What Changed

- Extended the existing item-use fixture with:
  - one selectable decompose source item (`101`)
  - two obtainable selectable reward templates (`201`, `202`)
  - one Asmodian-only reward candidate (`203`) used to prove filtering for an Elyos ranger
- Added `HandleUseItemAsync_SelectableDecomposeShowsChoicesWithoutSchedulingUse`.
- Added serialized payload assertions for `SmFirstShowDecomposable`.
- The new test verifies:
  - selectable decompose sends exactly one `SmFirstShowDecomposable`
  - the packet contains source object id `5001`
  - only two obtainable choices are serialized
  - reward indices, item ids, minimum counts, and trailing byte pattern match the C# packet contract
  - `Player.UsingItemObjectId` remains unset, so the first-show branch does not schedule positive-time item use

## Tests

Focused:

```powershell
dotnet test dotnetConversion\tests\Aion.GameServer.Tests\Aion.GameServer.Tests.csproj --filter "GameServerConnectionInventoryExpansionUseItemTests"
```

Result: passed, 9 tests.

Full:

```powershell
dotnet test dotnetConversion\tests\Aion.GameServer.Tests\Aion.GameServer.Tests.csproj
```

Result: passed, 1425 tests.

New test:

| Test Name | What It Validates | Java Comparison |
|---|---|---|
| `GameServerConnectionInventoryExpansionUseItemTests.HandleUseItemAsync_SelectableDecomposeShowsChoicesWithoutSchedulingUse` | C# selectable decompose first-show runtime behavior, obtainable reward filtering, serialized first-show packet contents, and no scheduled item-use state. | Source-derived Java review of `DecomposeAction` selectable branch and `CM_SELECT_DECOMPOSABLE`; no Java runtime comparison. |

## Migration Parity Snapshot

| Java Artifact | C# Artifact | Type | Port Status | Test Status | Parity Status | Notes |
|---|---|---|---|---|---|---|
| `com.aionemu.gameserver.model.templates.item.actions.DecomposeAction` | `GameServerConnection.HandleDecomposeUseItemAsync` / `GameServerConnectionInventoryExpansionUseItemTests` | Dynamic Item Action Caller / Runtime Test | Partial | Regression Tested | Partial Parity | Runtime test covers selectable first-show behavior and no positive-time scheduling. Selection completion, source consume, reward mutation, success message, secondary-show packet, Java runtime comparison, and live-client behavior remain unverified. |
| `com.aionemu.gameserver.network.aion.serverpackets.SM_FIRST_SHOW_DECOMPOSABLE` | `Aion.GameServer.Network.Aion.ServerPackets.SmFirstShowDecomposable` | Packet / Selectable Decompose UI | Partial | Regression Tested for this call site | Partial Parity | Serialized C# first-show payload is asserted for source object id and two reward choices. Java-generated bytes, opcode/frame/crypto, and live-client UI rendering remain unverified. |
| `com.aionemu.gameserver.dataholders.DecomposableItemsData` | fixture `DecomposableItemTable` / `DecomposeService` | Static Data / Service Dependency | Partial | Regression Tested through fixture load | Partial Parity | Fixture covers selectable race/class filtering for this slice. Broader data combinations, random rewards, special-cube checks, missing-template behavior, and Java runtime output remain unverified. |
| `com.aionemu.gameserver.network.aion.clientpackets.CM_SELECT_DECOMPOSABLE` | C# selectable reward-selection handler path in `GameServerConnection` / `DecomposeService` | Client Packet Handler / Follow-up Dependency | Partial | No Tests in this unit | Needs Verification | Java selection source was audited to scope the next unit. C# selection handling still needs runtime coverage for source consume, reward add, success message, and secondary-show packet ordering. |
| `com.aionemu.gameserver.network.aion.serverpackets.SM_SECONDARY_SHOW_DECOMPOSABLE` | `Aion.GameServer.Network.Aion.ServerPackets.SmSecondaryShowDecomposable` | Packet / Selection Completion Dependency | Partial | No Tests in this unit | Needs Verification | Java selection sends an empty secondary-show list after source consume. C# caller behavior and packet bytes remain unverified. |
| `com.aionemu.gameserver.network.aion.serverpackets.SM_ITEM_USAGE_ANIMATION` | `Aion.GameServer.Network.Aion.ServerPackets.SmItemUsageAnimation` | Packet / Selection Completion Dependency | Partial | No Tests in this unit | Needs Verification | Java selection sends a default item-use animation. First-show branch intentionally has no positive-time scheduling; selection animation remains next-unit work. |
| `com.aionemu.gameserver.network.aion.serverpackets.SM_SYSTEM_MESSAGE` | `SmSystemMessage` selectable success path | System Message / Selection Completion Dependency | Partial | No Tests in this unit | Needs Verification | Java selection sends `STR_UNCOMPRESS_COMPRESSED_ITEM_SUCCEEDED`. C# success-message id/payload, ordering, localization, and live-client output remain unverified. |

## Remaining Risks

- `CM_SELECT_DECOMPOSABLE` reward selection remains untested: source consume, selected reward add, default item-use animation, success system message, secondary-show empty list, invalid index behavior, and unobtainable reward handling.
- First-show parity is based on C# serialized payload assertions and Java source review, not Java-generated packet bytes or a live Java runtime comparison.
- Broader selectable data combinations, random rewards, special-cube inventory checks, packet opcode/frame/crypto, socket fanout, threading/date-time precision, reflection/dynamic item actions, serialization side effects, and live-client validation remain unverified.

## Summary Metrics

- Total Java artifacts discovered: 7
- Total artifacts ported: 1 C# runtime regression slice for selectable decompose first-show behavior
- Total artifacts with verified parity: 0
- Total artifacts needing verification: 7
- Total blocked artifacts: 27 blocked/not-started categories
- Estimated overall migration completion: Phase 6 remains about 66% complete

## Next Recommended Unit of Work

Add a narrow runtime test for `CM_SELECT_DECOMPOSABLE` / C# selectable reward selection: choose one obtainable reward from the selectable fixture, verify source consume, selected reward add, default `SmItemUsageAnimation`, `STR_UNCOMPRESS_COMPRESSED_ITEM_SUCCEEDED` equivalent, `SmSecondaryShowDecomposable` empty-list packet, and document invalid-index/unobtainable reward gaps separately if they do not fit the unit.

Suggested scope:

- Reuse the item-use fixture and selectable source item `101` if the selection handler can be invoked directly.
- Prefer one valid selection index first; leave invalid index and unobtainable reward negative tests for a follow-up unless tiny.
- Assert packet order and inventory state separately so failures identify packet parity versus mutation parity.
- Do not combine normal decompose completion success/failure with selectable selection in the same unit.

## Safe Parallel Work Candidates

| Candidate | Files | Parallel Safe? | Notes |
|---|---|---|---|
| C# selectable selection runtime test | likely the same test file | Yes, if exclusive | Best next runtime slice. |
| Java `CM_SELECT_DECOMPOSABLE` deeper audit | Java client packet read-only | Yes | Check invalid index, unobtainable filtering, and packet order before implementation. |
| C# selection handler audit | `GameServerConnection.cs`, `DecomposeService.cs`, packet files read-only | Yes | Identify exact handler seam and expected packet sequence. |
| Progress/handoff docs | docs | No | Orchestrator-owned after validation. |

## Do Not Parallelize

- Multiple writers in `GameServerConnectionInventoryExpansionUseItemTests.cs`.
- Broad `GameServerConnection.cs` item-use edits unless the selection handler is actually missing or wrong.
- Progress and handoff docs.

## Resume Checklist

1. Read `docs/csharp-port.md`, orchestration docs, `docs/PHASE-6-PROGRESS.md`, and this handoff.
2. Confirm branch status and latest commit.
3. Run parallel work discovery before selecting subagents.
4. Use Java as source of truth and preserve breadcrumbs.
5. Keep the next unit narrow: selectable reward selection completion.
6. Run focused and full tests.
7. Update Migration Parity Table, Remaining Risks, Summary Metrics, and Next Recommended Unit.
8. Create the next handoff and commit the completed unit.
