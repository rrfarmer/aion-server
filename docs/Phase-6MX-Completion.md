# Phase 6MX Completion Handoff - Exp-Extract Cancel-State Metadata

Date: May 24, 2026
Unit of Work: UOW-850
Branch: `4.8`
Commit: pending at handoff creation (`[Phase 6][UOW-850] Map exp extract cancel state`)

## Status

Phase 6 is still in progress. This unit added source-derived metadata for the `ExpExtractAction` positive-time cancel path and corrected stale animation-add trace metadata.

No live exp-extract runtime fixture was added. The work is a metadata alignment slice backed by Java and C# source review.

## Files Changed

- `dotnetConversion/src/Aion.GameServer/Services/PlayerSummonSkillExecutionService.cs`
- `dotnetConversion/tests/Aion.GameServer.Tests/PlayerSummonSkillExecutionServiceTests.cs`
- `docs/PHASE-6-PROGRESS.md`
- `docs/Phase-6MX-Completion.md`

## What Changed

- Added `ExpExtract` to `PlayerSummonKnownObjectNpcSkillItemUsageStatePathKind`.
- Added `Decompose` to `PlayerSummonKnownObjectNpcSkillItemUsageStateCancelMessage`.
- Added an exp-extract positive-time path to `PlayerSummonKnownObjectNpcSkillItemUsageStateTrace`:
  - self-only send
  - 5000 ms duration
  - decompose-canceled message category
  - cancel `end=2`
  - sets C# `UsingItemObjectId`
- Corrected animation-add metadata from cancel `end=2` to `end=3`, matching UOW-849 and Java generic `PlayerController.cancelUseItem`.
- Updated the existing metadata regression to assert both paths.

## Tests

Focused:

```powershell
dotnet test dotnetConversion\tests\Aion.GameServer.Tests\Aion.GameServer.Tests.csproj --filter "PlayerSummonSkillExecutionServiceTests|GameServerConnectionInventoryExpansionUseItemTests"
```

Result: passed, 77 tests.

Full:

```powershell
dotnet test dotnetConversion\tests\Aion.GameServer.Tests\Aion.GameServer.Tests.csproj
```

Result: passed, 1423 tests.

Updated test:

| Test Name | What It Validates | Java Comparison |
|---|---|---|
| `PlayerSummonSkillExecutionServiceTests.ProjectMercenaryNpcSkillItemUsageStateTrace_MapsPositiveTimeUsingItemTiming` | Metadata records animation-add generic cancel `end=3` and exp-extract self-only 5000 ms decompose-cancel `end=2`. | Source-derived Java/C# review; no Java runtime comparison. |

## Migration Parity Snapshot

| Java Artifact | C# Artifact | Type | Port Status | Test Status | Parity Status | Notes |
|---|---|---|---|---|---|---|
| `com.aionemu.gameserver.model.templates.item.actions.ExpExtractAction` | `PlayerSummonKnownObjectNpcSkillItemUsageStateTrace` / `GameServerConnection.HandleExpExtractUseItemAsync` | Dynamic Item Action Caller / Metadata | Partial | Unit Tested as metadata | Partial Parity | Trace records Java/C# source-aligned exp-extract start/cancel metadata: 5000 ms self-only positive-time animation, decompose-canceled message, and `end=2`. Runtime scheduling, observer abort, exp/reward mutation, persistence, and Java runtime comparison remain unverified. |
| `com.aionemu.gameserver.model.templates.item.actions.AnimationAddAction` | `PlayerSummonKnownObjectNpcSkillItemUsageStateTrace` / `HandleAnimationAddUseItemAsync` | Dynamic Item Action Caller / Metadata | Partial | Unit Tested as metadata and runtime-tested in prior UOW | Partial Parity | Stale metadata is corrected to Java generic cancel `end=3`. Active-player completion and Java runtime behavior remain unverified. |
| `com.aionemu.gameserver.controllers.PlayerController.cancelUseItem` | item-use state trace cancel rows | Runtime Cancel Dependency / Metadata | Partial | Unit Tested as metadata | Needs Verification | Trace distinguishes generic cancel `end=3` from exp-extract observer cancel `end=2`. Broader trigger parity and item-reference state remain unverified. |
| `com.aionemu.gameserver.network.aion.serverpackets.SM_ITEM_USAGE_ANIMATION` | `SmItemUsageAnimation` / item-use state trace | Packet / Cancel Payload Dependency | Partial | Unit Tested as metadata | Partial Parity | Source-derived cancel end states are recorded. Java-generated bytes, opcode/frame/crypto, and packet-write `usingItem` side effect remain unverified. |
| `com.aionemu.gameserver.network.aion.serverpackets.SM_SYSTEM_MESSAGE` | `PendingItemUseCancelMessage.Decompose` / `SmSystemMessage.DecomposeItemCanceled(...)` | System Message / Cancel Feedback Metadata | Partial | Unit Tested as metadata | Needs Verification | C# exp-extract maps to the decompose cancel-message category matching Java naming. Message id/payload bytes are not validated here. |
| `com.aionemu.gameserver.model.TaskId` / Java `ItemUseObserver` | C# pending item-use trace rows | Scheduler / Observer Dependency | Partial | Unit Tested as metadata | Needs Verification | Java exp-extract cancel is observer-driven; C# cancellation is centralized pending cleanup. Observer attach/remove semantics and task races remain unverified. |

## Remaining Risks

- Exp-extract runtime cancellation and mutation flow remain untested.
- C# centralized pending cleanup may differ from Java action-specific observer abort.
- Other `cancelEndState=2` paths still need audits.
- Packet-write `usingItem` mutation, system-message bytes, opcode/frame/crypto, socket fanout, scheduler races, threading/date-time precision, reflection/dynamic item actions, serialization side effects, and live-client validation remain unverified.

## Summary Metrics

- Total Java artifacts discovered: 6
- Total artifacts ported: 1 source-derived metadata alignment slice covering exp-extract cancel-state and animation-add trace correction
- Total artifacts with verified parity: 0
- Total artifacts needing verification: 6
- Total blocked artifacts: 27 blocked/not-started categories
- Estimated overall migration completion: Phase 6 remains about 66% complete

## Next Recommended Unit of Work

Continue the adjacent cancel-state audit with a runtime or metadata slice for `DecomposeAction`, comparing Java selectable/non-selectable paths, observer abort `end=2`, inventory-full checks, and C# pending decompose scheduling/cancel-message behavior.

Suggested scope:

- Audit Java `DecomposeAction.canAct`, selectable branch, non-selectable scheduled branch, observer abort, success/failure packets.
- Audit C# `HandleDecomposeUseItemAsync` and pending decompose scheduling.
- Prefer a runtime test if minimal static-data setup is feasible; otherwise add source-derived metadata and explicit gaps.
- Keep docs clear that Java runtime parity is not verified unless a real comparison is added.

## Safe Parallel Work Candidates

| Candidate | Files | Parallel Safe? | Notes |
|---|---|---|---|
| Java decompose audit | Java `DecomposeAction` and decomposable data models | Yes | Read-only. |
| C# decompose audit | `GameServerConnection.cs`, decompose service/tests | Yes if read-only | Find minimal runtime fixture feasibility. |
| Runtime/metadata test | One test file only | Yes, if exclusive | Avoid broad shared edits. |
| Progress/handoff docs | docs | No | Orchestrator-owned after validation. |

## Do Not Parallelize

- Broad `GameServerConnection.cs` item-use edits.
- Progress and handoff docs.
- Packet serialization changes unless the unit explicitly proves a packet parity issue.

## Resume Checklist

1. Read `docs/csharp-port.md`, orchestration docs, `docs/PHASE-6-PROGRESS.md`, and this handoff.
2. Confirm branch status and latest commit.
3. Run parallel work discovery before selecting subagents.
4. Use Java as source of truth and preserve breadcrumbs.
5. Keep the next unit narrow: decompose cancel-state audit/test.
6. Run focused and full tests.
7. Update Migration Parity Table, Remaining Risks, Summary Metrics, and Next Recommended Unit.
8. Create the next handoff and commit the completed unit.
