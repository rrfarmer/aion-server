# Phase 6NE Completion Handoff - Selectable Decompose Reward Merge

Date: May 24, 2026
Unit of Work: UOW-857
Branch: `4.8`
Commit: pending at handoff creation (`[Phase 6][UOW-857] Test selectable decompose reward merge`)

## Status

Phase 6 is still in progress. This unit added a runtime regression for selectable decompose reward selection when the chosen reward merges into an existing stack.

The test verifies the C# existing-stack merge branch and packet/update-type behavior. It does not cover persistence failure, missing source/data/template paths, inventory-full behavior, packet-factory dispatch, Java runtime comparison, or live-client validation.

## Files Changed

- `dotnetConversion/tests/Aion.GameServer.Tests/GameServerConnectionInventoryExpansionUseItemTests.cs`
- `docs/PHASE-6-PROGRESS.md`
- `docs/Phase-6NE-Completion.md`

## What Changed

- Added `HandleSelectDecomposableAsync_SelectableRewardMergesExistingStack`.
- Added `AssertInventoryUpdatePayload` to verify the leading object id and trailing update type in `SmInventoryUpdateItem` payloads.
- The new test verifies:
  - valid selection index `0` maps to reward item `201 x2`
  - source item `101` decrements from count `2` to `1`
  - existing reward stack object `6001` increments from count `4` to `6`
  - source update packet uses `SmInventoryUpdateItem.DecreaseItemUse`
  - reward merge packet uses `SmInventoryUpdateItem.IncreaseItemCollect`
  - packet order remains default `SmItemUsageAnimation`, success `SmSystemMessage`, source update, empty `SmSecondaryShowDecomposable`, reward update

## Tests

Focused:

```powershell
dotnet test dotnetConversion\tests\Aion.GameServer.Tests\Aion.GameServer.Tests.csproj --filter "GameServerConnectionInventoryExpansionUseItemTests"
```

Result: passed, 14 tests.

Full:

```powershell
dotnet test dotnetConversion\tests\Aion.GameServer.Tests\Aion.GameServer.Tests.csproj
```

Result: passed, 1430 tests.

New test:

| Test Name | What It Validates | Java Comparison |
|---|---|---|
| `GameServerConnectionInventoryExpansionUseItemTests.HandleSelectDecomposableAsync_SelectableRewardMergesExistingStack` | C# selectable selection existing-stack merge behavior, source decrement packet, reward collect-update packet, and packet order. | Source-derived Java review of `CM_SELECT_DECOMPOSABLE.runImpl` and its `ItemService.addItem` update predicate; no Java runtime comparison. |

## Migration Parity Snapshot

| Java Artifact | C# Artifact | Type | Port Status | Test Status | Parity Status | Notes |
|---|---|---|---|---|---|---|
| `com.aionemu.gameserver.network.aion.clientpackets.CM_SELECT_DECOMPOSABLE` | `GameServerConnection.HandleSelectDecomposableAsync` / `GameServerConnectionInventoryExpansionUseItemTests` | Client Packet Handler / Runtime Test | Partial | Regression Tested | Partial Parity | Existing-stack reward merge is covered. Persistence failure, missing source/data/template, inventory-full behavior, packet-factory dispatch, Java runtime comparison, and live-client behavior remain unverified. |
| `com.aionemu.gameserver.services.item.ItemService` / `ItemPacketService.ItemAddType.DECOMPOSABLE` / `ItemUpdateType.INC_ITEM_COLLECT` | `InventoryAddService.CreateAddItemPlan` / `SmInventoryUpdateItem.IncreaseItemCollect` | Inventory Mutation / Reward Merge Dependency | Partial | Regression Tested for existing-stack merge | Partial Parity | Reward `201` merges into existing object `6001` from count `4` to `6` and emits update type `0x19`. Full packet bytes, overflow remainder behavior, inventory-full behavior, and Java DB/runtime comparison remain unverified. |
| `com.aionemu.gameserver.model.gameobjects.player.Inventory.decreaseByObjectId` | `ApplySourceItemMutationAsync` / `SmInventoryUpdateItem.DecreaseItemUse` | Inventory Mutation / Source Consume Dependency | Partial | Regression Tested for decrement with reward merge | Partial Parity | Source object `5001` decrements and emits update type `0x16`. Delete path is covered by prior unit; persistence rollback remains unverified. |
| `com.aionemu.gameserver.network.aion.serverpackets.SM_INVENTORY_UPDATE_ITEM` | `Aion.GameServer.Network.Aion.ServerPackets.SmInventoryUpdateItem` | Packet / Inventory Update Dependency | Partial | Regression Tested for object id and update type | Partial Parity | Test asserts object id and trailing update-type fields for source and reward packets. Full item blob bytes, opcode/frame/crypto, Java-generated bytes, and live-client rendering remain unverified. |
| `com.aionemu.gameserver.network.aion.serverpackets.SM_SECONDARY_SHOW_DECOMPOSABLE` | `SmSecondaryShowDecomposable` | Packet / Selection Completion | Partial | Regression Tested for ordering | Partial Parity | Empty secondary-show remains before reward update. Java bytes and non-empty TODO behavior remain unverified. |
| `com.aionemu.gameserver.network.aion.serverpackets.SM_ITEM_USAGE_ANIMATION` | `SmItemUsageAnimation` | Packet / Selection Completion | Partial | Regression Tested for this call site | Partial Parity | Default selection animation is asserted. Broadcast fanout, write-time `usingItem` side effect, and Java bytes remain unverified. |
| `com.aionemu.gameserver.network.aion.serverpackets.SM_SYSTEM_MESSAGE` | `SmSystemMessage.UncompressCompressedItemSucceeded(...)` | System Message / Selection Completion | Partial | Regression Tested for packet type/order | Needs Verification | Packet type/order is covered; payload bytes and localization remain unverified. |
| `com.aionemu.gameserver.dataholders.DecomposableItemsData` | fixture `DecomposableItemTable` / `DecomposeService.CreateSelectableRewardPlan` | Static Data / Reward Selection Dependency | Partial | Regression Tested through valid index | Partial Parity | Fixed reward `201 x2` is covered. Broader race/class combinations and random min/max ranges remain unverified. |

## Remaining Risks

- Persistence failure, missing source/data/template paths, and inventory-full behavior still need coverage.
- Full `SM_INVENTORY_UPDATE_ITEM` item-blob byte parity remains unverified; this unit only asserts object id and trailing update type.
- The selection tests invoke a private handler reflectively; packet-factory dispatch for real client frames remains unverified.
- Parity is based on C# runtime tests and Java source review, not Java-generated packet bytes or live Java runtime comparison.
- Packet opcode/frame/crypto, broadcast fanout, socket visibility, threading/date-time precision, serialization side effects, and live-client validation remain unverified.

## Summary Metrics

- Total Java artifacts discovered: 8
- Total artifacts ported: 1 C# runtime regression slice for selectable decompose existing-stack merge
- Total artifacts with verified parity: 0
- Total artifacts needing verification: 8
- Total blocked artifacts: 20 blocked/not-started categories
- Estimated overall migration completion: Phase 6 remains about 66% complete

## Next Recommended Unit of Work

Add a selectable decompose persistence-failure regression: use a repository returning `false` from `SaveDecomposeActionMutationAsync`, select a valid reward, and verify no packets are emitted and runtime inventory remains unchanged for both source and reward state.

Suggested scope:

- Reuse the selectable fixture with `EmptyPlayerEnterWorldRepository`.
- Add or expose a repository result flag for `SaveDecomposeActionMutationAsync` if it does not already exist.
- Select a valid reward and assert no packet emission.
- Assert source count and any pre-existing reward stack remain unchanged.
- Keep missing-template/data negatives separate unless the repository test stays tiny.

## Safe Parallel Work Candidates

| Candidate | Files | Parallel Safe? | Notes |
|---|---|---|---|
| Persistence failure selection test | same test file and possibly repository helper | Yes, if exclusive | Best next unit. |
| Missing-template/data negative audit | handler/service/test read-only | Yes after persistence test | Separate follow-up candidate. |
| Packet-factory dispatch audit | packet factory/tests read-only or separate packet test | Yes if not editing fixture | Useful later to reduce reflection seam risk. |
| Progress/handoff docs | docs | No | Orchestrator-owned after validation. |

## Do Not Parallelize

- Multiple writers in `GameServerConnectionInventoryExpansionUseItemTests.cs`.
- Broad handler edits unless the persistence test exposes a mismatch.
- Progress and handoff docs.

## Resume Checklist

1. Read `docs/csharp-port.md`, orchestration docs, `docs/PHASE-6-PROGRESS.md`, and this handoff.
2. Confirm branch status and latest commit.
3. Run parallel work discovery before selecting subagents.
4. Use Java as source of truth and preserve breadcrumbs.
5. Keep the next unit narrow: selectable persistence failure rollback/no-op.
6. Run focused and full tests.
7. Update Migration Parity Table, Remaining Risks, Summary Metrics, and Next Recommended Unit.
8. Create the next handoff and commit the completed unit.
