# Phase 6ZN Completion Handoff

Date: May 26, 2026
Latest Unit of Work: UOW-1178
Status: Phase 6 continues; trade-list/trade-in live sends remain disabled.

## Session Summary

UOW-1178 added the guarded C# byte/parameter comparison helper for future generated no-sell `SM_SYSTEM_MESSAGE` trade-vector artifacts. The artifact reader now has future body-byte comparison gates for all three packet families in the current trade-list vector design: `SM_TRADELIST`, `SM_TRADE_IN_LIST`, and no-sell `SM_SYSTEM_MESSAGE`.

Files changed:

- `dotnetConversion/tests/Aion.GameServer.Tests/TradeListJavaVectorArtifactReaderTests.cs`
- `docs/PHASE-6-PROGRESS.md`
- `docs/Phase-6ZN-Completion.md`

No Java generator artifacts, live sends, or production runtime behavior changed.

## What Changed

- Broadened the no-sell semantic-key guard to allow the documented keys:
  - `buy-no-trade-list`
  - `buy-no-sellable-goods`
  - `buy-restricted-goods`
  - `trade-in-no-template`
- Added `AssertGeneratedSystemMessageBodyMatchesCSharpWhenPresent`.
- The generated-artifact path now:
  - parses schema-v1 JSON;
  - validates packet-family semantic shape;
  - when `SM_TRADELIST.bodyHex` is present, compares it to C# `SmTradeList` body bytes;
  - when `SM_TRADE_IN_LIST.bodyHex` is present, compares it to C# `SmTradeInList` body bytes;
  - when no-sell `SM_SYSTEM_MESSAGE.bodyHex` is present, reconstructs `SmSystemMessage` from decoded `messageId` and ordered `messageParams`, then compares normalized Java `bodyHex` to C# body bytes.

Inline samples remain contract samples only. They are not treated as Java-generated byte evidence.

## Validation

- `git diff --check` passed with an existing line-ending warning only.
- `dotnet test dotnetConversion/tests/Aion.GameServer.Tests/Aion.GameServer.Tests.csproj --filter "TradeListJavaVectorArtifactReaderTests" --nologo` passed 4 tests.

## Migration Parity Table - UOW-1178

| Java Artifact | C# Artifact | Type | Port Status | Test Status | Parity Status | Notes |
|---|---|---|---|---|---|---|
| future `com.aionemu.gameserver.parity.trade.TradeListVectorGenerator` artifact schema | `Aion.GameServer.Tests.TradeListJavaVectorArtifactReaderTests` | Test Utility / Artifact Reader / Verifier | Partial | Unit Tested | Needs Verification | Reader now has guarded body-byte comparison paths for future generated `SM_TRADELIST`, `SM_TRADE_IN_LIST`, and no-sell `SM_SYSTEM_MESSAGE` artifacts. No Java generator output exists locally, so byte branches are not executed yet. |
| `com.aionemu.gameserver.network.aion.serverpackets.SM_TRADELIST` | `Aion.GameServer.Network.Aion.ServerPackets.SmTradeList` / `SmTradeListPacketPlanService` | Packet / Verifier Target | Partial | Unit Tested | Needs Verification | Future generated `bodyHex` comparison remains ready but still unexecuted locally. |
| `com.aionemu.gameserver.network.aion.serverpackets.SM_TRADE_IN_LIST` | `Aion.GameServer.Network.Aion.ServerPackets.SmTradeInList` / `SmTradeInListPacketPlanService` | Packet / Verifier Target | Partial | Unit Tested | Needs Verification | Future generated `bodyHex` comparison remains ready but still unexecuted locally. |
| `com.aionemu.gameserver.network.aion.serverpackets.SM_SYSTEM_MESSAGE` | `Aion.GameServer.Network.Aion.ServerPackets.SmSystemMessage` | Packet / Verifier Target | Partial | Unit Tested | Needs Verification | Future generated no-sell `bodyHex` can now be compared to C# serialized body bytes reconstructed from decoded message id and ordered parameters. Missing Java artifacts prevent runtime comparison. |

Tests added or updated:

| Test Name | Type | Java Behavior Source | What It Validates | Parity Evidence | Gaps |
|---|---|---|---|---|---|
| Existing `TradeListJavaVectorArtifactReaderTests.FindTradeListJavaArtifacts_IsGuardedUntilGeneratorOutputExists` | Unit / Guarded Artifact Reader and Future Byte Verifier | Future `parity-artifacts/trade-list/java/*.json`; Java `SM_TRADELIST.writeImpl`; Java `SM_TRADE_IN_LIST.writeImpl`; Java `SM_SYSTEM_MESSAGE.writeImpl` | If future artifacts exist, validates schema, semantic shape, and compares generated trade-list/trade-in/system-message `bodyHex` to C# packet body serialization. | Guarded future-artifact comparison code is compiled and covered for the no-artifact branch only. | No artifacts exist locally, so Java-vs-C# byte comparison has not executed. |
| Existing inline artifact reader tests | Unit / Artifact Contract | Future Java generator schema and documented no-sell semantic keys | Continue validating contract samples and semantic guards, including the broader no-sell semantic-key set. | Deterministic schema/shape tests only. | Inline samples are not treated as Java runtime byte evidence. |

## Summary Metrics

- Total Java artifacts discovered: 4 grouped artifact rows in this unit
- Total artifacts ported: 0 runtime artifacts; 1 guarded C# artifact reader test file extended with future no-sell `SM_SYSTEM_MESSAGE` body-byte comparison
- Total artifacts with verified parity: 0 in this unit
- Total artifacts needing verification: 4 grouped rows
- Total blocked artifacts: 4 blocked/partial categories: Java generator, generated artifacts, canonical/wire-frame comparison, and live packet sends
- Estimated overall migration completion: Phase 6 remains about 71% complete

## Remaining Risks

- Java runtime vector generator and artifacts are still missing because local Java 25/Maven tooling remains unavailable.
- The `SM_TRADELIST`, `SM_TRADE_IN_LIST`, and no-sell `SM_SYSTEM_MESSAGE` body-byte helpers are not executed locally until generated artifacts are present.
- `canonicalPayloadHex` and encrypted `wireFrameHex` comparison remain unverified.
- Reconstructing C# packets from decoded artifact fields assumes generated artifacts include complete decoded packet metadata; malformed artifacts are still rejected by semantic guards first.
- Live `SM_TRADELIST`, `SM_TRADE_IN_LIST`, and no-sell `SM_SYSTEM_MESSAGE` sends remain disabled.

## Next Recommended Unit of Work

Primary next unit:

- Add `canonicalPayloadHex` comparison for generated trade-vector artifacts.

Suggested scope:

- Reuse the generated-artifact branch and semantic guard pattern.
- Compare only generated Java `canonicalPayloadHex` when present.
- Preserve the existing body-only comparisons and do not treat inline samples as Java runtime byte evidence.
- Do not enable live sends.

## Next Work Options

### Recommended Sequential Task

- Task: Add generated-artifact `canonicalPayloadHex` comparison for `SM_TRADELIST`, `SM_TRADE_IN_LIST`, and no-sell `SM_SYSTEM_MESSAGE`.
- Why: Body-byte gates now exist for all three packet families; canonical opcode-plus-body comparison is the next evidence layer before generated Java artifacts can prove serializer parity.
- Files: `dotnetConversion/tests/Aion.GameServer.Tests/TradeListJavaVectorArtifactReaderTests.cs`, `docs/PHASE-6-PROGRESS.md`, next `docs/Phase-6ZO-Completion.md`.

### Safe Parallel Candidates

| Candidate | Task | Files | Risk | Notes |
|---|---|---|---|---|
| A | DB integration proof for `Player.LegionLevel` hydration | New or existing focused DB test file only | Medium | Requires integration DB availability; avoid touching trade-vector test file concurrently. |
| B | Broader `PricesService` parity audit | New docs-only audit file | Low | Read-only Java/C# analysis plus a standalone doc is safe if progress/handoff docs remain orchestrator-owned. |
| C | Java generator skeleton feasibility rerun | Java test-only files under `game-server/test` only | High | Only safe in an environment with Java 25 JDK and Maven; local tooling was previously blocked. |
| D | Trade-vector canonical comparison | `TradeListJavaVectorArtifactReaderTests.cs` only | Low | Recommended sequential task because it touches the same verifier file as recent units. |

### Suggested Parallel Batch

| Agent | Task | Allowed Files | Forbidden Files |
|---|---|---|---|
| Orchestrator | Canonical payload comparison | `dotnetConversion/tests/Aion.GameServer.Tests/TradeListJavaVectorArtifactReaderTests.cs`, progress/handoff docs | None |
| Agent A | PricesService audit, if spawned | New `docs/TradeList-PricesService-*.md` only | `TradeListJavaVectorArtifactReaderTests.cs`, progress/handoff docs, production code |
| Agent B | Legion DB proof discovery, if spawned | Read-only or a new focused DB test file after ownership is assigned | Trade-vector tests, production repository files, progress/handoff docs |

### Do Not Parallelize

- `TradeListJavaVectorArtifactReaderTests.cs`: active verifier surface; one owner at a time.
- `docs/PHASE-6-PROGRESS.md` and Phase 6 handoff docs: orchestrator-owned shared migration history.
- Java generator implementation: do not combine with C# verifier edits unless Java 25/Maven tooling is available and file ownership is explicit.

Do not start live packet send wiring until Java vector artifacts and C# verifier tests exist and execute against generated Java output.
