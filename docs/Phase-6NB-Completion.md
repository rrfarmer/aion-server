# Phase 6NB Completion Handoff - Selectable Decompose Reward Selection

Date: May 24, 2026
Unit of Work: UOW-854
Branch: `4.8`
Commit: pending at handoff creation (`[Phase 6][UOW-854] Test selectable decompose reward selection`)

## Status

Phase 6 is still in progress. This unit added a narrow runtime regression for valid selectable decompose reward selection through `GameServerConnection`.

The test verifies one successful C# selection path. It does not cover invalid indices, filtered unobtainable indices, source delete, existing-stack merge, persistence failure, Java runtime comparison, or live-client validation.

## Files Changed

- `dotnetConversion/tests/Aion.GameServer.Tests/GameServerConnectionInventoryExpansionUseItemTests.cs`
- `docs/PHASE-6-PROGRESS.md`
- `docs/Phase-6NB-Completion.md`

## What Changed

- Added `HandleSelectDecomposableAsync_SelectableRewardConsumesSourceAndAddsReward`.
- Added a `CmSelectDecomposable` packet helper and private-handler invocation helper for the fixture.
- Let the fixture accept an optional `IDFactory`, so new reward rows get deterministic object ids rather than the fallback id.
- Added payload assertions for the empty `SmSecondaryShowDecomposable`.
- The new test verifies:
  - valid selection index `1` maps to reward item `202`
  - source item `101` decreases from count `2` to `1`
  - reward item `202` is added with count `3`
  - packet order is default `SmItemUsageAnimation`, success `SmSystemMessage`, source `SmInventoryUpdateItem`, empty `SmSecondaryShowDecomposable`, and reward `SmInventoryAddItem`
  - default selection animation uses `time=0`, `end=1`, and final unknown/cooldown field `1`, matching the reviewed Java constructor

## Tests

Focused:

```powershell
dotnet test dotnetConversion\tests\Aion.GameServer.Tests\Aion.GameServer.Tests.csproj --filter "GameServerConnectionInventoryExpansionUseItemTests"
```

Result: passed, 10 tests.

Full:

```powershell
dotnet test dotnetConversion\tests\Aion.GameServer.Tests\Aion.GameServer.Tests.csproj
```

Result: passed, 1426 tests.

New test:

| Test Name | What It Validates | Java Comparison |
|---|---|---|
| `GameServerConnectionInventoryExpansionUseItemTests.HandleSelectDecomposableAsync_SelectableRewardConsumesSourceAndAddsReward` | C# valid selectable reward-selection runtime behavior, source decrement, selected reward add, default animation payload, success/secondary packet ordering, and reward add packet. | Source-derived Java review of `CM_SELECT_DECOMPOSABLE.runImpl`; no Java runtime comparison. |

## Migration Parity Snapshot

| Java Artifact | C# Artifact | Type | Port Status | Test Status | Parity Status | Notes |
|---|---|---|---|---|---|---|
| `com.aionemu.gameserver.network.aion.clientpackets.CM_SELECT_DECOMPOSABLE` | `GameServerConnection.HandleSelectDecomposableAsync` / `GameServerConnectionInventoryExpansionUseItemTests` | Client Packet Handler / Runtime Test | Partial | Regression Tested | Partial Parity | Valid selectable reward index is covered. Invalid index, unobtainable filtered index, missing data, source delete path, existing-stack merge, persistence failure, packet-factory dispatch, Java runtime comparison, and live-client behavior remain unverified. |
| `com.aionemu.gameserver.model.templates.item.actions.DecomposeAction` | selectable first-show plus selection follow-up | Dynamic Item Action Caller / Follow-up Flow | Partial | Regression Tested through two runtime tests | Partial Parity | Sessions 853 and 854 cover first-show and one valid selection completion. Normal completion success/failure and selectable edge cases remain unverified. |
| `com.aionemu.gameserver.network.aion.serverpackets.SM_ITEM_USAGE_ANIMATION` | `Aion.GameServer.Network.Aion.ServerPackets.SmItemUsageAnimation` | Packet / Selection Completion | Partial | Regression Tested for this call site | Partial Parity | Default animation payload is asserted. Java-generated bytes, opcode/frame/crypto, broadcast visibility, and write-time `usingItem` side effects remain unverified. |
| `com.aionemu.gameserver.network.aion.serverpackets.SM_SYSTEM_MESSAGE` | `SmSystemMessage.UncompressCompressedItemSucceeded(...)` | System Message / Selection Completion | Partial | Regression Tested for packet type/order | Needs Verification | Packet type and order are covered; message id/payload bytes and localization remain unverified. |
| `com.aionemu.gameserver.network.aion.serverpackets.SM_SECONDARY_SHOW_DECOMPOSABLE` | `Aion.GameServer.Network.Aion.ServerPackets.SmSecondaryShowDecomposable` | Packet / Selection Completion | Partial | Regression Tested for empty-list payload | Partial Parity | Empty secondary-show payload is asserted for source object id `5001`. Java-generated bytes, opcode/frame/crypto, and non-empty TODO behavior remain unverified. |
| `com.aionemu.gameserver.services.item.ItemService` / `ItemPacketService.ItemAddType.DECOMPOSABLE` | `InventoryAddService.CreateAddItemPlan` / `SmInventoryAddItem.CreateDecomposable` | Inventory Mutation / Reward Add Dependency | Partial | Regression Tested for new-row reward add | Partial Parity | New reward row is covered. Existing-stack merge, inventory-full behavior, persistence rollback, object-id allocation beyond this fixture, and Java DB/runtime comparison remain unverified. |
| `com.aionemu.gameserver.model.gameobjects.player.Inventory.decreaseByObjectId` | `ApplySourceItemMutationAsync` / `SmInventoryUpdateItem.DecreaseItemUse` | Inventory Mutation / Source Consume Dependency | Partial | Regression Tested for decrement path | Partial Parity | Source decrement is covered. Single-count delete path and delete-packet ordering remain unverified. |
| `com.aionemu.gameserver.dataholders.DecomposableItemsData` | fixture `DecomposableItemTable` / `DecomposeService.CreateSelectableRewardPlan` | Static Data / Reward Selection Dependency | Partial | Regression Tested through valid index | Partial Parity | Valid index after obtainable filtering is covered. Invalid index, filtered-index semantics, broad race/class combinations, and random min/max ranges remain unverified. |

## Remaining Risks

- Selectable reward negative paths remain untested: invalid index, index after unobtainable filtering, missing source item, missing selectable data, missing reward template, and persistence failure.
- Source consume covers decrement only; single-count delete and delete-packet ordering remain unverified.
- Reward add covers new-row add only; existing-stack merge and inventory-full handling remain unverified.
- The test invokes a private handler reflectively; packet-factory dispatch for real client frames remains unverified.
- Parity is based on C# runtime tests and Java source review, not Java-generated packet bytes or live Java runtime comparison.

## Summary Metrics

- Total Java artifacts discovered: 8
- Total artifacts ported: 1 C# runtime regression slice for selectable decompose valid reward selection
- Total artifacts with verified parity: 0
- Total artifacts needing verification: 8
- Total blocked artifacts: 25 blocked/not-started categories
- Estimated overall migration completion: Phase 6 remains about 66% complete

## Next Recommended Unit of Work

Add a compact negative-path regression for selectable reward selection: invalid index and/or filtered unobtainable index should produce no packets and no inventory mutation, matching Java's early return. If small, include missing selectable data; otherwise leave persistence failure and source-delete path for later units.

Suggested scope:

- Reuse the selectable fixture and invoke `HandleSelectDecomposableAsync` with an out-of-range index.
- If fixture shape permits, also choose an index that would only exist before unobtainable filtering and confirm C# returns early.
- Assert source and reward inventory state remains unchanged and no packets are emitted.
- Keep persistence failure, source-delete, and existing-stack merge as separate follow-ups unless the negative tests stay tiny.

## Safe Parallel Work Candidates

| Candidate | Files | Parallel Safe? | Notes |
|---|---|---|---|
| Selectable negative-path tests | same test file | Yes, if exclusive | Best next unit. |
| Source-delete selection audit | test file and handler read-only/code | Yes after negative tests | Separate follow-up candidate for count `1` source. |
| Existing-stack merge selection audit | test file and inventory add planner read-only | Yes after negative tests | Separate follow-up candidate for stack update packets. |
| Progress/handoff docs | docs | No | Orchestrator-owned after validation. |

## Do Not Parallelize

- Multiple writers in `GameServerConnectionInventoryExpansionUseItemTests.cs`.
- Broad handler edits unless a negative-path test exposes a behavioral mismatch.
- Progress and handoff docs.

## Resume Checklist

1. Read `docs/csharp-port.md`, orchestration docs, `docs/PHASE-6-PROGRESS.md`, and this handoff.
2. Confirm branch status and latest commit.
3. Run parallel work discovery before selecting subagents.
4. Use Java as source of truth and preserve breadcrumbs.
5. Keep the next unit narrow: selectable negative selection paths.
6. Run focused and full tests.
7. Update Migration Parity Table, Remaining Risks, Summary Metrics, and Next Recommended Unit.
8. Create the next handoff and commit the completed unit.
