# Phase 6ZM Completion Handoff

Date: May 26, 2026
Latest Unit of Work: UOW-1177
Status: Phase 6 continues; trade-list/trade-in live sends remain disabled.

## Session Summary

UOW-1177 added the guarded C# packet-body comparison helper for future generated `SM_TRADE_IN_LIST` Java vector artifacts. The artifact reader now has future body-byte comparison gates for both trade-list and trade-in packet families, but neither branch executes locally until Java artifacts exist.

Files changed:

- `dotnetConversion/tests/Aion.GameServer.Tests/TradeListJavaVectorArtifactReaderTests.cs`
- `docs/PHASE-6-PROGRESS.md`
- `docs/Phase-6ZM-Completion.md`

No Java generator artifacts, live sends, or production runtime behavior changed.

## What Changed

- Added `AssertGeneratedTradeInListBodyMatchesCSharpWhenPresent`.
- The generated-artifact path now:
  - parses schema-v1 JSON;
  - validates packet-family semantic shape;
  - when `SM_TRADELIST.bodyHex` is present, compares it to C# `SmTradeList` body bytes;
  - when `SM_TRADE_IN_LIST.bodyHex` is present, reconstructs `SmTradeInListPacketPlan`, serializes `SmTradeInList`, and compares normalized Java `bodyHex` to C# body bytes.

Inline samples remain contract samples only. They are not treated as Java-generated byte evidence.

## Validation

- `git diff --check` passed with an existing line-ending warning only.
- `dotnet test dotnetConversion/tests/Aion.GameServer.Tests/Aion.GameServer.Tests.csproj --filter "TradeListJavaVectorArtifactReaderTests" --nologo` passed 4 tests.

## Migration Parity Table - UOW-1177

| Java Artifact | C# Artifact | Type | Port Status | Test Status | Parity Status | Notes |
|---|---|---|---|---|---|---|
| future `com.aionemu.gameserver.parity.trade.TradeListVectorGenerator` artifact schema | `Aion.GameServer.Tests.TradeListJavaVectorArtifactReaderTests` | Test Utility / Artifact Reader / Verifier | Partial | Unit Tested | Needs Verification | Reader now has guarded body-byte comparison paths for future generated `SM_TRADELIST` and `SM_TRADE_IN_LIST` artifacts. No Java generator output exists locally, so byte branches are not executed yet. |
| `com.aionemu.gameserver.network.aion.serverpackets.SM_TRADELIST` | `Aion.GameServer.Network.Aion.ServerPackets.SmTradeList` / `SmTradeListPacketPlanService` | Packet / Verifier Target | Partial | Unit Tested | Needs Verification | Future generated `bodyHex` comparison remains ready from UOW-1176 but still unexecuted locally. |
| `com.aionemu.gameserver.network.aion.serverpackets.SM_TRADE_IN_LIST` | `Aion.GameServer.Network.Aion.ServerPackets.SmTradeInList` / `SmTradeInListPacketPlanService` | Packet / Verifier Target | Partial | Unit Tested | Needs Verification | Future generated `bodyHex` can now be compared to C# serialized body bytes reconstructed from decoded artifact fields. Missing Java artifacts prevent runtime comparison. |
| `com.aionemu.gameserver.network.aion.serverpackets.SM_SYSTEM_MESSAGE` | future C# artifact verifier using `SmSystemMessage` | Packet / Verifier Target | Partial | Unit Tested | Needs Verification | Semantic guard remains in place, but system-message byte/parameter serialization comparison remains future work. |

Tests added or updated:

| Test Name | Type | Java Behavior Source | What It Validates | Parity Evidence | Gaps |
|---|---|---|---|---|---|
| Existing `TradeListJavaVectorArtifactReaderTests.FindTradeListJavaArtifacts_IsGuardedUntilGeneratorOutputExists` | Unit / Guarded Artifact Reader and Future Byte Verifier | Future `parity-artifacts/trade-list/java/*.json`; Java `SM_TRADELIST.writeImpl`; Java `SM_TRADE_IN_LIST.writeImpl` | If future artifacts exist, validates schema, semantic shape, and compares generated trade-list/trade-in `bodyHex` to C# packet body serialization. | Guarded future-artifact comparison code is compiled and covered for the no-artifact branch only. | No artifacts exist locally, so Java-vs-C# byte comparison has not executed. |
| Existing inline artifact reader tests | Unit / Artifact Contract | Future Java generator schema | Continue validating contract samples and semantic guards. | Deterministic schema/shape tests only. | Inline samples are not treated as Java runtime byte evidence. |

## Summary Metrics

- Total Java artifacts discovered: 4 grouped artifact rows in this unit
- Total artifacts ported: 0 runtime artifacts; 1 guarded C# artifact reader test file extended with future `SM_TRADE_IN_LIST` body-byte comparison
- Total artifacts with verified parity: 0 in this unit
- Total artifacts needing verification: 4 grouped rows
- Total blocked artifacts: 4 blocked/partial categories: Java generator, generated artifacts, system-message byte comparison, and live packet sends
- Estimated overall migration completion: Phase 6 remains about 71% complete

## Remaining Risks

- Java runtime vector generator and artifacts are still missing because local Java 25/Maven tooling remains unavailable.
- The `SM_TRADELIST` and `SM_TRADE_IN_LIST` body-byte helpers are not executed locally until generated artifacts are present.
- `canonicalPayloadHex` and no-sell `SM_SYSTEM_MESSAGE` byte/parameter serialization remain unverified.
- Reconstructing C# plans from decoded artifact fields assumes generated artifacts include Java-filtered tab ids and complete decoded packet metadata; malformed artifacts are still rejected by semantic guards first.
- Live `SM_TRADELIST`, `SM_TRADE_IN_LIST`, and no-sell `SM_SYSTEM_MESSAGE` sends remain disabled.

## Next Recommended Unit of Work

Primary next unit:

- Add guarded byte/parameter comparison for no-sell `SM_SYSTEM_MESSAGE`.

Suggested scope:

- Reuse the generated-artifact branch and semantic guard pattern.
- Compare only generated Java system-message bytes or decoded parameters when present.
- Preserve the distinction between decoded semantic parameter checks and byte serialization checks.
- Keep inline samples contract-only and do not enable live sends.

Safe parallel candidates:

- DB integration proof for `Player.LegionLevel` hydration if an integration database is available.
- Broader `PricesService` parity audit for `SM_PRICES`, taxes, influence, service prices, and sell rewards.
- Java generator skeleton only in an environment with Java 25 JDK and Maven.

Do not start live packet send wiring until Java vector artifacts and C# verifier tests exist.
