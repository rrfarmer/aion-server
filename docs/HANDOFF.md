# HANDOFF — current state

The single rolling state doc. Updated **in place** every Unit of Work — keep it lean: detail only the most recent round, collapse older batches to one-liners (git history holds the rest), but **never drop a TODO/backlog/blocker**. Must be usable with zero prior conversation. Read after the canonical docs in `csharp-port.md`.

Last updated: 2026-06-08

## ⏯️ RESUME HERE (cold-start — read this first)

**You are mid-way through the object-spine big-bang on a branch. Pick up the dedicated core-convergence push.**

1. **Branch:** `git checkout feature/object-spine-bigbang` (work happens here; `main` is the green baseline — do NOT commit core WIP to `main`). Confirm with `git branch --show-current`.
2. **Mode:** `/loop` dynamic, **60-second** cadence (user wants minimal dead time). Each iteration: port the next faithful unit(s), run guardrail, commit, update this doc, then `ScheduleWakeup delaySeconds:60` with the verbatim `/loop` prompt.
3. **What's done (this session, big):** ALL independently-portable substrate harvested — the **geoEngine + Commons.Nio query stack (37 files: math/collision/BIH/bounding/scene/models + nio buffers + `GeoService` singleton)**, the **observer cone** (`ObserveController`+bases+subclasses), combat/AI/stats-fn leaves (`AggroList`/`DamageList`/`Rnd`/`ThreadPoolManager` singleton-bridge), and **Tier 0 of the convergence push is UNDERWAY**: `EffectTemplate`, condition bases (`Condition`/`Conditions`), `ActionModifier(s)`, `Change`/`Func`, and condition subclasses 12/24.
4. **What's left = the indivisible Effect/Skill/Creature core** — see the **🎯 CONVERGENCE PUSH PLAN (Tiers 0–4)** lower in this doc (the authoritative ordered plan). Tier 0 = skill/effect data templates + cones (in progress); Tier 1 = runtime `Effect`/`Skill`/`StatFunctions`/`EffectController`; Tier 2 = Creature stat containers (+SM_ATTACK_STATUS reconciliation); Tier 3 = `Player`→`Creature` reparent + `Creature`/`Npc`/etc; Tier 4 = packets/spatial/singleton-bridge → green → merge. Branch stays RED until it compiles together. **Prerequisite decisions already made:** singleton-bridge (DataManager statics), polymorphic-XML `[XmlInclude]`, SM_ATTACK_STATUS byte-safe reconciliation — all documented below.
5. **Next concrete action — TIER 0 IS EXHAUSTED (✅), TIER 1 IS NOW THE ACTIVE PHASE.** Every remaining Tier-0 cone file (effect mid-bases/leaves, action leaves+`Actions`, periodicaction leaves+`PeriodicActions`, properties `*Property` processors+`Properties`, `Effects` wrapper) is BLOCKED on the indivisible Tier-1/2 SCC (runtime `Effect`/`Skill`/`EffectController`/`Creature`/`Player`) + packets (`PacketSendUtility`/`SM_SYSTEM_MESSAGE` STR_*/`SM_RESURRECT`) + SM_ATTACK_STATUS (TYPE/LOG). Clean Tier-0 substrate fully harvested incl. abstract bases `Action`/`PeriodicAction` (lowercase ns) and the AbstractAbsoluteStat data holders. **The SCC must now be ported as a focused multi-tick push (frontier method, heavy red OK), suggested order:** runtime `Effect`(1232L, keystone — the whole ported effect cone waits on it) → `EffectController`(778L, unblocks abnormal-state/dispel/heal/transform effect bases) → `StatFunctions`(695L static formulas) → `Skill`(1023L, unblocks action/properties cones) → Creature stat containers (`CreatureGameStats`/`CreatureLifeStats`/`Stat2`✅) + **SM_ATTACK_STATUS reconciliation** (add nested TYPE/LOG SCREAMING_SNAKE + Creature ctor + migrate 5 callers — unblocks Hp/Mp conditions, HpUse/MpUse actions+periodicactions, damage/heal effect cones) → `Creature` → `Player` reparent → green → merge. **Deferred geoEngine leaves:** `GeoWorldLoader` (needs image-lib decision) + `Matrix4f` (single caller).
6. **Per-unit loop:** read the Java file fully → port 1:1 (NO gaps/stubs) → `dotnet build src/Aion.GameServer` (RED expected on branch; just confirm no NEW non-forward-ref errors in files you wrote) → `python scripts/parity/check_fidelity.py` (must stay 363/5) → `git -c user.name="rrfarmer" -c user.email="ryanfarmer@mac.com" commit --author="rrfarmer <ryanfarmer@mac.com>"` (NO AI co-author) → update this doc.
7. **Conventions/gotchas (apply consistently):** XML data classes use **public properties** (C# XmlSerializer needs public; Java `@XmlAccessorType(FIELD)`). `getType()`→a `Type` property or `GetTypeValue()` (never a `GetType()` method/extension — clashes with `Object.GetType()`). `afterUnmarshal`→`AfterUnmarshal()` method. Java annotations→C# attributes. SCREAMING_SNAKE preserved for `@XmlEnum`/data-bound enums. `System.currentTimeMillis()`→`DateTimeOffset.UtcNow.ToUnixTimeMilliseconds()`. Java enums-with-data → C# enum + extension class (data in dictionaries).

## Current position

- Phase 6, **re-baselined** to the Port Fidelity & Remediation Plan (`docs/Port-Fidelity-Remediation-Plan.md`). Earlier Phase-6 work is behaviorally faithful but structurally slop (plan-service sprawl + a god-class `GameServerConnection.cs`); being re-ported to 1:1 Java fidelity.
- **Phase A (foundation): DONE except formula golden capture.** Golden packet pipeline proven (Java generator → `parity-artifacts/golden/packets/`; C# `GoldenPacketFixtureTests` asserts byte-for-byte). Structural audit + fidelity guardrail (`scripts/parity/check_fidelity.py`, baseline 363 slop / 6 god-classes, in CI) done. **TODO: formula golden capture not built** (last foundation item).
- Build: `main` is green (nullable warnings only). The **`feature/object-spine-bigbang` branch is RED BY DESIGN** during the core convergence (see RESUME HERE) — that is expected, not a regression.
- **Active work: the object-spine big-bang core convergence on the branch** (see RESUME HERE + below).

## Direction: FOUNDATION-FIRST (bottom-up), 1:1 Java

There is NO object hierarchy in C#: `Player` is a flat `sealed class`; `VisibleObject`/`Creature`/`Npc`/`World`/`WorldMap` are MISSING. The C# model is a flat parallel collection built for the packet path. Build the spine bottom-up so the faked feature clusters can later collapse.

**Spine sequence** (each a faithful 1:1 port; Java line counts in parens):
- **F1 `AionObject`** (79) — **DONE** (`Model/GameObjects/AionObject.cs`).
- **F2 `VisibleObject`** (256) + `VisibleObjectController` (124).
- **F3 `Creature`** (522) + `CreatureController` (568) — incl. the TaskId task system.
- **F4 reparent flat `Player` → `Creature`** (Java 1655; C# 1442, **329 referencing files**) — high-risk integration.
- **F5 `Npc`** (393) + `NpcController` (340).
- **F6 `KnownList`**; **F7 `World`/`WorldMap`/`MapRegion`**.

**How to choose the next unit:** never start a node with an unmet dependency. From the in-scope spine cone, pick a unit whose deps all already exist in C#; if none, port the deepest still-missing dep. Read the Java fully, port 1:1, build, commit (code + HANDOFF). **Never stub/defer.** Don't port dependency-free files unrelated to the spine cone.

## Strategy resolved (user, 2026-06-07): big-bang + SCC-leaves-first

- **Doctrine rule 8** (in `Port-Fidelity-Remediation-Plan.md` §3 + memory): when a new faithful port conflicts with existing C# code, **default to 1:1 Java parity and replace the existing code**; only when the conflict is a genuine **C#-vs-Java foundational language difference** take the closest-to-1:1 path instead.
- **Execution model:** `VisibleObject ↔ Creature ↔ World/MapRegion/WorldMapInstance/WorldPosition ↔ KnownList ↔ subsystems` form one big SCC. In a single assembly a partial SCC won't compile, so the SCC core lands as **one final big-bang commit**. Until then, port the SCC's **dependency-free leaves** bottom-up (green each batch). The `WorldPosition` struct→class swap (below) is part of that closing big-bang.

## Creature SCC cone — leaf progress

Older spine/leaf batches (git has detail): animations, templates (`BoundRadius`/`IL10n`/`VisibleObjectTemplate`), world/zone enums (`WorldType`,`WorldDropType`,`ZoneName`,`ZoneAttributes`,`ZoneClassName`,`AIEventType`), `Race`/`TribeClass`/siege/vortex, gametime, **F2 spawn-data cluster (50 files)**, **world-infra batch (6)**, **geometry+zone-template batch (19)**.

Recent (this program of work):
- `22e208019` Creature prereqs: `CreatureState`,`CreatureVisualState`,`TaskId`,`NpcObjectType`,`CreatureTemplate`,`RegionZone`
- `3994a5c75` stats/skill enums: `StatEnum`,`SkillElement`,`ItemAttackType`,`AbnormalState`
- `fb0b2091c` AI/observer: `AISubState`,`AIState`,`ObserverType`
- `449d687e5` `TransformType`,`HopType`,`ShieldType`,`CalculationType`,`IStatOwner`
- `effb209b7` SkillTemplate enums (9): `ActivationAttribute`,`DispelCategoryType`,`HostileType`,`SkillCategory`,`SkillSubType`,`SkillType`,`StigmaType`,`SkillTargetSlot`,`DispelSlotType`
- `3538242ab` effect enums: `SpellStatus`,`EffectResult`,`HitType`,`AttackType`,`EffectType`
- `28e8fc389` skill condition/properties/change: `TargetAttribute`,`AreaDirections`,`FirstTargetAttribute`,`TargetRangeAttribute`,`TargetRelationAttribute`,`TargetSpeciesAttribute`,`Func`
- `634e6196a` effect-controller: `CumulativeResistType`,`CumulativeResist`
- `310ce8879` item enums: `ArmorType`,`EquipType`,`ItemSubType`
- `17c02776a` item: `ItemSlot` (long-mask), `ItemGroup`
- `dc5c40c03` stats-template: `StatsTemplate`,`CreatureSpeeds`
- `6d981ed5d` `PlayerClass` + `PlayerStatCalculator` (mutual SCC) — **PlayerClass unblocked**. (Java `implements L10n`; C# enums can't implement interfaces → `GetL10nId()` extension per rule 8. New `enum PlayerClass` coexists additively with legacy `string Player.PlayerClass`, reconciled at F4.)
- `c1ff3c9c1`+`f55e20a9a` item enums: `ItemQuality`,`ItemType`,`WeaponType`,`LeftHandSlot`,`AcquisitionType`,`RandomType`,`ExceedEnchantSkillSetType`(116)
- item data-classes (13): `Acquisition`,`GodstoneInfo`,`WeaponStats`,`Improvement`,`Idian`,`Disposition`,`RequireSkill`,`AssemblyItem`,`AssembledItem`,`TradeinItem`,`TradeinList`,`ExtraInventory`,`RandomItem`
- `Gender`,`ItemId`,`ItemMask`; NpcTemplate-cone (7): `AbyssNpcType`,`NpcRank`,`NpcTemplateType`,`SubDialogType`,`MassiveLoot`,`TalkInfo`,`KiskStatsTemplate`; `CreatureType`
- **`Race` re-ported to SCREAMING_SNAKE** (XML data parity; restored `LIVINGWATER(28)` alias; migrated `BaseOccupier`/`SiegeRace`) + `ItemActivationTarget` unblocked
- **`DialogAction`** — 1:1 port of the 6246-line Java file (6205 `const int` + reflection name lookup); guardrail refined to exempt faithful large Java-mirrored files (baseline 6→5 god-classes)

**Leaf-vein status — clean leaves essentially EXHAUSTED.** skillengine/stats/effect/AI/item-enum/item-data/npc-template-enum veins all harvested. Remaining cone nodes block on the SCC core or are special cases (see TODOs). **No further trivially-clean leaves to port without a decision or the big-bang.** Out-of-cone, deliberately NOT ported: `PlumStatEnum`,`DropRewardEnum`,`XPLossEnum`,`XPRewardEnum`.

## ⭐ BIG-BANG IN PROGRESS — branch `feature/object-spine-bigbang`

Executing the SCC on a branch via the **compiler-error-frontier** method: port faithful (pure-Java) files in dependency order; the branch build stays **red until the SCC converges**, then merge to main only when green + guardrail + golden pass. **No transitional fakery** — since nobody else is blocked and we merge only when faithful+green, `instanceId` derives from `MapRegion` per Java (the old stored-InstanceId struct was a non-Java artifact). `main` stays green; do NOT merge until convergence.

**CONVERGENCE = FULL LITERAL PORT, NO GAPS (user, 2026-06-08):** port the entire transitive cone faithfully — AI/stats/effect/skill/combat engines AND **de-god-class the packet layer** (`PacketSendUtility` + the ~200 `SM_`/`CM_` packets out of `GameServerConnection` into faithful packet classes). No method-body stubs. This effectively completes the bulk of the server port; grind bottom-up. (Tier-B substrate is almost entirely MISSING in C# — confirmed: `PacketSendUtility`/`AttackResult`/`Effect`/`Item`/`Skill`/`StatFunctions`/`Summon`/stats-containers all absent.)

**✅ geoEngine + Commons.Nio SUBSTRATE COMPLETE (2026-06-08, query path):** ported the entire geoEngine query stack so the Creature SCC's geo deps resolve — math (`Vector2f`/`Vector3f`/`FastMath`/`Matrix3f`/`Ray`), collision (`Collidable`/`CollisionResult(s)`/`CollisionIntention`/`IgnoreProperties`/`UnsupportedCollisionException`), `bih` (`BIHNode`/`BIHTree`), bounding (`BoundingVolume`/`BoundingBox`), scene (`Spatial`/`Node`/`Geometry`/`Mesh`/`DespawnableNode`/`CollisionData`/`mesh` index arrays), models (`GeoMap`/`Terrain`), `utils/TempVars`, `world/geo/GeoService` (Java singleton → `GetInstance()`), AND a faithful `Commons.Nio` (`ByteOrder`/`Buffer`/`Byte/Float/ShortBuffer`+Heap+AsFloat/AsShort views). **`GeoService.GetInstance().CanSee/getZ` now exist** (resolves AggroList + the spine's geo calls). **TWO geoEngine items DEFERRED:** (1) `GeoWorldLoader`(285L) — the `.geo`/PNG **data loader**; terminal leaf (only `GeoService.Init→Load`), **blocked on an external image-library decision** (decodes 16-bit/8-bit terrain PNGs via `ImageIO`/`BufferedImage`/`DataBufferUShort`/`DataBufferByte` — pick System.Drawing vs ImageSharp vs SkiaSharp) + mmap→`File.ReadAllBytes`+`ByteBuffer.Wrap` + parallelStream→PLINQ; (2) `Matrix4f`(1829L) — last math gap, only `BoundingBox.Transform` uses `MultProj`/`ToRotationMatrix`/`SetRotationMatrix`/`SetTranslation`. Neither blocks the SCC compile (both are forward-ref leaves). **Loop now PIVOTS back to the Creature SCC** (engines + spine + packets).

**Done on branch (~67 files, all faithful 1:1, guardrail green):**
- spine scaffolding: `WorldPosition`(struct→class), `RegionUtil`, `IZoneHandler`/`IAdvancedZoneHandler`, F2 layer (`VisibleObject`, `VisibleObjectController` base+generic, `KnownObject`, `KnownList`), `ZoneInstance`, `PositionUtil` VisibleObject overloads.
- stats engine: `Stat2`(+`AdditionStat`/`ReverseStat`), `StatCondition`, `NpcStatCalculation`, StatFunction cone (`IStatFunction`,`StatFunction`,`StatAdd/Rate/Sub/Set/Abs`,`StatFunctionProxy`).
- skillengine enums+data (~24): state/dispel/heal/provoke/etc. enums; `ChatType`; `Motion`/`Times`/`SignetData(+Template)`/`SkillAlias*`/`EffectReserved`(+`ResourceType`)/`WeaponTypeWrapper`/`SubEffect`/`ChainSkill(s)`/`Charged Skill`/`ChargeSkillEntry`.
- combat/AI/team: `AttackStatus`/`AttackResult`/`AttackerCriticalStatus`/`AggroInfo`/`AggroTarget`/`DamageInfo<T>`, `AttackIntention`/`AiNameAttribute`, `TeamType`/`ITeamEvent`/`ITeamMember<out M>`/`PlayerTeamMember`.
- base/model: `IPersistable`(+`PersistentState`)/`IExpirable`, `EmotionId`/`AttendType`/`DuelResult`/`LetterType`, `ItemStone`.

**Porting conventions (apply consistently):** clean-candidate scan finds batches but MISSES same-package `extends`/member usage — always verify. XML data classes use **public properties** (C# XmlSerializer needs public; Java `@XmlAccessorType(FIELD)`). `afterUnmarshal`→`AfterUnmarshal()` method. Java `getType()`→`GetTypeValue()` (an extension `GetType()` is shadowed by `Object.GetType()`); other `getType()`→property. Java annotations→C# attributes.

**Deferred (blocked — port with their prereq):** `StatArmor/Weapon/ShieldMasteryFunction`+`PlayerStatFunctions`+`StatCapUtil` (need Creature-`Player`/`Item`/`ItemTemplate`); `Conditions` cone (~25 subclasses → `Effect`/`Skill`); `MotionTime` (Player members); effect subclasses + `Effects` wrapper (→`EffectTemplate`); `SkillLearnTemplate` (→`SkillTemplate`). Out-of-cone (skip): `PlumStatEnum`, `Announcement`, `Letter`.

**✅ LEAF-HARVEST PHASE COMPLETE (~67 files).** Verified by scan: every remaining cone file needs the indivisible core. Specifically the condition cone (all ~25 subclasses) needs `Skill`+`Effect`+`Player`+`Item`+`PacketSendUtility`; all Tier-B containers (`AbstractAI`/`CreatureGameStats`/`CreatureLifeStats`/`EffectController`/`CreatureMoveController`/`ObserveController`/`AggroList`/`TransformModel`) need `Creature` (+packets); and `Creature` needs those containers as field types → one mutual SCC.

**⛔ REMAINING = the indivisible core convergence (NOT a leaf loop).** Must be written as one concentrated multi-file push and compiled together (red until it closes):
`Effect`(1232L)+`EffectTemplate`(572L)+~170 effect subclasses+`Effects` wrapper; `Skill`(1023L)+`SkillTemplate`(409L)+action/condition/property/periodic sub-cones; `Creature`(522L)+`CreatureController`+Tier-B containers; `Npc`/`Pet`/`StaticObject`/`StaticDoor`; spatial container `World`/`WorldMap`/`WorldMapInstance`/`MapRegion`; `PacketSendUtility` (substrate exists: 183 `Sm*`+`GameServerPacket`); reparent flat `Player`→`Creature` (F4, 329 files) + connection send path; reconcile the 4 `WorldPosition` InstanceId sites. Then build green → merge.
**Recommendation:** tackle as a dedicated focused push (hold the Creature/Effect/Skill APIs in-context, port in dependency tiers, build to shrink the frontier) — not 60s loop nibbles, since nothing here is independently verifiable.

### 🎯 CONVERGENCE PUSH PLAN (ordered tiers — execute in a focused session, `dotnet build` after each tier to shrink the error frontier)
**State (2026-06-08):** all independently-portable substrate is harvested — geoEngine+Commons.Nio query stack (37 files), observer cone, combat/AI/stats-fn leaves, geo configs. `GeoService.GetInstance().CanSee/GetZ` exist. Remaining = the mutually-entangled core below. Forward-refs between tiers are mechanical Java→C# PascalCase translations (low churn) — port bases first so later tiers resolve them.
- **⚠️ Tier-0 PREREQUISITE DECISION (polymorphic XML):** the C# port has **no `[XmlInclude]`/polymorphic-XML pattern** and already ships a **divergent `Dataholders/SkillTemplateTable`** (not the faithful `SkillTemplate`/`EffectTemplate` hierarchy). Java binds `EffectTemplate`(abstract)→~170 subclasses + `SkillTemplate` via JAXB `@XmlSeeAlso`/`@XmlElements`. C# `XmlSerializer` needs `[XmlInclude(typeof(SubA))]` on the base (or a custom polymorphic loader). **Before Tier 0:** decide the C# polymorphic-XML approach (recommend `[XmlInclude]` per-subclass on `EffectTemplate`, generated from Java `@XmlSeeAlso`; `[XmlElement(name=…, typeof=…)]` for the `Actions`/`ActionModifiers`/`Conditions` element lists) AND decide how the faithful `SkillTemplate`/`EffectTemplate` hierarchy reconciles with/replaces `SkillTemplateTable` (rule 8: Java wins → replace, migrate the skill-data loader). This is why Tier 0 is a focused-push item, not a 60s nibble (it's a binding-architecture decision, not transcription).
- **Tier 0 — skill/effect DATA templates + cones (forward-ref runtime Effect/Skill):** ✅`EffectTemplate`(572L) PORTED (WIP) — XML-binding decision applied (public props + [XmlAttribute/XmlElement]; add [XmlInclude] as subclasses land); field/type clashes renamed `Hittype`/`Hoptype`; `change`/`condition` sub-namespaces LOWERCASE (FQN clash w/ Change/Condition classes — same rule as scene.mesh). the ~170 `*Effect` subclasses (14L leaves under skillengine/effect) now need only [XmlInclude] wiring + ApplyEffect bodies. **Effect cone order:** EffectTemplate✅ → mid-bases (DamageEffect/DispelEffect/BufEffect/StatupEffect/etc) → leaves → `Effects` wrapper last. ✅DummyEffect/✅DispelEffect(mid-base)/✅EvadeEffect. ✅BufEffect mid-base (+✅Statup/Statdown/Statboost). ✅**18 more BufEffect leaves**: 14 empty (APBoost/DRBoost/SkillXPBoost/BoostDropRate/BoostHate/BoostHeal/BoostSpellAttack/DeboostHeal/ExtendAuraRange/OneTimeBoostHeal/SubTypeBoostResist/SubTypeExtendDuration/WeaponStatboost/WeaponStatup) + 4 `Calculate→AddSuccessEffect` overriders (HiPass/NoDeathPenalty/NoResurrectPenalty/XPBoost); base `Calculate(Effect)` made **virtual** (Java-overridable). DEFERRED: DamageEffect + damage sub-cone → SM_ATTACK_STATUS group (TYPE/LOG + onAttack). **NEW BLOCKER GROUP — abnormal-state effects** (SlowEffect/SnareEffect/CurseEffect + likely others): need `Effect.SetAbnormal(AbnormalState)` + `EffectController.SetAbnormal/UnsetAbnormal` — both absent (EffectController is Tier 1) → DEFER to Tier 1. ✅**AbstractAbsoluteStatEffect cone** (+leaves AbsoluteStatToPCBuff/Debuff): harvested data holders `ModifiersTemplate` (Model/Templates/Stats, polymorphic [XmlElement typeof Stat{Sub,Add,Rate,Set,Abs}Function]), `AbsoluteStatsTemplate`, `AbsoluteStatsData` (Dataholders, AfterUnmarshal→index-by-id then drop list); `BufEffect.GetModifiers` made **virtual**; AbstractAbsoluteStatEffect refs `DataManager.ABSOLUTE_STATS_DATA` (red like existing WORLD_MAPS_DATA refs — resolved when singleton-bridge statics land). **Remaining mid-bases ALL BLOCKED on Tier 1 / packets — DEFER:** AbstractOverTimeEffect (EffectController.setAbnormal + Effect.setPeriodicTask/onPeriodicAction), AbstractHealEffect (LifeStats.increaseHp/Mp/Fp TYPE/LOG + Player + EffectReserved + EffectController.isAbnormalSet), ShieldEffect (AttackShieldObserver — SM_ATTACK_STATUS), TransformEffect (TransformModel + EffectController.removeTransformEffects), ResurrectEffect (Player + PacketSendUtility + SM_RESURRECT), SummonEffect (Player/Summon/SummonsService + EffectController), AbstractDispelEffect (EffectController.removeEffectByDispelCat). → their leaves → Effects wrapper last. **Effect cone is now harvested down to the Tier-1 wall: no more effect classes are portable until runtime `Effect`/`EffectController` land.** ✅`Action`+`PeriodicAction` abstract bases ported (lowercase `action`/`periodicaction` ns; forward-ref Skill/Effect). **ALL action/periodicaction/properties leaves+wrappers BLOCKED (Skill/Effect/Creature/Player/Properties.ValidationResult/packets/SM_ATTACK_STATUS) → Tier 1. ⇒ TIER 0 EXHAUSTED.** ✅`SkillTemplate`(409L) ported. Remaining Tier 0: `Effects` wrapper (DEFER — needs all ~170 effect subclasses' typeof refs; port it LAST in the effect cone); ✅`Change`(+`Func`, lowercase `change` ns); ✅`skillengine/condition` `Condition` base + `Conditions` wrapper ported (24-subclass polymorphic XML list wired) — condition subclasses (20/24): +ChargeCondition base+SkillCharge/ChargeWeapon/ChargeArmor/ItemCharge/PolishCharge — clean portion DONE; deferred: Hp/Mp (SM_ATTACK_STATUS group) + LeftHand/Form/Target (PacketSendUtility + SM_SYSTEM_MESSAGE STR_* factories, both missing from divergent C# SmSystemMessage);
- **Tier 1 — runtime engine:** ✅**`Effect`(1232L) PORTED** (SkillEngine/Model/Effect.cs — full 1:1: all fields/4 ctors/~120 accessors + initialize/applyEffect/startEffect/endEffect/endEffects/broadcastHate/schedule+stopPeriodicActions/calculateEffectsDuration/calculateTemplateDuration/applyCumulativeResistDurationMultiplier/initializePower/getSuccessfulEffectsAsByte; nested `ForceType` static-instance pool; two private `ActionObserver` subclasses for cancel-on-dmg). Translation notes: `Future<?>`→`ScheduledTask` (Utils), `.cancel(false)`→`.Cancel()`, `ThreadPoolManager.schedule/scheduleAtFixedRate`→`Schedule/ScheduleAtFixedRateTask(Func<CancellationToken,ValueTask>, TimeSpan)`, `AtomicBoolean`(Utils Get/Set/CompareAndSet), `System.currentTimeMillis()`→`DateTimeOffset.UtcNow.ToUnixTimeMilliseconds()`, `ConcurrentHashMap`→`ConcurrentDictionary`(.IsEmpty/.Values/.ContainsKey), `getType()`→`GetTypeValue()`, `er.getType()`→`er.GetType_()`, `ResourceType`→`EffectReserved.ResourceType`, `SubEffectType.GetId()`/`ShieldType.GetId()`/`AbnormalState.GetId()` extensions, `AttackStatus.getBaseStatus`→`AttackStatusExtensions.GetBaseStatus`, `instanceof X p`→`is X p`, switch type-pattern for Fear/Paralyze/Sleep. **EXPECTED RED refs (resolve as SCC closes):** `Creature`/`Npc`/`Player` (+their APIs GetEffectController/GetLifeStats/GetGameStats/GetAi/GetAggroList/GetKnownList/GetObserveController/GetPosition/GetController/GetMaster), `Skill`, `EffectController` (IsConflicting/AddEffect/ClearEffect/CalculateAndApplyCumulativeResistDuration), `StatFunctions.CalculateHate`, `Aion.GameServer.SkillEngine.SkillEngine` singleton (CreateCriticalEffect — namespace/type clash to resolve on port), `Aion.GameServer.Services.Event.Event` (IsEventEffectForceType), `SM_SKILL_ACTIVATION` packet, `SM_ATTACK_STATUS.TYPE.REGULAR` (SmAttackStatus nested — reconciliation), `Effects` wrapper (SkillTemplate.GetEffects), effect leaves `FearEffect`/`ParalyzeEffect`/`SleepEffect`. ✅**`EffectController`(778L) PORTED** (Controllers/Effect/EffectController.cs — full 1:1; base class, `PlayerEffectController` will override AddEffect/ClearEffect/GetOwner(covariant Player)/RemoveAllEffects×2/CanRemoveOnDie → marked `virtual`). Translation notes: Java `StampedLock`→`ReaderWriterLockSlim` (readLock/unlockRead→Enter/ExitReadLock, write similarly; no nesting so NoRecursion is safe); `Collections.emptyMap()` sentinel→static `EMPTY_MAP` + `ReferenceEquals`; `volatile` passiveEffectMap ref; `Predicate.test`→direct invoke; method-refs→lambdas; `getFirst()`→`[0]`; labeled `break mainLoop`→`goto mainLoopEnd`; `EffectType`/`DispelSlotType` params made nullable (Java passes null); **namespace/type clash `Controllers.Effect` vs `Effect` resolved via `using Effect = ...SkillEngine.Model.Effect;` alias** (keeps namespace consistent w/ existing CumulativeResistType). **FIDELITY CAVEAT:** Java `LinkedHashMap` (strict order across removals) → C# `Dictionary` (insertion-order common-path only); a dedicated ordered-map port is the follow-up for exact effect-eviction parity (noted in-file). EXPECTED RED: Creature/Player APIs (GetObserveController/GetPlayerClass/IsInState/UnsetState), packets `SM_ABNORMAL_EFFECT`/`SM_EMOTION`/`SM_SYSTEM_MESSAGE.STR_MSG_NOT_ENOUGH_DISPEL*`, `DataManager.SKILL_DATA`, effect leaves `SilenceEffect`/`HideEffect`/`TransformEffect`(+GetTransformType). **This unblocks the deferred abnormal-state effect bases (Slow/Snare/Curse — EffectController.SetAbnormal/UnsetAbnormal now exist) + dispel/heal/transform bases.** ✅**`StatFunctions`(695L) PORTED** (Utils/Stats/StatFunctions.cs — full 1:1 combat formulas: exp/DP/AP/PvP reward calcs, calculateAttackDamage (main/off-hand weapon math), magical-skill-damage + elemental-defense reduction, magical/physical crit & dodge/parry/block/resist checks, fall damage, movement-modifier stat adjust, hate). Translation notes: **`Math.round` parity** via `JRound(float)->int` / `JRound(double)->long` = `floor(x+0.5)` (C# `Math.Round` is banker's rounding — would diverge); `@SuppressWarnings("lossy-conversions")` compound assigns made explicit `(int)(...)`; `ArrayUtils.contains`→`Array.IndexOf>=0`; varargs→`params`; `instanceof X p`→`is X p`; enum switches qualified (`NpcRating.JUNK`); `adjustStatByMovementModifier` param `StatEnum? stat` w/ `==null` guard (Java nullable; `GetStatForElement` returns `StatEnum?`; StatEnum has no NONE); `var` for not-yet-ported GameStats locals. Method names PascalCase (matches faithful callers Effect/EffectTemplate; the camelCase `*PlanService` slop callers are in the 363-slop set, slated for replacement). EXPECTED RED: `CreatureGameStats`/`PlayerGameStats` (+~40 stat getters) — next to port; `StatCapUtil`, `Rates`, `RatesConfig`, `XPRewardEnum`, `Influence`, `CombatMode`/`RatioType`/`CalculationType`; Creature/Player/Npc/Homing/Servant/Trap/Summon/SummonedObject APIs; `Item`/`Equipment`/`WeaponStats` getters. ✅**`CreatureGameStats<T>` base (396L) PORTED** (Model/Stats/Container/CreatureGameStats.cs) + ✅ clean enum leaves `CombatMode`/`RatioType`/`PlumStatEnum` (resolves StatFunctions red). **GENERICS DECISION:** kept faithful single generic `CreatureGameStats<T> where T : Creature` (typed `owner`); Java `Creature.getGameStats()` returns wildcard `CreatureGameStats<?>` — C# has no wildcard generics, so **`Creature.GetGameStats()` return type will be settled at Creature-port time** (likely a non-generic base/interface extracted then; callers currently use `var`, unaffected). Translation: `ConcurrentHashMap.compute`→`ConcurrentDictionary.AddOrUpdate`; `removeIf`→`RemoveAll>0`; `sort(null)`→`Sort()`; `Math.round`→`JRound`; `List<? extends IStatFunction>`→`List<IStatFunction>` (matches callers); resistance switch→C# switch expression; item/enchant red types inline-qualified. All 28 StatsTemplate getters + IStatFunction(GetName/IsBonus/Validate/Apply/GetOwner) + AdditionStat/ReverseStat/StatFunctionProxy/CalculationType verified present (no gaps). EXPECTED RED: `Creature`/`Item` + `owner.GetLifeStats()` (CreatureLifeStats next), `StatCapUtil`(CalculateBaseValue/CalcBase…), `StatArmorMasteryFunction`, `EnchantEffect`/`ItemSlot`/`ManaStone`/`RandomBonusEffect`/`ItemSetTemplate`, `SM_EMOTION`. ✅**SM_ATTACK_STATUS RECONCILED to faithful 1:1** (Network/Aion/ServerPackets/SmAttackStatus.cs — class name kept PascalCase for opcode/convention; **nested `TYPE` modeled as a Java-style CLASS-ENUM** (static instances) because Java TYPE has dup-id constants in different write branches (DAMAGE_MP=20 writes -value vs ABSORBED_MP=20 writes +value) — a plain C# enum collapses aliases & can't switch them; **nested `LOG` plain enum** (no dup ids); faithful `Creature` ctors + `writeImpl` switch→if/else by reference-identity. **Replaced divergent `SmAttackStatusType`/`SmAttackStatusLog` + int-ctor (rule 8).** Resolves `SM_ATTACK_STATUS.TYPE.*`/`LOG.*` red in Effect + the deferred Hp/Mp conditions + HpUse/MpUse actions+periodicactions + heal/damage effect cones. EXPECTED RED now: **4 slop callers** (`Services/{PlayerIncomingDamageObserverFanoutService,WorldNpcDamageService,WorldNpcResourceStatsService,WorldNpcSkillDamageService}.cs`) used the old divergent API — they're slop, slated for replacement. Also references `Creature.GetObjectId/GetLifeStats` (red until Creature/CreatureLifeStats land). ✅**`CreatureLifeStats<T>` (377L) PORTED** (Model/Stats/Container/CreatureLifeStats.cs) — faithful 1:1; consumes reconciled SM_ATTACK_STATUS via `using TYPE=/LOG=/SM_ATTACK_STATUS=` aliases (reads identically to Java); `synchronized`→`lock`, `Future`→`ScheduledTask`, `Objects.requireNonNull`→`ArgumentNullException`; `LOG`/`type` made nullable where Java passes null (increaseMp(int)); virtual on the 7 methods PlayerLifeStats overrides (OnHpChanged/OnMpChanged/SynchronizeWithMaxStats/UpdateCurrentStats/GetMaxFp/GetCurrentFp/CancelAllTasks). Resolves `owner.GetLifeStats()`/`GetHpPercentage`/`GetMpPercentage`/`IncreaseHp`/`ReduceHp`/`SetCurrentHp` red across Effect/StatFunctions/CreatureGameStats. EXPECTED RED: Creature APIs (GetGameStats/GetEffectController/GetController.OnDie/GetObserveController/IsInvulnerable), `LifeStatsRestoreService` (red singleton). ✅**`StatCapUtil` (183L) PORTED** (Model/Stats/Calc/StatCapUtil.cs) — stat lower/upper/diff caps + PvP/PvE ratio limits; `EnumMap`→`Dictionary` w/ static-ctor init, `record Cap`→C# record, Java switch-expr/stmt→C#; resolves `CalculateBaseValue`/`ClampStatValue`/`GetDifferenceLimit`/`GetElementalDefenseBaseValue`/`LimitValueForPvpOrPveStat` red in CreatureGameStats + StatFunctions. RED: `Creature.GetLevel`, `Player.IsStaff`. ✅**Npc/Summon container subclasses PORTED**: `NpcGameStats`(227L)/`NpcLifeStats`(23L)/`SummonLifeStats`(22L). Made base `CreatureGameStats.GetStat(StatEnum,Stat2,params)` + `CreatureLifeStats.TriggerRestoreTask` **virtual** (subclasses override). RED: Npc/Summon APIs (GetObjectTemplate/GetAi/GetWalkerGroup/IsFlying/GetMoveController/GetTarget/GetAbyssNpcType), `AbyssNpcType`/`AISubState`/`NpcSkillEntry`/`AILogger`/`PositionUtil`, `LifeStatsRestoreService`.
- **⚠️ ENUM-CASING RULE (discovered, important for all faithful ports):** the C# port is **MIXED** — most enums are SCREAMING_SNAKE (StatEnum/AbnormalState/AttackStatus/SkillElement/ActivationAttribute/SkillSubType/etc.) BUT some **runtime/non-XML-token enums were pre-ported PascalCase**: confirmed `CreatureState` (Resting/WeaponEquipped/WalkMode/Flying…) and `EmotionType` (Stand/ChangeSpeed…). When referencing an *already-ported* enum, **match its existing casing** (don't assume SCREAMING_SNAKE); verify before use. Fixed latent gaps this tick: EffectController `CreatureState.RESTING→.Resting`, `EmotionType.STAND→.Stand`; CreatureGameStats `EmotionType.CHANGE_SPEED→.ChangeSpeed`. **DEFERRED reconciliation (low priority, non-data-affecting):** CreatureState/EmotionType SCREAMING_SNAKE rename to match Java is a 21-file (mostly slop) ripple — its own focused pass later; wire values already match so no data risk.
✅**Summon/Npc-derived GameStats subclasses PORTED**: `SummonedObjectGameStats`(44L)/`ServantGameStats`(60L)/`HomingGameStats`(81L)/`TrapGameStats`(87L)/`SummonGameStats`(149L). Made base `GetStat(StatEnum,float,params)`/`GetMBoost`/`GetMAccuracy`/`GetMainHandMAttack` **virtual** (subclasses override). RED: Npc/Summon/Player/Homing APIs (GetMaster/GetObjectTemplate/GetMode/IsFlying/GetSkillId), `SummonMode`, `SM_SUMMON_UPDATE`, `DataManager.SKILL_DATA`. **Container hierarchy now COMPLETE except the Player side.** ✅**`PlayerLifeStats`(280L) PORTED** — FP/fly-time mechanics (increase/reduce/setFp, trigger/cancel FpRestore+FpReduce), HP/MP packet updates; overrides the 7 virtual base methods; `Future.isCancelled()`→`ScheduledTask.Completion.IsCanceled`. **CreatureLifeStats hierarchy COMPLETE.** RED: Player APIs (GetAggroList/IsSpawned/IsInTeam/GetFlyState/IsInSprintMode/HasAccess/IsInsideZoneType/IsFlying/IsInGlidingState/ride), packets `SM_STATUPDATE_HP/_MP`/`SM_FLY_TIME`, `TeamStatUpdater`/`LifeStatsRestoreService`, `AdminConfig`/`ZoneType`. ✅**`PlayerGameStats`(431L) PORTED** — weapon/dual-wield/power-shard attack calcs, movement-speed (ride/fly/walk), regen, dependent-stat additions; overrides 14 base getters. Made base `GetParry`/`GetMainHandPAttack`/`GetMainHandPCritical`/`GetMainHandPAccuracy`/`GetMCritical`/`CheckSpeedStats` **virtual**. `ArrayUtils.add/contains/removeElement`→private `ArrAdd/ArrContains/ArrRemove` helpers; CreatureState/EmotionType referenced PascalCase per the casing rule. RED: Player/Equipment/Item/WeaponStats getters, `PlayerMode`/`RideInfo`/`CustomConfig`, `owner.ride`, packets `SM_STATS_INFO`/`SM_EMOTION`. **⇒ STAT-CONTAINER HIERARCHY 100% COMPLETE** (CreatureGameStats/CreatureLifeStats bases + all Npc/Summon/Trap/Homing/Servant/SummonedObject + Player game & life stats + StatCapUtil + container enums).
✅**runtime `Skill`(1023L) PORTED** (SkillEngine/Model/Skill.cs) — full cast lifecycle (canUseSkill/useSkill/startCast/endCast/applyEffect/sendCastSpellEnd), hit-time & animation calc, charge/magical cast-duration, penalty/chain/multicast, cooldowns, condition pre/use/end checks, target validation. Translation: nested `SkillMethod` enum; `var` for the still-unported `Properties`/`Actions` cone types (sidesteps the lowercase-`properties`-ns clash); `CastState` via alias to `...properties.Properties.CastState`; `Future`-schedule→ThreadPoolManager lambda+TimeSpan; `Math.round`→JRound; `instanceof X y`→`is X y`; signed `byte h`→`sbyte`; `getType()`→`GetTypeValue()`; SkillEngine/QuestEngine/AbyssService/ShoutEventHandler/SkillAttackManager/AIEventType fully-qualified (red). EXPECTED RED: `Properties`/`Actions` cones (next), `Creature`/`Player`/`Npc` (+SkillList/ChainSkills/Inventory/Controller/Ai/MoveController/ObserveController APIs), many packets/services/configs, `DataManager.MOTION_DATA/SKILL_CHARGE_DATA`, `PenaltySkill`. NpcGameStats.GetLastSkill/SetNextSkillDelay now green ✅. ✅**WILDCARD-GENERICS DECISION SETTLED + applied to stat containers.** Java `Creature` fields are `CreatureGameStats<? extends Creature>`, `CreatureLifeStats<? extends Creature>`, `AbstractAI<? extends Creature>`, `CreatureMoveController<? extends Creature>`, `CreatureController<? extends Creature>` — C# has NO wildcard generics. **RESOLUTION (the project-wide pattern for these subsystems): split each `Foo<T>` into a non-generic abstract base `Foo` (holds all members; `protected Creature owner`) + a generic shim `Foo<T> : Foo where T : Creature` (typed `owner`/`GetOwner()` via `new` shadowing). Creature references the NON-GENERIC base.** Applied to `CreatureGameStats` + `CreatureLifeStats` this tick (Model/Stats/Container/*.cs) — **all 10 subclasses UNCHANGED** (still `: CreatureGameStats<Npc>` etc.; `owner` stays typed via the shim). Guardrail green. **When porting `AbstractAI`/`CreatureMoveController`/`CreatureController`, apply the SAME non-generic-base+shim pattern** so Creature can hold them.
✅**`Creature`(522L) — THE HUB — PORTED** (Model/GameObjects/Creature.cs, abstract, extends `VisibleObject`). gameStats/lifeStats held as **non-generic bases** (✅ resolves the stat/life-stat call mass in Effect/StatFunctions/EffectController/containers). State/visual/see flags via the PascalCase enums (Active/Visible/Normal, GetId/MustMatchExact extensions), zone-type counters, skill cooldowns (ConcurrentDictionary), transform/aggro/observe. `getLevel()` abstract → `sbyte` (Java byte). RED (frontier): `AbstractAI`/`AIEngine` (Aion.GameServer.Ai), `CreatureController` (covariant override of VisibleObject.GetController), `CreatureMoveController`, `CreatureTemplate`, `Npc`/`Pet`/`NpcObjectType`, `Race`/`TribeClass`/`ItemAttackType`/`NpcEquippedGear`, `SpawnTemplate`, `ZoneType`/`ZoneName`/`ZoneInstance`/`MapRegion`. ✅**`CreatureMoveController` refactored** to non-generic base + generic shim (`<T extends VisibleObject>`; existing C# was PascalCase-field generic — subclasses untouched). Creature.moveController/GetMoveController() now resolve to the non-generic base. **`AIEngine`(151L) DEFERRED** — it's reflection/scripting infra (Java `Class`/`Constructor.getDeclaredConstructors`/`isAssignableFrom`/`getGenericSuperclass`/`ScriptManager`/annotation scanning); a foundational C#-vs-Java mechanism needing its own strategy decision (C# assembly reflection + activator), not a 1:1 transcription. Creature.ctor's `AIEngine.GetInstance().NewAI(...)` stays red until then.
✅**`CreatureController`(568L) PORTED** (Controllers/CreatureController.cs) — non-generic base `CreatureController : VisibleObjectController` + generic shim `CreatureController<T>` (re-exposes typed SetOwner/GetOwner; `GetOwner()` in base returns Creature). onAttack overload chain (TYPE/LOG via aliases, HopType✅), attackTarget (phys/mag AttackUtil), die/useSkill/useChargeSkill/abortCast/cancelCurrentSkill, godstone proc, task map (ConcurrentDictionary; `Future`→ScheduledTask, `compute`→AddOrUpdate, `isDone`→Completion.IsCompleted, `tasks.remove(k,v)`→TryRemove(KeyValuePair)), `DelayedOnAttack` inner class. CreatureState/EmotionType PascalCase. RED: AttackUtil, GodStone/GodstoneInfo, AttackHand/TypeAnimation/ObjectDeleteAnimation, TaskId, NpcAI/AISubState/AIEventType, MovementNotifyTask/ZoneUpdateService/ZoneInstance, TerrainZoneCollisionMaterialActor, SM_ATTACK/SM_EMOTION/SM_SKILL_CANCEL, ChargeSkill, Item/Equipment APIs, Npc/Player.
✅**Fixed latent AI enum-casing gaps** in CreatureController/Skill/NpcGameStats — the pre-ported AI enums are **PascalCase** (`Aion.GameServer.Ai.AiSubState` {None/Talk/Cast/WalkPath/WalkRandom/WalkWaitGroup/Freeze/TargetLost}, `Aion.GameServer.Ai.Event.AiEventType` {Attack/AttackComplete/CreatureNeedsSupport/CreatureNeedsHelp/…}, `AiState`), NOT `AISubState`/`AIEventType`/SCREAMING. **ENUM-CASING RULE reminder: the whole `Aion.GameServer.Ai` enum family is PascalCase — verify before referencing.** AI classes (`AbstractAI`/`NpcAI`/`AIEngine`/`AILogger`/`ShoutEventHandler`/`SkillAttackManager`/`AIEventLog`) remain red (unported).
✅**`AI` interface + `AbstractAI`(465L) PORTED** (Ai/AI.cs, Ai/AbstractAI.cs) — non-generic base `AbstractAI : AI` + generic shim `AbstractAI<T>` (typed GetOwner). Event dispatch (HandleGeneralEvent/HandleCreatureEvent switches over AiEventType), state machine (AiState/AiSubState, SetStateIfNot/SetSubStateIfNot under `lock`), DEPTH recursion guard (`ThreadLocal<int>`), 30 abstract `Handle*` hooks, spawn helpers (`Math.toRadians`→`*PI/180`, byte→sbyte), default modify* passthroughs. **The 8 AI-interface methods AbstractAI doesn't implement (Think/CanThink/Ask/OnStartUseSkill/OnEndUseSkill/OnEffectApplied/OnEffectEnd/IsDestinationReached) are declared `abstract`** (Java leaves them implicitly-abstract via interface; C# requires explicit) — implemented in NpcAI/concrete subclasses. AI enums PascalCase. RED: `AILogger`/`AIEventLog`(AddFirst)/`FreezeEventHandler`/`AIConfig`/`AggroTarget`/`SpawnEngine`/`ShoutEventType`/animations/`ItemAttackType`. **Creature.getAi() now resolves to AbstractAI (non-generic) ✅.**
✅**`AITemplate<T>`(181L) PORTED** (Ai/AITemplate.cs) — `AITemplate<T> : AbstractAI<T>`; default (mostly no-op) overrides of all 30 Handle* hooks + the 8 AI-interface methods (Think/CanThink/Ask/etc) + ChooseAttackIntention (→`AttackIntention.SIMPLE_ATTACK`) + OnPatternShout. **NOTE: `AttackIntention` enum is SCREAMING (`SIMPLE_ATTACK`), unlike the PascalCase AiState/AiSubState/AiEventType — casing is mixed even within `Ai`; always verify.** ✅`CreatureTemplate` already green (extends VisibleObjectTemplate, GetAiName) — Creature objectTemplate dep resolved. **`DummyAI` and concrete NPC AIs are script handlers** (game-server/data/scripts, loaded by AIEngine's ScriptManager) — out of `src`, deferred with the AIEngine reflection-infra decision.
✅**`NpcAI`(198L) PORTED** (Ai/NpcAI.cs, abstract, extends AITemplate<Npc>) — typed Npc accessors, event-handler dispatch (Handle*→`Ai.Handler.*` red), `Ask()`/`IsDestinationReached()` as C# switch-expressions (Java block-body arms extracted to private helpers `RewardApFor`/`ReturningReached`). AiState/AiSubState PascalCase, AIQuestion SCREAMING. RED: `Ai.Handler.*`/`Ai.Manager.*` (ActivateEventHandler/SpawnEventHandler/ShoutEventHandler/DiedEventHandler/MoveEventHandler/CreatureEventHandler/FollowEventHandler/SimpleAttackManager/WalkManager), `Race`/`TribeClass`/`WorldType`, `NpcShoutsService`/`SiegeService`, `Npc`/`SiegeNpc`/`NpcTemplate`/`NpcSkillList`/`NpcMoveController`, `PositionUtil`, `Configs.Main.SiegeConfig/AIConfig`.
✅**`Npc`(393L) PORTED** (Model/GameObjects/Npc.cs, extends Creature) — was MISSING entirely (not divergent). Covariant overrides GetMoveController/GetObjectTemplate/GetLifeStats/GetGameStats/GetController/GetLevel(sbyte)/GetAttackType/GetTribe/GetBaseTribe/IsEnemy/IsEnemyFrom×3/GetSeeState/IsFlag/IsRaidMonster/GetRace/GetOverrideEquipment; skill queue (Java LinkedList-Queue→C# Queue+lock, peek-null-safe), tribe/type/aggro/trade. **Made base Creature/VisibleObject methods virtual** (GetMoveController/GetLifeStats/GetGameStats/IsEnemy/GetSeeState/GetObjectTemplate) for the covariant overrides. `getType(Creature)`→`GetTypeValue(Creature)` (Object.GetType clash); Java pre-super `requireNonNull` dropped (C# can't run code before base()). **This resolves the large `Npc` red surface across NpcAI/CreatureController/Skill/StatFunctions/NpcGameStats.** RED: NpcController/NpcMoveController/NpcTemplate(+AbyssNpcType/NpcRating/NpcRank/NpcTemplateType/GroupDropType)/NpcSkillList/NpcSkillEntry/WalkerGroup/CreatureType/IDFactory/TribeRelationService/NpcEquippedGear/DialogAction/SM_CUSTOM_SETTINGS.
**⛰️ PLAYER-REPARENT SURVEY + STAGED PLAN (the convergence inflection point):**
- **Finding:** existing C# `Player.cs` = **wholly divergent `sealed class Player`(1442L)**, NOT extends Creature, shares **ZERO** API with faithful Java Player (every faithful method the SCC calls — GetEquipment/GetEffectController/GetChainSkills/GetInventory/GetPlayerClass/IsDueling/GetStore/GetController/GetGameStats/GetLifeStats/GetAi/GetSkillList/GetCommonData/GetAbyssRank/SetHitTimeBoost — MISSING). The SCC is therefore **already red on faithful-Player-API** (expected). Java Player(1655L extends Creature) has **~40 field-subsystem cones** (Storage/Cooldowns/Equipment/Account/AbsoluteStatOwner/TitleList/Summon/RecipeList/QuestStateList/PrivateStore/PlayerSkillList/PlayerSettings/PlayerGroup/PlayerAllianceGroup/PetList/Pet/PanesterraFaction/NpcFactions/PortalCooldownList/PlayerAccountData/…). **370 C# files reference `Player`** — the divergent ecosystem.
- **Decision (rule 8):** REPLACE divergent Player with faithful `Player : Creature`. Divergent-API callers (largely slop services/packets) go red → replaced/migrated later; this is the same replace-divergent pattern used for SmAttackStatus etc. Class-name collision means faithful cone subsystems (Equipment/Storage/CommonData/…) must replace their divergent namesakes too (can't coexist).
- **Staged approach (bottom-up, like the SCC):** (1) port the MISSING/faithful Player-cone leaves first where they don't collide; (2) replace divergent cone subsystems with faithful 1:1 as needed; (3) port faithful `Player : Creature`(1655L) LAST once its cone is ready; (4) reparent `Summon`/`Pet`/`Trap`/`Homing`/`SiegeNpc`/`StaticObject` (Creature/Npc subclasses); (5) PlayerAI/SummonAI/etc; (6) `AIEngine` reflection-infra; (7) Properties/Actions/Effects-wrapper cones; (8) spatial `World`/`WorldMap*`/`MapRegion`/zones; (9) packets (SM_* cone), DataManager singleton-bridge statics; → green → merge.
- **⚠️ SCALE NOTE:** turning the branch green from here ≈ re-porting the player half of the server (hundreds of files) — the largest remaining phase by far; proceed bottom-up, one faithful unit per tick, red-by-design, guardrail green each commit.
**NEXT concrete action:** begin the Player cone with a MISSING faithful leaf (e.g. `PlayerController`(754L, CreatureController<Player>) or a small player-subsystem), continuing bottom-up toward the faithful `Player` class.
**✅ Creature/Npc subclass cone (started — all were MISSING, small):** `SummonedObject`(99L) PORTED — non-generic base + generic shim `SummonedObject<T:VisibleObject>` extends Npc (creator/level, master/enemy/race/type overrides); made Npc GetCreator/GetMasterName/GetCreatorId/GetTypeValue virtual. ✅`Trap`/`Servant`/`Homing` PORTED (extend SummonedObject<Creature>). ✅**`AionObject.GetName()` bridge added** (Java getName() → C# Name property) — resolves `.GetName()` across all faithful ports. ✅`Summon`(223L) PORTED (extends Creature; master/mode/skill-orders ConcurrentQueue/resist-element; covariant GetGameStats/GetMaster:Player/GetController/GetMoveController/GetActingCreature:Player; getType→GetTypeValue; resolves SummonGameStats/SummonLifeStats red). SummonController/SiegeWeaponController/UnsummonType ✅PORTED (Summon controller cone COMPLETE). ✅NpcTemplate(329L) PORTED (extends CreatureTemplate; XmlUid setter for npc_id; nullable type/abyss_type enums via string setter-props [Java enum-null→NONE semantics]; talk/massive-loot/bound-radius delegation; AfterUnmarshal siege_teleporter+intern). Fixed SiegeWeaponController NpcRating.LEGENDARY→Legendary casing. ✅NpcEquippedGear+NpcEquipmentList PORTED (TreeMap→SortedDictionary [ItemSlot bitmask values ascend=ordinal-order, faithful]; IEnumerable<KVP>; lazy init lock; @XmlIDREF item refs). **⇒ NpcTemplate keystone fully satisfied.** RED: SummonsService/PET_SKILL_DATA/SmSummonUpdate/MoveTaskManager/FollowStartService/NpcSkillTemplates til services/Tier4 (PlayerAggroList/SummonMoveController+PlayableMoveController ✅NOW PORTED — made AggroList.IsAware + CreatureMoveController.SetNewDirection virtual; SmMove(Creature)/World.GetInstance().UpdatePosition/PacketSendUtility/PlayerMoveTaskManager red til Tier4) (SummonMode/SkillOrder ✅NOW PORTED). ✅`SiegeNpc`/`Pet`/`StaticObject`/`Gatherable` PORTED (made VisibleObject.GetSpawn() virtual for SiegeNpc covariant override; Pet uses nested concrete `PetDefaultMoveController` since CreatureMoveController<Pet> is abstract — Java used anon subclass; Pet overrides `Name` property). RED: SiegeSpawnTemplate/SiegeRace, PetController/PetTemplate/PetCommonData, StaticObjectController/GatherableController/GatherableTemplate/PlayerAwareKnownList, IDFactory, DataManager.GATHERABLE_DATA. **⇒ Creature/Npc/VisibleObject gameobject subclass cone COMPLETE except Player** (Item is a separate divergent class — referenced everywhere; check during Player cone). ✅**`PlayerController`(754L) PORTED** (Controllers/PlayerController.cs, extends CreatureController<Player>) — see/notSee/onDie/attackTarget/onAttack/cancelCurrentSkill/zone/fly/stance/level-change/protection lifecycle. Made base CreatureController OnHide/OnHideEnd/OnStartMove/OnMove/OnStopMove/CancelCurrentSkill×2 virtual. Near-100% red (additive — doesn't break divergent Player.cs callers): ~50 services/packets/subsystems (QuestEngine/DropService/DuelService/InstanceService/PvpService/SummonsService/ConquerorAndProtectorService/LegionService/SkillLearnService/PlayerRestrictions/PlayerMoveTaskManager/TeamMoveUpdater/Kisk/House/HouseObject/SM_* packets/DialogAction/PlayerMode/FlyState/etc). **⚡ ANCHOR IN PROGRESS — faithful `Player : Creature` is REPLACING divergent Player.cs, ported as PARTIAL CLASSES over a few ticks** (1655L/238 methods too large for one tick). ✅**Player.cs = PARTIAL #1 DONE**: `public partial class Player : Creature` + all ~40 subsystem fields + constructor + accessors (Java lines 194-514: IsInPlayerMode/Set/Unset, GetMoveController/CreateAggroList(sealed)/GetCommonData/GetName/GetPlayerAppearance/connection/Macros/SkillList/Pet/FriendList/BlockList/PetList/captcha/gather-restriction/GetLifeStats:Player/GetGameStats:Player/GetController:Player/GetLevel/GetEquipment/GetUsingItem/GetStore/GetQuestStateList/GetRecipeList/expands/GetPlayerClass/GetGender). **Divergent Player.cs DELETED → its ~188 not-yet-ported methods + the 370 divergent-API callers are now RED (expected detonation).** Field-type namespaces are best-effort guesses (red until subsystems ported; realign when they land). ✅**Player.Part2.cs DONE** (Java 515-861): storage/warehouse/dirty-items, settings/titles/group/abyss, GetEffectController:PlayerEffectController (made Creature.GetEffectController virtual), legion, fly-state, prison, protection/invulnerable, mailbox, fly-controller/crafting/flight-path, access/staff. ✅**Player.Part3.cs DONE** (Java 863-1209): IsEnemy/IsEnemyFrom(Npc/Player) overrides (Creature already virtual), FFA-team-mode, panesterra-hostile, CanPvP/IsDueling/IsInSameTeam, CanSee override, GetTribe/GetBaseTribe overrides (TribeClass? from transform), summon/kisk getters, item-cooldowns (ConcurrentDictionary<int,Model.Items.ItemCooldown>), res-status/resurrection-skill/flying-before-death, alliance/league/team accessors, GetCurrentTeam/GetCurrentGroup/GetCurrentTeamId (wildcard-generic→TemporaryPlayerTeam<TeamMember<Player>>), portal/craft/houseObject cooldowns, postman/account/creation-date, IsCompleteQuest, next-skill-use, SetCasting override (made Creature.SetCasting virtual), hit-time-boost, GetChainSkills, last-counter-skill (AttackStatusExtensions.GetBaseStatus). RED deps (expected): WorldMapType.IsPanesterraMap, ZoneType.PVP, DuelService, NpcTemplateType.MERCENARY, TRIBE_RELATIONS_DATA, Team.Alliance.*, PortalCooldownList/Cooldowns, Questengine.Model.QuestState/QuestStatus, GetPanesterraFaction (defined in Part4). ✅**Player.Part4.cs DONE** (Java 1210-1655) — **faithful `Player : Creature` class now COMPLETE (Java 194-1655, 4 partials).** Part4: res-positional-state (X/Y/Z), IsInSiegeWorld, HasPermission, emotions/motions/bind-point, GetAttackType override, fly-path/abyss-rank-update-mask, AddSalvationPoints, IsPvpTarget override, IsTargetingNpcWithFunction, npc-factions, fly-reuse, self-rez stones, looting, mentor, GetRace/GetOppositeRace override, GetSkillCooldown override, flood-msg, rebirth, supplements, port-animation, IsSkillDisabled override (chain-condition multicast exception), houses, battle-return, sprint, ride-observers (lock), GetAbsoluteStats, SetPosition override, robot-mode, CanPerformMove override, ToString, custom-states, panesterra. Added public fields speedHackCounter/abnormalHackCounter. Made virtual: Creature.CanPerformMove/IsSkillDisabled, VisibleObject.SetPosition (+SetCasting in Part3). RED deps (expected): AbyssRankUpdateType, PacketSendUtility/SmStatsInfo/SmSystemMessage, SecurityConfig/AdminConfig, HousingService, ChainCondition, Equipment.GetMainHandWeapon, Storage.GetFirstItemByItemId/DecreaseByItemId, CommonData salvation/setters, House.IsInactive, Npcfaction.NpcFactions. **PLAYER CONE SUBSYSTEMS STARTED (all were entirely MISSING; namespace `Aion.GameServer.Model.GameObjects.Player`):** ✅**PlayerCommonData(611L) PORTED** (extends CreatureTemplate:VisibleObjectTemplate) — exp/level/repose/salvation/daeva-promotion, expands, title/dp/mailbox, position mirror (mapId/x/y/z/heading), GetName/GetTemplateId/GetL10nId/GetBoundRadius overrides; Math.round(double)→`(long)Math.Floor(x+0.5)`, Timestamp→DateTime?, getMaxLevel()→`.MaxLevel` property. RED deps (expected): Rates (sibling player pkg), Utils.Stats.XPLossEnum, Utils.PacketSendUtility (Send/Broadcast), SmStatUpdateExp/SmDpInfo/SmStatUpdateDp/SmSystemMessage.STR_* factories, World.GetInstance().GetPlayer, **Dataholders.DataManager.PLAYER_EXPERIENCE_TABLE singleton-bridge static (Tier-4)**, Questengine.Model.QuestStateList/QuestStatus, Dao.PlayerQuestListDAO.Load. Also fixed Part3 DataManager ns → `Aion.GameServer.Dataholders.DataManager`. ⚠️**STRUCTURAL FIX (critical, this tick):** the Player class partials were wrongly declared in namespace `Aion.GameServer.Model.GameObjects`; the entire codebase references the class as `Player.Player` (= ns `...GameObjects.Player` + class `Player`, matching Java pkg `model.gameobjects.player.Player`), AND a class `Player` in `...GameObjects` + namespace `...GameObjects.Player` is a hard **CS0101** conflict (verified via scratch csc) once any type occupies `.Player`. **FIX: moved all 4 Player partials to `namespace Aion.GameServer.Model.GameObjects.Player`** (class FQN now `...GameObjects.Player.Player`). Verified `class Player` inside `namespace ...Player` compiles. Enclosing-namespace visibility means Player still sees Creature/Npc/VisibleObject/Kisk/Summon unqualified; cone siblings reference `Player` directly; outside refs use `Player.Player` (already the convention everywhere). ✅**Cooldowns(48L) PORTED** — Java `extends ConcurrentHashMap<Integer,Long>`; **foundational C#-vs-Java difference: ConcurrentDictionary is SEALED** → wraps one instead (closest-to-1:1), reproduces get/put auto-expiry, implements IEnumerable + Count. ✅**PortalCooldown(37L)+PortalCooldownList(100L) PORTED** — portal/instance reuse tracking; HashMap→Dictionary, internal ctor (Java pkg-private), TryGetValue for null-on-missing. RED deps: Dataholders.DataManager.INSTANCE_COOLTIME_DATA, Dao.PortalCooldownsDAO, SmInstanceInfo, PacketSendUtility, TemporaryPlayerTeam.SendPackets. ✅**BindPointPosition(91L) PORTED** (implements IPersistable; nested IPersistable.PersistentState; fallthrough switch→goto default). ✅**Emotion/EmotionList (.emotion subpkg→ns `...Player.Emotion`) + Motion/MotionList (.motion→`...Player.Motion`) PORTED** — both Emotion/Motion implement IExpirable; LinkedHashMap→Dictionary (insertion-order caveat); Motion.motionType static dict; activeMotions.put(k,v)-returns-old → TryGetValue+assign pattern. **BLOCKER FIXED:** `Expirable.cs` IExpirable.OnExpire/OnBeforeExpire used bare `Player` (resolved to namespace, not class) → fully-qualified to `...GameObjects.Player.Player`. Also fixed Player.cs/Part4 EmotionList/MotionList refs to the `.Emotion`/`.Motion` subnamespaces (faithful to Java subpackages). RED deps: ExpireTimerTask, PlayerEmotionListDAO/MotionDAO, SmEmotionList/SmMotion/SmSystemMessage.STR_* factories, EmotionLearnAction, PacketSendUtility. **REMINDER: Java subpackages → C# sub-namespaces (.title→.Title, .npcfaction→.Npcfaction already used in Part4, etc.); fix forward refs as each lands.** ✅**Title + TitleList (.title→ns `...Player.Title`) PORTED** — Title implements IExpirable; LinkedHashMap→Dictionary; bonus/display title + race-gate; fixed Player.cs/Part2 TitleList refs to `.Title.TitleList`. RED deps: DataManager.TITLE_DATA, TitleTemplate, Stats.Listeners.TitleChangeListener, PlayerTitleListDAO, ExpireTimerTask, SmTitleInfo/SmSystemMessage.STR_* factories, PacketSendUtility.SendMessage/BroadcastPacketAndReceive. ✅**Mailbox(147L) PORTED** — mails/reserveMail ConcurrentDictionary<int,Letter>, 100-slot cap + overflow upload, unread counts by type, system-letter filter; Timestamp.getTime()→ToMillis(DateTime) helper. RED deps: Services.Mail.MailService (Model.GameObjects.Letter ✅NOW PORTED). (LetterType exists.) ✅**AbyssRank(286L) PORTED** — daily/weekly/all AP+GP+kill tracking, rank/maxRank, AP-cap, weekly/daily rollover via ServerTime+ISOWeek; implements IPersistable. **Nested enum AbyssRankUpdateType** kept nested (Java parity) with value()→`Value()` extension; **fixed Part4 refs** from top-level to `AbyssRank.AbyssRankUpdateType`. Java ZonedDateTime/IsoFields→DateTimeOffset + System.Globalization.ISOWeek.GetWeekOfYear. RED deps: Utils.Stats.AbyssRankEnum (GetRankById/GetRankForPoints/GetRequiredGP/GetId), Configs.Main.CustomConfig (ENABLE_AP_CAP/AP_CAP_VALUE), Utils.Time.ServerTime (OfEpochMilli/Now). ✅**DeniedStatus(enum, id-bearing→Value-as-enum + GetId() ext) + faithful PlayerSettings(134L) PORTED** (ns `...Player`, implements IPersistable; ui/shortcuts/houseBuddies byte[], deny/display bitmasks). NOTE: faithful PlayerSettings lives at `...GameObjects.Player.PlayerSettings`; the **divergent `Model.GameObjects.PlayerSettings` still exists** (different ns, no collision) — migrate/remove its callers later. NOTE: **ChainSkills(skillengine.model) was ALREADY ported** (faithful) — Part3 ref satisfied. ✅**Macros(33L) PORTED** (nested record Macro(Id,Xml); synchronized→lock; 1-12 id range). ✅**BlockedPlayer(35L) + BlockList(111L) PORTED** (BlockList : IEnumerable<BlockedPlayer>; ConcurrentDictionary; MAX_BLOCKS=100; equalsIgnoreCase→StringComparison.OrdinalIgnoreCase). ✅**Friend(73L) + FriendList(186L) PORTED** (FriendList : IEnumerable<Friend>; ConcurrentDictionary; nested enum Status:byte {OFFLINE=0,ONLINE=1,AWAY=3} with GetId()/static GetByValue ext; setStatus login/logout broadcast). RED deps: World.GetInstance().GetPlayer, Configs.Main.CustomConfig.FRIENDLIST_SIZE, SmFriendUpdate, SmFriendNotify.LOGIN/LOGOUT, PacketSendUtility. ✅**PlayerAppearance(1064L) PORTED** — 52 int appearance fields (face/hair/RGB/body proportions/etc.) + float height; pure getters/setters + GetBoundHeight (height*1.75f). No deps (zero RED). Generated mechanically from the Java accessor list to avoid transcription error. **STORAGE FAMILY STARTED (Model/Items/Storage, all MISSING):** ✅**StorageType PORTED as a CLASS-ENUM** (rule-8 foundational diff: Java enum has per-instance fields id/limit/length/specialLimit → C# enums can't carry ctor data; SM_ATTACK_STATUS precedent). Static instances CUBE..MAILBOX (chained private ctors), int consts PET_BAG_MIN/MAX/HOUSE_WH_MIN/MAX, Values()/GetStorageTypeById/GetStorageId. **Fixed Part2** `Enum.GetValues<StorageType>()` → `StorageType.Values()` (no longer a CLR enum). ✅**IStorage(interface) PORTED** (extends IPersistable; full kinah/item add/put/remove/delete/decrease API; default GetStorageIsFullMessage → if/else over class-enum StorageType via reference equality). **FINDING: the `Item` class is MISSING entirely (not divergent) — clean port ahead.** RED deps: Model.GameObjects.Item (missing), Services.Item.ItemPacketService.ItemAddType/ItemDeleteType/ItemUpdateType (nested service enums), Questengine.Model.QuestStatus, SmSystemMessage.STR_* factories. **ITEM KEYSTONE STARTED (959L, extends AionObject impl Expirable/StatOwner/Persistable; ported as PARTIALS like Player):** ✅**Item.cs = Part1** — all ~45 fields, 3 constructors (simple/service/DAO), UpdateChargeInfo, Name override (itemTemplate.GetName()), core accessors through SetItemCount (creator/sockets/enchantBonus/skin/color/count). Helpers SetBonusStats/SetFusionedItemBonusStats/SetPersistentState/CalculateMaxChargeLevel referenced (land in later parts). **NOTE: the entire item subsystem is unported** — RED deps: Templates.Item.ItemTemplate(+ ~30 methods), Model.Items.{ManaStone,GodStone,IdianStone,ChargeInfo,RandomBonusEffect,PendingTuneResult}, Model.Enchants.{EnchantEffect,TemperingEffect}, Model.Stats.Calc.{StatOwner,Functions.StatFunction}, Storage.ItemStorage.FIRST_AVAILABLE_SLOT, DataManager.ITEM_DATA. ✅**Item.Part2 (Java 314-607) DONE** — increase/decrease count, equip/slot, mana/fusion stones (TreeSet→SortedSet w/ slot comparer), godstone add/set, enchant level, **Persistable impl Get/SetPersistentState (fallthrough switch→goto default)**, item location/mask/soulbound, EquipType, fusion item. Logger: `private static readonly ILogger log = NullLogger.Instance` (codebase convention); log.warn→LogWarning. Later-part helpers referenced: RemoveRemainingTuningCountIfPossible, SetFusionedItemBonusStats, GetBonusStatsId, GetSockets. ✅**Item.Part3 (Java 609-958) DONE — ITEM KEYSTONE COMPLETE (959L, 3 partials).** Part3: sockets, storable/trade ItemMask checks, Expirable (GetExpireTime/OnExpire/OnBeforeExpire — class-enum StorageType iteration via Values()+if/else), charge/conditioning, CalculateMaxChargeLevel/AvailableChargeLevel, Improvement, StatOwner GetCurrentModifiers, idian, bonus-stats roll (RandomBonusEffect), tune count/identify, fusioned bonus-stats, enchant/temper effects, pack, MaxEnchantLevel/ItemEnchantParam, amplify, buffSkill, plume, pendingTune, ToString. log.warn(msg,exc)→LogWarning(exc,msg). ✅**Storage(397L abstract base) PORTED** — implements IStorage; kinah/item add/put/remove/delete/decrease with internal actor-overloads + public no-actor variants declared `public abstract` (subclasses supply actor, faithful to Java's abstract-via-subclass). ConcurrentLinkedQueue→ConcurrentQueue (updated IStorage.GetDeletedItems return type to match). final get/setPersistentState→non-virtual. RED deps: ItemStorage(next), ItemFactory/ItemId.KINAH, Services.Item.ItemPacketService(+ItemAddType/ItemDeleteType/ItemUpdateType nested enums, Send*Packet statics, ItemDeleteType.FromQuestStatus/FromUpdateType), ItemService.DEFAULT_UPDATE_PREDICATE, Questengine.QuestEngine/Model.QuestStatus, Configs.Main.LoggingConfig.LOG_ITEM. ✅**ItemStorage(137L) PORTED** (ConcurrentDictionary<int,Item> by objId; FIRST_AVAILABLE_SLOT=65535; putIfAbsent→TryAdd; remove→TryRemove; streams→LINQ Where/ToList; cube vs special-cube via extraInventoryId). Only RED dep now: ItemTemplate (GetTemplateId/GetExtraInventoryId). ✅**PlayerStorage(141L) PORTED** — extends Storage; bridges all abstract no-actor IStorage methods to the owner actor; sealed override SetOwner; override OnLoadHandler (routes equipped items to Equipment) — made Storage.OnLoadHandler virtual. ✅**LegionStorageProxy(218L) PORTED — STORAGE FAMILY COMPLETE** (IStorage, StorageType, ItemStorage, Storage, PlayerStorage, LegionStorageProxy). Delegating proxy to inner LegionWarehouse; made 15 Storage methods virtual (GetKinah/GetKinahItem/GetStorageType/Remove/GetFirstItemByItemId/GetItemsWithKinah/GetItems/GetItemsByItemId/GetDeletedItems/GetItemByObjId/IsFull/GetFreeSlots/SetLimit/GetLimit/Size); UnsupportedOperationException→NotSupportedException (quests can't touch LWH, no owner). RED dep: Team.Legion.LegionWarehouse. **EQUIPMENT STARTED (802L, implements Persistable, ported as PARTIALS):** ✅**Equipment.cs = Part1 (Java 45-325)** — fields (SortedDictionary<long,Item> + GetEquip helper for null-on-missing; synchronizedSortedMap caveat), ctor, EquipItem (full validation: class/level/race/gender/rank/slots/skills + slot-mask checks), Equip/UnEquip/Unequip core, GetUnequipSlots, NotifyItemEquipped/Unequip, TryUpdateSummonStats, UnEquipItem x2, CheckInventorySlots/DualWield/AvailableEquipSkills, GetEquippedItemByObjId. synchronized(this)/synchronized(equipment)→lock(this)/lock(equipment) (faithful). Enums verified: CreatureState.Powershard, EmotionType.PowershardOff; ItemSlot real enum w/ ItemSlotExtensions (GetSlotIdMask/GetSlotsFor/IsTwoHandedWeapon). Helpers VerifyRankLimits/SoulBindItem referenced (later parts). RED deps: ItemTemplate(+many methods), SkillEngine.Effect.WeaponDualEffect, Services.{StigmaService, Item.ItemPacketService}, Model.Stats.Listeners.ItemEquipmentListener, Utils.Audit.AuditLogger, Utils.Stats.AbyssRankEnum, Questengine.QuestEngine/Model.QuestEnv, Templates.Item.Enums.{ItemGroup}, SmEmotion/SmSystemMessage. ✅**Equipment.Part2 (Java 327-535) DONE** — equipped-item queries (byItemId/distinct/ids-set/withoutStigma/forAppearance/all-regular-advanced-stigma via ItemSlotExtensions.Is*), ItemSetPartsEquipped, OnLoadHandler (putIfAbsent→ContainsKey/else), PutItemBackToInventory, OnLoadApplyEquipmentStats, shield/weapon-type/power-shard getters. streams→LINQ Distinct/Select/ToHashSet. GetMainHand/OffHandWeaponType return ItemGroup? (enum nullable). ✅**Equipment.Part3 (Java 538-697) DONE** — UsePowerShard, Increase/DecreaseEquippedItemCount, SwitchHands (full main/off-hand swap w/ slot XOR), IsWeaponEquipped/IsDualWeaponEquipped/IsSlotEquipped, GetMainHand/OffHandWeapon, **Persistable impl Get/SetPersistentState (now defined — satisfies Part1/2 calls)**. List.of→array; packets SmDeleteItem/SmUpdatePlayerAppearance/SmInventoryUpdateItem. ✅**Equipment.Part4 (Java 699-802) DONE — EQUIPMENT COMPLETE (802L, 4 partials).** SoulBindItem (stance checks → CreatureState.Chair/Resting/Gliding/Flying/WeaponEquipped) with the two anonymous classes translated to **private nested named classes** (SoulBindResponseHandler : RequestResponseHandler<Player>, SoulBindMoveObserver : ActionObserver) — nested classes access enclosing Equipment private members (eq.owner/eq.Equip); ThreadPoolManager.schedule(Runnable,5000)→Schedule(ct=>{...;ValueTask.CompletedTask}, TimeSpan). VerifyRankLimits/CheckRankLimitItems. RED deps: Utils.Request.RequestResponseHandler(+GetResponseRequester/PutRequest), SmItemUsageAnimation/SmQuestionWindow(+STR_ const), Model.TaskId, Model.Actions.PlayerMode, Utils.ChatUtil, ItemTemplate.GetUseLimits().VerifyRank. **⇒ MAJOR PLAYER-CONE SUBSYSTEMS DONE: Player(4 parts)+PlayerCommonData+PlayerAppearance+Equipment(4)+Storage family(6)+Item(3)+cooldowns/portal/bindpoint/emotion/motion/title/mailbox/abyssrank/settings/macros/block/friend/chainskills. ✅**QuestStateList(107L) PORTED** (ns Model.GameObjects.Player — Java pkg is gameobjects.player, NOT questEngine; **fixed mis-namespaced refs in Part3 GetQuestStateList/IsCompleteQuest + PlayerCommonData.UpdateDaeva + Player.cs field/accessors** to Model.GameObjects.Player.QuestStateList). SortedDictionary<int,QuestState>; streams→LINQ; synchronized→lock. RED deps: Questengine.Model.QuestState/QuestStatus, Templates.Quest.QuestCategory, DataManager.QUEST_DATA. ✅**RecipeList(54L) PORTED** (HashSet<int>, learn/delete recipe; RED PlayerRecipesDAO/SmLearnRecipe/SmRecipeDelete). ✅**SkillList<T:Creature> interface + PlayerSkillList(155L, model/skill) PORTED** — ConcurrentDictionary skills; addSkill/addSkillXp (fallthrough switch→goto case for gather/craft skill caps), removeSkill, sealed; RED: PlayerSkillEntry, SkillEngine.Model.SkillLearnTemplate, Services.SkillLearnService, DataManager.SKILL_TREE_DATA, Configs.Main.CraftConfig. ✅**SkillEntry(abstract base, volatile skillLevel, virtual SetSkillLvl) + PlayerSkillEntry(157L) PORTED** — skill-type/stigma/profession classification, profession-bar sizing, currentXp, IPersistable (fallthrough switch). RED: SkillEngine.Model.{SkillLearnTemplate,StigmaType}, DataManager.SKILL_TREE_DATA/SKILL_DATA. ⇒ **PlayerSkillList cone now self-consistent.** ✅**AbsoluteStatOwner(45L) PORTED** (ns Model.GameObjects.Player — implements Stats.Calc.StatOwner; apply/cancel via gameStats addEffect/endEffect; **fixed Player.cs/Part4 refs from Model.Stats.Calc → Model.GameObjects.Player.AbsoluteStatOwner**). RED: DataManager.ABSOLUTE_STATS_DATA, Templates.Stats.ModifiersTemplate. ✅**PetList(93L) PORTED** (LinkedHashMap→Dictionary; load/add/delete pets; Timestamp→DateTime via DateTimeOffset; RED PetCommonData/PlayerPetsDAO/ExpireTimerTask/IDFactory).✅**PetCommonData(291L) PORTED** (IExpirable; pet feed/mood/gift CDs, shuggle, refeed ScheduledTask; Timestamp→DateTime?, Future→Utils.ScheduledTask, Math.round(float)→floor+0.5f, Pet.cs already used PetCommonData — cone now consistent). RED: Templates.Pet.{PetTemplate,PetFunctionType,PetDopingBag}, Services.Toypet.{PetFeedProgress,PetHungryLevel,PetAdoptionService}, DataManager.PET_DATA/PET_FEED_DATA.✅**PrivateStore(56L) PORTED** (ns Model.GameObjects.Player; LinkedHashMap→Dictionary, order-preserving removeItem rebuild; **fixed Player.cs store field/accessors from Model.GameObjects.PrivateStore→.Player.PrivateStore**). RED: Model.Trade.TradePSItem ✅NOW PORTED (+TradeItem base).✅**ENpcFactionQuestState(enum) + NpcFaction(147L) PORTED** (ns ...Player.Npcfaction — lowercased to avoid CS0101 vs class NpcFaction; IPersistable fallthrough switch). RED: DataManager.NPC_FACTIONS_DATA.✅**NpcFactions(273L) PORTED — npcFaction cone COMPLETE.** join/leave guild, daily-quest cycle, level-up eviction, mentor-flag, getNextTime (Java time→DateTimeOffset 9AM logic); anon RequestResponseHandler→nested AskLeaveResponseHandler. **KEY FINDING: RequestResponseHandler is in `model.gameobjects.player` (NOT utils) — FIXED Equipment.Part4 refs** (Utils.Request→Model.GameObjects.Player.RequestResponseHandler). computeIfAbsent→TryGetValue/add; Rnd.get→Commons.Utils.Rnd.Get. RED: RequestResponseHandler(next), Templates.Factions.{NpcFactionTemplate,FactionCategory}, Templates.{QuestTemplate,Quest.QuestMentorType}, Services.QuestService, DataManager.NPC_FACTIONS_DATA/QUEST_DATA, Sm{DialogWindow,QuestAction}.✅**RequestResponseHandler<T:Creature>(56L) PORTED** (abstract AcceptRequest, virtual DenyRequest, Handle dispatch) — satisfies the nested handlers in Equipment.Part4 + NpcFactions.**LEGION CONE STARTED (Model/Team/Legion):** ✅**LegionRank(enum+GetRankId), LegionPermissionsMask(enum+Can), LegionWarehouse(168L) PORTED** — LegionWarehouse extends Storage, all mutators throw NotSupportedException (proxy-only) except IncreaseKinah(long) routing via LegionStorageProxy; AtomicInteger currentUser→Interlocked.CompareExchange/Volatile.Read; setLimit guarded, updateLimit via base.SetLimit (rows*8). **⇒ LegionStorageProxy ctor dep satisfied.** RED: Legion (next, GetWarehouseExpansions), World.GetInstance().GetPlayer.✅**LegionMember(125L) + LegionEmblemType(enum) + LegionHistoryAction(class-enum w/ nested Type, per-instance id+type) PORTED** — LegionMember hasRights→switch expression; cached player fields; getTime()→ToMillis. RED: Legion (next).✅**LegionEmblem(154L, IPersistable, upload-data assembly) + LegionHistoryEntry(record, interns name/desc) PORTED.**✅**Legion(398L) PORTED — LEGION CONE COMPLETE** (Legion, LegionMember, LegionWarehouse, LegionEmblem, LegionRank, LegionPermissionsMask, LegionEmblemType, LegionHistoryAction, LegionHistoryEntry). extends AionObject; level-gated member/kinah/contribution tables; history-by-Type (EnumMap→Dictionary, Java SequencedCollection addFirst/getLast/removeLast→Insert(0)/[^1]/RemoveAt), bonus via Utils.AtomicBoolean, nested Announcement record (Timestamp→DateTime). CopyOnWriteArrayList→List (foundational caveat). RED: Configs.Main.LegionConfig, Services.LegionService, World.GetInstance, SmIconInfo. **⇒ Player.GetLegion()/GetLegionMember()/LegionStorageProxy cone fully satisfied.** **ITEM-SUBSYSTEM STARTED:** ItemId/ItemMask/ItemStone/PendingTuneResult already existed. ✅**ManaStone(extends ItemStone) + RandomBonusEffect(IStatOwner) PORTED.** **BLOCKER FIXED: the StatOwner interface is `IStatOwner` in C# (StatOwner.cs) — fixed Item.cs + AbsoluteStatOwner.cs refs (Stats.Calc.StatOwner→IStatOwner).** RED: ITEM_DATA.GetModifiers, ITEM_RANDOM_BONUSES, Templates.Item.Bonuses.StatBonusType, GameStats.AddEffect/EndEffect. ✅**GodStone, ChargeInfo, IdianStone PORTED** — GodStone (ItemStone; AtomicLong→Volatile; proc-rate Math.round(float)→floor+0.5f; StatEnum.PROC_REDUCE_RATE), ChargeInfo (ActionObserver; charge bar steps, dot/attack burn), IdianStone (ItemStone; anon ActionObserver→nested IdianActionObserver; polish charge). **⇒ all Item stone/charge field deps now ported (ManaStone/GodStone/IdianStone/ChargeInfo/RandomBonusEffect/PendingTuneResult).** RED: Templates.Item.{GodstoneInfo,Actions.ItemActions,Bonuses.StatBonusType}, ITEM_RANDOM_BONUSES, GameStats.GetStat/AddEffect. ✅**EnchantStat, TemperingStat, EnchantEffect, TemperingEffect PORTED (Model.Enchants)** — XML-bound stat rows ([XmlAttribute] field-style + GetX, matching StatFunction convention); EnchantEffect/TemperingEffect implement IStatOwner, build StatAddFunction lists; plume vs accessory tempering, PlumStatEnum boost. **⇒ ALL Item field-type deps now ported (stones/charge/enchant/temper/bonus).** RED: DataManager.TEMPERING_DATA, PlumStatEnum members, StatAddFunction/IStatFunction. **ITEMTEMPLATE KEYSTONE STARTED (569L, extends VisibleObjectTemplate, ported as PARTIALS):** ✅**ItemTemplate.cs = Part1 (Java ~32-388)** — all ~45 XML-bound fields ([XmlElement]/[XmlAttribute]), id via XmlUid setter-prop, restrict/restrict_max byte[] via SpaceSeparatedBytes setter-props (ParseBytes), AfterUnmarshal (loader must call — JAXB callback has no XmlSerializer equiv), core accessors (mask/slot/class-restrict via PlayerClass.GetClassId()=ordinal/quality/flags/weapon-armor/kinah/stigma/itemset). **NOTE: pulls ~25 sub-template/enum types — most RED/missing** (Templates.Item.{ItemQuality,ItemType,ItemAttackType,GodstoneInfo,Stigma,WeaponStats,ItemActivationTarget,TradeinList,Acquisition,Disposition,Improvement,ItemUseLimits,ExtraInventory,Idian,ExceedEnchantSkillSetType}, .Enums.{ItemGroup,ItemSubType,EquipType}, .Actions.ItemActions, Stats.ModifiersTemplate, Itemset.ItemSetTemplate). ✅**ItemTemplate.Part2 (Java 389-569) DONE — ItemTemplate keystone COMPLETE (569L, 2 partials).** enchant/expire, weapon-stats, extra-inventory, ModifyMask, area/world restrictions (ZoneName), tune/bonus, improvement/uselimits/disposition, isCloth/isPotion, activation-target/race, robot, item-group/required-skills, tempering/enchant names, exceed-enchant. **FINDING: nearly all ItemTemplate sub-deps ALREADY EXIST** (all item enums ItemGroup/ItemSubType/EquipType/ArmorType/ItemQuality/ItemType/ItemAttackType + GodstoneInfo/WeaponStats/ItemActivationTarget/TradeinList/Acquisition/Disposition/Improvement/ExtraInventory/Idian/ExceedEnchantSkillSetType/ModifiersTemplate). Only 4 were missing: ✅**ItemUseLimits(119L) + Stigma(55L) PORTED** (XML-bound; ZoneName.CreateOrGet, AbyssRankEnum rank defaults, gender/ride nullable via setter-props, gain-skill groups). REMAINING MISSING: ItemActions(151L, templates/item/actions), ItemSetTemplate(58L, templates/itemset). RED: Utils.Stats.AbyssRankEnum, World.Zone.ZoneName, SKILL_DATA.GetSkillTemplatesByGroup. ✅**ItemActions(151L) + ItemSetTemplate(58L) PORTED — ItemTemplate sub-dep cone COMPLETE.** ItemActions: polymorphic [XmlElement(name,typeof)] list of 33 AbstractItemAction subtypes + typed getters (is-pattern); ItemSetTemplate: IStatOwner, itempart/partbonus/fullbonus, afterUnmarshal sets fullbonus item count. NOTE: codebase loader reads private [Xml*] fields (custom reflection loader, JAXB FIELD-style) — confirmed pattern. RED: the 33 action subtypes (most missing, templates/item/actions), AbstractItemAction, Itemset.{ItemPart,PartBonus,FullBonus}. **⇒ ItemTemplate keystone + its immediate sub-deps done; Item/Equipment/Storage cone now mostly satisfied (action subtypes still RED but only ItemActions getters touch them).**✅**FlightPath(35L, nested Type enum, GetType_()) + FlyPathEntry(XML) PORTED** (ns Model.Templates.Flypath — Java pkg is templates/flypath; **fixed Player.cs/Part2/Part4 + PlayerController refs from Model.Flightpath→Model.Templates.Flypath**).✅**FlyController(147L) PORTED** (start/end/glide fly state machine; broadcastToSightedPlayers, FP triggers). **BLOCKER/casing fixes:** ZoneType is PascalCase (Fly/NoFly/Pvp/Siege) not SCREAMING — fixed FlyController (NoFly/Fly) AND **Part3 CanPvP (World.Zone.ZoneType.PVP→Model.Templates.Zone.ZoneType.Pvp)**; fixed **CraftingTask ns in Player.cs/Part2 (Model.Gametime→SkillEngine.Task)**. RED: AbnormalState.NOFLY, AdminConfig.FREE_FLIGHT, TransformModel.GetRes6.**DEFERRED: CraftingTask** (extends AbstractCraftTask → deep task/service cone; Player field stays RED on frontier — low value to chase now). ✅**AbstractItemAction(base) PORTED** (abstract CanAct/Act, Object...→params object[]) — unblocks ItemActions field + all 33 action subtypes can now extend it. **ITEM-ACTION LEAVES BATCH 1:** ✅DecorateAction, RemodelAction, SummonHouseObjectAction, ReadAction PORTED (extend AbstractItemAction; CanAct/Act params object[]; ReadAction schedules via ThreadPoolManager). **BATCH 2:** ✅EmotionLearnAction (concurrent learnable-id set via ConcurrentDictionary, IsLearnable — **satisfies EmotionList.CanUse**), CraftLearnAction, AdoptPetAction PORTED. **BATCH 3:** ✅PolishAction PORTED (anon ItemUseObserver→nested PolishUseObserver; scheduled polish→IdianStone creation; **satisfies IdianStone.getActions().getPolishAction() + ItemActions.GetPolishAction**). **BATCH 4-6:** ✅UseTarget,Fireworks,Megaphone,TitleAdd,QuestStart,SkillLearn,ExpandInventory(+local StorageType enum),ChargeAction PORTED. ✅BATCH 8: AnimationAddAction PORTED (IExpirable.SecondsUntilExpiration needs explicit cast — C# default-interface-methods not inherited, foundational diff). DecomposeAction(300+L, RandomType switch ~30 cases + static id arrays) DEFERRED to dedicated tick. ✅BATCH 9: CosmeticItemAction(string switch→appearance setters/preset; template.GetType_()), AssemblyItemAction(anon obs→nested) PORTED. ✅BATCH 10: CompositionAction(4-arg overloads + anon obs), TuningAction(anon obs, PendingTuneResult record) PORTED. ✅BATCH 11: TamperingAction(static setTemperingLevel+anon obs), PackAction(switch-expr), MultiReturnAction PORTED. ✅BATCH 12: InstanceTimeClear, ToyPetSpawnAction PORTED. ✅BATCH 13: ExpExtractAction, EnchantItemAction(StartMovingListener anon→nested, 5-arg Act overload) PORTED. ✅BATCH 14: SkillUseAction PORTED (effect-type instanceof checks, CastState, isIneffectiveHealSkill LINQ All). ✅BATCH 15: RideAction PORTED (3 anon ActionObservers→nested: ABNORMALSETTED/ATTACKED/DOT_ATTACKED; ItemUseObserver; ride mount/dismount state machine). ✅BATCH 16: DyeAction PORTED (HouseObject<PlaceableHouseObject> forward-ref [type not yet ported—red ok]; dyeItem/dyeHouseObject; nullable minutes via string setter-prop; GetColor hex parse). ✅BATCH 17: DecomposeAction PORTED (424L — static race→int[] maps + 7 id arrays in static ctor; Chance.SelectElement[red]; grouped RandomType switch via stacked case labels [no fallthrough]; manastone/ancient-stone quality filter via randomType.ToString().Contains; do/while id-validate loops; ItemService.ItemUpdatePredicate; anon ItemUseObserver→nested; Math.round(float)→floor+0.5f). **⇒ ITEM-ACTION CONE COMPLETE — all 33+ AbstractItemAction subtypes ported.** ItemActions getters + ItemSetTemplate/ItemTemplate action refs now fully satisfiable.
- **⚠️ KNOWN-RED BLOCKER (namespace, codebase-wide):** `Rnd` is physically at `src/Aion.GameServer/Commons/Utils/Rnd.cs` → real ns `Aion.GameServer.Commons.Utils`, BUT all 13+ ported refs use `Aion.Commons.Utils.Rnd` (matches Java pkg `commons.utils` + the `Aion.Commons` project). The faithful fix is to RELOCATE Rnd.cs (and siblings) into the `Aion.Commons` project under `Aion.Commons.Utils`; until then every `Aion.Commons.Utils.Rnd.*` ref is red. Do this during the convergence/green phase (Tier 4), not piecemeal. DecomposeAction/RideAction follow the `Aion.Commons.Utils.Rnd` convention for consistency.
- **Config cone progress:** ✅AIConfig + CustomConfig(278L) PORTED (SCREAMING field names + @Property defaults; Set<Integer>→ISet<int>). **NEW Tier-4 blocker:** CustomConfig CronExpression fields use `Quartz.CronExpression` — needs Quartz.NET package ref (5 fields). ✅CONFIG CONE COMPLETE for all currently-referenced configs: AIConfig/CustomConfig/AdminConfig/SecurityConfig/CraftConfig/GroupConfig/LegionConfig/LoggingConfig/HTMLConfig/MembershipConfig(+fixed PlayerController ns)/SiegeConfig/GSConfig/RatesConfig PORTED. Remaining Tier-4 blockers: (1) Quartz.CronExpression (CustomConfig/SiegeConfig cron fields); (2) IDFactory.GetInstance() singleton bridge — existing IDFactory is divergent DI-style (no static GetInstance), join the DataManager singleton-bridge group at Tier4. **ITEM-SERVICE CONE:** ✅ItemService(209L)+ItemPacketService(237L, ItemAdd/Update/DeleteType enums)+ItemFactory PORTED; remaining red: SM_* item packets, PetFeedUnusualStorageArtifactCapture, ExpireTimerTask✅/QuestStatus✅. **QUEST VALUE CONE:** ✅QuestStatus/QuestEnv/QuestState/QuestVars PORTED (QuestTemplate[389L,~14 sub-deps] DEFERRED as multi-tick cone). ✅KiskService + ✅Kisk(214L, extends SummonedObject<Player>) PORTED — useMask bind rules, member-id ConcurrentDictionary<int,byte> (newKeySet idiom), GetTypeValue/IsEnemyFrom/GetNpcObjectType overrides, resurrection/lifetime. Resolves Kisk refs in PlayerController/ToyPetSpawnAction/KiskService.
- **Tier 2 — Creature-field containers:** `CreatureGameStats`(396L; +SM_EMOTION/Item/ItemSet/mastery-fns), `CreatureLifeStats`(377L; **do the SM_ATTACK_STATUS reconciliation here** — see scoped note in Blockers, byte-safe), `CreatureController`. (`AggroList`/`ObserveController`/`CreatureMoveController`/`TransformModel`/`KnownList` already ported as WIP.)
- **Tier 3 — spine reparent (F4, HIGH RISK, 329 files):** flat `Player`→`: Creature`; `Creature`(522L)+`Npc`/`Pet`/`Summon`/`StaticObject`/`StaticDoor`; reconcile the 4 `WorldPosition` InstanceId sites.
- **Tier 4 — packets + spatial + wiring:** `PacketSendUtility`; **migrate the 5 SmAttackStatus callers + add Creature ctor** (now callers have Creature); `World`/`WorldMap`/`WorldMapInstance`/`MapRegion`; **singleton-bridge** static accessors for `DataManager.*` statics (WORLD_MAPS_DATA/NPC_DATA/TRIBE_RELATIONS_DATA/MATERIAL_DATA/SKILL_DATA) wired at composition root (like `ThreadPoolManager` already is); deferred `GeoWorldLoader` (image-lib decision) + `Matrix4f`. → build green → guardrail + golden → merge to `main`.

**CORE PUSH STARTED (F3 spine tier):** ported `Utils/AtomicBoolean` (JDK equiv), `CreatureMoveController`, `TransformModel` (faithful WIP).
**KEY FINDING — core + F4 are ONE convergence (not sequential):** spine classes branch on `instanceof Player`/`instanceof Npc` and call container/`Effect`/`Skill` APIs, so they can't compile until (a) flat `Player` is reparented `: Creature` (F4) and (b) the packet layer gets **Creature/Npc-based ctors** — the existing 183 `Sm*` were built for flat `Player`/`WorldNpc` (e.g. `SmMove`/`SmTransform`/`SmCustomSettings` take `Player`/`WorldNpc`, not Java's `SM_X(Creature)`), and there's no `PacketSendUtility`. So convergence = **port Effect/Skill/Creature/containers/Npc + reparent Player + reconcile the packet ctors + add PacketSendUtility, all together, then build green.** Scope ≈ most of the remaining server (Effect/Skill transitively pull item/npc-template/spawn/geo/ai/skill engines + services + dozens of packets) — multi-session.

**REVISED packet-layer finding (2026-06-08): substrate ALREADY EXISTS.** Earlier "de-god-class ~200 packets" was WRONG — C# already has **183 standalone `Sm*` packet classes** + the `GameServerPacket` abstract base (`Network/Aion/ServerPackets/`, `Network/Aion/GameServerPacket.cs`). The only missing piece is **`PacketSendUtility`** (broadcast helpers). Its blockers: `Legion` (model/team/legion — MISSING), and a **`Player`→connection send path** (C# sends async via `GameServerConnection.SendPacketAsync`, NOT Java's `player.getClientConnection().sendPacket(packet)`; flat `Player` exposes `IsOnline` but no send method). Both are **F4 integration** (Player reparent + connection wiring). So `PacketSendUtility` lands around F4; the engines that call it are likewise gated there. Engines can otherwise reference the existing `Sm*` classes directly.
**After packets:** Effect engine (`Effect`/`EffectTemplate`+subclasses/`EffectController`), Skill engine (`Skill`/`SkillTemplate`+actions/conditions), AI engine (`AbstractAI`/`AIEngine`), stats containers (`CreatureGameStats`/`CreatureLifeStats`), move/observe/transform → then `Creature`+`CreatureController` → `Npc`/`Pet`/`Static*` → spatial container `World`/`WorldMap`/`WorldMapInstance`/`MapRegion` → reparent `Player` (F4) → green → merge.
**Current error frontier:** `Creature` (24 — now the central blocker), `MapRegion` (8), `WorldMapInstance` (4), `WorldMap` (2), `Npc` (2), `GeoService` (2), + `RespawnService`, faithful-`World` (flat-World swap pending).
**CRITICAL PATH = `Creature` + Tier-B cone** (the dominant remaining bulk): `Creature` fields force `AbstractAI`+`AIEngine`, `CreatureGameStats`/`CreatureLifeStats`(+`Stat2`/stat-fns), `EffectController`(+`Effect`/`EffectTemplate`), `CreatureMoveController`, `ObserveController`(+observers), `AggroList`(+`AggroInfo`), `TransformModel`, `Skill`/`SkillTemplate`, `CreatureController`. Then `Npc`/`Pet`/`StaticObject`/`StaticDoor`; spatial container `World`/`WorldMap`/`WorldMapInstance`(+2D/3D/factory)/`MapRegion` (+`ThreadPoolManager` singleton, `ZoneService`/`QuestEngine`/`InstanceEngine`, `PlayerContainer`/`SiegeNpc`/`ShieldService`); reparent `Player` (F4); reconcile InstanceId sites → green → merge.
**⚠ flat-`World` conflict:** the faithful `World` singleton (Java) must REPLACE the existing flat `World/World.cs` (different design) — `ZoneInstance`/`VisibleObject` etc. call `World.GetInstance().GetWorldMap(id)`. Measure flat-World consumers before swapping.
**Backlog (data-compat, like Race):** `ZoneAttributes` is PascalCase with `@XmlEnumValue` tokens (FLY/PVP/DUEL_SAME_RACE/…) only in comments — verify worldmaps XML `flags` parsing actually works; likely needs `[XmlEnum("FLY")]`/SCREAMING_SNAKE re-port for data parity.
**Convergence reconciliation (will resurface once WorldPosition resolves):** 4 legacy sites set the old stored InstanceId — `GameServerConnection.cs:14718/14722`, `InstanceRuntimeService.cs:147` (`with { InstanceId = … }`), `PlayerTeleportService.cs:159` (6-arg ctor). Rework to set position via the spawn/instance flow (derived).
**Spine-wide design decisions (resolved, Java-guided — apply consistently):**
1. **flat-`World` replacement:** swap the 133-line flat `World/World.cs` (packet-path API: `TryAddObject(object)`, `GetNpcs()→IWorldNpcObject`, houses) for the faithful Java `World` singleton (350L: `storeObject(VisibleObject)`, `getWorldMap(int)→WorldMap`, `findVisibleObject`, `updatePosition`, spawn/despawn). Rewrite its ~4 packet-path consumers to the Java API (they're slated to collapse anyway). Pulls in `PlayerContainer`/`SiegeNpc`/`ShieldService`/`AuditLogger`/`BindPointPosition` (port or track).
2. **Controller generics (Java `VisibleObjectController<? extends VisibleObject>` wildcard fields):** C# has no wildcards → use a **non-generic abstract base** `VisibleObjectController` (holds `VisibleObject Owner`, virtual `See/NotSee/OnDelete/...`) + **generic** `VisibleObjectController<T> : VisibleObjectController where T : VisibleObject` (typed `GetOwner():T`/`SetOwner(T)`). Same shape for `CreatureController`/`NpcController`. `VisibleObject` stores the non-generic base; `Creature.GetController()` returns the typed one via `new`. (Rule 8 closest-to-1:1.)
3. **`ThreadPoolManager` (Java static `getInstance()` vs C# DI):** add a Java-faithful static `GetInstance()` + `Execute(Action)` + `Schedule(Action, TimeSpan)` to the existing C# `ThreadPoolManager` (singleton assigned at startup), coexisting with the DI instance the packet path uses.
4. **`AionObject(objId, autoReleaseObjectId)`:** C# base ctor is `AionObject(int)` (the GC auto-release branch is FR-1, deferred). `VisibleObject`'s `super(objId, autoReleaseObjectId)` → C# `base(objId)`; the autoRelease flag wires in at FR-1.

**Build order (bottom-up):** WorldPosition✅ → MapRegion → WorldMapInstance(+2D/3D+factory) → WorldMap → ZoneInstance(+ZoneHandler/AdvancedZoneHandler) → VisibleObjectController → VisibleObject → KnownList/KnownObject → InstanceHandler/GeneralInstanceHandler → GeneralTeam → Creature(+CreatureController) + Tier-B subsystem containers → Npc/NpcController/Pet/StaticObject/StaticDoor → World singleton (+PlayerContainer/SiegeNpc/services) → reparent Player (F4) → reconcile the 4 InstanceId sites + legacy WorldPosition readers → green → merge.

## ⭐ BIG-BANG SCOPE (tiers — full membership)

The object-model spine is one strongly-connected component: in a single C# assembly a partial SCC won't compile, so it lands as **one large red-until-green effort** (recommend a dedicated branch/worktree). Build it bottom-up internally; commit only when the whole assembly builds + guardrail passes. Validate against golden packet/formula fixtures where they exist.

**Tier A — core spine (~3.7k Java L, ~20 files):** `WorldPosition`(class,224) · `MapRegion`(223) · `WorldMap`(215) · `WorldMapInstance`(349)+`WorldMap2D/3DInstance`+`WorldMapInstanceFactory` · `World`(350) · `VisibleObject`(256)+`VisibleObjectController`(124) · `Creature`(522)+`CreatureController`(568) · `Npc`(393)+`NpcController`(340) · `Pet`(49) · `StaticObject`(18)+`StaticDoor`(87) · `KnownList`(269)+`KnownObject`(51) · `ZoneInstance`(177)+`ZoneHandler`/`AdvancedZoneHandler` · `InstanceHandler`+`GeneralInstanceHandler` · `GeneralTeam` · support: `RespawnService`/`GeoService`/`ThreadPoolManager`/`ZoneService`/`QuestEngine` (minimal), `PositionUtil` game-object overloads.

**Tier B — Creature field subsystem cones (~5.5k+ Java L *before* their own sub-cones; the real bulk):** `AbstractAI`(465)+`AIEngine`(151) · `CreatureGameStats`(396)+`CreatureLifeStats`(377)+`Stat2`(113)+stat-functions/modifiers · `EffectController`(778)+`EffectTemplate`(572)+`Effect`+effect-subclass cone · `Skill`(1023)+`SkillTemplate`(409)+skill action/condition/property cone · `CreatureMoveController`(103) · `ObserveController`(270)+observer cone · `AggroList`(216)+`AggroInfo`(64) · `TransformModel`(220). Each drags in further subclasses — Tier B is the dominant cost and should be sized again before starting.

**Tier C — F4 Player reparent (highest risk):** flat C# `Player` (~1442L, sealed, own `ObjectId`/`Name`/`Position`) → `Creature`. Java `Player` 1655L. **329 referencing files.** Reconcile inherited vs flat fields.

**WorldPosition migration (part of Tier A):** struct→class touches **64 files / ~273 `.WorldId` + ~101 `.InstanceId` + ~72 `.Heading` + `with{InstanceId=…}` sites**; behavior-affecting (value→ref, `with`→setters, **stored→derived InstanceId** via `mapRegion.getParent()`).

**Open scoping question (decide before starting):** can the new spine land *parallel* (new abstract types + Tier B containers) while flat `Player`/`WorldNpc` keep working, deferring Tier C (F4) — OR must `WorldPosition` struct→class (used by both new `VisibleObject` and legacy flat code) force F4 simultaneously? The single `WorldPosition` type is the coupling point. Resolve this first; it determines whether the big-bang is "spine+subsystems" (Tier A+B) with F4 as a follow-on, or all three tiers at once.

**Item/npc cone nodes blocked on the SCC core (port during/after big-bang):** `ResultedItem`→`ResultedItemsCollection`/`ExtractedItemsCollection`/`ReturnLocList`/`MultiReturnItem`/`DecomposableItemInfo` (need `Player`+`DataManager`); `Stigma` (`SkillTemplate`); `ItemUseLimits` (`AbyssRankEnum`→`Player`); `ItemActions` (action hierarchy→`Player`/`Item`); `ModifiersTemplate`/`StatFunction` (→`Stat2`); `ItemSetTemplate` (`ItemPart`/`PartBonus`/`FullBonus`); then `ItemTemplate` itself → unblocks `NpcEquippedGear` → `Npc`. `CustomConfig` (port like `WorldConfig`). `AbyssRankEnum` (needs `Player`+`RankingConfig`+`ChatUtil`).

## Backlog / TODOs (come back to these)

**Owed upward features (port when their prereq lands):**
- **FR-1 `AionObject` GC objectId auto-release** — needs process-wide IDFactory accessor + `RespawnService.setAutoReleaseId` (Java `AionObject(int,boolean)` Cleaner branch).
- `Buff.BuffMapTypeExtensions.Matches(WorldMapInstance)` — needs `WorldMapInstance`.
- `GlobalDropItem` `DataManager.ITEM_DATA` validation — needs DI DataManager in load pipeline.
- `SpawnsData`: `saveSpawn`,`getNearestSpawnByNpcId`,`getFirstSpawnByNpcId`,`getRelativePath`,`loadSpawnsFromTemplateFiles`,`findSpawnTemplate`,`positionMatches`,`getNearestSpawn`,`toSpawnSearchResult` — need `VisibleObject`/`Player`/`WorldMapInstance`.
- `WorldConfig` config-framework loading (currently hardcoded Java defaults).
- `PositionUtil` game-object-aware overloads (only pure-coordinate methods ported) — at F2/F3.
- `WorldMapTemplate.GetTwinCount`/`GetBeginnerTwinCount` — WorldConfig cap.
- `DuplicateAionObjectException` — `Player.GetPosition()` detail at F4.
- `Polygon2D` rendering methods (`getPolyline2D`/`getPolygon`/`getBounds`/`getPathIterator`) and `RectangleArea.IntersectsRectangle` (Java TODO stub preserved) — no server callers.

## Validation

Fidelity Gate (foundation/additive): `dotnet build src/Aion.GameServer` + `python scripts/parity/check_fidelity.py` after each batch commit. Commit author `rrfarmer <ryanfarmer@mac.com>`, no AI co-author.

## Blockers / risks

- **✅ substrate progress (2026-06-08):** `Rnd` (commons/utils, full API) now ported at `Commons/Utils/Rnd.cs` — canonical; sites inlining `Random.Shared` (e.g. `SpawnGroup`) can migrate later. `AtomicBoolean` also exists. `DamageList` (controllers/attack) ported (WIP — forward-refs `Creature` spine + `TeamDamageList`).
- **🔁 `TeamDamageList` (controllers/attack, 50L) blocked on the team hierarchy cone:** needs `TemporaryPlayerTeam<?>` → `GeneralTeam<P,TM>` → `AbstractTeam` + `LootGroupRules` + team packets + `PacketSendUtility` (deep F4; flat C# Player only has a `CurrentTeamId`/`PlayerGroupRuntime` shim). Also has a **Java-erasure cast** `(DamageInfo<Player>)(DamageInfo<? extends Creature>)` that C#'s reified generics reject → port via **reconstruct**: `var d = new DamageInfo<Player>(player); d.AddDamage(src.GetDamage());` (observably identical; the source DamageInfo is read-only at that point). When porting `TemporaryPlayerTeam<?>` as a Dictionary key, use the non-generic-base convention (define the team base shape during the team-cone port, not guessed here).
- **`Predicates` (utils/collections) is Player-blocked:** only `alwaysTrue()` is pure; the `Players` nested class needs `Player`/`Pet`/`PetFunctionType` (F4). Port with the team/Player cone.
- **✅ GeoService/GeoMap non-spine forward-refs resolved:** `HouseDoorState`, `AIQuestion`, `GeoDataConfig`, `GameServerError` ported (`GameEngine`/`ZoneName`/`WorldMapType` already existed).
- **🔁 SM_ATTACK_STATUS reconciliation — SCOPED (do at F4 with Player:Creature):** the existing C# `SmAttackStatusType`/`SmAttackStatusLog` **already carry Java's exact ids** (NaturalHp=3, Hp=7, HealMp=19, NaturalMp=22, UsedMp=23…, Log Regular=191 etc.) — only PascalCase names + a divergent objectId-based ctor. Java's `SM_ATTACK_STATUS` nests `TYPE`/`LOG` (SCREAMING_SNAKE, dup ids DAMAGE=7/HP=7 etc.) + Creature-based ctors that compute objectId+hp/mp% at write time (writeImpl switch: damage/used→ −value+hp%, used_mp/damage_mp→ −value+mp%, mp/heal_mp/natural_mp/absorbed_mp→ value+mp%, default→ value+hp%). **Reconciliation (byte-format UNCHANGED → golden-safe):** nest faithful `TYPE`/`LOG` (Java names+ids) in `SmAttackStatus`, store `Creature` + compute in WritePayload like Java, add the 3 Creature ctors, migrate the 5 existing objectId-based callers. **Gated on `Player:Creature`** (callers currently hold objectId not Creature). Unblocks `CreatureLifeStats` + `AttackShieldObserver`.
- **🔁 `CreatureLifeStats` is packet-reconciliation-blocked:** its public API uses Java's `SM_ATTACK_STATUS.TYPE`/`.LOG` nested enums (specific client ids: TYPE NATURAL_HP=3/REGULAR=5/HP=7/HEAL_MP=19/NATURAL_MP=22…, LOG REGULAR/MPHEAL=4…). The existing C# `SmAttackStatus` **diverged**: separate PascalCase `SmAttackStatusType`/`SmAttackStatusLog` + an objectId-based ctor `SmAttackStatus(int creatureObjectId, type, skillId, value, hpOrMpPercentage, log, usesNegativeValue?)` instead of Java's `SM_ATTACK_STATUS(Creature owner, TYPE, skillId, value, LOG)`. **Reconcile per rule 8 (Java wins):** restore faithful nested `TYPE`/`LOG` (Java ids) + `Creature`-based ctor on `SmAttackStatus`, migrate the packet-path callers, THEN port `CreatureLifeStats` referencing the faithful enums. Do this as a dedicated packet step, not a rushed forward-ref.
- **📐 geoEngine cone (C# had no geoEngine at all):** ✅ math `Vector3f`/`Vector2f`/`FastMath`; ✅ collision `Collidable`/`UnsupportedCollisionException`/`CollisionIntention`(+`CollisionIntentions`)/`IgnoreProperties`/`CollisionResult`/`CollisionResults`; ✅ `utils/TempVars` (forward-refs Matrix3f + BIHNode.BIHStackData). ✅ math `Matrix3f`(1242L). ✅ `bounding/BoundingVolume`(201L)+`BoundingBox`(616L). **Remaining:** math `Matrix4f`(1829L) (pure, big — needed by BoundingBox.Transform `MultProj`/`ToRotationMatrix`; mostly rendering API, port when collision actually needs it); `scene` ✅`CollisionData`/✅`Spatial`(257L)/✅`scene/mesh` `IndexArray`+`IndexByteArray`+`IndexShortArray` — ✅`Geometry`(167L)/✅`Node`(496L)/✅`DespawnableNode`(139L) — remaining `Mesh`(150L, needs BIHTree); ✅`collision/bih` `BIHNode`(235L)+`BIHTree`(238L). ✅`math/Ray`(373L, complete — no forward refs). ✅`scene/Mesh`(150L). **Remaining:** `models/GeoMap` (the .geo loader); `Commons.Nio` buffers: ✅`ByteOrder`+✅`Buffer`+✅ **`Commons.Nio` COMPLETE** (ByteOrder, Buffer, Byte/Float/Short buffers + Heap + AsFloat/AsShort views). Unblocks Mesh/IndexArray/BoundingBox/Matrix3f buffer refs + the geo loader. Geo loader uses memory-mapped `ByteBuffer` → C# read file to byte[] + wrap (FileChannel/MappedByteBuffer adapted). ✅`models/GeoMap`(333L)+✅`models/Terrain`(188L)+✅`world/geo/GeoService`(204L, genuine Java singleton → GetInstance() resolves AggroList's geo call). **Remaining geoEngine:** `GeoWorldLoader`(285L, the .geo loader — uses MappedByteBuffer→C# file→byte[]+ByteBuffer.Wrap, ImageIO for terrain PNG, ZoneService/WorldMapTemplate; GeoService.Init forward-refs `GeoWorldLoader.Load`); `Matrix4f`(1829L, last math gap, only BoundingBox.Transform uses it). Forward refs surfaced by GeoService/GeoMap: `GameEngine`(model interface), `GeoDataConfig`, `HouseDoorState`, `WorldMapType.GetWorld/ORIEL/PERNON`, `AIQuestion.CONSIDER_BOUNDS_*`, `NpcTemplate`, `DataManager.WORLD_MAPS_DATA/NPC_DATA` (statics — singleton-bridge), + the Creature/Npc/Player/VisibleObject spine (the SCC). **NOTE:** scene-cone namespace caveat — index arrays are in lowercase `...Scene.mesh` (mirrors Java `scene.mesh`) to avoid clashing with the `Mesh` class FQN in `...Scene`. **Order:** Matrix3f → Matrix4f → BoundingVolume → BoundingBox → Geometry/Mesh/Spatial/Node → BIH → Ray → GeoMap → GeoService. **Watch:** Java signed-`byte` semantics (see CollisionIntention GetId→sbyte). **Convention:** Java `protected` fields are package-accessible (cross-instance within a package); C# `protected` is NOT (CS1540 when reading another instance's protected field from a sibling/subclass). When a port needs that (e.g. `DespawnableNode.copyFrom` reading another `Node`'s fields), map Java `protected` → **C# `protected internal`** (same-assembly = same-package-ish). Applied to `Spatial.name`, `Node.collisionIntentions/materialId`. **Forward dep:** `FloatBuffer` (java.nio) — referenced by `Matrix3f.FillFloatBuffer` (and likely Matrix4f); no C# equivalent, niche rendering helper. Port a minimal faithful `FloatBuffer` (Put(float) chainable, position/limit) under `Aion.GameServer.Commons.Nio` only when something actually calls it; otherwise it stays a red-branch forward ref. **NOW a real subsystem need:** the `.geo` loader (GeoMap) + IndexArray use java.nio `Buffer`/`ByteBuffer`/`ShortBuffer`/`FloatBuffer` (Limit()/Get/Put/position, big-endian `order`). Port a faithful minimal `Commons.Nio` buffer set before `GeoMap`. Watch endianness — Java nio default is BIG-endian; .geo files have a defined byte order, so match Java's buffer order exactly (don't assume BinaryReader little-endian). geoEngine math uses **PascalCase** API (`X/Y/Z`, `Add`/`Distance`…) + `GetX`/`SetX`; namespace `Aion.GameServer.GeoEngine.Math` (use `using JMath = System.Math;` to avoid the `Math` clash); constants keep Java SCREAMING names.
- **Remaining observers are each subsystem-blocked (not clean):** `FlyRingObserver` (Effect engine + QuestEngine), `RoadObserver` (TeleportService), `AbstractQuestZoneObserver` (Vector3f✓ + `ThreadPoolManager.Execute` — note: TPM has no fire-and-forget Execute yet), `AbstractCollisionObserver` (geoEngine collision: `CollisionResults`/`Ray`/`GeoMap`/`Spatial`/`GeoService`), `ShieldObserver` (`FortressLocation`/`ShieldTemplate`/`CollisionDieActor`). Port each with its subsystem.
- **✅ ObserveController cone DONE** — `ObserveController` (270L) ported (notify dispatch, COW attack-calc observers, shield/crit/multiplier checks); gives `Creature` its `GetObserveController()`/`NotifyHPChangeObservers` surface. Only deferred member: `AttackShieldObserver` (SM_ATTACK_STATUS reconciliation). Original cone notes:
- **🔭 ObserveController cone (no packets/singletons — clean SCC entry):** ✅ bases `ActionObserver`+`AttackCalcObserver`; ✅ subclasses `AttackStatusObserver`/`AttackerCriticalStatusObserver`/`DeathObserver`/`StartMovingListener`/`DialogObserver`/`ItemUseObserver`/`StanceObserver`. **Remaining before `ObserveController` (270L):** `ShieldObserver`, `FlyRingObserver`, `RoadObserver`, `AbstractQuestZoneObserver`, `AbstractCollisionObserver`+actors (`AbstractMaterialSkillActor`/`CollisionDieActor`/`TerrainZoneCollisionMaterialActor`/`ZoneCollisionMaterialActor`), and **deferred `AttackShieldObserver`** (244L — needs SM_ATTACK_STATUS TYPE/LOG reconciliation + Effect engine; port with `CreatureLifeStats`). Then `ObserveController` itself (forward-refs `Creature`/`Player`/`Item`/`Effect`/`Skill`). `ObserverType`/`AttackerCriticalStatus`/`ShieldType` ported.
- **✅ `AggroList` ported (controllers/attack, WIP)** — first application of the singleton-bridge convention below. Still forward-refs: `Creature`/`SummonedObject`(non-generic base)/`Player` spine, `StatFunctions.CalculateHate` (→`Utils.Stats`, MISSING), `GeoService.GetInstance().CanSee` (→`World.Geo`, MISSING), `DataManager.TRIBE_RELATIONS_DATA` (static member, MISSING — C# DataManager is DI-instance), `ThreadPoolManager.GetInstance()` (static accessor, MISSING — C# is DI).
- **🏛️ SINGLETON-BRIDGE CONVERGENCE DECISION (2026-06-08, adopted per doctrine):** Java exposes engine services as **static singletons** (`ThreadPoolManager.getInstance()`, `GeoService.getInstance()`) and data as **static fields** (`DataManager.TRIBE_RELATIONS_DATA`); the C# port chose **DI**. Per-instance domain objects (`Creature`, `AggroList`, life/game stats, controllers…) can't easily receive DI and must match Java 1:1. **Resolution:** add **Java-style static accessors** — `ThreadPoolManager.GetInstance()`, `GeoService.GetInstance()`, static `DataManager.TRIBE_RELATIONS_DATA` (etc.) — **bridged to the DI container at server startup** (one assignment in composition root). Domain code stays 1:1 with Java; DI services keep working. This is the agreed convention for the whole Creature SCC; apply it as engines/`DataManager` statics are added. (User doctrine: "Java is the only truth; refactor C# to match Java better.") **✅ `ThreadPoolManager.GetInstance()`/`RegisterInstance()` implemented + wired in `Program.cs` after `host.Build()`.** Still needed: static `GeoService.GetInstance()`, static `DataManager.TRIBE_RELATIONS_DATA` (+ other statics), `StatFunctions` (static class — naturally static, no bridge needed).
- Big slop clusters (BindPointTeleport, FindGroup, WorldNpc, PlayerKnown, summons, vortex) are **substrate-blocked** — they fake unported runtime; don't attempt top-down by file count. Port substrate first.
- Golden harness covers deterministic, constructor-driven packets + pure formulas only; singleton/time-dependent packets need a deterministic config harness first.
- `check_fidelity.py` matches by simple (normalized) class name — a faithful port named differently from its Java class can trip it; fix by matching the Java name, not by editing the baseline.

---
## SESSION PROGRESS — bottom-up red-frontier cones (rrfarmer autonomous /loop)
Branch `feature/object-spine-bigbang`, guardrail green every commit. Cones completed this run (all faithful 1:1, deep service/packet/AI/DataManager deps remain RED by design):
- **Item-action cone COMPLETE** — all 33+ AbstractItemAction subtypes (Skill/Ride/Dye/Decompose last).
- **Item-service cone** — ItemService(209L), ItemPacketService(237L: ItemAdd/Update/DeleteType), ItemFactory, ExpireTimerTask.
- **Summon controller cone** — PlayerAggroList, SummonMode/SkillOrder/UnsummonType, PlayableMoveController/SummonMoveController/SiegeWeaponMoveController, SummonController, SiegeWeaponController, SummonsService.
- **Gameobject-controller set** — NpcController(340L)/PetController/StaticObjectController/GatherableController(204L) (+Summon/SiegeWeapon).
- **Taskmanager cone** — AbstractPeriodic/AbstractFIFOPeriodic bases + PlayerMoveTaskManager/MoveTaskManager/MovementNotifyTask/TeamMoveUpdater/ExpireTimerTask.
- **Config cone COMPLETE (all referenced)** — AI/Custom/Admin/Security/Craft/Group/Legion/Logging/HTML/Membership/Siege/GS/Rates/Punishment.
- **Audit cone** — AuditLogger/AutoBan/GMService/PunishmentConfig; +ChatUtil.Name(Player) overload, AdminConfig byte→sbyte.
- **Quest value cone** — QuestStatus/QuestEnv/QuestState/QuestVars.
- **Gather cone** — GatherableTemplate/Materials/ExMaterials/Material + GatheringTask + AbstractInteractionTask/AbstractCraftTask (+ScheduledTask.IsCancelled).
- **Drop/loot cone** — Drop/DropItem/DropNpc + LootGroupRules/LootRuleType/InRoll.
- **Team/group/alliance/league cone COMPLETE** — Predicates, GeneralTeam, TemporaryPlayerTeam, PlayerGroup(+Member/Stats), PlayerAlliance(+Group/Member), League(+Member), TeamType, PlayerMode.
- **Gameobjects** — Letter, Kisk(214L), DropNpc; services KiskService, PunishmentService, ChatBanService, BannedMacManager(+gameserver BannedMacEntry); npcskill cone (NpcSkillTemplates/Template/Condition/Spawn + enums); NpcTemplate(329L)+NpcEquippedGear; SkillEngine.Task chain; AILogger; FollowStartService/FollowSummonTaskAI; TradeItem/TradePSItem.
- **Npcshout/walker cone** — ShoutEventType/NpcShout/ShoutList/ShoutGroup + NpcShoutsService + ShoutEventHandler; RouteStep/WalkerTemplate + WalkerGroupType.
- **AbyssService**(37L) — high-ranked-death + abyss-skill world announcements (PacketSendUtility/SmSystemMessage red).
- **RespawnService**(229L) — static ScheduleDecayTask(Npc)/(VisibleObject,long)/ScheduleRespawn + Has/SetAutoReleaseId/CancelRespawn(x2)/CancelRespawns(Predicate)/CancelEventRespawns; nested DecayTask + RespawnTask (Runnables→Run()). Idioms: Future→ScheduledTask via Schedule lambda+TimeSpan; ConcurrentHashMap.put→TryGetValue+indexer; remove(k,v)→`((ICollection<KVPair>)dict).Remove(new KVPair(k,this))`; synchronized(this)→`lock(this)`; anyMatch→.Any. RED deps: DropRegistrationService, EventService/Event, InstanceService, SpawnEngine, TemporarySpawnEngine, RiftService, IDFactory.GetInstance(), World.GetInstance().
- **TemporarySpawnEngine**(91L) — OnHourChange despawn/spawn, RegisterSpawned/UnregisterSpawned/AddSpawnGroup/Unregister(EventTemplate)/OnInstanceDestroy. Idioms: `synchronized` static→`lock(syncLock)`; HashMap/HashSet→Dictionary/HashSet; retainAll→IntersectWith; removeIf→RemoveWhere; computeIfAbsent→TryGetValue-else-add; groupingBy→GroupBy().ToDictionary; getOrDefault→TryGetValue. RED deps: SpawnGroup.ResetPoolSpots/ReserveRandomFreePoolSpot, controller.DeleteIfAliveOrCancelRespawn, SpawnEngine.
- **Housing spawn templates** — HouseSpawn (x/y/z/`byte h`/SpawnType; getType()→GetType_()), HouseSpawns (`getSpawns` null→empty List), Housing/TownSpawnTemplate (: SpawnTemplate base(group,spot)+townId). **Spawns template cone CONFIRMED COMPLETE** (all subdirs basespawns/housing/mercenaries/panesterra/riftspawns/siegespawns/vortexspawns verified ported). `byte` heading kept (matches sibling SpawnSpotTemplate.H).
- **Flyring template cone** — FlyRingPoint (x/y/z + ctor(Point3D)), FlyRingTemplate (name/map/radius/center/p1/p2 + ctor). Point3D green.
- **Plane3D** (geometry, Neon) — ctor(p1,p2,p3) normal=cross; `intersection`→`Intersection` (PascalCase convention) ray-plane test. Vector3f.Subtract/Cross/Dot/Add/MultLocal all green.
- **FlyRing** (: VisibleObject) + **FlyRingController** (: VisibleObjectController<FlyRing>, override See/NotSee, ConcurrentHashMap→ConcurrentDictionary, observed.remove→TryRemove). getName()→`override string Name`.
- **FlyRingObserver** (: ActionObserver, ObserverType.MOVE) + **FlyRingService** (singleton via nested static SingletonHolder). **Flyring cone COMPLETE.** RED deps across cone: IDFactory.GetInstance(), World.GetInstance().CreatePosition/Spawn, DataManager.FLY_RING_DATA/SKILL_DATA, Effect (Initialize/AddAllEffectToSucess/ApplyEffect), QuestEngine.OnPassFlyingRing, InstanceHandler.OnPassFlyingRing, Player.GetQuestStateList. log kept instance-level (matches Java instance Logger field).
- **Road template cone** — RoadPoint (x/y/z), RoadExit (mapid→GetMap()/x/y/z), RoadTemplate (name/map/radius/center/p1/p2/roadexit). Clean leaves, no red deps.
- **Road** (: VisibleObject; Spawn does StoreObject+Spawn) + **RoadController** (: VisibleObjectController<Road>, override See/NotSee). Mirrors FlyRing/FlyRingController.
- **RoadObserver** (: ActionObserver, ObserverType.MOVE; cross→TeleportService.TeleportTo by WorldType/Race) + **RoadService** (singleton, per-map per-instanceId Road spawn). **Road cone COMPLETE.** Casing confirmed: WorldType.Elysea/Asmodae (PascalCase), TeleportAnimation.FadeOutBeam, Race.ELYOS/ASMODIANS (SCREAMING). RED deps: TeleportService.TeleportTo, IDFactory.GetInstance(), World.GetInstance().CreatePosition/StoreObject/Spawn/GetWorldMap().GetAvailableInstanceIds(), DataManager.ROAD_DATA.
- **NameConfig** (config; ALLOW_CUSTOM_NAMES/CHAR_NAME_PATTERN/PET_NAME_PATTERN/FORBIDDEN_SEQUENCE_PATTERN/FORBIDDEN_WORDS/RESERVE_OLD_NAME_DAYS; Pattern→Regex raw default strings) + **NameRestrictionService** (IsValidName/Pet/Legion, IsForbidden/IsForbiddenWord, FilterMessage). **IDIOM: Java `Pattern.matcher(s).matches()` (full match) ≠ Regex.IsMatch (partial find)** — added private `Matches(Regex,string)` helper checking m.Success && Index==0 && Length==input.Length; Java `.find()`→Regex.IsMatch directly. equalsIgnoreCase→string.Equals(OrdinalIgnoreCase); "*".repeat(n)→new string('*',n). No red deps.
- **Housing enums (housing-template cone start)** — HousingCategory([XmlType("HousingObjectType")]), PlaceLocation, PlaceArea (each + {Enum}Extensions Value/FromValue), **LimitType** (enum + LimitTypeExtensions: ordinals match Java id 0..7 so GetId=(int)t; per-member PersonalLimits/TrialLimits int[] via switch; GetObjectPlaceLimit(HouseType)→limits[HouseType.GetLimitTypeIndex()]). RED: HouseType (unported). HouseType (enum+ext; GetLimitTypeIndex=(int)t, GetId/GetAbbreviation switch) DONE → LimitType green. **Housing base DONE: AbstractHouseObject(:VisibleObjectTemplate; talkingDistance/quality/category/nameId/id/canDye; GetTemplateId/GetL10nId/GetName overrides; [XmlInclude(PlaceableHouseObject)]) + PlaceableHouseObject(:it; useDays int?/limit LimitType?/location/area nullable; GetUseDays null→0, GetPlacementLimit null→NONE; abstract GetTypeId(); **@XmlSeeAlso→10 [XmlInclude] of leaf subclasses — RED until leaves ported**). **Housing leaf subclasses DONE (11): HousingChair(5)/JukeBox(6)/MovieJukeBox(:JukeBox,0)/Picture(0)/Postbox(3)/PassiveItem(0)/MoveableItem(0)/Storage(2,warehouseId)/Npc(7,npcId)/Emblem(11,level)/UseableItem(1,action+owner/cd/delay/useCount/requiredItem) + UseItemAction(finalRewardId/rewardId/removeCount/checkType int?). getTypeId()→override byte GetTypeId(). PlaceableHouseObject [XmlInclude] refs now GREEN.** Housing house-data leaves DONE so far: BuildingType(enum+ext GetId), PartType(enum+ext GetStartLineNr/GetEndLineNr/GetRooms/GetForLineNr), Sale([XmlRoot("sale")] pointPrice/goldPrice/level). BuildingCapabilities([XmlRoot("caps")]) + HousePart([XmlRoot("house_part")]; Set<String> building_tags→BuildingTagsRaw space-sep; getType()→GetType_(); IsForBuilding→Building red) DONE. HouseAddress(101L) DONE ([XmlRoot("address")]; exit_x/y/z/map nullable→float?/int?; **afterUnmarshal(parent)→public AfterUnmarshal(object) sets land — XmlSerializer has no parent callback, HousingLand loader must invoke it**; getLand→HousingLand red). Parts(88L) DONE (fence/garden/frame/outwall/roof int? + infloor/inwall/door int as XML ELEMENTS, propOrder=decl order). Building(110L) DONE (EnumMap<PartType,Integer>→Dictionary<PartType,int>; afterUnmarshal→AfterUnmarshal(parent); getType()→GetType_(); size/type HouseType?/BuildingType?; **sibling private-field access `getBuilding(id).size/.type/.partsMatch/.partsByType` kept 1:1 — class-level private allows it in C#**; getDefaultDecorId→int? via TryGetValue). RED: DataManager.HOUSE_BUILDING_DATA. **HousingLand DONE — housing-template cone complete. (was: last housing node: HousingLand (addresses+buildings list; invokes HouseAddress.AfterUnmarshal + Building.AfterUnmarshal).**
- **6 more template leaves** — MaterialActCondition(SUNNY/NIGHT), FactionCategory(MENTOR/DAILY/COMBINESKILL/SHUGO), QuestExtraCategory(NONE/COIN/DRACONIC/DEVANION/GOLD), RewardType([XmlType("Rates")] NONE/POINT/SPAWN + **value()/fromValue() → RewardTypeExtensions.Value()/FromValue() static class** since C# enums can't have methods), PetFeedResult(item), RouteVersion(id).
- **6 template enum leaves** — ItemGroupIndex(NONE/JUNK), BountyType(PER_X_KILLS/SEASONAL_KILLS), CPType(CONQUEROR/PROTECTOR), QuestMentorType(NONE/MENTOR/MENTE), TeleportType([XmlType("type")] REGULAR/FLIGHT), StatBonusType([XmlType("StatBonusType")] INVENTORY/POLISH). @XmlEnum (no name)→[XmlType(AnonymousType=true)]; @XmlEnum+@XmlType(name)→[XmlType(name)]. **NOTE: ~25+ small unported template leaves remain (scan: model/templates leaves) — good steady leaf supply for ticks.**
- **Upgradearcade template leaves** — ArcadeLevel (level/icon/upgrade_chance), ArcadeLevels (min_resumable_level + levels + GetMaxUpgradeLevel→[Count-1]), ArcadeRewardItem (item_id/normal_count/frenzy_count), ArcadeRewards (min_level + items). Clean leaves, no red deps. Start of UpgradeArcadeService(191L) cone (still deep: ArcadeProgress/ArcadeTask + UpgradeArcadeService deferred).
- **Vortex point templates (VortexService cone start)** — StartPoint/HomePoint/ResurrectionPoint (map/x/y/z/byte h; GetWorldId + Get*Point()→new WorldPosition(map).SetXyzh(x,y,z,h)). WorldPosition.SetXyzh(float?,float?,float?,byte?) ✅. VortexTemplate(64L) DONE (id/dRace/oRace/home/resurrection/start; Race + 3 points green). **VortexService cone remaining RED & deep: VortexLocation(200L, : IZoneHandler, RVController/DimensionalVortex/InvasionZoneInstance) → DimensionalVortex<?>/Invasion (services/vortex) → VortexService. DEFERRED (VortexLocation is large + RVController/DimensionalVortex unported).** Also **DatabaseCleaningService DEFERRED — entangled with JAXBUtil (deferred) + raw JDBC metadata API (Connection/getExportedKeys; JDBC→ADO.NET foundational diff) + many DAOs.**
- **Base enum/exception leaves (BaseService cone start)** — BaseType (CASUAL/SIEGE/STAINED/PANESTERRA/PANESTERRA_ARTIFACT/PANESTERRA_FACTION_CAMP), BaseColorType (BLUE/GREEN/RED/YELLOW), BaseException (: Exception; RuntimeException→Exception, ctor(Throwable cause)→base(null,cause)). BaseOccupier already green. BaseTemplate (templates/base; id/world/type/color/default_occupier=BALAUR; getType()→GetType_()) + BaseLocation (template/type/occupier; getType()→GetType_()) DONE. SiegeBaseLocation/StainedBaseLocation (color)/PanesterraBaseLocation (PEACE if FACTION_CAMP) (: BaseLocation) DONE. **Base<T>(276L) DONE** — applied non-generic-base+shim pattern (abstract `Base` holds all members w/ bLoc:BaseLocation; `Base<T>:Base where T:BaseLocation` re-types GetLocation() via `new`). Idioms: Future→ScheduledTask (isDone→`.Completion.IsCompleted`, cancel(true)→.Cancel()); AtomicBoolean.CompareAndSet/Get; schedule(Runnable,ms)→Schedule lambda+TimeSpan; SpawnHandlerType nullable param (despawnNpcs(null))→SpawnHandlerType?; getType()→GetType_(); Rnd.Get(coll).Value/Rnd.Chance(); List.of→new List{}. **SpawnHandlerType @ namespace `Aion.GameServer.SpawnEngine` (capital E) ≠ SpawnEngine class @ `Aion.GameServer.Spawnengine` — case-distinct namespaces; fully-qualified the engine.** **RESOLVED: added 26 SmSystemMessage.MsgLdf4Advance{Chief,Killer}V01..13 (IDs 1402506-1402531, no-arg) — Base getBossSpawnMsg/getAssaultMsg now GREEN.** RED: BaseService.Capture, PacketSendUtility.BroadcastToMap, World/Npc/Creature APIs, DataManager.SPAWNS_DATA.GetBaseSpawnsByLocId. **Base subclasses DONE:** CasualBase(:Base<BaseLocation>), SiegeBase(:Base<SiegeBaseLocation>), PanesterraBase(:Base<PanesterraBaseLocation>; overrides ChooseAssaultRace→BALAUR + GetOccupier(Creature)→FindBy(PanesterraFaction).Value), PanesterraArtifact/PanesterraFactionCamp(:PanesterraBase), StainedBase(:Base<StainedBaseLocation>; enhancedSpawnTask Future→ScheduledTask, override HandleStop, schedule/deactivate EnhancedSpawns, GetColor()). Made **Base.GetOccupier(Creature)+HandleStart/HandleStop+ChooseAssaultRace virtual** for overrides. Rnd.Get(min,max)*6000. SpawnHandlerType.GUARDIAN/OUTRIDER_ENHANCED ✅. **BaseService DONE** — init/start/stop/capture/stained-features. Idioms: switch-expr over BaseType (`or` combined arm, `_ => throw` for C# exhaustiveness); switch-statement in InitBases (fall-through cases, no default); Map<Integer,Base<?>>→ConcurrentDictionary<int,Base> (non-generic Base); synchronized(this)→lock(this); BaseException|NullPointerException→`catch (Exception e) when (e is BaseException || e is NullReferenceException)`; stream filter/map/toList→Where/Cast-Select/ToList; instanceof→is X y; **Java null BaseOccupier param→BaseOccupier? (capture/handleStainedFeatures)** w/ .Value on SetOccupier; loc.getType()→GetType_(); BaseOccupier.GetPanesterraFaction extension. **BASE CONE COMPLETE** (BaseType/Color/Exception/Occupier/Location+3 subloc/Base<T>+6 subclasses/BaseService). RED: DataManager.BASE_DATA.GetAllBaseTemplates, PanesterraService.HandleTeamElimination. **NOTE: loc.getType()→loc.GetType_() in BaseService switches when porting.**
- **BLOCKER RESOLVED: SmSystemMessage +16 STR factories** — extracted client msg IDs from Java SM_SYSTEM_MESSAGE.java and added: CannotFindPoll(1300684)/GetPollRewardMoney(1300947)/GetPollRewardItem(1300945)/GetPollRewardItemMulti(1300946) [SurveyService]; CompoundErrorEquipedItem(1400648)/CompoundItemNoTargetItem(1400717)/CompoundErrorNotAvailable(1400289)/CompoundErrorNotEnoughMoney(1400337)/CompoundErrorTemporaryExchangeItem(1400853)/CompoundErrorDifferentType(1400364)/CompoundErrorMainRequireHigherLevel(1400288)/CompoundErrorNotComparableItem(1400365)/CompoundSuccess(1400336)/DecompoundItemNoTargetItem(1400718)/DecompoundErrorNotAvailable(1400373)/CompoundedItemDecompoundSuccess(1400335) [ArmsfusionService]. Pattern: `SmSystemMessage(int, params string?[])`, longs→InvariantCulture strings in Java arg order. **The earlier SurveyService + ArmsfusionService red SM refs are now GREEN.** (Java SM_SYSTEM_MESSAGE.java carries the numeric IDs inline — future missing STR methods are sourceable the same way.)
- **CronJobService** (singleton; Moltenus/Ahserion/IdianDepthPortal/LegionDominion schedules). Idioms: anonymous **stateful** Runnable (field moltenus persists across cron fires)→private nested MoltenusSpawnTask, passed as `new MoltenusSpawnTask().Run` method-group (instance persists); Java switch-expression→C# switch-expression; `ThreadPoolManager.schedule(Runnable,ms)`→Schedule lambda+TimeSpan; `schedule(this, rnd, MILLISECONDS)`→self-reschedule lambda; Rnd @ Aion.GameServer.Commons.Utils. SiegeConfig.MOLTENUS_SPAWN_SCHEDULE/AHSERION_START_SCHEDULE are Quartz.CronExpression ✅. RED: CronService.Schedule (NOT ported), PanesterraService/LegionDominionService, SpawnEngine.NewSingleTimeSpawn (missing on SpawnEngine).
- **AccountService DEFERRED (model entanglement):** DAO front-end, but entangled with unported `Account` model + Java `Iterator.remove()` during iteration idiom (no clean C# analog without Account's removal API) + ~10 unported DAOs. Revisit when Account model + DAOs land.
- **BUGFIX: AdminConfig** — ~18 fields were malformed `public static sbyteFIELDNAME` (a prior byte→sbyte edit ate the space) = latent compile error; regex-fixed all to `sbyte FIELDNAME`.
- **StaticDoorService** (singleton; OpenStaticDoor/ChangeStaticDoorState + key check). Idioms: synchronized(door)→lock(door); instanceof→is; hasAccess(sbyte) with AdminConfig.INSTANCE_DOOR_INFO/INSTANCE_OPEN_DOORS (sbyte). SmSystemMessage.CannotOpenDoorNeedKeyItem ✅. RED: StaticDoor model (gameobject, NOT ported), VisibleObject.GetObjectByStaticId chain, InstanceHandler.OnOpenDoor, Player.GetInventory.DecreaseByItemId.
- **WarehouseService DEFERRED (packet-cone entanglement):** expandWarehouse/expand/canExpand mirror CubeExpandService (StorageExpansionTemplate now green, RequestResponseHandler nested capturing price, Warehouse* STR methods all ✅), BUT `sendWarehouseInfo` is entangled with a **redesigned** SmWarehouseInfo — C# port replaced Java's per-chunk `new SM_WAREHOUSE_INFO(items.subList(..),id,size,first,player)` + chunking loop with a private ctor + static factory methods (CreateLoginPackets/CreateRegularWarehouseUpdatePackets/...) over `WarehousePacketItem`. Faithful port needs reconciling against that factory API (Tier-4 packet cone), not a rushed red per-chunk ctor. Revisit with the SM_* packet convergence work.
- **ClassChangeService** (static; ShowClassChangeDialog/ChangeClassToSelection/CompleteAscensionQuest/SetClass(x2)/GetClassSelectionDialogPageId/GetSelectedPlayerClass). Idioms: `import static DialogAction.*`→`DialogAction.SELECT5_1` (int consts) in int-switch case labels; **Java null PlayerClass→C# `PlayerClass?`** (value-type enum, no NONE member; GetSelectedPlayerClass returns null; SetClass 4-arg takes PlayerClass? + `.Value` after null-guard); ActionAnimation.CLASS_CHANGE→`SmActionAnimation.ClassChange` const(=4, matches ClassChangeSetClassBroadcastPlanService); SM_QUEST_ACTION(ADD/UPDATE,qs)→`SmQuestAction.Add(qs)`/`Update(qs)`. **DIVERGENCE NOTED: QuestStateList.GetQuestState returns Questengine.Model.QuestState but SmQuestAction.Add wants PlayerQuestState — red type mismatch (quest-state type split unreconciled); faithful Java logic kept.** PlayerClass.IsStartingClass/GetClassId extensions, all packets + SkillLearnService green. RED: PacketSendUtility, Player.GetCommonData/GetGameStats/GetController APIs, SmQuestAction param type.
- **Guide leaves (HTMLService cone start)** — SurveyTemplate (model/templates/Guides; itemId/count) + Guide (model/guide; guide_id/player_id/title + ctor). Clean leaves, no red deps. Note namespace **Aion.GameServer.Model.Templates.Guides** (Java pkg `Guides` capital-G preserved). GuideTemplate(117L) DONE (level/classType/title/race/reward_info/message/select/surveys/rewardCount; @XmlTransient→[XmlIgnore]; PlayerClass/Race enums green). **Guide-template leaves complete.**
- **HTMLCache**(254L; cache/HTMLCache) — singleton html file cache + compaction. **FOUNDATIONAL DECISION: Java `ObjectOutputStream`/`ObjectInputStream` cache-file (Java-native serialization, no C# analog) → custom BinaryReader/BinaryWriter round-trip (int count + key/value strings). Justified: the html.cache file is an internal runtime optimization, NOT data Java reads — the "backward-compat with Java-read data" rule (XML/config) does NOT bind it.** Idioms: FileFilter→`HtmlFilter(path)` predicate; dir.listFiles(filter)→Directory.GetFileSystemEntries+filter; static initializer block→static ctor; **iterative `replaceAll(sb,pat,val)` replicated faithfully** (C# StringBuilder.Replace does single-pass non-overlapping, NOT equivalent to Java's re-scan-from-same-index cascade — wrote manual IndexOf(StringBuilder,...) + Remove/Insert); sb.setCharAt/charAt→indexer; sb.substring(f,t)→ToString(f,t-f); Character.isWhitespace→char.IsWhiteSpace; new String(raw,enc)→Encoding.GetEncoding(enc).GetString; toURI relpath→Path.GetRelativePath(...).Replace('\\','/'); String.format %.3f→ToString("F3"); synchronized→lock(this). Only dep HTMLConfig (green). **Resolves the recurring HTMLCache red ref (HTMLService + SurveyService).**
- **HTMLService** (static; GetHTMLTemplate/PushSurvey/ShowHTML/SendData chunking/SendGuideHtml/OnPlayerLogin/GetReward). **IDIOM: Java `String.substring(from,to)`→C# `Substring(from, to-from)`** (length not end-index); Short.MAX_VALUE→const 32767; StringBuilder.append→Append (+.ToString() for Replace); isEmpty()→.Length==0; log.warn(msg, new Throwable())→LogWarning(new Exception(), msg); log.error(msg, e)→LogError(e, msg); String.format→string.Format; anon player lambda for ForEachPlayer. Guide/SurveyTemplate/GuideTemplate green, DiceInventoryError ✅, LoggingConfig.LOG_ITEM ✅. RED: HTMLCache, SmQuestionnaire, GuideDAO, DataManager.GUIDE_HTML_DATA, ItemService, IDFactory, World.ForEachPlayer, Player.GetInventory.GetFreeSlots. **This resolves the SurveyService HTMLService.SendData red ref.**
- **ArmsfusionService** (static; FusionWeapons/BreakWeapons COMPOUND/DECOMPOUND + GetBasePricePerLevelSquared). ItemQuality SCREAMING switch (JUNK/COMMON→200 fall-through/RARE/LEGEND/UNIQUE/EPIC/MYTHIC+default→600); Persistable.PersistentState→`IPersistable.PersistentState` (using-alias). **NEW RED BATCH on SmSystemMessage — 12 missing STR_COMPOUND_*/DECOMPOUND_* methods referenced PascalCase: CompoundErrorEquipedItem/CompoundItemNoTargetItem/CompoundErrorNotAvailable(l10n)/CompoundErrorNotEnoughMoney(a,b)/CompoundErrorTemporaryExchangeItem/CompoundErrorDifferentType/CompoundErrorMainRequireHigherLevel/CompoundErrorNotComparableItem/CompoundSuccess(a,b)/DecompoundItemNoTargetItem/DecompoundErrorNotAvailable(l10n)/CompoundedItemDecompoundSuccess(l10n) — add when SmSystemMessage compound domain completed (needs client msg IDs).** RED: InventoryDAO, ItemSocketService.CopyFusionStones, ItemPacketService.UpdateItemAfterInfoChange, PricesService.GetPriceForService, Item.GetImprovement/HasFusionedItem APIs.
- **SurveyItem** (model/templates/survey; POJO public fields ownerId/uniqueId/itemId int, count long, html/radio string). Clean leaf.
- **SurveyService** (singleton; poll-reward item distribution + fixed-rate update task). Idioms: **non-static inner `TaskUpdate` Runnable→public nested class capturing outer via ctor (`new TaskUpdate(this)`)**; **NAME-COLLISION: Java method `taskUpdate()` + class `TaskUpdate` coexist (case) but C# can't → method renamed `TaskUpdateImpl()`**; LinkedHashMap→Dictionary (insertion-order until removal, caveat); putIfAbsent(k,v)==null→`TryAdd(k,v)`; scheduleAtFixedRate(Runnable,…)→ScheduleAtFixedRateTask lambda. **NEW RED on SmSystemMessage (missing STR_ methods, referenced as CannotFindPoll/GetPollRewardMoney(count)/GetPollRewardItem(l10n)/GetPollRewardItemMulti(count,l10n) — add later);** FullInventory ✅, ItemId.KINAH ✅, SecurityConfig.SURVEY_DELAY ✅. RED: HTMLCache/HTMLService, SurveyControllerDAO, ItemService, Player.GetInventory, World.GetPlayer.
- **Worldraid template cone** — MarkerSpot (x/y/z/byte h + ToString), WorldRaidNpc (npc_id + death_msg_id→int?), WorldRaidLocation (npcPool/locationMarkers + loc/map/xyz/h). Idiom: **@XmlElementWrapper+@XmlElement→[XmlArray]+[XmlArrayItem]**; nullable Integer attr→int?. No red deps.
- **WorldRaidSchedules** (configs/schedule; outer list + nested WorldRaidSchedule id/min/max/isSpecial/locations/raidTimes). Idioms: **@XmlList @XmlAttribute List<Integer>→space-separated string prop `LocationsRaw`→List<int>**; getType-free; static Load() via JAXBUtil. **NEW RED: JAXBUtil NOT ported — referenced `JAXBUtil.Deserialize<T>(FileInfo)`.** **DECISION (Tier-4 deferral): JAXBUtil is a JAXB→XmlSerializer foundational bridge sitting atop an ENTIRELY unported XML schema-validation stack (XmlUtil/XmlValidationHandler/StringSchemaOutputResolver all MISSING; zero `new XmlSerializer` usage in the port yet). It belongs to the DataManager/XML-load convergence pillar, NOT a clean leaf — deferred with that pillar. Faithful form: reproduce serialize/deserialize/preLoadContextAsync/generateSchema overloads backed by System.Xml.Serialization.XmlSerializer + a ConcurrentDictionary context cache; schema-validation parts (Schema/XmlValidationHandler) have no clean C# analog and need design. Do NOT rush a partial port mid-loop.** EventsConfig.ENABLE_WORLDRAID also missing. **WorldRaidService cone remaining RED & deep: WorldRaid(191L)/WorldRaidRunnable, CronService, JAXBUtil, EventsConfig; WorldRaidService deferred.**
- **SocialService** (static; block-list add/del/set-reason + friend make/delete/set-memo). **API DIVERGENCE: Java SM_*_RESPONSE static factories (`SM_FRIEND_RESPONSE.TARGET_ADDED(name)`) → C# const-code + ctor (`new SmFriendResponse(SmFriendResponse.TargetAdded, name)`).** SM_BLOCK_RESPONSE consts→SmBlockResponse.BlockSuccessful/UnblockSuccessful/EditNote; SM_FRIEND_NOTIFY.DELETED→SmFriendNotify.Deleted (const+ctor). All packets/models (BlockedPlayer/Friend/PlayerCommonData) green. RED: BlockListDAO/FriendListDAO, PacketSendUtility, World.GetPlayer, Player.GetBlockList/GetFriendList.
- **Expand** (templates/expand; level/price) + **StorageExpansionTemplate** (templates; expansions list + ids). Idioms: **`@XmlAttribute int[] ids`→space-separated string prop `IdsRaw`→int[]** (Buff.cs pattern); stream().mapToInt(Expand::getLevel).min/max().orElse(0)→`.Select(e=>e.GetLevel()).DefaultIfEmpty(0).Min()/Max()`; getPrice→int? (null when absent). No red deps. **Unblocks CubeExpandService.**
- **CubeExpandService** (static; ExpandCube question-flow + npc/item/quest Expand + ticket/limit checks). **Anonymous RequestResponseHandler<Npc>→private nested CubeExpandResponseHandler(npc, price) capturing price via ctor** (override AcceptRequest). SM_ STR_ mappings (all ✅): STR_EXTEND_INVENTORY_CANT_EXTEND_DUE_TO_MINIMUM..→InventoryCantExtendBelowNpcMinimum, ..MAXIMUM..→InventoryCantExtendAboveNpcMaximum, STR_EXTEND_INVENTORY_SIZE_EXTENDED→InventorySizeExtended, STR_EXTEND_INVENTORY_CANT_EXTEND_MORE→InventoryCantExtendMore, STR_WAREHOUSE_EXPAND_NOT_ENOUGH_MONEY→WarehouseExpandNotEnoughMoney; SM_QUESTION_WINDOW.STR_WAREHOUSE_EXPAND_WARNING→SmQuestionWindow.WarehouseExpandWarning (const). **API CONSOLIDATION: `SM_CUBE_UPDATE.cubeSize(StorageType.CUBE, player)`→`SmCubeUpdate.CubeSize(player)`** (StorageType.CUBE implied). ItemUpdateType via using-alias; getPrice→int? +.Value after null-guard. RED: DataManager.CUBEEXPANDER_DATA, PacketSendUtility, Player.GetNpcExpands/GetResponseRequester/GetCommonData/SetCubeLimit, Inventory.TryDecreaseKinah.
- **LifeStatsRestoreService** (singleton; 4 fixed-rate tasks: HpRestore/HpMpRestore/FpReduce/FpRestore). Idioms: **`CreatureLifeStats<? extends Creature>`/`<?>` wildcard→non-generic base `CreatureLifeStats`** (project wildcard pattern); Future→ScheduledTask; scheduleAtFixedRate(Runnable,initial,period)→`new Task(...)`+ScheduleAtFixedRateTask(ct=>{task.Run();...},TimeSpan,TimeSpan); nested Runnables→private classes w/ Run(). **CASING: Java `AIState.FIGHT`→C# enum `AiState.Fight` (PascalCase, file AiState.cs).** PlayerLifeStats @ Model.Stats.Container. RED: all CreatureLifeStats/PlayerLifeStats restore/reduce APIs, Creature.GetAi/GetFlyController.
- **Siege/shield leaves** — SiegeType (enum FORTRESS/ARTIFACT/OUTPOST/AGENT_FIGHT/INDUN/UNDERPASS), ShieldPoint (x/y/z), ShieldTemplate (name/map/id/radius/center). Clean leaves, no red deps. RectangleArea/BoundingBox/GeoDataConfig.GEO_SHIELDS_ENABLE already green.
- **SiegeShield** (: IZoneHandler; OnEnterZone attaches CollisionDieActor to enemy players, OnLeaveZone detaches; SetSiegeLocationId; geometry.GetParent() is DespawnableNode→SetType/SetId SHIELD). instanceof→is X y; ConcurrentHashMap→ConcurrentDictionary(.remove→TryRemove). IZoneHandler @ World.Zone.Handler, DespawnableNode.DespawnableType.SHIELD green. RED: SiegeService.GetInstance().GetFortress, FortressLocation.GetRace, SiegeRace.GetByRace, Creature.GetObserveController. **ShieldService cone remaining RED: FortressLocation/SiegeLocation (large), SiegeService — ShieldService still deferred.**
- **AbstractCollisionObserver** (abstract : ActionObserver, ObserverType.MOVE_OR_DIE; nested CheckType TOUCH/PASS; async geo ray-collision check) + **CollisionDieActor** (: it; kills cross-shield enemies, static Kill). Idioms: **AtomicBoolean ported util** (GetAndSet/Set); **ThreadPoolManager has no `execute()` → `Schedule(lambda, TimeSpan.Zero)`** (run-now-on-pool); anon Runnable→lambda; Java byte intentions→sbyte (CollisionResults ctor + CollisionIntention.GetId() both sbyte); Vector3f.Clone/Distance/SubtractLocal/NormalizeLocal/SetZ + Ray.SetLimit + GeoMap.COLLISION_CHECK_Z_OFFSET + Spatial.CollideWith all green. RED: GeoService.GetZ, FortressLocation.IsUnderShield/GetRace, SiegeRace.GetByRace, PlayerReviveService.ScheduleReviveAtBase, Creature.GetObjectTemplate/IsFlying/Controller.Die, Player.GetMoveController/IsInGlidingState.
- **ShieldObserver** (: ActionObserver, ObserverType.MOVE; upper-hemisphere shield-crossing → CollisionDieActor.Kill). Idioms: `instanceof X y &&`→`is X y &&` with pre-declared lastPos; synchronized(oldPosition)→lock(oldPosition); Point3D.SetX/Y/Z present ✅. RED: FortressLocation.IsUnderShield, CollisionDieActor.Kill, Player.GetMoveController().GetLastPositionFromClient. **ShieldService cone remaining RED & deep: SiegeShield (ZoneHandler+CollisionDieActor+DespawnableNode+SiegeService), FortressLocation/SiegeLocation (large), CollisionDieActor — ShieldService deferred until those land.**
- **Town** (model/town; : IPersistable, IL10n; id/level/points/levelUpDate/race + spawn lifecycle). Idioms: implements **IPersistable with nested `IPersistable.PersistentState` (SCREAMING)**; getL10nId→GetL10nId; Timestamp→DateTime?; new Timestamp(60000)→DateTimeOffset.FromUnixTimeMilliseconds(60000).UtcDateTime; **levelUpDate.setTime(currentTimeMillis)→reassign DateTime (immutable value type)**; synchronized method→`lock(this)`; anon Consumer→World.ForEachPlayer lambda; Java var `object`→`obj`. SpawnGroup(worldId,spawn) ctor + Spawn.GetSpawnSpotTemplates verified ✅. RED: GeoService.GetInstance().UpdateTown, DataManager.TOWN_SPAWNS_DATA, SmTownsList, PacketSendUtility, Npc/World APIs. **Unblocks TownService.**
- **TownService** (singleton; per-race town registry, GetTownById/GetTownResidence/GetTownIdByPosition/OnEnterWorld). Idioms: `instanceof X y`→`is X y`; Map.get null-on-absent→TryGetValue-else-null (asmos branch); switch on Race enum (case Race.ELYOS/ASMODIANS). RED: TownDAO.Load/Store, DataManager.HOUSE_DATA/NPC_DATA, HousingLand/HouseAddress, SmTownsList, PacketSendUtility, Player.GetActiveHouse, Creature.FindZones.
- **Announcement** (model; id/faction/announce/chatType/delay; getType()→GetType_(); getChatType() string→ChatType map) + **AnnouncementService** (singleton; reload/schedule/add/del fixed-rate world broadcasts). **IDIOM: Java nullable enum (Race faction = null for unrestricted) → C# `Race?`** (value-type enum can't be null; preserves `==null`/`!=` semantics 1:1 — foundational diff, doctrine rule 8). Idioms: anon Runnable→ScheduleAtFixedRateTask lambda; Future→ScheduledTask (.cancel→.Cancel); ConcurrentHashMap→ConcurrentDictionary (.remove→TryRemove); equalsIgnoreCase→string.Equals(OrdinalIgnoreCase). SmMessage/ChatType members verified ✅. RED: AnnouncementsDAO, PacketSendUtility.BroadcastToWorld, Player.GetOppositeRace.
- **CommandsAccessService** (static; per-player admin command-access grants Load/Give/GiveTemporary/Remove/RemoveAll/HasAccess). Idioms: Map<Integer,Set<String>>→Dictionary<int,ISet<string>>; **Map.compute(k,(k,v)->{...})→TryGetValue+null-init+indexer-store**; Collections.emptyMap()→new Dictionary (mutable — minor diff, loadAccesses always replaces first). RED: CommandsAccessDAO (Load/Add/Remove/RemoveAll), ChatProcessor.IsCommandExists, PacketSendUtility.SendMessage.
- **AdminService** (singleton; loads ./config/administration/item.restriction.txt, CanOperate GM item-trade gating + GMITEMRESTRICTION log). Idioms: BufferedReader/FileReader→`using StreamReader`+ReadLine loop; **printStackTrace()→`Console.Error.WriteLine(e)`** (no codebase precedent); split("#")[0].replaceAll(" ","")→Split('#')[0].Replace(" ",""); Integer.parseInt→int.Parse; HasAccess(sbyte) with AdminConfig.UNRESTRICTED_ITEMTRADE (sbyte). RED: PacketSendUtility.SendMessage, Player.IsStaff/HasAccess.
- **RepurchaseList** (model/trade; **LinkedHashSet<Integer>→List<int>+Contains-guard** per codebase convention, preserves insertion-order+uniqueness) + **RepurchaseService** (singleton; ConcurrentHashMap<Integer,Set<Item>>→ConcurrentDictionary<int,ISet<Item>>; getOrDefault→TryGetValue-else-empty; stream().anyMatch→.Any; stream().filter().findAny().orElse(null)→.FirstOrDefault; remove(k)→TryRemove). SmSystemMessage.DiceInventoryError (STR_MSG_DICE_INVEN_ERROR ✅), AuditLogger.Log ✅. RED: PlayerRestrictions.CanTrade, ItemService.AddItem, PacketSendUtility, Item/Inventory APIs.
- **Recipe template cone** — Component (itemid/quantity), ComboProduct (itemid), ComponentsData (List<Component>), RecipeTemplate(194L, : IL10n; getL10nId→GetL10nId; nullable Integer XmlAttribute→`int?` for max_production_count/craft_delay_time/craft_delay_id; getComponents null→empty List; getComboProduct→int?; Race [XmlAttribute("race")] serializes SCREAMING member names; primitive-int getters returning Integer collapse to int). No red deps. **Unblocks RecipeService.**
- **RecipeService** (ValidateNewRecipe/AddRecipe/AutoLearnRecipes; 1600-recipe cap, race/skill/skillpoint checks). SM_SYSTEM_MESSAGE STR_* mapped to SmSystemMessage PascalCase factories: STR_RECIPEITEM_CANT_USE_NO_RECIPE→RecipeItemCannotUseNoRecipe, STR_CRAFTRECIPE_RACE_CHECK→CraftRecipeRaceCheck, STR_CRAFT_RECIPE_LEARNED_ALREADY→CraftRecipeLearnedAlready, STR_CRAFT_RECIPE_CANT_LEARN_SKILL→CraftRecipeCantLearnSkill, STR_CRAFT_RECIPE_CANT_LEARN_SKILLPOINT→CraftRecipeCantLearnSkillPoint, STR_CRAFT_RECIPE_LEARN→CraftRecipeLearn (all exist ✅). RED (Tier-4): **PacketSendUtility.SendMessage/SendPacket (class NOT ported — confirmed deferred bridge)**, Player.GetRecipeList()/GetSkillList(), DataManager.RECIPE_DATA/SKILL_DATA.
- **RewardItem** (model/templates/rewards; id int + count long + ToString) + **FactionPackService** (singleton; faction-join lvl-65 6-item mail rewards gated by account creation window + opposite-race filter). **IDIOM: Java LocalDateTime (zoneless, nullable) → C# DateTime (non-nullable value type)** — `LocalDateTime.of(y,Month.X,d,h,m,s)`→`new DateTime(y,monthInt,d,h,m,s)`; `isBefore`/`isAfter`→`<`/`>`; Java null-guards on the LocalDateTime fields drop out (value type, callers always pass non-null — foundational diff, doctrine rule 8). `ServerTime.ofEpochMilli(x).toLocalDateTime()`→`ServerTime.OfEpochMilli(x).DateTime`. RED: FactionPackDAO, SystemMailService, DataManager.ITEM_DATA, Player.GetOppositeRace/GetAccount.GetCreationDate.
- **BonusPackService** (singleton; AddPlayerCustomReward — lvl-65 first-char gets 9 item rewards via mail). Idioms: HashMap<Integer,Integer>→Dictionary<int,int>, .put→indexer, entrySet→foreach KeyValuePair (e.Key/e.Value), isEmpty→Count==0. LetterType.EXPRESS (SCREAMING). RED: BonusPackDAO.LoadReceivingPlayer/StoreReceivingPlayer (dao), SystemMailService.SendMail (Services.Mail), Player.GetCommonData/GetAccount.
- **Curing-zone cone** — CuringTemplate (map_id/x/y/z/range get+set), CuringObject (: VisibleObject; getName()→`override Name => ""`; **anonymous empty `new VisibleObjectController<CuringObject>(){}`→private sealed nested CuringObjectController**), CuringZoneService (singleton; 1s fixed-rate: in-range players w/o abnormal 8751 get skill 8751 via UseNoAnimationSkill). Idioms: anon Runnable+Consumer→nested lambdas; forEachPlayer(Consumer)→ForEachPlayer(Action). **`SkillEngine` namespace-vs-class collision → fully-qualify `Aion.GameServer.SkillEngine.SkillEngine` (no `using`).** RED: DataManager.CURING_OBJECTS_DATA, IDFactory/World, EffectController.HasAbnormalEffect, SkillEngine.GetSkill.
- **DebugService** (singleton; ScheduleAtFixedRateTask analysis every 30min of world players' connection/ping). Idioms: method-ref `this::analyzeWorldPlayers`→`ct=>{AnalyzeWorldPlayers();return ValueTask.CompletedTask;}`; log.warn("...{}...",a,b)→LogWarning named placeholders {Player}/{Spawned}/{PingMS}; System.currentTimeMillis()→DateTimeOffset.UtcNow.ToUnixTimeMilliseconds(). RED deps (Tier-4 network bridge): **AionConnection** (type missing — maps to divergent GameServerConnection; deferred), Player.GetClientConnection()/connection.GetLastPingTime() (NOT ported), CmPing.CLIENT_PING_INTERVAL (member missing on CmPing), World.GetAllPlayers().
**Recurring idioms applied:** enum-with-fields→class-enum or enum+extension(GetType→GetTypeValue/GetType_); Java pkg-private→internal; ConcurrentLinkedDeque→LinkedList (concurrency caveat); CopyOnWriteArrayList→List; AionServerPacket→GameServerPacket; Predicate/Consumer/Function→System.Predicate/Action/Func; TemporaryPlayerTeam<?> wildcard→bound generic (covariance caveat); Java byte→sbyte where signed. **Tier-4 bridges still RED:** World.GetInstance()/UpdatePosition, IDFactory.GetInstance(), PacketSendUtility, SM_* packets, Quartz.CronExpression, most DAOs, DialogService/TeleportService/DropService/RespawnService (big services). DEFERRED: CAPTCHAUtil (java.awt image-lib, joins GeoWorldLoader), DecomposeAction done, QuestTemplate(389L, ~14 sub-deps).
- **Rewards leaves** — ArenaRewardItem (Java record→C# `record ArenaRewardItem(int ItemId,...)` + GetTotalCount), RewardEntryItem (: RewardItem green, entryId). FoodItem/MedicineItem still RED (need IdLevelReward base + Rnd.NextBoolean — next).
- **Bounds** (: BoundRadius green; altitude) + **SiegeRelatedBases** (@XmlList ids→BaseIdsRaw space-sep→List<int>). **DEFERRED: FoodItem/MedicineItem** — base IdLevelReward→ItemRaceEntry(111L, implements Chance, afterUnmarshal w/ StaticData/StaticDataListener/ItemData loader infra + QuestTemplate-deferred cone) is a deep red node, not a clean leaf. ai/Bombs (BombTemplate) + ai/Summons (Percentage) also pending their leaf deps.
- **ai BombTemplate + Bombs** DONE (skillId/cd; bomb element). NEXT ai cone: SummonGroup(79L) → Percentage → Summons (Percentage refs List<SummonGroup>).
- **ai SummonGroup(79L)+Percentage+Summons** DONE (npcId/xyz/h/min-maxCount/distance/schedule; afterUnmarshal→AfterUnmarshal(parent) validates min/max; Percentage→List<SummonGroup>; Summons→List<Percentage>). ai summon-template chain complete.
- **QueuedNpcSkillTemplate** (: NpcSkillTemplate; 3 ctor overloads, prob=100, NpcSkillTargetAttribute.MOST_HATED) + **QuestTarget** (enum NONE/AREA/LEAGUE/ALLIANCE) + **DoorRepairStone** (static_id/door_id). All deps green.
- **ItemPart (itemset) + BountyTemplate (bounty) + IGSubCategory (ingameshop)** DONE — clean leaves. MultiReturnItem still pending ReturnLocList leaf.
- **QuestCategory** (enum 13 values) + **purification/RequiredMaterial** (item_id/item_count) DONE. item ReturnLocList/MultiReturnItem chain pending ResultedItem (→ResultedItemsCollection→ReturnLocList→MultiReturnItem; RandomItem green).
- **item ResultedItem(74L)** DONE (id/min-maxCount/race/playerClasses; @XmlList player_classes→PlayerClassesRaw space-sep→List<PlayerClass>; **afterUnmarshal StaticDataListener(Unmarshaller) has no C# analog→fall back to DataManager.ITEM_DATA (else-branch behavior)**; IsObtainableFor). NEXT: ResultedItemsCollection(items+randomItems; both green now) → ReturnLocList → MultiReturnItem.
- **item ResultedItemsCollection + ReturnLocList(: it) + MultiReturnItem** DONE — item-result/return-loc chain complete (items/random_item; index/worldid/desc/alias; id/return_loc list). getItems/getRandomItems null→empty list.
- **MailPartType(enum+ext)/IMailFormatter(interface, getType()→GetType_())/TradeNpcType(enum+ext Index() 1-5)/SiegeLegionReward/FinishedQuestCond** DONE. StreamLocations deferred (needs Location2D leaf, MISSING).
- **FlyPathType(enum+ext GetId=(int)t) + Location2D + StreamLocations** DONE (windstreams; id/state/fly_path; location list null→new). NEXT windstreams: WindstreamTemplate.
- **WindstreamTemplate** ([XmlType("WindFlight")]; locations/mapid) DONE — **windstreams cone COMPLETE** (FlyPathType/Location2D/StreamLocations/WindstreamTemplate).
- **ChallengeType(enum+ext GetId/Value/FromValue)/KeyItem(item_ids space-sep+count)/OpenRift(schedule/spawnGuards)/MeshMaterial(only GetZoneName exposed)** DONE — clean leaves.
- **MailMessage (enum+ext GetId=(int)t, ids 0-6=ordinals) + walker RouteParent (version list + id)** DONE. PortalUse/ItemPurificationTemplate pending PortalPath/PurificationResult leaves; EventGroup/MedalGroup pending BonusItemGroup (itemgroups/StaticData deep cone — deferred).
- **purification cone COMPLETE**: PurificationResult (result/enchant/abyss/kinah + req_material list) + ItemPurificationTemplate (purification_result list + base_item_id). RequiredMaterial green. PortalUse pending PortalPath→QuestReq/ItemReq leaves.
- **portal cone COMPLETE**: QuestReq/ItemReq (get+set) + PortalPath (dialog/loc/siege/race/min_level/rank/kinah/title/err + quest_req/item_req lists) + PortalUse (portal_path list + npc_id). Race green.
- **IChance (model/Chance; GetChance + static SelectElement<T> overloads; Java Iterator.remove→(elements as ICollection<T>).Remove after select, return guards against post-mutation MoveNext) + ExtractedItemsCollection(: ResultedItemsCollection, IChance) + DecomposableItemInfo(isIsSelectable) + LegionDominionInvasionRift + ChestTemplate(KeyItem green)** DONE. IChance now green — partially unblocks ItemRaceEntry (still has StaticData/QuestTemplate deep deps).
- **TownLevel (level+spawns, Spawn green) + BindPointTemplate (name/npcid/price) + MaterialTarget** DONE. MaterialTarget: **Java enum w/ per-instance Predicate<Creature> bodies → enum + extension Matches(this,Creature) switch** (ALL→true/NPC→is Npc/PLAYER→is Player/PLAYER_WITH_PET→PLAYER.Matches||Summon w/ master). RED: Npc/Player/Summon. HandlerSideDrop pending QuestDrop base.
- **challenge ChallengeReward (msg_id/value int?, type RewardType green, getType()→GetType_()) + ChallengeQuestTemplate (score/repeat_count/id)** DONE. TeleLocIdData pending TeleportLocation leaf.
- **teleport cone COMPLETE**: TeleportType✓ + TeleportLocation (loc_id/teleportid/price/pricePvp/required_quest/type; getType()→GetType_()) + TeleLocIdData (telelocation list + GetTeleportLocation(int) lookup).
- **mail StringParamList(+nested Param) + MailPart base (: StringParamList, IMailFormatter; abstract; id int?; getType()→GetType_()=CUSTOM; GetFormattedString(IMailFormatter)+(MailPartType); abstract GetParamValue)** DONE. @XmlSeeAlso→[XmlInclude]. NEXT: Body/Header/Sender/Tail/Title (: MailPart, override GetType_ default + GetParamValue="") to resolve MailPart [XmlInclude] reds.
- **mail Body/Header/Sender/Tail/Title (: MailPart; MailPartType? type, GetType_ null→default BODY/HEADER/SENDER/TAIL/TITLE; GetParamValue="")** DONE — **mail-formatter cone COMPLETE** (StringParamList/Param/MailPart + 5 parts; IMailFormatter/MailPartType/MailMessage green). MailPart [XmlInclude] refs now GREEN.
- **LegionDominionReward (rank/item/count) + IGCategory (sub_category list + id/name, IGSubCategory green) + CollectItem (item_id/count int?)** DONE. PartBonus pending ModifiersTemplate/StatFunction (stats cone); QuestWorkItems pending QuestItems.
- **FoodType(enum+ext) + QuestRepeatCycle + RiftTemplate** DONE. **IDIOM: Java enum `implements L10n` + @XmlEnum → C# enum (preserves XML name-serialization) + extension GetDay=(int)t/GetL10nId(switch)/GetL10n=ChatUtil.L10n — C# enums can not implement interfaces; callers passing it as IL10n would be red (rare).** RiftTemplate id/world/hasSpawns/autoCloseable.
- **materials MaterialSkill + MaterialTemplate** DONE (materials cone). MaterialSkill: @XmlList @XmlAttribute List<MaterialActCondition> conditions→ConditionsRaw space-sep→List; target null→ALL (init field=ALL, ordinal 0 == default); getConditions null→empty list; frequency/level/id. MaterialTemplate: skill list + nullable Integer skill_obstacle→int? + id. MaterialActCondition/MaterialTarget green. **materials cone COMPLETE** (MaterialActCondition/MaterialTarget/MeshMaterial/MaterialSkill/MaterialTemplate).
- **challenge ContributionReward** (item_count/reward_id/number/rank ints) + **portal PortalDialog** (portal_path list + npc_id + teleport_dialog_id=1011; PortalPath green — **portal cone now COMPLETE**) + **quest QuestItems** (item_id int + count long=1L; default ctor + (itemId,count) ctor) DONE — clean leaves.
- **quest InventoryItem (item_id/count Integer→int?) + InventoryItems (inventory_item list, null→empty) + QuestWorkItems (quest_work_item list of QuestItems, null→empty; QuestItems green) + CollectItems (collect_item list lazy-init null→new, start_check Boolean→bool? getStartCheck null→false; CollectItem green) + pet PetDopingEntry (@XmlType("dope"); id/usedrink/usefood/usescroll)** DONE — clean leaves. NOTE: quest InventoryItem is a TEMPLATE class distinct from Model/GameObjects/InventoryItem (game object). QuestBonuses deferred (needs rewards/BonusType, unported).
- **rewards BonusType (plain enum 23 values, no @XmlEnum — serialized by SCREAMING member name as @XmlAttribute) + quest QuestBonuses (type BonusType getType()→GetType_() + level int; Java schema mentions skill attr but no actual field) + pet PetFunction (@XmlRoot("petfunction"); type/id/slots/rate_price + static CreateEmpty() type=NONE; PetFunctionType green) + pet PetRewards (result list lazy-init, group FoodType getType()→GetType_(), loved bool=false; PetFeedResult/FoodType green)** DONE — clean leaves. BonusType unblocks QuestBonuses (done). itemset/FullBonus + cp/CPRank still deferred (need stats ModifiersTemplate/StatFunction cone).
- **siegelocation SiegeMercenaryZone (id/costs/cooldown[*1000]/msg_id/announce_id) + panels SkillPanel (panel_id byte→sbyte; panel_skills List<Integer>→SkillsRaw space-sep; **getSkills() returns null intentionally (Java parity)**; canUseSkill/isSkillPresent bit-ops skill>>8==id & skill&0xFF==lvl) + staticdoor StaticDoorState (enum+ext; per-instance flag→GetFlag switch 0/1/2/4/8; static SetStates/GetFlags EnumSet→ISet<StaticDoorState>) + bounty KillBountyTemplate (type BountyType/kill_count/is_random_reward/race=PC_ALL + bounty list; BountyType/BountyTemplate/Race green) + model/siege AssaulterType (enum-with-fields spawnStake/spawnCosts List<Float>→enum+ext GetSpawnStake/GetSpawnCosts switch; Collections.emptyList→new List) + siegelocation AssaulterTemplate (type AssaulterType/heading_offset=60/distance_offset + @XmlList npc_ids→NpcIdsRaw space-sep; AssaulterType now green)** DONE — clean leaves. NOTE SkillPanel panel_skills has NO @XmlList in Java but is space-sep data → Raw idiom for parity. StaticDoorWorld/TownSpawn deferred (afterUnmarshal Map-dedup; need StaticDoorTemplate / TownLevel-Map cone — TownLevel green so TownSpawn portable next).
- **staticdoor StaticDoorTemplate (: VisibleObjectTemplate; id/keyid/x/y/z/state + override GetTemplateId=300001/GetName="door"/GetL10nId=0) + StaticDoorWorld (@XmlType("World"); afterUnmarshal Map-dedup→Dictionary, putIfAbsent→TryAdd-else-throw dup, templates=null; GetStaticDoors=.Values, GetStaticDoor TryGetValue) + towns TownSpawn (afterUnmarshal townLevels→Dictionary keyed by GetLevel, list=null; TownLevel green) + TownSpawnMap (afterUnmarshal townSpawns→Dictionary keyed by GetTownId)** DONE. **IDIOM CONFIRMED: Java afterUnmarshal Map-build pattern → public AfterUnmarshal(object parent) invoked by data-loader; final Map field→readonly Dictionary, getX(k)→TryGetValue-else-null, getValues()→.Values (ICollection).** staticdoor cone advancing (StaticDoorState/Template/World done); towns spawn-map cone advancing.
- **teleport TeleporterTemplate (@XmlRoot("teleporter_template"); npc_ids List<Integer>→NpcIdsRaw space-sep, teleportId, locations→TeleLocIdData [green]; containNpc) + restriction ItemCleanupTemplate (@XmlRoot("item_restriction_cleanups"); id + trade/sell/wh/awh/lwh byte→sbyte default -1; resultTrade/... getters) + siegelocation ArtifactActivation (item_id/count/skill/cd + repeat_count=1/repeat_interval=1; getCd→long cd*1000) + siegelocation DoorRepairData (afterUnmarshal door_repair_stone list→Dictionary keyed by repairStone.staticId; getCd→cd*1000; DoorRepairStone green)** DONE. **IDIOM: Java cross-class sibling package-private FIELD access (DoorRepairData reads repairStone.staticId) → made DoorRepairStone.staticId `internal` (closest C# analog to Java package-private; protected would NOT permit sibling-instance access).** TitleTemplate still deferred (StatOwner/ModifiersTemplate/StatFunction stats cone).
- **mail cone COMPLETE**: MailTemplate (@XmlElements 5 element-names→MailPart subtypes Sender/Title/Header/Body/Tail via [XmlElement(name,typeof(X))] stacked on private List<MailPart>; afterUnmarshal→Dictionary<MailPartType,MailPart> keyed by ((IMailFormatter)part).GetType_(); getFormattedTitle/getFormattedMessage isEmpty→.Length==0, "," joins; Race green) + SysMail (afterUnmarshal Map<String,List<MailTemplate>> lowercased caseName; getTemplate race==playerRace||PC_ALL) + Mails (@XmlRoot("mails"); afterUnmarshal Map<String,SysMail> lowercased; getMailTemplate→SysMail.getTemplate; size()→.Values.Count). All mail-formatter parts + IMailFormatter/MailPartType green. **mail cone (StringParamList/MailPart+5parts/MailTemplate/SysMail/Mails) DONE.**
- **pet PetStatsTemplate** (@XmlRoot("petstats"); reaction/run_speed/walk_speed/height/altitude) DONE — clean leaf. NOTE distinct from stats/PetStatsTemplate (different pkg, still unported). PetBuff deferred (ModifiersTemplate stats cone). instance_bonusatrr InstanceBonusAttr deferred (InstancePenaltyAttr needs StatEnum + skillengine.change.Func).
- **petskill PetSkillTemplate (@XmlType("pet_skill"); skill_id/pet_id/order_skill) + portal PortalItem (@XmlType("PortalItem"); id/itemid/quantity)** DONE — clean leaves.
- **BLOCKER NOTED — stats cone is a true convergence pillar (NOT a clean leaf):** StatFunction → Stat2/StatOwner/StatEnum(large enum)/Conditions(skillengine.condition cone)/CalculationType/IStatFunction; ModifiersTemplate(@XmlElements sub/add/rate/set/abs→StatSub/Add/Rate/Set/AbsFunction). Gates TitleTemplate, PetBuff, CPRank, FullBonus, PartBonus, RandomBonusSet. DEFER until stats pillar tackled as a focused unit.
- **BLOCKER NOTED — ZoneTemplate is a DIVERGENT SLOP file (in 363 baseline):** uses C# auto-properties + renamed enum members (ZoneClassName.Sub/AreaType.Polygon, NOT SCREAMING_SNAKE) + no protected points/zoneType/mapid/flags fields or setXmlName(). WorldZoneTemplate/MaterialZoneTemplate ctors need 1:1 protected-field access + Points(int,float) ctor + Point2D.x/y + ZoneClassName.DUMMY → require RE-PORTING ZoneTemplate to 1:1 first (doctrine rule: re-port slop). Candidate for a dedicated remediation unit (cascades to AreaType/Cylinder/Sphere/Semisphere/Points/Point2D/ZoneClassName — verify each for divergence).
- **LegionDominionLocationTemplate (: IL10n; id/world_id/race/zone/name_id + reward list[LegionDominionReward green] + invasion_rift[LegionDominionInvasionRift green]; getL10nId=nameId) + hotspot HotspotTemplate (id/worldId/x/y/z/race/price[long]) + teleport TelelocationTemplate (@XmlRoot("teleloc_template"): IL10n; loc_id/mapid/name_id/posX/posY/posZ/heading; getL10nId=nameId) + world WeatherEntry (static readonly NONE + private no-arg ctor[Java parity; note XmlSerializer needs public parameterless — defer to JAXBUtil pillar] + public (zoneId,weatherCode) ctor; before/after bool) + world WeatherTable (table list of WeatherEntry; getWeatherAfter/getWeathersForZone; equals→Equals)** DONE — clean leaves; **IL10n implemented via `: IL10n` + GetL10nId().** world weather cone (WeatherEntry/WeatherTable) DONE.
- **siegelocation SiegeReward (top/item_id/item_count/defeat-variants/gp_win/gp_defeat + hasItemRewardsForWin/Defeat) + assemblednpc AssembledNpcTemplate (nr/routeId/mapId/liveTime + assembled_part list; nested static class AssembledNpcPartTemplate→nested class [XmlType("AssembledNpcPart")] npcId/staticId) + portal PortalScroll (portal_path single PortalPath[green] + name; getName/setName) + materials MeshList (afterUnmarshal: meshMaterials→materialIdsByPath[path]=materialId & pathZones[path.GetHashCode()]=zoneName then path=null; getMeshMaterialId null→0; getMeshPaths→.Keys; size; **made MeshMaterial.path/materialId internal for sibling field access**) + quest QuestKill (seq/npc_ids→Raw space-sep/count→kill/var/step; transient npcIdSet; getNpcIds lazy-init+AddRange+Clear+null)** DONE — clean leaves. **NOTE MeshList.pathZones keyed by Java path.hashCode()→C# string.GetHashCode(): in-process-consistent (rebuilt each load), functionally equivalent; documented.** materials cone now MeshMaterial/MeshList/MaterialSkill/MaterialTemplate complete.
- **portal PortalLoc (world_id/loc_id/x/y/z/h[byte→sbyte]; full get/set pairs) + ride RideInfo (bounds[Bounds green] + cost_fp Integer→int?/start_fp/sprint_speed/fly_speed/move_speed + type Integer→int? getType()→GetType_()/id; canSprint sprintSpeed!=0) + factions NpcFactionTemplate (: IL10n; id/name/name_id/category[FactionCategory green]/min_level Integer→int? [getMinLevel()→minLevel.Value, .Value-throws ≈ Java NPE on null]/max_level=99/race/npc_ids→Raw space-sep/skill_points; isMentor category==MENTOR)** DONE — clean leaves. **DEFERRED AtreianPassport** (event): needs dataholders/loadingutils/adapters/LocalDateTimeAdapter (@XmlJavaTypeAdapter custom date-string parse, part of deferred XML-adapter/JAXBUtil stack) — AttendType green but adapter unported.
- **model/siege Assaulter (final int/float fields→readonly; ctor + 4 getters) + siegelocation AssaultData (assaulter list[AssaulterTemplate green] + dredgion_id/base_budget/base_delay; afterUnmarshal EnumMap<AssaulterType,List<Assaulter>>→Dictionary, builds Assaulter per npcId w/ TELEPORT cost=0 else spawnCosts[i]; AssaulterType/Assaulter green) + quest QuestDrop (npc_id/item_id/chance Integer→int? getChance null→100; drop_each_member/collecting_step int; transient questId; isDropEachMemberGroup/Alliance; **npcId/itemId/dropEachMember made `protected internal` — sibling subclass HandlerSideDrop reads them cross-instance; Java protected=subclass+package, C# protected internal=derived OR assembly is closest analog**) + quest Rewards (selectable_reward_item/reward_item QuestItems lists null→empty; gold→kinah long/exp/ap/dp/gp/title/extend_inventory/extend_stigma; ccheck List<Integer>→CollectItemChecksRaw space-sep; icheck)** DONE. **QuestDrop now green → unblocks HandlerSideDrop (next; needs DataManager.QUEST_DATA bridge — may stay RED on that).**
- **quest HandlerSideDrop (: QuestDrop; 2 ctors w/ `: this(...)` chaining; reads sibling drop.npcId/itemId/dropEachMember [now protected internal]; sets inherited this.questId/npcId/itemId/chance/collecting_step; neededAmount. **RED by design: DataManager.QUEST_DATA.GetQuestById().GetQuestDrop() — QuestTemplate/QuestsData unported bridge, tolerated**) + portal InstanceExit (instance_id/exit_world/race=PC_ALL/x/y/z/h[byte→sbyte]; getInstanceId declared Integer→int? [field primitive int]; full get/set) + tribe Tribe (6× @XmlList List<TribeClass> element→XxxRaw space-sep [TribeClass green]; getXxx null→new list assign+return; base=NONE default, getBase base==NONE?name:base; @base/name keyword-escaped; isGuard→name.IsGuard(); ToString override)** DONE. **DEFERRED: InstanceCooltime (InstanceCoolTimeType unported), CosmeticItemTemplate (Preset unported), AtreianPassport (LocalDateTimeAdapter).**
- **model/instance InstanceCoolTimeType (plain enum RELATIVE/WEEKLY/DAILY) + InstanceCooltime (all @XmlElement fields incl coolTimeType/typevalue/ent_cool_time/maxcount/max_member_light|dark/enter_min|max_level_light|dark/can_enter_mentor + @XmlAttribute id/worldId/race[green]/sync_id; getCanEnterMentor; getTypeValue) + cosmeticitems CosmeticItemTemplate (type→GetType_/cosmetic_name/id/race/gender_permitted + preset; nested static class Preset→nested class [XmlType("Preset")] scale/hair_type/face_type/hair_color/lip_color/eye_color/skin_color)** DONE — InstanceCoolTimeType unblocked InstanceCooltime; Preset was nested-not-separate (unblocked CosmeticItemTemplate). Remaining deferred blockers: AtreianPassport(LocalDateTimeAdapter), stats cone (TitleTemplate/PetBuff/CPRank/FullBonus/PartBonus/RandomBonusSet/InstancePenaltyAttr), ZoneTemplate slop re-port (WorldZoneTemplate/MaterialZoneTemplate).
- **challenge ChallengeTaskTemplate (: IL10n; quest[ChallengeQuestTemplate]/contrib[ContributionReward]/reward[ChallengeReward] elements + repeat/town_residence/name_id/max_level/min_level/prev_task Integer→int?/race/type[ChallengeType] getType()→GetType_()/id/legion_level_task; all deps GREEN — clean) + quest QuestNpc (final Set/List→readonly HashSet<int>/List<int>; 2 ctors `: this(npcId,20)`; addOnX guard+add; **RED by design: QuestEngine.GetInstance().RegisterCanAct — questEngine bridge**) + pet PetFlavour (food list[PetRewards green] lazy-init; getFoodType→FoodType? rewards.GetType_(); processFeedResult/isLovedFood; **RED by design: DataManager.ITEM_GROUPS_DATA.IsFood, services.toypet PetFeedCalculator/PetFeedProgress/PetHungryLevel bridges**)** DONE. ChallengeTaskTemplate clean; QuestNpc/PetFlavour faithful w/ tolerated red engine/service refs. pet cone advancing (PetRewards/PetFunction/PetStatsTemplate/PetDopingEntry/PetFlavour).
- **model/limiteditems LimitedItem (itemId/sellLimit/buyLimit/defaultSellLimit/salesTime + buyCounts Map→Dictionary; getOrDefault→TryGetValue-else-0; setToDefault/setBuyCount/etc) + goods GoodsList (item list + id/legion_lvl/advertise/gossip/salestime; afterUnmarshal builds itemIdList; getLimitedItems null-check both limits then new LimitedItem(.Value); nested static Item→nested class [XmlType("")] id/sell_limit/buy_limit Integer→int?) + tradelist TradeListTemplate (@XmlRoot("tradelist_template"); npc_id/npc_type=NORMAL[TradeNpcType green]/sell_price_rate=100/sell_price_rate2=100/ap_sell_price_rate2=100/buy_price_rate/save_count Boolean→bool?; tradelist list lazy-init; nested static TradeTab→nested class [XmlType("Tradelist")] id; isSaveCount→bool?)** DONE — clean leaves.
- **BLOCKER NOTED — PetFunctionType.cs is DIVERGENT slop (blocks PetTemplate):** C# port uses PascalCase members AND encodes Java's `id` as the enum VALUE → **conflates APPEARANCE(1) & FOOD(1) into aliases** (Java keeps them DISTINCT constants sharing id 1); also lacks getId()/isPlayerFunction(). Faithful 1:1 = class-enum (distinct ordinals WAREHOUSE..WING + GetId() byte→sbyte switch incl -1/-2 + IsPlayerFunction() switch APPEARANCE/BAG/WING=false). Re-port CASCADES to ~8 existing consumers (StaticData.cs ~6164, GameServerConnection.cs, PlayerEnterWorldRepository.cs, PetTemplateTable.cs, SmPet.cs, PetCommonData.cs, TradeSellToShopPlanService.cs, Predicates.cs) using PascalCase members → dedicated remediation unit. PetTemplate DEFERRED until then (also needs stats/PetStatsTemplate — distinct from ported pet/PetStatsTemplate; stats/ one is a clean leaf, portable independently).
- **stats/PetStatsTemplate (@XmlRoot("petstats"); reaction/run_speed/walk_speed/height/altitude — DISTINCT from pet/PetStatsTemplate, different namespace; unblocks PetTemplate's stats dep) + siegelocation SiegeLocationTemplate (: IL10n; id/type[SiegeType green]→GetType_/world + artifact_activation/door_repair_data/siege_reward list/legion_reward list/merc_zone list/assault_data/siege_related_bases [ALL green] + name_id/siege_duration/influence/occupy_count/legion_gp + kinah_rewards List<Integer>→Raw + @XmlList fortress_dependency→Raw; getKinahRewardByRewardLevel bounds-check; getFortressDependency null→empty; getRepeatCount/Interval delegate to artifactActivation)** DONE. **siegelocation cone COMPLETE** (ArtifactActivation/DoorRepairData/DoorRepairStone/SiegeReward/SiegeMercenaryZone/SiegeLegionReward/AssaulterTemplate/AssaultData/SiegeRelatedBases/SiegeLocationTemplate). PetTemplate now only blocked by PetFunctionType divergence remediation.
- **PetFunctionType 1:1 REMEDIATION DONE (doctrine rule 8)** — replaced divergent PascalCase value-as-id enum with faithful class-enum: SCREAMING members (distinct ordinals; **APPEARANCE now a DISTINCT constant from FOOD**, both id 1) + PetFunctionTypeExtensions GetId()[sbyte switch incl -1/-2] / IsPlayerFunction()[APPEARANCE/BAG/WING=false]. **Migrated all ~5 PascalCase consumers to SCREAMING** (StaticData.ReadPetFunctionTypeAttribute, PetTemplateTable, PlayerEnterWorldRepository, GameServerConnection ~12 sites, SmPet) via scoped Python token-rename. **SmPet value-dependent casts fixed**: `(int)PetFunctionType.Appearance`→`PetFunctionType.APPEARANCE.GetId()` (ordinal 7 ≠ id 1!), all `(byte)PetFunctionType.X`→`(byte)PetFunctionType.X.GetId()`, `(int)None`→`NONE.GetId()`. Faithful SCREAMING consumers (PetFunction.NONE, PetCommonData.FOOD/DOPING, Predicates.LOOT) now GREEN.
- **pet PetTemplate (: VisibleObjectTemplate, IL10n; @XmlRoot("pet"); id/name/nameid/condition_reward + petfunction list + petstats[stats/PetStatsTemplate green]; transient hasPlayerFuncs Boolean→bool?; getPetFunctions lazy adds CreateEmpty when no player funcs; getWarehouseFunction/containsFunction[type.GetId()<0 guard]/getPetFunction; override GetTemplateId/GetName/GetL10nId)** DONE. **pet cone COMPLETE** (PetFunctionType/PetFunction/PetStatsTemplate(both)/PetDopingEntry/PetRewards/PetFlavour/PetTemplate + PetFeedResult/FoodType/FoodType green).
- **CORRECTION: the stats cone was ALREADY FULLY PORTED** (StatEnum@Model/Stats/Container, IStatFunction/StatFunction/StatAdd|Sub|Rate|Set|AbsFunction@Model/Stats/Calc/Functions, ModifiersTemplate@Model/Templates/Stats, Stat2/StatOwner→IStatOwner/CalculationType, CPType, StatBonusType, Func@SkillEngine/Change) — prior "stats pillar deferred" deferrals were a FALSE assumption. Cleared the whole stats-blocked leaf backlog:
- **itemset PartBonus (count + modifiers→GetModifiers null→null) + itemset FullBonus (@XmlRoot; modifiers + private totalnumberofitems; getCount/setNumberOfItems) + TitleTemplate (: IStatOwner, IL10n; id[titleId]/modifiers/race/nameId/desc; getModifiers null→null) + pet PetBuff (id/feed_count + List<ModifiersTemplate> lazy-init) + item/bonuses RandomBonusSet (modifiers list + id + type[StatBonusType]) + cp CPRank (type[CPType]→GetType_/rank_num/visible_intruder_min_rank/modifiers; getStatModifiers null→empty) + instance_bonusatrr InstancePenaltyAttr (stat[StatEnum]/func[Func]/value get+set) + InstanceBonusAttr (penalty_attr list lazy-init + buff_id get/set)** ALL DONE — clean leaves on green stats deps. **IStatOwner used for Java `implements StatOwner`.** Only template leaves left: AtreianPassport(LocalDateTimeAdapter), quest/XMLStartCondition(QuestTemplate cone red), QuestTemplate(389L deep), WorldZoneTemplate/MaterialZoneTemplate(ZoneTemplate slop re-port).
- **dataholders/loadingutils/adapters LocalDateTimeAdapter (JAXB XmlAdapter<String,LocalDateTime> → plain Marshal(DateTime)→ISO "s"/Unmarshal(string)→DateTime.Parse RoundtripKind; no C# XmlAdapter base) + event AtreianPassport (@XmlRoot("login_event"); id/active + period_start/period_end via @XmlJavaTypeAdapter→**Raw string props delegating to LocalDateTimeAdapter** [pStart/pEnd DateTime XmlIgnore] + attend_type[AttendType green]/attend_num/reward_item/reward_item_num/reward_item_expire_time/reward_permit_level)** DONE. **IDIOM: @XmlJavaTypeAdapter on a typed attribute → [XmlIgnore] backing field + [XmlAttribute] string Raw property delegating to the ported adapter's Marshal/Unmarshal (data-compatible).** AtreianPassport unblocked. Remaining template leaves: quest/XMLStartCondition + QuestTemplate(389L deep quest cone, red on Player/QuestState/PacketSendUtility), WorldZoneTemplate/MaterialZoneTemplate (ZoneTemplate slop re-port).
- **TEMPLATES dir essentially exhausted — BROADENED into model/ (non-template) cones.** Spawned background task (task_1903432a) for ZoneTemplate 1:1 slop re-port + ~12-consumer migration (unblocks WorldZoneTemplate/MaterialZoneTemplate); too large/cascading for a 60s leaf tick.
- **broker BrokerRace (plain enum ELYOS/ASMODIAN) + gameobjects/player HouseOwnerState (enum-with-byte-id bitflags→enum+ext GetId() sbyte switch 1/2/4) + siege ArtifactStatus (id==ordinal→enum+ext GetValue()=(int)t) + broker BrokerMessages (id 2/3/5 non-ordinal→enum+ext GetId() switch) + broker/filter BrokerFilter (abstract; abstract Accept(ItemTemplate)) + BrokerAllAcceptFilter (: BrokerFilter; Accept→true; ItemTemplate green)** DONE — clean non-template leaves. broker/filter cone started (BrokerFilter base + AllAccept; MinMax/Contains/ContainsExtra filters next).
- **broker/filter BrokerMinMaxFilter (: BrokerFilter; final min/max→readonly; accept templateId/100000 in [min,max]) + BrokerContainsFilter (int... masks→params int[]; ArrayUtils.contains→masks.Contains(LINQ); /100000) + BrokerContainsExtraFilter (/10000) + autogroup AGQuestion (plain enum) + instance StageList (plain enum 6 stages)** DONE — **broker/filter cone COMPLETE** (BrokerFilter/AllAccept/MinMax/Contains/ContainsExtra). Idioms: varargs→params, org.apache.commons.lang3.ArrayUtils.contains→LINQ Contains. **DEFERRED: gameobjects PassiveObject/JukeBoxObject/ChairObject/PictureObject/EmblemObject etc — need HouseObject<T> base (unported gameobjects cone); HousingPassiveItem/HousingJukeBox green.**
- **autogroup AGPlayer (Java record→C# positional record (ObjectId,Race,PlayerClass,Name) + secondary ctor(Player) `: this(player.GetObjectId(),...)`; Race/PlayerClass/Player green) + instance/instanceposition InstancePositionHandler (interface Initialize(int,int)/Port(Player,int,int)) + limiteditems LimitedTradeNpc (readonly List<LimitedItem>=new; addLimitedItems→AddRange; LimitedItem green)** DONE — clean leaves. **DEFERRED: gameobjects MoveableObject/PassiveObject/etc (HouseObject<T> base unported); siege AgentLocation (SiegeLocation base unported — large siege cone w/ SiegeRace/STATE_* constants).**
- **team/common/events AlwaysTrueTeamEvent (abstract : ITeamEvent; checkCondition→true; HandleEvent left abstract — C# requires explicit `public abstract void HandleEvent()` since abstract class can't implicitly leave interface method) + team/league/events LeagueCreateEvent (: AlwaysTrueTeamEvent; league.ForEach lambda→alliance.SendPackets(new SmAllianceInfo(...LEAGUE_ALLIANCE_ENTERED...))) + LeagueDisbandEvent (league.ForEach→OnEvent(new LeagueLeftEvent(...LeaveReson.DISBAND)))** DONE. League/TemporaryPlayerTeam/ITeamEvent/SmAllianceInfo green; **RED-tolerated: LeagueLeftEvent (missing) + nested LeaveReson, alliance.SendPackets/OnEvent/GetLeader**. **DEFERRED: AbstractTeamPlayerEvent<T extends TemporaryPlayerTeam<?>> — wildcard needs non-generic TemporaryPlayerTeam base-shim (TemporaryPlayerTeam<TM>:GeneralTeam has no non-generic base); HouseObject<T> cone (PlaceableObjectController/House/HouseRegistry missing — multi-class, not a leaf).**
- **team/common/legacy GroupEvent (per-instance id, ENTER&UPDATE both id 13 but DISTINCT constants→enum+ext GetId() switch) + alliance/events AllianceDisbandEvent + group/events GroupDisbandEvent (forEach→OnEvent(new Player{Alliance,Group}LeavedEvent(...PlayerLeavedEvent.LeaveReson.DISBAND))) + ChangeAllianceLootRulesEvent + ChangeGroupLootRulesEvent + league/events LeagueLootRulesChangeEvent (SetLootGroupRules + SendPackets(SmAllianceInfo/SmGroupInfo))** DONE — all : AlwaysTrueTeamEvent (green). RED-tolerated: PlayerAlliance/PlayerGroup, Player{Alliance,Group}LeavedEvent, PlayerLeavedEvent.LeaveReson, LootGroupRules, SmGroupInfo. **IDIOM REINFORCED: per-instance-id enum w/ duplicate ids on distinct constants (GroupEvent ENTER/UPDATE=13, like PetFunctionType APPEARANCE/FOOD) → class-enum with distinct ordinals + GetId() switch (NEVER id-as-value which conflates).** team-events cone progressing.
- **instance InstanceScoreType (id 2-11 non-ordinal→enum+ext GetId() switch) + autogroup EntryRequestType (byte id 0/1/2→enum+ext GetId() sbyte + static GetTypeById(sbyte)→EntryRequestType? null-on-miss via Enum.GetValues loop) + instance InstanceProgressionType (id 1M-12M bit values→enum+ext GetId() + IsReinforcing/IsPreparing/IsStartProgress/IsEndProgress as `t==MEMBER`) + broker/filter BrokerPlayerClassFilter (: BrokerFilter; PlayerClass; accept→template.IsClassSpecific(pc)) + BrokerPlayerClassExtraFilter (: BrokerPlayerClassFilter; mask; base.Accept && mask==id/100000)** DONE — clean leaves. PlayerClass/ItemTemplate green (ItemTemplate.IsClassSpecific assumed green/red-tolerated). team-events cone PAUSED (remaining need wildcard-generic bases AbstractTeamPlayerEvent/PlayerEnteredEvent<T extends TemporaryPlayerTeam<? extends TeamMember<Player>>> + unported PlayerStopMentoringEvent/RequestResponseHandler — defer to a generic-base-shim focused effort).
- **enchants cone COMPLETE**: EnchantTemplateData (enchant_stat list[EnchantStat green] + level) + EnchantList (enchant_data list + item_group) + TemperingTemplateData (tempering_stat list[TemperingStat green] + level) + TemperingList (tempering_data list + item_group) DONE — clean leaves; EnchantStat/TemperingStat/EnchantEffect/TemperingEffect were already ported. Pure [XmlType]/[XmlElement]/[XmlAttribute] data holders.
- **ingameshop InGameShop (byte→sbyte subCategory/category=2 get/set) + ingameshop IGRequest (3 ctors, public fields gift/sync=false, accountId) + account CharacterBanInfo (readonly start/end/reason; end=duration+start) + instance DredgionRoom (readonly roomId + state=0xFF; captureRoom(Race) ASMODIANS?0x01:0x00; Race green) + assemblednpc AssembledNpcPart (object Integer→int? `@object` escaped; template→AssembledNpcTemplate.AssembledNpcPartTemplate nested[green]; getNpcId/getStaticId delegate) + model SellLimit (enum-with-fields min/max/limit→enum+ext GetPlayerMinLevel/MaxLevel/GetLimit + static GetSellLimit(Player) walks Enum.GetValues; **NoSuchElementException→InvalidOperationException**; Rates.SELL_LIMIT.CalcResult red-tolerated)** DONE — clean leaves. **IDIOM: java.util.NoSuchElementException → C# InvalidOperationException (closest analog).**
- **instance/playerreward InstancePlayerReward (points/pvpKills/monsterKills + readonly objectId; add/set methods) + craft ExpertQuestsList (enum-with-fields questIds[]/race/craftSkillId→enum+ext GetQuestIds()[]/GetRace()/GetCraftSkillId() switch + static GetQuestIds(int,Race) overload walks Enum.GetValues; IllegalArgumentException→ArgumentException; Race green)** DONE — clean leaves. **DEFERRED: instance/playerreward HarmonyGroupReward (PvPArenaPlayerReward base MISSING), instance/instancescore DarkPoetaScore (InstanceScore<T> base MISSING) — instance-score/reward base cone unported.**
- **autogroup AutoInstanceHandler (interface; OnInstanceCreate(WorldMapInstance)/AddLookingForParty(LookingForParty)→AGQuestion/OnEnter/OnLeave/OnPressEnter/Unregister(Player); AGQuestion green, WorldMapInstance/LookingForParty red-tolerated in signatures) + instance/instanceposition GeneralInstancePosition (abstract : InstancePositionHandler[green]; mapId/instanceId; Initialize; Teleport byte→sbyte→TeleportService.TeleportTo red-tolerated; explicit abstract Port re-decl) + stats/listeners TitleChangeListener (static OnBonusTitleChange<T>(CreatureGameStats<T> cgs,...) — **CreatureGameStats<?> wildcard→generic method param**; DataManager.TITLE_DATA/CreatureGameStats red-tolerated; TitleTemplate green)** DONE. **DEFERRED: house/PlayerScript (Java text-block byte-exactness via Encoding.Unicode + CompressUtil — fiddly, CompressUtil red); siege OutpostLocation (SiegeLocation base MISSING).**
- **assemblednpc AssembledNpc (assembledParts list[AssembledNpcPart green] + spawnTime=now/routeId/mapId; System.currentTimeMillis→DateTimeOffset.UtcNow.ToUnixTimeMilliseconds; getTimeOnMap) + gameobjects/findGroup FindGroupEntry (**Java sealed interface `permits ...` → plain C# marker interface; permits NOT enforceable in C# — documented divergence**) + gameobjects/player CustomPlayerState (per-instance bit mask, composite members OR others→enum+ext GetMask() switch incl (1<<6)|(1<<7)) + craft MasterQuestsList (enum-with-fields like ExpertQuestsList→enum+ext GetQuestIds/GetRace/GetCraftSkillId + static GetQuestIds(int,Race)) + broker/filter BrokerRecipeFilter (: BrokerFilter; params int[] masks; accept→template.GetActions().GetCraftLearnAction()...DataManager.RECIPE_DATA red-tolerated; ArrayUtils.contains→LINQ)** DONE. craft cone (Expert/Master QuestsList) + broker/filter cone fully done.
- **drop DropGroup (drop list[Drop green]/race=PC_ALL/name/level_based_chance_reduction/max_items=1; tryAddDropItems weighted nearest-chance selection w/ Rnd.Chance()/Rnd.Get(List); addDropItem per-member or single via new DropItem(drop).CalculateCount/SetIndex/SetPlayerObjId/SetWinningPlayer/IsDistributeItem; HashSet<>(drops)→new HashSet<Drop>(drops), Float.MAX_VALUE→float.MaxValue, isEmpty→Count==0) + drop NpcDrop (@XmlRoot("npc_drop"); drop_group list + npc_id; dropCalculator race-filter→dg.TryAddDropItems)** DONE — Drop/DropItem/Race/Player/Rnd green; **DropModifiers RED-tolerated (CalculateDropChance/GetDropRace — DropModifiers MISSING).** drop cone advancing (Drop/DropItem/DropGroup/NpcDrop done; DropModifiers + DropTemplate/etc remain).
- **NOTE: clean model leaves are drying up — most remaining are cone-coupled** (drop[DropModifiers], gameobjects/house[HouseObject<T>/UseableHouseObject/SummonedObject bases MISSING], siege[SiegeLocation base], instance-score[InstanceScore<T>/PvPArenaPlayerReward]). Next high-value moves: port a blocking BASE to unlock a cone, or the convergence pillars.
- **drop DropModifiers (isDropNpcChest/dropRace[Race? nullable-enum]/boostDropRate/reductionDropRate[Float→float?]/maxDropsPerGroup[Integer→int?] + get/set; calculateDropChance: allowReduction&&reduction!=null→chance*=reduction.Value, *boostDropRate)** DONE — **drop cone COMPLETE** (entire model/drop package ported: Drop/DropItem/DropGroup/DropModifiers/NpcDrop). DropGroup/NpcDrop now fully green (DropModifiers was their last red dep).
- **siege SiegeLocation (233L; : IZoneHandler[green]; STATE_INVULNERABLE/VULNERABLE consts; template + zones[List<SiegeZoneInstance> RED-tolerated] + creatures/players ConcurrentHashMap→ConcurrentDictionary[.put→indexer/.remove→TryRemove/.containsKey→ContainsKey/.values→.Values]; siegeRace=BALAUR; getType→GetType_; Integer.signum→Math.Sign w/ ±9 clamp; instanceof Player→is Player; Consumer→Action + CollectionUtil.ForEach; **GetNextState/GetRace marked `virtual` for subclass overrides**; getRace().getRaceId() red-tolerated) + AgentLocation (: SiegeLocation; override GetNextState/GetRace=BALAUR) + OutpostLocation (: SiegeLocation; override GetNextState; GetFortressDependency delegate; IsSilenteraAllowed→true)** DONE. SiegeRace/SiegeType/SiegeReward/SiegeLocationTemplate/IZoneHandler/ZoneInstance/CollectionUtil green; **RED-tolerated: SiegeZoneInstance (MISSING — zones/AddZone/IsInsideCreature/IsInsideCordinate), Creature.GetObjectId/Player.GetRace().GetRaceId().** siege-location base cone unblocked.
- **siege ArtifactLocation (: SiegeLocation[green]; ctor SetVulnerable(true); override GetNextState→STATE_VULNERABLE; status ArtifactStatus?→GetStatus null→IDLE; setInitialDelay/getCoolDown w/ System.currentTimeMillis→DateTimeOffset.UtcNow.ToUnixTimeMilliseconds, getActivation().GetCd(); getL10n via DataManager.SKILL_DATA red-tolerated; isStandAlone/getOwningFortress via SiegeService red-tolerated; ArtifactActivation/ArtifactStatus green)** DONE. RED-tolerated: SiegeService.GetInstance().GetFortresses/GetFortress, FortressLocation (still unported), DataManager.SKILL_DATA, SkillTemplate.GetL10n. Remaining siege: Influence(111L), FortressLocation(134L — deep, refs SiegeService/Influence/etc).
- **siege Influence (singleton; influencesByWorld Map<Int,Map<SiegeRace,Int>> + globalInfluences; **Java stream flatMap/groupingBy/summingInt → manual nested accumulation loops**; Map.compute(k,(k,v)->...) → TryGetValue-init-then-store; EnumMap→Dictionary; getOrDefault→TryGetValue-else-0; getPvpRaceBonusRatio switch-expr on Race; calculatePvpRaceBonusRatio thresholds; SiegeRace/Race/SiegeLocation green)** DONE. RED-tolerated: SiegeService.GetInstance().GetSiegeLocations(). Remaining siege: FortressLocation(134L — refs SiegeService/Influence[now green]/etc).
- **siege FortressLocation (134L; : SiegeLocation; shieldObservers ConcurrentHashMap→ConcurrentDictionary[.remove→TryRemove(out)]; getLegionRewards/getSiegeMercenaryZones delegate[green]; isEnemy raceId compare; override OnEnterZone/OnLeaveZone[base.* calls — made base virtual] w/ ShieldService.CreateShieldObserver+ObserveController; checkForBalanceBuff nested switch over SiegeBuffAction[nested enum] w/ effect 8867-8884 / SkillEngine.applyEffectDirectly buffs; override ClearLocation ForEachCreature lambda kills Kisk/teleports enemy Players; instanceof→is; **SkillEngine namespace-vs-class → fully-qualified Aion.GameServer.SkillEngine.SkillEngine**)** DONE. **siege model cone COMPLETE** (SiegeLocation/Agent/Outpost/Artifact/Fortress + Influence + Assaulter all ported). RED-tolerated: ShieldObserver/ShieldService, ZoneType, Kisk, TeleportService, SiegeConfig, PacketSendUtility, SmSystemMessage.STR_MSG_WEAK_RACE_BUFF_* factories, Creature.SetInsideZoneType/GetEffectController/GetObserveController, SiegeRaceExtensions.GetByRace.
- **stats/calc/functions StatShieldMasteryFunction (: StatRateFunction[green]; ctor(StatEnum,int,bool):base; override Apply(Stat2,params CalculationType[]) — cast stat.GetOwner()→Player, if player.GetEquipment().IsShieldEquipped() base.Apply; StatRateFunction.Apply confirmed `override void(Stat2,params CalculationType[])`)** DONE. RED-tolerated: Player.GetEquipment().IsShieldEquipped(). stats/calc/functions cone now complete (StatFunction/Add/Sub/Rate/Set/Abs/ShieldMastery/Proxy/IStatFunction).
- **NOTE: remaining model leaves heavily gated by HouseObject<T>/UseableHouseObject/SummonedObject bases (gameobjects house+summon cluster ~8 leaves) and team-event wildcard-generic bases (AbstractTeamPlayerEvent/PlayerEnteredEvent<T extends TemporaryPlayerTeam<? extends TeamMember<Player>>>/RequestResponseHandler/PlayerStopMentoringEvent). Next high-value: port HouseObject<T> (319L, refs VisibleObject[green]/Persistable[green]/PlaceableObjectController[missing]/House[missing]/HouseRegistry[missing] — multi-class cone) OR the team generic-base shims.**
- **gameobjects HouseObject<T> (319L; abstract : VisibleObject, IExpirable, IPersistable where T:PlaceableHouseObject) DONE.** Idioms: **VisibleObject.GetX/Y/Z/Heading are NON-virtual (position-derived) but Java HouseObject stores own x/y/z & overrides → used `new`-shadow (GetX/GetY/GetZ/GetHeading return stored fields; behavioral note: via VisibleObject ref→position-derived, via HouseObject ref→stored)**; covariant GetController()/GetObjectTemplate() also `new`-shadow w/ cast to PlaceableObjectController<T>/T; Java `position` field→C# `Position` property (SetXYZH); **nested PersistentState→IPersistable.PersistentState**; Java switch fallthrough (UPDATE_REQUIRED→default) → C# `goto default;`; Integer color→int?; byte heading→sbyte; System refs. RED-tolerated: PlaceableObjectController<T>/House/HouseRegistry/SM_HOUSE_EDIT/SmSystemMessage.STR_MSG_HOUSING_OBJECT_*/World.World/Spawnengine.SpawnEngine.BringIntoWorld/PositionUtil/ItemQuality (housing templates PlaceLocation/PlaceArea/LimitType/HousingCategory green). **Unblocks gameobjects house subclasses (PassiveObject/JukeBoxObject/MoveableObject/PictureObject/EmblemObject/ChairObject) — though they still ref HouseRegistry ctor param (red) + UseableHouseObject for Storage/Postbox.**
- **gameobjects house cluster: UseableHouseObject<T> (: HouseObject<T>; AtomicInteger usingPlayer[red-tolerated util]; canExpireNow→!isOccupied; setOccupant/releaseOccupant CompareAndSet) + EmblemObject(canExpireNow=false) + ChairObject/PictureObject/MoveableObject(OnUse empty override) + PassiveObject/JukeBoxObject(plain) + StorageObject/PostboxObject(: UseableHouseObject; OnUse w/ SetOccupant+SmSystemMessage.STR_MSG_HOUSING_OBJECT_* + SM_OBJECT_USE_UPDATE/SM_DIALOG_WINDOW; PostboxObject mailbox.mailBoxState=PlayerMailboxState.REGULAR)** DONE — 8 subclasses + UseableHouseObject. **ADDED `virtual CanExpireNow()` to HouseObject** (C# default-interface-method not class-overridable → rule-8 diff). RED-tolerated: AtomicInteger, House/HouseRegistry, packets (SM_OBJECT_USE_UPDATE/SM_DIALOG_WINDOW/STR_MSG_HOUSING_OBJECT_* factories), Player.GetMailbox/PlayerMailboxState/DialogPage. **gameobjects house cone COMPLETE.** Remaining gameobjects: GroupGate (SummonedObject<Creature> base).
- **gameobjects GroupGate (: SummonedObject<Creature>[already ported]; ctor base((sbyte)1, creator) + SetKnownlist(PlayerAwareKnownList)/SetEffectController(EffectController); override GetNpcObjectType→NpcObjectType.GROUPGATE)** DONE — SummonedObject/EffectController/NpcObjectType/PlayerAwareKnownList all green. **gameobjects model cone now COMPLETE** (all house objects + summoned + GroupGate). Note: SummonedObject was already ported (104L). Remaining model: team-event wildcard-generic bases (AbstractTeamPlayerEvent etc — deferred multi-file generic-shim), house/PlayerScript (text-block+CompressUtil), instance-score InstanceScore<T>/PvPArenaPlayerReward bases.
- **instance/instancescore InstanceScore<T : InstancePlayerReward> (66L; playerRewards ConcurrentHashMap→ConcurrentDictionary[.put→indexer/.remove→TryRemove/.get→TryGetValue-else-default/.values→.Values]; instanceProgressionType=START_PROGRESS; isRewarded/isReinforcing/isPreparing/isStartProgress delegate to InstanceProgressionType Is* extensions[green]) + DarkPoetaScore (: InstanceScore<InstancePlayerReward>; points/npcKills/rank=7/collections add/get)** DONE — InstancePlayerReward/InstanceProgressionType green. **DEFERRED still: HarmonyGroupReward (PvPArenaPlayerReward base MISSING — separate from InstanceScore).** instance-score base cone advancing.
- **instance/playerreward PvPArenaPlayerReward (166L; : InstancePlayerReward; nullable PlayerClass→PlayerClass?, byte buffId→sbyte; ArenaRewardItem(0,0,0,0) defaults; addPoints(int,PvPArenaScore) score-cap clamp; System.currentTimeMillis→DateTimeOffset; ap/gp/crucible/courageInsignia + reward1/2 + boostMorale[InstanceBuff]) + HarmonyGroupReward (: PvPArenaPlayerReward; AGPlayer list[green], agp.objectId()→agp.ObjectId, stream().anyMatch→.Any; SetCourageInsignia(ArenaRewardItem 186000137); grpObjectId)** DONE. RED-tolerated: InstanceBuff(GetRemainingTime/ApplyEffect/EndEffect), PvPArenaScore(GetUpper/LowerScoreCap — 250L unported). PlayerClass/ArenaRewardItem/RewardItem/AGPlayer/InstancePlayerReward green. Remaining instancescore/playerreward: PvpInstancePlayerReward/CruciblePlayerReward + scores (PvpInstanceScore/HarmonyArenaScore/NormalScore/LegionDominionScore/PvPArenaScore).
- **instance/playerreward CruciblePlayerReward (: InstancePlayerReward; spawnPosition/insignia + isRewarded/isPlayerLeave/isPlayerDefeated flags) + instance/instancescore NormalScore (: InstanceScore<InstancePlayerReward>; points[addPoints clamps >=0]/basicAp/finalAp/rank=7 + 4× rewardItem+count get/set)** DONE — clean InstanceScore/InstancePlayerReward subclasses. Remaining instancescore/playerreward: PvpInstancePlayerReward(202L)/PvpInstanceScore/HarmonyArenaScore/LegionDominionScore/PvPArenaScore(250L — bigger, ref deeper services).
- **instance/instancescore PvpInstanceScore<T : InstancePlayerReward> (: InstanceScore<T>; winner/loser/drawApReward readonly + asmo/elyos points&kills; getPointsByRace/getRaceWithHighestPoints[==→NONE]; **synchronized addPointsByRace/incrementKillsByRace → lock(this)**; Java arrow-switch ELYOS/ASMODIANS→C# switch statement, Math.max clamp; Race green incl NONE)** DONE. **DEFERRED: HarmonyArenaScore/LegionDominionScore/PvPArenaScore (extend PvPArenaScore[250L]/InstanceScore w/ WorldMapInstance/Comparator-sort/deep service refs — PvPArenaScore base unported), PvpInstancePlayerReward(202L).**
- **instance/playerreward PvpInstancePlayerReward (202L; : InstancePlayerReward; Race + AtomicInteger capturedZones[red-tolerated]; int[] reward1-4/bonusReward w/ null→0 getters + setReward(new int[]{...}); base/bonus Ap/Gp; getMythicKunaxEquipment(Player) — Java arrow-switch over PlayerClass[11 classes]→C# switch statement assigning weapons/armor arrays, default→log.LogWarning+return 0; Rnd.Chance()<17.5f?weapons:armor; Rnd.Get(int[]); **inline LoggerFactory.getLogger().warn → static ILogger log=NullLogger.Instance + LogWarning**)** DONE. PlayerClass/Race/Player/Rnd green; AtomicInteger red-tolerated. instance playerreward cone nearly done (only Pvp scores remain: HarmonyArenaScore/LegionDominionScore/PvPArenaScore extend unported PvPArenaScore[250L]).
- **Utils AtomicInteger (java.util.concurrent.atomic.AtomicInteger → Interlocked-backed; Get/Set/GetAndSet/CompareAndSet/IncrementAndGet/DecrementAndGet/GetAndIncrement/GetAndDecrement/AddAndGet/GetAndAdd/IntValue; matches AtomicBoolean util in Aion.GameServer.Utils)** DONE — clears recurring red dep. Fixed UseableHouseObject + PvpInstancePlayerReward usings (Commons.Utils→Utils for AtomicInteger; PvpInstancePlayerReward keeps Commons.Utils for Rnd). NOTE: AtomicBoolean/AtomicInteger live in Aion.GameServer.Utils; Rnd in Aion.GameServer.Commons.Utils.
- **instance InstanceBuff (106L; implements StatOwner→IStatOwner; functions List<IStatFunction>[green]; InstanceBonusAttr[green]; Future<?>→ScheduledTask[.cancel(true)→.Cancel()/.isDone()→.Completion.IsCompleted]; applyEffect builds StatAddFunction per InstancePenaltyAttr w/ Func.PERCENT→*value/100; **stateful nested Runnable InstanceBuffTask→nested class capturing outer InstanceBuff+player, scheduled via ThreadPoolManager.GetInstance().Schedule(ct=>{buffTask.Run();return ValueTask.CompletedTask;}, TimeSpan.FromMilliseconds(time))**; System.currentTimeMillis→DateTimeOffset; getGameStats().getStat(stat,0).getBase; notify() instanceof→is HarmonyArenaScore/PvPArenaScore + forEachPlayer lambdas)** DONE. Green: IStatFunction/InstanceBonusAttr/InstancePenaltyAttr/StatEnum/Func/StatAddFunction/InstanceScoreType/ScheduledTask/ThreadPoolManager. RED-tolerated: DataManager.INSTANCE_BUFF_DATA, Player.GetGameStats/GetWorldMapInstance/GetEffectController, WorldMapInstance.GetInstanceHandler, SM_INSTANCE_SCORE/SM_ABNORMAL_STATE/HarmonyScoreWriter/ArenaScoreWriter/PvPArenaScore/HarmonyArenaScore/Effect/PacketSendUtility.
- **itemgroups ItemRaceEntry (111L; : IChance[green]; @XmlSeeAlso(IdLevelReward)→[XmlInclude]; id/race=PC_ALL; afterUnmarshal→AfterUnmarshal(parent) StaticDataListener-fallback→DataManager.ITEM_DATA[ResultedItem idiom], IllegalArgumentException→ArgumentException; matches(Race,QuestTemplate) final + protected virtual MatchesQuest/MatchesLevel + private MatchesRace; GetCount virtual=1L; GetChance=100f) + rewards IdLevelReward (: ItemRaceEntry; level; override MatchesLevel level==bonusItemLevel) + rewards FoodItem (: IdLevelReward; override GetCount Rnd.NextBoolean?5:10) + MedicineItem (override GetCount Rnd.Get(1,3))** DONE — **unblocked the long-deferred FoodItem/MedicineItem reward cone** (IChance was the key; ItemRaceEntry portable with red-tolerated QuestTemplate.GetBonus().GetLevel() + DataManager.ITEM_DATA/ItemData/StaticData). Now unblocks itemgroups BonusItemGroup→EventGroup/MedalGroup/FeedItemGroup. Green: IChance/Race/ItemTemplate/Rnd.
- **itemgroups BonusItemGroup (56L abstract; : IChance; @XmlSeeAlso(10 subclasses)→[XmlInclude] each (EventGroup ported, other 9 RED-tolerated: CraftItemGroup/CraftRecipeGroup/ManastoneGroup/FoodGroup/MedicineGroup/OreGroup/GatherGroup/EnchantGroup/BossGroup); bonusType[BonusType green]/chance; **abstract getItems List<? extends ItemRaceEntry>→IReadOnlyList<ItemRaceEntry>**) + rewards FullRewardItem (: IdLevelReward; count long/chance float; override GetCount/GetChance — **made ItemRaceEntry.GetChance virtual**) + itemgroups EventGroup + MedalGroup (: BonusItemGroup; override GetItems List<FullRewardItem> null→new, returns IReadOnlyList<ItemRaceEntry>) + FeedItemGroup (abstract standalone; ItemGroupIndex[green]=NONE + items List<ItemRaceEntry> lazy)** DONE. itemgroups/rewards bonus cone substantially up (BonusItemGroup base + EventGroup/MedalGroup/FullRewardItem/FeedItemGroup); remaining 9 BonusItemGroup subclasses red in [XmlInclude] until ported.
- **rewards CraftReward (abstract : ItemRaceEntry; @XmlSeeAlso(CraftRecipe,CraftItem)→[XmlInclude]; skill; override MatchesQuest→combineSkill==skill) + CraftItem (: CraftReward; min/maxLevel; override GetCount Rnd.Get(3,5) + MatchesQuest combineSkillPoint range) + CraftRecipe (: CraftReward; level; override MatchesQuest + private GetMaxLevel Math.Min(level+40,level/100*100+99)) + itemgroups 9 BonusItemGroup subclasses (CraftItemGroup→CraftItem/CraftRecipeGroup→CraftRecipe/ManastoneGroup→ItemRaceEntry/FoodGroup→FoodItem/MedicineGroup→MedicineItem/OreGroup/GatherGroup/EnchantGroup→IdLevelReward/BossGroup; each override GetItems List<X> null→new→IReadOnlyList<ItemRaceEntry>)** DONE — **itemgroups+rewards bonus cone COMPLETE** (all 10 BonusItemGroup subclasses + Craft* + Food/Medicine/IdLevelReward/ItemRaceEntry; BonusItemGroup [XmlInclude] reds all cleared). RED-tolerated: QuestTemplate.GetCombineSkill/GetCombineSkillPoint.
- **house PlayerScript (Java record→C# positional record (Id,byte[] CompressedBytes,UncompressedSize); static LUA_SANDBOX_FIX init in static ctor; **Java text block→C# raw string literal """..."""** closing at col 0 (verbatim content incl tabs/flush-left lua lines); getBytes(UTF_16LE)→Encoding.Unicode.GetBytes; hasData())** DONE. RED-tolerated: CompressUtil.Compress (Commons.Utils). 
- **TEAM-EVENT WILDCARD CONE — deferred design decision (documented):** AbstractTeamPlayerEvent<T extends TemporaryPlayerTeam<?>> / PlayerEnteredEvent<T extends TemporaryPlayerTeam<? extends TeamMember<Player>>> — C# TemporaryPlayerTeam<TM>:GeneralTeam<Player,TM> has NO non-generic base, so the Java unbounded wildcard has no clean single-type-param C# mapping. Options: (a) two-param `<T,TM where T:TemporaryPlayerTeam<TM>>` (arity divergence across ~15 events), or (b) insert non-generic TemporaryPlayerTeam base-shim (invasive to green GeneralTeam/TemporaryPlayerTeam). Needs a deliberate decision — NOT a 60s leaf. ~15 team events (Abstract/PlayerEntered/ChangeLeader/mentoring/connected/update/invite/join/kinah events) blocked on this + RequestResponseHandler (PlayerGroupInvite).
- **BROADENED beyond model/ (model leaves now cone-gated).** world/zone SiegeZoneInstance (: ZoneInstance[green]; ctor(mapId,ZoneInfo[green]):base — **clears SiegeLocation/FortressLocation red dep**) + services/player PlayerMailboxState (byte consts→sbyte const CLOSED/REGULAR/EXPRESS — clears PostboxObject red dep) + utils TimeUtil (static IsExpired; System.currentTimeMillis→DateTimeOffset) + services/ban BanAction (enum-with-id ordinal→enum+ext GetId()=(int)t) DONE — clean leaves clearing 2 prior red deps. Note Services/Player dir created. Many more clean leaves available in skillengine/effect, world/zone, utils, ai, services for upcoming ticks.
- **world/zone InvasionZoneInstance (: ZoneInstance; ctor like SiegeZoneInstance) + world/zone/handler GeneralZoneHandler (: IZoneHandler; empty OnEnter/OnLeaveZone) + ZoneNameAnnotation/ai AIName/instance/handlers InstanceID (Java @interface RUNTIME/TYPE → C# sealed class : Attribute, [AttributeUsage(AttributeTargets.Class)]; required element value()→ctor param+Value prop; questId() default 0→QuestId{get;set;}=0)** DONE. **IDIOM: Java annotation @interface → C# Attribute class (named WITHOUT Attribute-suffix to match Java type name 1:1; usage [ZoneNameAnnotation(...)]); @Retention(RUNTIME)+@Target(TYPE)→[AttributeUsage(AttributeTargets.Class)]; elements→ctor params (required) / get;set props (defaulted).** Note: created top-level Instance/Handlers dir (distinct from Model.Instance). ZoneInfo/ZoneInstance/IZoneHandler/Creature green.
- **custom/instance/neuralnetwork Sigmoid (static Output/Derivative; Math.exp((float)-x)→Math.Exp((float)-x)) + questEngine/handlers HandlerResult (enum UNKNOWN/SUCCESS/FAILED + static fromBoolean(Boolean)→FromBoolean(bool?) extension)** DONE — clean leaves. **NAMESPACE-CASING TRAP: questengine→C# `Aion.GameServer.Questengine` (lowercase 'e'), UNLIKE SkillEngine (capital E). Match each pkg's existing C# casing.** DEFERRED: cron RunnableRunner subclasses (ThreadPoolManagerRunnableRunner/CurrentThreadRunnableRunner) — RunnableRunner base MISSING + ThreadPoolManager has no execute()/executeLongRunning (HANDOFF: execute→Schedule(lambda,TimeSpan.Zero)); skillengine/effect empty subclasses (DeathBlowEffect/AbsoluteSlow/Snare/SpellAttackInstant) blocked on DamageEffect/BufEffect bases (deep effect cone).
- **ai AIRequest (abstract; abstract AcceptRequest(Creature,Player,int) + virtual DenyRequest empty; Creature/Player green — clean) + skillengine/properties TargetSpeciesProperty (static Set(Properties, Properties.ValidationResult); switch TargetSpecies NPC/PC; **Java List.removeIf(pred)→C# List.RemoveAll(pred)**; instanceof→is; Npc/Player green; RED-tolerated: Properties/Properties.ValidationResult/TargetSpecies — skillengine.properties cone unported)** DONE. NOTE: SkillEngine namespace=capital E (Aion.GameServer.SkillEngine.Properties). skillengine.properties cone (Properties/ValidationResult/TargetSpecies + ~30 *Property classes) is a deeper subsystem; TargetSpeciesProperty faithful-but-red on Properties base.
- **custom/instance CustomInstanceRank (playerId readonly + rank/maxRank/dps/lastEntry get/set) + services/cron CronServiceException + services/siege SiegeException (both RuntimeException→Exception; 4-ctor: ()/(string)/(string,Exception):base(msg,cause)/(Exception cause):base(null,cause) per BaseException idiom; serialVersionUID dropped)** DONE — clean leaves. **IDIOM REINFORCED: Java RuntimeException subclass→C# : Exception, ctor(Throwable cause)→base(null,cause); drop serialVersionUID.** Created Services/Cron + Services/Siege dirs.
- **services/transfers TransferablePlayer (public fields playerId/accountId/targetAccountId/player[Player green]/targetServerId byte→sbyte/taskId + ctor) + services/player SecurityTokenService (private ctor; static GenerateToken(Account) — **SecureRandom→RandomNumberGenerator.Create().GetBytes; Base64.getEncoder().encodeToString→Convert.ToBase64String**; Account red-tolerated SetSecurityToken)** DONE — clean leaves. Created Services/Transfers dir. **IDIOM: java.security.SecureRandom→System.Security.Cryptography.RandomNumberGenerator; java.util.Base64→Convert.ToBase64String.** DEFERRED CommandsConfig (config @Properties/@Property annotation infra + Map<String,Byte>/File[] — config-loader cone).
- **world/exceptions AlreadySpawnedException (: Exception; ctor(VisibleObject):base(CreateMessage); getClass().getSimpleName()→GetType().Name; instanceof Player→is Player) + DuplicateAionObjectException (: Exception; ctor(AionObject,AionObject):base(CreateMessage); ((Player)obj).GetPosition())** DONE — clean (VisibleObject/AionObject/Player green). Java var `object`→`obj` (keyword). Created World/Exceptions dir.
- **questEngine/task/checker DestinationChecker (abstract; protected readonly Creature follower; package-private ctor→protected; abstract Check) + TargetDestinationChecker (: DestinationChecker; PositionUtil.IsInRange(target,follower,20) red-tolerated) + ZoneChecker (: DestinationChecker; follower.IsInsideZone(ZoneName[green]))** DONE — checker cone complete. Creature/ZoneName green; PositionUtil/Creature.IsInsideZone red-tolerated. Created Questengine/Task/Checker dir (lowercase-e Questengine).
- **configs/main ThreadConfig (BASE/SCHEDULED_THREAD_POOL_SIZE/MAXIMUM_RUNTIME_..._WITHOUT_WARNING long/USE_PRIORITIES) + InstanceConfig (cooldown_rate/EXCLUDED_MAPS Set<Integer>→ISet<int>=new HashSet/destroy delays/duel_enable/HANDLER_DIRECTORY File→string) + FallDamageConfig (percentage float/min-max distance/midair)** DONE — static-class config holders, @Property defaults baked as field initializers (established AIConfig idiom). ShutdownConfig deferred (RESTART_SCHEDULE CronExpression — Quartz pillar). CommandsConfig still deferred (@Properties Map keyPattern + File[] config-loader infra).
- **configs/main CleaningConfig (enable/min_account_inactivity=365/max_level=25) + PeriodicSaveConfig (player general/items=900, legion items=1200, pets=10) + DropConfig (MIN_ANNOUNCE_QUALITY no-default ItemQuality→ItemQuality? nullable; DISABLE_RANGE_CHECK_MAPS Set<Integer>→ISet<int> null no-default)** DONE — config holders. **IDIOM: config field with NO @Property defaultValue → C# field left default/null (no initializer); enum-typed no-default→T? nullable.** ItemQuality green.
- **configs/main PricesConfig (default prices/modifier/taxes=100, vendor buymod=100/sellmod=20) + InGameShopConfig (enable/gift=false, allow.gift=true)** DONE — clean config holders. DEFERRED RankingConfig (CronExpression[Quartz pillar] + AbyssRankEnum + @Properties Map<AbyssRankEnum,Integer> config-loader Maps).
- **configs/main PetFeedUnusualStorageArtifactCaptureConfig (enabled/output_dir/max_pending_contexts=4/max_queued=32/allowed_scenario; String→string)** DONE. DEFERRED PffConfig (network; @Properties Map<Integer,Integer> config-loader + getAllowedMillisBetweenPackets(AionClientPacket) red). **CONFIG-LOADER @Properties (keyPattern→Map) infra still unported — blocks CommandsConfig/RankingConfig/PffConfig and any Map-valued config.**
- **configs/main EventsConfig (DISABLED_EVENTS Set<String>→ISet<string> null; arcade enable/resume_token=3; worldraid enable=true/spawnmsg=true; headhunting enable/maps Set<Integer>→ISet<int>=new HashSet/consolation=50; advent_calendar enable)** DONE — clean. DEFERRED AutoGroupConfig (CronExpression[] DREDGION/KAMAR/etc TIMES — Quartz pillar). Remaining unported main configs: ShutdownConfig/RankingConfig/AutoGroupConfig (all CronExpression-blocked), PlayerTransferConfig/HousingConfig (larger, check).
- **configs/main PlayerTransferConfig (18 fields: MAX_KINAH long/BIND_ELYOS|ASMO string/13 allow-* bools/BLOCK_SAMENAME/REUSE_HOURS/REMOVE_SKILL_LIST="*")** DONE — clean config. DEFERRED HousingConfig (CronExpression HOUSE_AUCTION_END/MAINTENANCE/AUTO_FILL_TIME + int[] REGISTER_DAYS + Map<HouseType,Integer> AUTO_FILL_LIMITS — Quartz pillar + Map). configs/main nearly exhausted: only CronExpression/Map-blocked configs remain (Shutdown/Ranking/AutoGroup/Housing).
- **configs/ingameshop InGameShopProperty (@XmlRoot("in_game_shop"); category list[IGCategory green] lazy; size/clear; static Load()→JAXBUtil.Deserialize red) + configs/schedule RiftSchedule (@XmlRoot; rift list of nested Rift class[id/open→List<OpenRift> green]; getRiftsList/setRiftsList; static Load()→JAXBUtil red)** DONE — JAXB config classes. IGCategory/OpenRift green; **JAXBUtil.Deserialize RED-tolerated (deferred JAXB→XmlSerializer bridge pillar)**. Created Configs/Ingameshop + Configs/Schedule dirs.
- **configs/schedule SiegeSchedules (@XmlRoot("siege_schedule"); fortress/agent_fight lists; nested abstract SiegeSchedule[id + siegeTime List<String>→List<string>] + nested Fortress/AgentFight; static Load()→JAXBUtil red)** DONE. **IDIOM: Java `private` nested base of `public` nested subclasses → C# must widen base to `public` (CS0060: base class less accessible than derived). Documented.** No external deps; JAXBUtil red-tolerated.
- **world/knownlist CreatureAwareKnownList (: KnownList[green]; ctor(VisibleObject):base; `protected sealed override bool IsAwareOf` [Java final→sealed]; base.IsAwareOf && is Creature) + custom/instance/neuralnetwork DataSet (double[] values/targets POJO) + services/mail SiegeResult (enum-with-ordinal-id 0-5→enum+ext GetId()=(int)r)** DONE — clean leaves. KnownList.IsAwareOf confirmed protected virtual. Created Services/Mail dir.
- **world/knownlist NpcKnownList (: CreatureAwareKnownList[green]; override Update → Owner.GetPosition().IsMapRegionActive()?base.Update():Clear(ObjectDeleteAnimation.FADE_OUT); **made KnownList.Update virtual** [Java overridable])** DONE. ObjectDeleteAnimation green; IsMapRegionActive red-tolerated. DEFERRED: services/cron CronExpressions (CronExpression Quartz pillar + computeIfAbsent + ParseException), ai/handler FreezeEventHandler (AbstractAI<? extends Creature> wildcard + AISubState red — ai cone).
- **world/knownlist FlagKnownList (: PlayerAwareKnownList[green]; **Java pre-super() validation [Java 22 feature] → static Validate(owner) called in base(...)**; override Update synchronized→lock(this); **knownObjects.values().removeIf(pred)→foreach KnownObjects.ToArray() + TryRemove**; getVisibleDistance→float.MaxValue)** DONE — knownlist cone complete (Player/Creature/Npc/Flag aware). **IDIOM: Java pre-super statements (validation/throw before super()) → C# static factory-validate method passed to base(...).** Owner/KnownObjects.Get green; WorldMapInstance/GetWorldMapInstance red-tolerated.
- **services/conquerorAndProtectorSystem CPBuff (: IStatOwner; applyEffect(Player,CPType,int rank) — DataManager.CONQUEROR_AND_PROTECTOR_DATA.GetRank red-tolerated, cpRank.GetStatModifiers().Count!=0, player.GetGameStats().AddEffect red-tolerated; CPType/CPRank green) + services/mail AbyssSiegeLevel (enum id 0-4=ordinal→GetId+static GetLevelById, IllegalArgumentException→ArgumentException) + AuctionResult (enum id 0-7=ordinal→GetId+static GetResultFromId null-on-miss→AuctionResult?)** DONE — clean leaves. Created Services/ConquerorAndProtectorSystem dir (Java pkg conquerorAndProtectorSystem→PascalCase).
- **Utils AtomicLong (java.util.concurrent.atomic.AtomicLong → Interlocked-backed; Get[Interlocked.Read]/Set/GetAndSet/CompareAndSet/Increment/Decrement/AddAndGet/GetAndAdd/LongValue/IntValue; matches AtomicBoolean/AtomicInteger in Aion.GameServer.Utils)** DONE — clears recurring dep (needed by SiegeRaceCounter + others). NOTE: clean-leaf supply across the tree is largely exhausted; most remaining files couple to unported core types (Trap/SiegeNpc/SiegeRaceCounter/AbstractAI/Effect/Quartz Job/config-loader PropertyTransformer) — convergence-pillar territory. RunnableRunner(:Job Quartz)/CronExpressionTransformer(:PropertyTransformer) deferred to cron/config pillars.
- **services/siege SiegeRaceCounter (171L; : IComparable<SiegeRaceCounter>; AtomicLong[green]+ConcurrentDictionary counters; addToCounter<K> double-checked lock(this); getOrderedCounterMap<K> sort-desc-by-value LinkedHashMap→Dictionary, GenericValidator.isBlankOrNull→null/Count==0; CompareTo desc by totalDamage; getWinnerLegionId team/league aggregation; RED-tolerated World.GetPlayer/Player team APIs/Legion) + SiegeCounter (EnumMap→Dictionary 3 races; addDamage instanceof Player/SiegeNpc; getWinnerRaceCounter Where+Sort[IComparable]+first-or-fallback; SiegeNpc/SiegeRaceExtensions.GetByRace red-tolerated)** DONE — siege-counter cone up. **IDIOM: Java stream .filter().sorted().findFirst().orElseGet(fb) → List.Where(..).ToList(); list.Sort(); count>0?[0]:fb. Apache GenericValidator.isBlankOrNull(Map)→null||Count==0.**
- **services/siege SiegeStartRunnable (Java Runnable→plain class+Run(); SiegeService.GetInstance().CheckSiegeStart red) + services/rift RiftOpenRunnable (worldId/guards; Run() PrepareRiftOpening + ThreadPoolManager.GetInstance().Schedule(ct-lambda CloseAutoCloseableRifts, TimeSpan.FromMilliseconds(GetDuration()*3540*1000)) + RiftInformer.SendRiftsInfo; RiftService/RiftInformer red-tolerated, ThreadPoolManager green)** DONE. **IDIOM: standalone `implements Runnable` → plain C# class with public void Run() (no C# Runnable iface; consumed as method-group); schedule(Runnable,ms)→Schedule(ct=>{...;return ValueTask.CompletedTask;}, TimeSpan.FromMilliseconds(ms)).** Created Services/Rift dir.
- **services/worldraid WorldRaidRunnable (Java Runnable→plain class+Run(); static ILogger log=NullLogger; stream().filter().collect(toList())→.Where(...).ToList(); list.remove(Rnd.nextInt(size))→RemoveAt(Rnd.NextInt(Count)); Rnd.Get/NextInt green; WorldRaidSchedules.WorldRaidSchedule + WorldRaidService red-tolerated)** DONE. Created Services/Worldraid dir. DEFERRED ArtifactAssault (extends Assault<ArtifactSiege> — Assault base + ArtifactSiege unported siege-assault cone; SpawnEngine.newSiegeSpawn red).
- **world/zone/handler AdvancedZoneHandler (interface : IZoneHandler; OnDie(Creature,Creature,ZoneInstance)) + custom/instance CustomInstanceRankedPlayer (: CustomInstanceRank[green]; name/playerClass[PlayerClass green]) + questEngine/handlers/models/xmlQuest/operations QuestOperation (abstract; @XmlSeeAlso(6 ops)→[XmlInclude] red-tolerated until ported; abstract DoOperate(QuestEnv) red-tolerated)** DONE. Created Questengine/Handlers/Models/XmlQuest/Operations dir. **xmlQuest operations cone base up — 6 op subclasses (TakeItem/StartQuest/SetQuestVar/NpcDialog/GiveItem/SetQuestStatus) next, all : QuestOperation w/ DoOperate(QuestEnv) bodies (QuestEnv/QuestService red-tolerated).**
- **xmlQuest operations cone COMPLETE: TakeItemOperation (DecreaseByItemId) + StartQuestOperation (TODO stub) + SetQuestVarOperation (qs.GetQuestVars().SetVarById + SM_QUEST_ACTION) + GiveItemOperation (ItemService.AddItem) + SetQuestStatusOperation (SetStatus + COMPLETE→UpdateNearbyQuests) + NpcDialogOperation (quest_id Integer→int?; SM_DIALOG_WINDOW 2/3-arg)** DONE — all : QuestOperation override DoOperate(QuestEnv). RED-tolerated: QuestEnv/QuestState/QuestStatus, ItemService.AddItem, PacketSendUtility, SM_QUEST_ACTION/ActionType/SM_DIALOG_WINDOW, Player.GetInventory/GetQuestStateList/GetController. QuestOperation base + all 6 [XmlInclude] subclasses now present.
- **questEngine/model ConditionOperation (@XmlEnum 8 ops→enum+ext Value/FromValue) + ConditionUnionType (AND/OR→enum+ext) + xmlQuest/conditions QuestCondition (abstract; op ConditionOperation; GetOp; abstract DoCheck(QuestEnv); @XmlSeeAlso(5)→[XmlInclude] red until ported) + QuestConditions (@XmlElements 5 element→subtype→stacked [XmlElement(name,typeof)]; checkConditionOfSet AND/OR short-circuit)** DONE — xmlQuest conditions cone base up. NEXT: 5 condition subclasses (NpcId/DialogId/PcInventory/QuestVar/QuestStatus) all : QuestCondition override DoCheck w/ switch on GetOp() EQUAL/GREATER/etc. QuestEnv green.
- **xmlQuest conditions cone COMPLETE: DialogIdCondition (EQUAL/NOT_EQUAL env.GetDialogActionId) + NpcIdCondition (is Npc→GetNpcId, 6 ops) + PcInventoryCondition (item_id/count long; GetItemCountByItemId) + QuestVarCondition (var_id/value; qs.GetQuestVars().GetVarById) + QuestStatusCondition (value QuestStatus/quest_id Integer→int?; qs.GetStatus().Value())** DONE — all : QuestCondition override DoCheck switch on GetOp()[ConditionOperation]. QuestEnv/ConditionOperation green; RED-tolerated Player.GetInventory/GetQuestStateList, QuestState/QuestStatus.Value(). QuestCondition base + all 5 [XmlInclude] subclasses present. xmlQuest operations+conditions cones both complete.
- **xmlQuest operations: KillOperation (is Npc→GetController().Die(player)) + QuestOperations container (@XmlElements 8 op subtypes→stacked [XmlElement]; override Boolean→bool? IsOverride null→true; Operate(env) foreach DoOperate→return IsOverride; ActionItemUseOperation red until ported) + CollectItemQuestOperation (_true/_false QuestOperations; removeItems Boolean→bool?; QuestService.CollectItemCheck red-tolerated; ternary `removeItems==null?true:false` kept 1:1)** DONE. Remaining op: ActionItemUseOperation. xmlQuest cone nearly complete (operations base+8 subclasses+container, conditions base+5+container).
- **xmlQuest operations cone FULLY COMPLETE: ActionItemUseOperation (finish QuestOperations[green]; is Npc; SM_USE_OBJECT/SM_EMOTION(EmotionType.START_QUESTLOOT) red-tolerated; **anonymous Runnable→ThreadPoolManager.Schedule(ct=>{...finish.Operate(env);return ValueTask.CompletedTask;}, TimeSpan.FromMilliseconds(defaultUseTime))**)** DONE. All QuestOperation subclasses (Take/Start/SetQuestVar/GiveItem/SetQuestStatus/NpcDialog/Kill/CollectItem/ActionItemUse) + QuestOperations container present. xmlQuest operations+conditions subsystem complete. Remaining quest models: events (OnKill/OnTalk/QuestEvent) + data holders (NpcInfos/FountainRewardsData/etc — likely : AbstractQuestHandler big base).
- **xmlQuest events: QuestEvent (abstract; conditions QuestConditions[green]/operations QuestOperations[green]; ids @XmlAttribute List<Integer>→IdsRaw space-sep + getIds lazy; @XmlSeeAlso(OnKill/OnTalk)→[XmlInclude]; virtual Operate→false) + OnKillEvent (monster List<Monster> + complite QuestOperations; var-counter logic + complete check; Monster red-tolerated) + OnTalkEvent (var List<QuestVar>; conditions.CheckConditionOfSet + questVar.Operate; QuestVar red-tolerated)** DONE. RED-tolerated: Monster/QuestVar (small quest helper classes — next), QuestState.GetQuestVarById/SetQuestVarById, SM_QUEST_ACTION. xmlQuest cone (operations+conditions+events) nearly done — Monster/QuestVar helpers remain.
- **questEngine/handlers/models Monster (var/start_var/end_var/npc_ids→Raw space-sep/npc_seq/step/spawner_object_id; getters/setters + AddNpcIds dedup) + xmlQuest QuestDialog (@XmlType("DialogAction"); conditions/operations green; Operate(env,qs) dialogActionId+CheckConditionOfSet+Operate) + xmlQuest QuestNpc (dialog list; is Npc→GetNpcId; distinct from model/templates/quest/QuestNpc) + xmlQuest QuestVar (npc list; qs.GetQuestVars().GetQuestVars() red-tolerated)** DONE — clears OnKillEvent(Monster)/OnTalkEvent(QuestVar) reds. **xmlQuest cone (operations+conditions+events+Monster/QuestVar/QuestNpc/QuestDialog) essentially COMPLETE.** QuestEnv/QuestConditions/QuestOperations/Npc green.
- **questEngine/handlers/models XMLQuest (abstract base @XmlType("QuestScriptData"); @XmlSeeAlso(16 data-holders)→[XmlInclude]; id/movie/mission; abstract Register(QuestEngine)+GetAlternativeNpcs(int)→ISet<int>)** DONE — base of the quest-script-data cone. 16 [XmlInclude] subclasses (Crafting/Fountain/ItemCollecting/ItemOrders/KillInWorld/KillInZone/KillSpawned/MentorMonsterHunt/MonsterHunt/Relic/ReportOnLevelUp/ReportTo/ReportToMany/SkillUse/WorkOrders/XmlQuestData) RED until ported — most extend XMLQuest with register()/getAlternativeNpcs() bodies referencing QuestEngine (deferred questEngine cone). QuestEngine red-tolerated.
- **questEngine/handlers/models XMLQuest subclasses (4): ReportToData (start/end_npc_ids→Raw + start_dialog_id; Register→ReportTo) + ReportOnLevelUpData (end_npc_ids; ReportOnLevelUp) + ReportToManyData (npc_infos List<NpcInfos>[red]/start_item_id/start_npc_ids/start_dialog_id; ReportToMany; getAlternativeNpcs iterates npcInfo.GetNpcIds()) + KillInWorldData (10 attrs incl worlds→Raw; KillInWorld)** DONE — all : XMLQuest override Register(QuestEngine)+GetAlternativeNpcs(int). **IDIOM REAFFIRMED: @XmlAttribute List<Integer>→private List<int>+XxxRaw [XmlAttribute] string (null-preserving for `!=null` checks); stream .filter(x!=npcId).collect(toSet())→new HashSet<int>(list.Where(...)).** RED-tolerated: template classes ReportTo/ReportOnLevelUp/ReportToMany/KillInWorld + NpcInfos + QuestEngine.AddQuestHandler (questEngine cone). 12 XMLQuest subclasses remain.
- **questEngine/handlers/models MonsterHunt family (4): MonsterHuntData (base @XmlSeeAlso(KillSpawned/Mentor)→[XmlInclude]; start/end/aggro_npc_ids→Raw + 7 attrs; Register builds List<Monster> from DataManager.QUEST_DATA.GetQuestById(id).GetQuestKill() w/ QuestKill→Monster mapping, Collections.emptyList→new List; MonsterHunt handler) + KillInZoneData (zones List<String>→Raw; KillInZone) + KillSpawnedData : MonsterHuntData (monster List<Monster> @XmlElement; KillSpawned; getAlternativeNpcs iterates monster then base) + MentorMonsterHuntData : MonsterHuntData (min/max_mente_level=1/99; MentorMonsterHunt)** DONE — all override Register/GetAlternativeNpcs. Monster/QuestKill accessors verified green (SetEndVar/SetVar/SetStep/AddNpcIds; GetKillCount/GetVar/GetQuestStep/GetSequenceNumber/GetNpcIds). RED-tolerated: QuestTemplate + DataManager.QUEST_DATA.GetQuestById (QuestTemplate NOT yet ported — recurring quest-cone dep), handler templates MonsterHunt/KillInZone/KillSpawned/MentorMonsterHunt, QuestEngine.AddQuestHandler. **NOTE: heredoc with `string.Join` + char-literal `' '` choked bash (unmatched-quote); switched to Write tool — prefer Write for files containing char literals.** 8 XMLQuest subclasses remain (Crafting/Fountain/ItemCollecting/ItemOrders/Relic/SkillUse/WorkOrders/XmlQuestData).
- **questEngine/handlers/models XMLQuest subclasses (4): CraftingRewardsData (start/end_npc_id/skill_id/level_reward; CraftingRewards; getAlternativeNpcs→null) + FountainRewardsData (start_npc_ids→Raw; FountainRewards) + ItemCollectingData (start/end_npc_ids→Raw + 6 dialog/zone attrs; ItemCollecting) + ItemOrdersData (talk_npc_id1/2/end_npc_id; ItemOrders; →null)** DONE — all : XMLQuest override Register/GetAlternativeNpcs. RED-tolerated: handler templates CraftingRewards/FountainRewards/ItemCollecting/ItemOrders + QuestEngine.AddQuestHandler. 4 XMLQuest subclasses remain (Relic/SkillUse/WorkOrders/XmlQuestData).
- **questEngine/handlers/models XMLQuest subclasses FINAL 4 + QuestSkillData: QuestSkillData (clean leaf; ids List<Integer>→Raw + start/end_var/var_num; getters) + RelicRewardsData (start_npc_ids→Raw; RelicRewards) + SkillUseData (skills List<QuestSkillData>[green now]; start/end_npc_ids; SkillUse) + WorkOrdersData (give_component List<QuestItems>[green]/start_npc_ids/recipe_id; WorkOrders) + XmlQuestData (@XmlType("XmlQuest")!; onTalk/onKillEvents List<On*Event>[green]; getAlternativeNpcs walks events via OnTalkEvent.GetIds/OnKillEvent.GetMonsters→Monster.GetNpcIds — all verified green)** DONE — **xmlQuest data-holder cone (XMLQuest base + ALL 16 subclasses) COMPLETE.** RED-tolerated: handler templates RelicRewards/SkillUse/WorkOrders/XmlQuest + QuestEngine.AddQuestHandler. NOTE XmlQuestData→template XmlQuest fully-qualified to disambiguate from xmlQuest namespace segment. **questEngine/handlers/models package fully ported (XMLQuest + 16 subclasses + Monster/QuestSkillData + xmlQuest events/operations/conditions cones).** Next frontier: questEngine/handlers/template/* (the red handler classes: ReportTo/MonsterHunt/XmlQuest/etc — these extend AbstractQuestHandler, larger) OR other faithful leaves elsewhere.
- **questEngine/model QuestActionType (clean enum ITEM_USE/ACTION_ITEM_USE) + questEngine/handlers/template AbstractTemplateQuestHandler (10L; abstract : AbstractQuestHandler[RED — 1290L god-class pillar, NOT ported]; ctor(int questId):base)** DONE. **questEngine/model package now fully ported** (QuestActionType/QuestEnv/QuestState/QuestStatus/QuestVars/ConditionOperation/ConditionUnionType). AbstractTemplateQuestHandler is the bridge base for the 17 template handlers (FountainRewards/MonsterHunt/ReportTo/etc) — those remain RED-blocked on AbstractQuestHandler (1290L, references QuestEngine/QuestService/AbstractAI cones — deferred convergence pillar). **NEXT FRONTIER NOTE: the quest-handler engine (AbstractQuestHandler 1290L god-class + 17 template handlers + QuestEngine) is now the gating pillar for the entire quest subsystem — large coupled cone, deferred. Resume by hunting faithful leaves in other packages (network/services/model) or tackling AbstractQuestHandler as a dedicated multi-tick effort.**
- **dataholders XML-container leaves (4): BaseData (@XmlRoot base_locations; List<BaseTemplate>[green]; Size/GetAllBaseTemplates) + KillBountyData (kill_bounties; List<KillBountyTemplate>[green]) + LegionDominionData (legion_dominion_template; List<LegionDominionLocationTemplate>[green]) + AssemblyItemsData (assembly_items; List<AssemblyItem>[green]; GetAssemblyItem(itemId) linear)** DONE — fully GREEN (all element templates already ported; verified AssemblyItem.GetId). **PIVOT: quest cone gated on AbstractQuestHandler god-class pillar → shifted to dataholders/ frontier (88 unported, mostly clean @XmlRoot+List+lookup containers whose element templates are largely already ported — rich clean-leaf supply). Approach: pick small dataholder, confirm element template ported, port 1:1.** Frontier scan recorded: 1198 unported java classes; biggest cones network/aion serverpackets(243)/clientpackets(190) [packet pillar], skillengine/effect(151) [effect pillar], dataholders(88) [CLEAN LEAVES], dao(57).
- **dataholders XML-container leaves (4): ConquerorAndProtectorData (@XmlRoot conqueror_protector_ranks; List<CPRank>; GetRank(CPType,rank) uses template.GetType_()[Java getType→GetType_ idiom]+GetRankNum) + ItemRestrictionCleanupData (item_restriction_cleanups; List<ItemCleanupTemplate>; Collections.emptyList→new List; stream anyMatch→.Any(t=>GetId&&(ResultAccountWH==0||ResultLegionWH==0))[sbyte]) + SkillAliasLocationData (alias_locations; afterUnmarshal→AfterUnmarshal(object); @XmlTransient Map→[XmlIgnore] Dictionary<string,SkillAliasLocation>; SkillEngine[capital E] namespace) + BindPointData (bind_points; afterUnmarshal map by GetNpcId; @XmlIgnore Dictionary)** DONE — fully GREEN (CPRank/CPType/ItemCleanupTemplate/SkillAliasLocation/BindPointTemplate all ported; accessors verified). **IDIOM REAFFIRMED: dataholder afterUnmarshal(Unmarshaller,Object parent)→public void AfterUnmarshal(object parent); @XmlTransient lookup Map→[XmlIgnore] Dictionary built in AfterUnmarshal, source list nulled.** ~80 dataholders remain.
- **dataholders XML-container leaves (4): TeleLocationData (@XmlRoot teleport_location; map by TelelocationTemplate.GetLocId) + FlyPathData (flypath_template; FlyPathEntry.GetId) + PanelSkillsData (polymorph_panels; SkillPanel.GetPanelId) + PetDopingData (dopings; PetDopingEntry.GetId)** DONE — fully GREEN, all afterUnmarshal-map idiom ([XmlIgnore] Dictionary built in AfterUnmarshal, source list nulled). Element templates + accessors all verified ported. ~76 dataholders remain.
- **dataholders XML-container leaves (4): CosmeticItemsData (@XmlRoot cosmetic_items; @XmlElement(type=X)→[XmlElement("cosmetic_item",typeof(CosmeticItemTemplate))]; map by GetCosmeticName — NOTE map field had NO @XmlTransient in Java, kept plain-private[XmlSerializer ignores private regardless]) + InstanceBuffData (instance_bonusattrs; InstanceBonusAttr.GetBuffId; ns Instance_bonusatrr) + MultiReturnItemData (multi_return_item; Map<int,List<ReturnLocList>> via MultiReturnItem.GetId/GetReturnLocList; returnLocList.Clear() preserved) + WindstreamData (windstreams; WindstreamTemplate.GetMapId)** DONE — fully GREEN. **IDIOM: @XmlElement(name,type=X.class)→[XmlElement(name, typeof(X))].** ~72 dataholders remain.
- **dataholders XML-container leaves (4): AssembledNpcsData (@XmlRoot assembled_npcs; [XmlElement(typeof)]; map by GetNr; map field no @XmlTransient→plain private) + MapWeatherData (weather; WeatherTable.GetMapId) + RideData (rides; RideInfo.GetNpcId) + SignetDataTemplates (signet_data_templates; EnumMap<SignetEnum>→Dictionary; SignetData/SignetDataTemplate/SignetEnum SkillEngine.Model; GetSignetData null-guard + GetSignetDataForSignetLevel[returns SignetData?])** DONE — fully GREEN. All deps/accessors verified ported. ~68 dataholders remain.
- **model/rift RiftLocation (62L plain class; ConcurrentHashMap→ConcurrentDictionary<int,SpawnTemplate>; delegates to RiftTemplate GetId/GetWorldId/HasSpawns/IsAutoCloseable; AddSpawned via VisibleObject.ObjectId+GetSpawn; ReplaceSpawned spawned.remove!=null→TryRemove; getSpawned→IDictionary) + dataholders (4): CubeExpandData (cube_expander; NESTED loop over StorageExpansionTemplate.GetNpcIds()[int[]] mapping each npcId) + PortalLocData (portal_locs; PortalLoc.GetLocId) + RiftData (rift_locations; LinkedHashMap→Dictionary<int,RiftLocation>; new RiftLocation(template); **riftTemplates NOT nulled — matches Java**) + TitleData (player_titles; TitleTemplate.GetTitleId)** DONE — fully GREEN (ported RiftLocation this tick to unblock RiftData; all deps verified). ~64 dataholders remain.
- **dataholders XML-container leaves (4): WarehouseExpandData (warehouse_expander; nested GetNpcIds() map — twin of CubeExpandData) + CuringObjectsData (curing_objects; copies list into @XmlIgnore List; curingObject NOT nulled matches Java) + GatherableData (gatherable_templates; **List.sort(null)→List.Sort() natural-order on Materials/ExMaterials.GetMaterial()**; map by GetTemplateId) + InstanceExitData (instance_exits; **computeIfAbsent→TryGetValue+init; getOrDefault(emptyList)→TryGetValue else new; stream mapToInt(size).sum→Values.Sum(l=>l.Count); getInstanceId()[Java Integer over int field]→.Value**)** DONE — fully GREEN. Deps verified (Curing/Gatherable/InstanceExit templates + Materials.GetMaterial). ~60 dataholders remain.
- **dataholders XML-container leaves (4): PetBuffsData (pet_buffs; LinkedHashMap→Dictionary; buffs.Clear()+null; PetBuff.GetId) + CustomDrop (custom_drop; **putIfAbsent!=null→!TryAdd + log.LogWarning**; NpcDrop.GetNpcId; static ILogger=NullLogger.Instance) + FlyRingData (fly_rings; no afterUnmarshal; lazy-null-init List; AddAll(ICollection)→AddRange) + HotspotData (hotspot_template; lazy-null List; GetHotspotTemplateById linear; HotspotTemplate.GetId)** DONE — fully GREEN. **IDIOM: Map.putIfAbsent(k,v)!=null → !dict.TryAdd(k,v) (TryAdd returns false when key exists).** ~56 dataholders remain.
- **dataholders XML-container leaves (4): RoadData (roads; twin of FlyRing — lazy-null List + AddAll→AddRange) + ShieldData (shields; twin) + ChestData (chest_templates; chestData.Clear()+map by GetNpcId; chests nulled but map cleared first for reload) + EventData (timed_events; [XmlType+XmlRoot]; afterUnmarshal date-validation **!startDate.isBefore(endDate)→!(start.Value<end.Value)** on DateTime? + ArgumentException; SetEvents calls AfterUnmarshal(null); **AddAllNpcIdsToSet stream map/filter/forEach→events.Select(GetSpawns).Where(!=null) foreach spawns.AddAllNpcIdsToSet**; SpawnsData in Dataholders ns)** DONE — fully GREEN. EventTemplate.GetStartDate/GetEndDate→DateTime?, GetSpawns→SpawnsData? verified. ~52 dataholders remain.
- **model/templates/ai AITemplate (XML data POJO @XmlType("Ai"); summons/bombs/npcId — DISTINCT from Aion.GameServer.Ai.AITemplate<T> AI-behavior base; NAME COLLISION resolved by namespace Model.Templates.Ai) + dataholders (4): AIData (ai_templates; uses the POJO AITemplate; map by GetNpcId) + SkillChargeData (skill_charge; ChargeSkillEntry.GetId + nested GetSkills().forEach→foreach ChargedSkill.GetId into HashSet; IsChargeSkill via SkillTemplate.GetSkillId) + UpgradeArcadeData (arcadelist; ArcadeLevels/ArcadeLevel/ArcadeRewards; delegates to levels) + HouseBuildingData (NO @XmlRootElement→plain class; put!=null→!TryAdd+ArgumentException on dup building ID)** DONE — fully GREEN. **TRAP: two AITemplate classes in Java (ai/ behavior vs model/templates/ai/ data POJO) — keep in distinct C# namespaces; AIData binds the data POJO.** ~51 dataholders remain.
- **dataholders (4) + ItemTemplate getters: HousingObjectData (housing_objects; **@XmlElements(11 element→PlaceableHouseObject subtype)→stacked [XmlElement(name,typeof)]** postbox/use_item/move_item/chair/picture/passive/npc/storage/jukebox/moviejukebox/emblem; map by GetTemplateId[inherited AbstractHouseObject]) + HousePartsData (house_parts; HousePart.GetId) + TemperingData (tempering_templates; **nested Dictionary<string,Dictionary<int,List<TemperingStat>>>**; GetTemplates uses ItemTemplate.GetTemperingName ?? GetItemGroup().ToString()) + WorldRaidData (world_raid_locations; putIfAbsent→TryAdd; .Clear()+null)** DONE — fully GREEN. **Added missing ItemTemplate.GetItemGroup()/GetTemperingName() getters (exist in Java; god-class had fields but not getters) — faithful, unblocked TemperingData.** Java enum getItemGroup().toString()→C# enum .ToString() (SCREAMING_SNAKE member names match). ~47 dataholders remain.
- **dataholders (3) + ItemTemplate.GetEnchantName: EnchantData (enchant_templates; twin of TemperingData — nested Dictionary<string,Dictionary<int,List<EnchantStat>>>; ItemTemplate.GetEnchantName ?? GetItemGroup().ToString()) + PetSkillData (pet_skill_templates; DOUBLE computeIfAbsent→TryGetValue+init for petSkillData[order→{pet→skill}] AND petSkillsMap[pet→List<skill>]; GetPetOrderSkill unbox) + ItemPurificationData (item_purifications; maps init in afterUnmarshal; nested possibleResultItems[base→{result→PurificationResult}])** DONE — fully GREEN. Added faithful **ItemTemplate.GetEnchantName()** getter. **DEFERRED VortexData → blocked on model/vortex/VortexLocation (200L : ZoneHandler cone class; RVController/DimensionalVortex<VortexLocation>/InvasionZoneInstance/Kisk/PacketSendUtility/ThreadPoolManager-kick-timers — substantial, needs dedicated tick like RiftLocation but heavier).** ~46 dataholders remain (incl deferred VortexData).
- **dataholders (4): HouseNpcsData (house_npcs; map address→List<HouseSpawn>; **Set<SpawnType> dedup → HashSet.Add()==false→throw ArgumentException**; HouseSpawn.GetType_(); stream sum) + StaticDoorData (staticdoor_templates; putIfAbsent→!TryAdd+throw; Collections.emptyList→new List on ICollection return; StaticDoorWorld.GetStaticDoors/GetStaticDoor) + WalkerVersionsData (walker_versions; nested RouteParent.GetRouteVersion→RouteVersion.GetId→Map<string,string>; null-guarded lookups; .Clear()+null) + TeleporterData (npc_teleporter; map by GetTeleportId; GetTeleporterTemplateByNpcId linear via ContainNpc)** DONE — fully GREEN. All deps/accessors verified. ~42 dataholders remain (incl deferred VortexData).
- **dataholders (4): AtreianPassportData (login_events; AtreianPassport.GetId; GetAll returns Dictionary) + PetData (pets; PetTemplate.GetTemplateId; **GetPetIds keySet→new HashSet<int>(petData.Keys) [copy not live-view; callers read-only]**) + NpcFactionsData (npc_factions; dual map factionsById[GetId]+factionsByNpcId[per GetNpcIds]; GetNpcFactionsData→Values) + PetFeedData (pet_feed; PetFlavour.GetId; GetPetFlavours values().toArray→Values.ToArray())** DONE — fully GREEN. AtreianPassport/PetTemplate/NpcFactionTemplate/PetFlavour verified ported. **IDIOM: Map.keySet() returned as Set → new HashSet<>(dict.Keys); values().toArray(new T[0])→Values.ToArray().** ~38 dataholders remain.
- **dataholders (4): NpcSkillData (npc_skill_templates; nested per-NpcId putIfAbsent→!TryAdd+warn; SetNpcSkillTemplates→Clear+AfterUnmarshal(null)) + DecomposableItemsData (decomposable_items; dual map; IsIsSelectable→selectable[itemGroups[0].GetItems] else info; GetSelectableItems returns new List copy) + RecipeData (recipe_templates; recipeData + autoLearnRecipes; GetAutolearnRecipes filters skillId/skillpoint/race[Race.PC_ALL]) + MaterialData (material_templates; map + skillIds HashSet via GetSkills().forEach→foreach MaterialSkill.GetId)** DONE — fully GREEN. All deps/accessors verified (ExtractedItemsCollection.GetItems inherited from ResultedItemsCollection). ~34 dataholders remain.
- **dataholders (4): WorldMapsData (world_maps; **Iterable<WorldMapTemplate>→IEnumerable + GetEnumerator/explicit IEnumerable.GetEnumerator; parallelStream().forEach→.AsParallel().ForAll; equalsIgnoreCase→string.Equals(...,OrdinalIgnoreCase)**; LinkedHashMap→Dictionary) + ItemSetData (item_sets; dual map sets[GetId]+setItems[per ItemPart.GetItemId]) + ChallengeData (challenge_tasks; tasksById; GetTaskByQuestId/GetQuestByQuestId nested ChallengeQuestTemplate loops; task NOT nulled) + HouseData (house_lands; nested land.GetAddresses dup-check put!=null→!TryAdd+throw; GetAddresses(worldId) stream filter→Where.ToList; GetStudioAddress Race.ELYOS?2001:3001; lands NOT nulled)** DONE — fully GREEN. **DEFERRED AutoGroupData → blocked on model/autogroup/AutoGroup (unported; autogroup pkg 7 unported — instance/team-coupled cone). IDIOM: Java Iterable→C# IEnumerable<T> w/ explicit non-generic GetEnumerator; parallelStream→AsParallel().ForAll.** ~30 dataholders remain.
- **dataholders (3): GoodsListData (goodslists; triple map list/in_list/purchase_list by GoodsList.GetId; size sums all 3) + TownSpawnsData (town_spawns_data; nested TownSpawnMap→TownSpawn→TownLevel→Spawn; **4-deep stream flatMap→nested foreach for AddAllNpcIdsToSet(ISet<int>)** + GetSpawnsCount/GetSpawns/GetWorldIdForTown) + GlobalNpcExclusionData (global_npc_exclusions; **@XmlList element Set<T>→Raw space-sep string property per set (5: int/string/NpcTemplateType/TribeClass/AbyssNpcType enums via Enum.Parse)**; getters Collections.emptySet→new HashSet on null; isEmpty flag)** DONE — fully GREEN. **IDIOM: @XmlList ELEMENT (not attribute) Set<T>→private HashSet<T> + XxxRaw [XmlElement] string (join/split); enum tokens via Enum.Parse (SCREAMING_SNAKE match).** **DEFERRED QuestsData → blocked on model/templates/QuestTemplate (NOT ported — large quest template; also QuestEngine/QuestService red). QuestTemplate is a recurring dep (MonsterHuntData/AIData-adjacent quest cone) — candidate for a dedicated port tick.** ~27 dataholders remain.
- **dataholders (1) + GuideTemplate nullable fix: GuideHtmlData (guides; ordinal-hash map; **.ordinal()→(int)enum [PlayerClass/Race are plain-ordinal enums]**; MakeHash bit-shift; GetTemplateByTitle stream flatMap.findFirst→SelectMany.FirstOrDefault; GetTemplatesFor 4-tier hash lookup+AddRange; toArray→ToArray)** DONE — fully GREEN. **FAITHFULNESS FIX: GuideTemplate.classType/race were non-nullable PlayerClass/Race in C# but Java fields are nullable (object enums, null when XML attr absent) → changed to PlayerClass?/Race? + getters return nullable (Java truth; null-check in GuideHtmlData.AddTemplate requires it).** **DEFERRED WalkerData (saveData JAXB Marshaller/JAXBContext/XmlUtil.getSchema — JAXB-marshal bridge, no established C# serialize bridge yet) + GlobalDropData (NpcTemplate unported — big template).** **DATAHOLDER VEIN THINNING: most remaining ~26 blocked on big templates (NpcTemplate/QuestTemplate/SkillData) or JAXB-marshal or nullable-enum divergences. Candidates for dedicated ticks: QuestTemplate, NpcTemplate, VortexLocation. Consider pivoting to a fresh clean-leaf vein (model/account, utils/collections, network/loginserver).**
- **PIVOT to utils/collections clean-leaf vein — SplitList family (4): ListPart<TType> (abstract : List<TType>; **RULE 8: Java ArrayList.add overridable → C# List<T>.Add non-virtual → declare `public new virtual bool Add` (Java boolean return); fits/setLast package-private→protected internal**) + SplitList<TType> (abstract : IEnumerable<ListPart<TType>>; PartitionList; singletonList/emptyList→new List; GetEnumerator+explicit IEnumerable.GetEnumerator) + FixedElementCountSplitList<TType> (nested part Fits→Count<max; IllegalArgumentException→ArgumentException; **nested class takes owner ref [C# nested has no implicit outer]**) + DynamicElementCountSplitList<TType> (Function<Type,Integer>→Func<TType,int>; override Add tracks currentLength via base.Add; IllegalStateException→InvalidOperationException)** DONE — fully GREEN, self-contained generics. **IDIOM: Java inner class implicit-outer-ref → C# nested class with explicit `owner` field passed via ctor (nested CAN access outer privates through the instance).** **DEFERRED DynamicServerPacketBodySplitList (AionServerPacket.MAX_USABLE_PACKET_BODY_SIZE — packet pillar).** Dataholder vein paused (~26 left, mostly template/JAXB-blocked); now mining utils/collections + other util leaves.
- **clean leaves (3): model/account AccountTime (EvilSpirit; zero-dep POJO; online/rest millis→hours/minutes parts) + model/account CharacterPasskey (cura; objectId/wrongCount/isPass/connectType + nested ConnectType enum ENTER/DELETE) + utils/xml CompressUtil (Rolandas; **java.util.zip.Deflater/Inflater[zlib RFC1950]→System.IO.Compression.ZLibStream [NOT raw DeflateStream]**; Compress/Decompress; namespace Utils.Xml)** DONE — fully GREEN. CompressUtil unblocks PlayerScript's compression reference. **SPAWNED task_1b3c5acb: re-port Passport (Java class) to replace divergent PlayerPassport record + port PassportsList + migrate consumers (slop-remediation, IPersistable/Timestamp/ServerTime).** **IDIOM: java.util.zip Deflater/Inflater → ZLibStream (zlib-compatible); raw DeflateStream would drop the zlib header and break Java interop.**
- **utils leaves (4): Util (-Nemesiss-; ConvertName uses NameConfig.ALLOW_CUSTOM_NAMES[ported]; substring/toUpper/toLower→Substring/ToUpper/ToLower; isEmpty→Length!=0) + utils/stats DropRewardEnum + XPRewardEnum (ATracer) + XPLossEnum (ATracer,Jangan) — all Java enum-with-per-instance-fields → C# enum + XxxEnumExtensions (LevelDifference/RewardPercent/GetLevel/GetParam switch accessors; static DropRewardFrom/XpRewardFrom/GetExpLoss iterate Enum.GetValues<T>(); NoSuchElementException→InvalidOperationException)** DONE — fully GREEN. **IDIOM: Java Math.round(double)→long [=floor(x+0.5)] → C# (long)Math.Floor(x+0.5) [NOT Math.Round which is banker's rounding]; expNeed/100 kept as long integer division before *param.** utils/stats AbyssRankEnum (121L) remains (likely @Properties/config-Map coupled — check).
- **clean enums (2): model/gameobjects/player ReviveType (non-sequential ids 0-4,6,8 → enum+ReviveTypeExtensions; GetReviveTypeId switch + GetReviveTypeById iterate; IllegalArgumentException→ArgumentException) + model/team/common/events TeamCommand (ATracer; per-instance commandCode + Java static-block lookup map → enum+TeamCommandExtensions w/ **static ctor building Dictionary<int,TeamCommand> from GetCodeId**; Objects.requireNonNull→TryGetValue+throw ArgumentNullException)** DONE — fully GREEN. **IDIOM: Java enum static{} block populating a lookup map → C# static constructor on the XxxExtensions class building a static readonly Dictionary; Objects.requireNonNull(x,msg)→ArgumentNullException (the .NET ArgumentNullException.ThrowIfNull analog).** **DEFERRED: ResponseRequester (RequestResponseHandler<? extends Creature> wildcard-generic field, no non-generic base on RequestResponseHandler<T> — needs rule-8 non-generic base; wildcard-generic deferred pattern) + AbyssRankEnum (RankingConfig.TOP_RANKING_GP_LOSS/QUOTA Maps — deferred config-loader @Properties pillar + ChatUtil.l10n).** Clean-leaf supply thinning across model/utils; most remaining couple to pillars (packets/effect/AI/config-Map/big-templates) or wildcard generics.
- **clean leaves (2): questEngine/handlers/models NpcInfos (Hilgert,Pad,Neon; @XmlType("NpcInfos"); npc_ids List<Integer>→Raw + movie; **CLEARS ReportToManyData's red NpcInfos dep**) + utils/idfactory IDFactoryError (SoulKeeper; **Java extends Error→C# : Exception** 4-ctor[ctor(cause)→base(null,cause)], serialVersionUID dropped)** DONE — fully GREEN. **DEFERRED this scan: AIEventLog (extends java LinkedBlockingDeque<AIEventType> — concurrency-collection primitive, no C# analog) + XmlValidationHandler/StringSchemaOutputResolver (JAXB ValidationEventHandler/SchemaOutputResolver — JAXB bridge pillar) + Link (neuralnetwork PlayerModel/PlayerModelLink unported) + skillengine/effect leaves (effect pillar) + services/cron RunnableRunner/CronExpressions (Quartz cron pillar) + spawnengine WalkerFormations* (SpawnEngine cone).** **IDIOM: Java `extends Error`→C# : Exception (no Error base in .NET).**
- **spawnengine WalkerGroupShift (Rolandas; clean POJO sagittalShift/coronalShift + DISTANCE=2f const + Set(shift))** DONE — fully GREEN. **SCAN-TOOLING LESSON: a background find rebuilding /tmp/ported.txt mid-run + case-sensitive grep gave FALSE "unported" hits — AiState/AiEventType already exist (divergent PascalCase casing vs Java AIState/AIEventType; AISubState too). Always verify with `find -iname` before porting.** **DIVERGENCE NOTED (not churned): ai cone uses AiState/AiEventType PascalCase, Java is AIState/AIEventType — widespread, low-impact casing slop; leave unless a rename pass is requested.** **DEFERRED this scan: WorldWalkerFormations/WalkerFormationsCache (InstanceWalkerFormations 161L cone: ClusteredNpc/WalkerGroup/DataManager) + AIEventLog (LinkedBlockingDeque) + HpPhases (NpcAI/AIState/PhaseHandler intersection-generic) + cron RunnableRunner/CronExpressions/CurrentThreadRunnableRunner (Quartz pillar) + Actions/PeriodicActions (effect/action pillar) + Link (PlayerModel) + SpaceSeparatedBytesAdapter/XmlValidationHandler/StringSchemaOutputResolver (JAXB bridge) + NetFlusher (network cone).** NEXT CLEAN CANDIDATE: custom/instance CustomInstanceRankEnum (52L — likely clean enum).
- **custom/instance CustomInstanceRankEnum (Jo; 9 ranks IRON..ANCIENT_PLUS w/ per-instance minRank/minReward/rankIcon[PUA char ..] → enum + CustomInstanceRankEnumExtensions; GetByRank iterates Enum.GetValues backwards; GetRankDescription uses name()→ToString() + ANCIENT_PLUS special-case; IllegalArgumentException→ArgumentException)** DONE — fully GREEN. **TOOLING NOTE: PUA char literals ( etc) get byte-mangled through Write/Edit (empty '' literals) — authored the GetRankIcon switch via Python `chr(92)`-built `'\uXXXX'` escape sequences written to file (avoid literal backslash-u in Python source which trips unicodeescape).** Verified-clean leaf supply now near-exhausted; most remaining couple to pillars (effect/AI/cron/JAXB/spawn-cone/packets) or big templates (NpcTemplate/QuestTemplate/SkillData) or are divergent-casing already-ported.
- **model/templates/quest XMLStartCondition (138L; antness,vlog; @XmlType("QuestStartConditions"); quest-start-condition checks) DONE** — **@XmlList element Integer lists (unfinished/noacquired/acquired/equipped)→Raw space-sep [XmlElement] string props; finished List<FinishedQuestCond>[green]@XmlElement**; QuestState.GetRewardGroup()[int?] null-guard + .Value; QuestStatus COMPLETE/START/REWARD/LOCKED. RED-tolerated: QuestTemplate (next big port — IsRepeatable/GetMaxRepeatCount), DataManager.QUEST_DATA/ITEM_DATA statics, PacketSendUtility/SM_SYSTEM_MESSAGE.STR_QUEST_ACQUIRE_ERROR_EQUIP_ITEM, Player.GetEquipment/GetCommonData APIs. **MILESTONE: QuestTemplate's full leaf-dep set is now present (all 14 model/templates/quest deps + XMLStartCondition) → QuestTemplate (389L) is now portable as a dedicated multi-tick effort; only red-tolerated runtime deps (CraftConfig/DataManager/packets) remain.** FinishedQuestCond/QuestStateList verified green.
- **model/templates QuestTemplate (389L; MrPoke,vlog,Neon; @XmlType("Quest"); implements L10n→: L10n + GetL10nId) DONE — fully GREEN.** **MAJOR UNBLOCK: QuestTemplate now ported** — clears the recurring red dep across QuestsData/MonsterHuntData/MentorMonsterHuntData/XMLStartCondition/DataManager.QUEST_DATA.GetQuestById and the whole quest data cone. IDIOMS: **@XmlList class_permitted List<PlayerClass> → ClassPermittedRaw [XmlElement] space-sep (Enum.Parse); @XmlAttribute List<QuestRepeatCycle> repeat_cycle → RepeatCycleRaw [XmlAttribute] space-sep**; 11 per-class selectable_reward List<QuestItems> plain [XmlElement]; nullable Race?/Gender? (Java object-enums null-when-absent); enum @XmlAttribute defaults (category=QUEST/extra=NONE/mentor=NONE/target=NONE); Collections.emptyList→new List; switch(playerClass) GLADIATOR→fighter/SORCERER→wizard/SPIRIT_MASTER→elementalist/TEMPLAR→knight/CLERIC→priest mapping preserved; Math.min→Math.Min. All 14 quest-leaf deps + L10n/Gender/CraftConfig/Race/PlayerClass verified green. **NOW UNBLOCKED for porting: QuestsData (dataholder) + GlobalDropData-adjacent + many quest consumers.**
- **dataholders QuestsData (MrPoke; @XmlRootElement(quests); NOW UNBLOCKED by QuestTemplate) DONE — GREEN** (questTemplates map + sortedByFactionId[non-timeBased, npcFactionId!=0]; AfterUnmarshal clears+rebuilds; GetQuestById/GetQuestsByNpcFaction/GetQuestTemplates). RED-tolerated: QuestEngine.GetInstance().IsHaveHandler + QuestService.CheckStartConditions (quest-engine/service cones). ~20 dataholders remain (mostly NpcTemplate/SkillData/Motion/TradeList big-template-blocked + VortexData[VortexLocation cone] + AutoGroupData[AutoGroup] + WalkerData[JAXB-marshal] + XMLQuests[?] + JAXB-bridge/effect/AI-coupled).
- **dataholders GlobalDropData (AionCool,Bobobear,Neon; @XmlRootElement(global_rules)) DONE — GREEN** — **CORRECTION: NpcTemplate was ALREADY ported (335L); my earlier GlobalDropData deferral wrongly assumed NpcTemplate missing (I'd verified QuestTemplate, not NpcTemplate).** ProcessRules(ICollection<NpcTemplate>); GetAllowedNpcs name-matching via StringFunction (Contains→.Contains/EndWith→.EndsWith/StartWith→.StartsWith/Equals→string.Equals OrdinalIgnoreCase); ToLowerCase→ToLower; allowedNpcs.Contains(gdNpc). **DIVERGENCE NOTED: StringFunction enum members are PascalCase (StartWith/EndWith/Contains/Equals) in C# vs Java SCREAMING_SNAKE (START_WITH/...) — pre-existing parity slop in StringFunction.cs (XML token risk); consumed as-is.** **LESSON RE-CONFIRMED: verify the TARGET class exists via find -iname before deferring on a 'missing dep' — both QuestTemplate-era assumptions about NpcTemplate were wrong.** ~18 dataholders remain.
- **dataholders (2) + ItemTemplate.ModifyMask: XMLQuests (MrPoke; @XmlRootElement(quest_scripts); **@XmlElements(16 element→XMLQuest subtype)→stacked [XmlElement(name,typeof)]** — all 16 subtypes now ported; questsById map; SetData→AfterUnmarshal(null)) + ItemData (Luno; @XmlRootElement(item_templates); MANASTONE/SPECIAL_MANASTONE bucketing into manastones/ancientManastones; **Map.compute→TryGetValue+init**; AddStonesForHigherLevels(60→70 special id 167000563; 10/30/50 COMMON/RARE "Manastone: Attack +" +10); Cleanup via DataManager.ITEM_CLEAN_UP[red-tolerated]+ApplyCleanup switch sbyte 1/0→ModifyMask)** DONE — GREEN. Added faithful **ItemTemplate.ModifyMask(bool,int)** (mask|=/&=~filter — Java had it, C# god-class lacked it). **DEFERRED NpcData (StaticDataListener.registerForAsyncExecutionOrRun unported + NpcStatCalculation cone).** ~16 dataholders remain.
- **dataholders (2): TradeListData (Luno; npc_trade_list; triple map tlist/tInlist/plist by TradeListTemplate.GetNpcId; ValidateBuyLists stream filter[SupportsAction DialogAction.BUY/TRADE_IN]/map/sorted→Where.Select.OrderBy.ToList; logger) + SiegeLocationData (Sarynth,antness; siege_locations; LinkedHashMap→Dictionary; switch template.GetType_()[SiegeType FORTRESS→Fortress+Artifact/ARTIFACT/OUTPOST→SetRace(SiegeRace.Elyos@2111/Asmodians@3111)/AGENT_FIGHT]; model/siege ctors green)** DONE — GREEN. **NOTE: SiegeRace enum is PascalCase (Elyos/Asmodians/Balaur) in C# vs Java SCREAMING — consumed as-is (another casing divergence; SiegeType is correctly SCREAMING).** ~14 dataholders remain (NpcData[StaticDataListener]/MotionData/ItemRandomBonusData/Portal2Data/ZoneData/TribeRelations/InstanceCooltime/NpcShout/PlayerInitial/SkillTree/SkillData/ItemGroups/VortexData/AutoGroupData/WalkerData).
- **dataholders ItemRandomBonusData (Rolandas; @XmlRootElement(random_bonuses); EnumMap<StatBonusType>→Dictionary nested [type→{id→RandomBonusSet}]; AfterUnmarshal pre-seeds all StatBonusType keys; SelectRandomBonusNumber chance-roll via Rnd.NextFloat(sumOfChances); AreBonusSetsEqual/GetTemplate/CalculateSumOfChances; stream mapToInt(Map::size).sum→Values.Sum(m=>m.Count)) DONE — GREEN.** RandomBonusSet/StatBonusType/ModifiersTemplate/Rnd verified. **DEFERRED MotionData (deep skill/player runtime cone: Skill/SkillTemplate/Motion/Times/ChargeSkill/Stat2/Player.GetGameStats/isHitTimeBoosted/isInRobotMode — too many red deps for a clean tick) + ZoneData (JAXB Marshaller saveData + ZoneTemplate slop[task_1903432a]) + Portal2Data (PortalService + portal runtime — assess later).** ~13 dataholders remain.
- **dataholders TribeRelationsData (ATracer; @XmlRootElement(tribe_relations); Dictionary<TribeClass,Tribe>; 6 relation predicates [IsAggressive/Support/Friendly/Neutral/None/Hostile] + CanSupport via Tribe.GetAggro/.../GetBase .Contains; afterUnmarshal) DONE — GREEN.** Tribe[templates.tribe] already ported. NOTE: GetBaseTribe uses direct indexer (Java didn't null-check — faithful; predicates use TryGetValue null-guard per Java's `==null` checks). **LESSON (again): `find -path "*lowercase*"` is case-sensitive and misses Capital-cased dirs (Tribe/) — use `-iname` alone. Tribe.cs existed; my path-filtered find falsely reported MISSING.** ~12 dataholders remain (NpcData/MotionData/Portal2/ZoneData/InstanceCooltime/NpcShout/PlayerInitial/SkillTree/SkillData/ItemGroups/VortexData/AutoGroupData/WalkerData).
- **dataholders NpcShoutData (Rolandas; @XmlRootElement(npc_shouts); nested Dictionary<int,Dictionary<int,List<NpcShout>>> shoutsByWorldNpcs; afterUnmarshal reverse-index mutation [remove-by-index/clear/MakeNull on ShoutGroup/ShoutList]; getNpcShouts global[world 0]+world merge; removeIf→RemoveAll; stream filter→Where.ToList) DONE — GREEN.** ShoutGroup/ShoutList/NpcShout accessors verified. **FAITHFULNESS: getNpcShouts(w,n,type) Java type is nullable object-enum (callers pass null) → C# overloads take ShoutEventType? + restored `|| type == null` guard (value enum can't be null).** ~11 dataholders remain.
- **dataholders InstanceCooltimeData (VladimirZ; @XmlRootElement(instance_cooltimes)) DONE — GREEN** — **ZonedDateTime→DateTimeOffset idioms: ServerTime.Now(); now.with(LocalTime.of(h,m))→new DateTimeOffset(now.Y/M/D,h,m,0,now.Offset); isAfter→>; plusDays→AddDays; toEpochSecond()*1000→ToUnixTimeSeconds()*1000; getDayOfWeek().getValue()[Mon=1..Sun=7]→`day==Sunday?7:(int)day`; System.currentTimeMillis()→DateTimeOffset.UtcNow.ToUnixTimeMilliseconds()**; getMaxMemberCount Race.ELYOS ternary; getInstanceCooltimes returns new Dictionary copy; getDay Mon..Sun→1..7 (IllegalArgumentException→ArgumentException); switch DAILY/WEEKLY/RELATIVE. RED-tolerated: InstanceService.GetInstanceRate (services/instance cone). ~10 dataholders remain.
- **dataholders Portal2Data (xTz; @XmlRootElement(portal_templates2); triple map portalUses[GetNpcId]/portalDialogs[GetNpcId]/portalScrolls[GetName]; GetPortalDialogPath/GetPortalUsePath race-match[player.GetRace()==path.GetRace()||Race.PC_ALL, fallback opposite-race path]; IsPortalNpc; GetTeleportDialogId default 1011) DONE — fully GREEN.** **NOTE: Java's PortalService import is javadoc-only ({@link}) — no runtime dep; dropped it (no red).** PortalUse/Dialog/Scroll/Path + Player.GetRace verified. ~9 dataholders remain (NpcData/MotionData/ZoneData/PlayerInitial/SkillTree/SkillData/ItemGroups/VortexData/AutoGroupData/WalkerData).
- **skillengine/model SkillLearnTemplate (ATracer,Neon; @XmlType("skill"); classId PlayerClass?/skillLearn int? nullable[Java object]; race=PC_ALL; stigma sbyte; GetSkillLevel via DataManager.SKILL_DATA[red]) + dataholders SkillTreeData (ATracer; @XmlRootElement(skill_tree); dual map templates[classOrdinal<<8|raceOrdinal<<8|level hash]+templatesById; .ordinal()→(int)enum; classId==null→all PlayerClass.values(); recursive CreateSkillTree[addList.add(0,)→Insert(0,)]; GetTemplatesForSkill PlayerClass? param[both null+non-null callers]; getHighestSkill stream.max→Aggregate; removeIf→RemoveAll; **renamed local `templates` shadow→stackTemplates** to avoid field clash) DONE — GREEN.** RED-tolerated: DataManager.SKILL_DATA.GetSkillTemplate/GetSkillTemplatesByStack (SkillData cone). ~8 dataholders remain.
- **dataholders SkillData (ATracer,Neon; @XmlRootElement(skill_data)) DONE — GREEN** — skillTemplateById + skillTemplatesByGroup/byStack (LinkedHashMap→Dictionary; computeIfAbsent→TryGetValue+init); ValidateMotions(StringBuilder+HashSet dedup, DataManager.MOTION_DATA[red] lookup, substring trim). SkillTemplate/Motion/MotionTime verified. **NOTE: SkillData is the DataManager.SKILL_DATA backing — its presence makes SkillTreeData/SkillLearnTemplate/QuestTemplate's SKILL_DATA refs resolvable once DataManager statics land.** **DEFERRED PlayerInitialData (@XmlIDREF ItemTemplate cross-doc reference — needs JAXB IDREF-bridge pillar; + nested PlayerCreationData/ItemsType/ItemType/LocationData classes).** ~7 dataholders remain (NpcData/MotionData/ZoneData/PlayerInitialData/ItemGroupsData/VortexData/AutoGroupData/WalkerData).
- **model/templates/itemgroups FeedGroups (Rolandas; static holder of 18 trivial pet-feed group subtypes [FeedFluid/Armor/Thorn/Balaur/Bone/Soul/Exclude/StinkingJunk/HealthyFoodAll/HealthyFoodSpicy/AetherPowderBiscuit/AetherCrystalBiscuit/AetherGemBiscuit/PoppySnack/PoppySnackTasty/PoppySnackNutritious/ShugoEventCoin/AetherCherry] each : FeedItemGroup w/ [XmlType("X")]) DONE — GREEN.** Java `public final class FeedGroups`→C# `public static class` holding nested classes. **UNBLOCKS ItemGroupsData (348L) — its FeedGroups.* deps now present; next tick port ItemGroupsData.** ~7 dataholders remain (ItemGroupsData now portable).
- **dataholders ItemGroupsData (Rolandas, 348L; @XmlRootElement(item_groups)) DONE — GREEN** — 37 group fields (craft/manastone/medal/food/medicine/ore/gather/enchant/event/boss + 18 FeedGroups.* nested); 9 BonusItemGroup category lists via Arrays.asList/singletonList→new List; **EnumMap<FoodType,Set<Integer>>→Dictionary<FoodType,HashSet<int>>**; AfterUnmarshal builds petFood per FoodType (stream map GetId→HashSet, food.Clear()); IsFood EXCLUDES/STINKY/MISC-fan-out via petFood[key] indexer (mirrors Java get().contains() fail-fast); GetPetFood big FoodType switch; BonusSize sums .Count; FeedItemGroup.GetItems()[List, mutable] vs BonusItemGroup.GetItems()[IReadOnlyList] both .Count-able. **dataholders milestone: only ~6 remain, all pillar/cone-blocked (NpcData[StaticDataListener]/MotionData[skill cone]/ZoneData[JAXB-marshal]/PlayerInitialData[XmlIDREF]/VortexData[VortexLocation cone]/AutoGroupData[AutoGroup cone]/WalkerData[JAXB-marshal]).**
- **model/autogroup AutoGroup (MrPoke; @XmlType("AutoGroup"); : L10n; @XmlAttribute List<Integer> npc_ids→Raw; clean POJO — NO AutoGroupType dep) + dataholders AutoGroupData (auto_groups; autoGroupByInstanceId[GetMaskId] + recruitableInstanceMaskIdByPortalNpc[per npcId, computeIfAbsent→TryGetValue+init]; GetRecruitableInstanceMaskIds() flatMap distinct→Values.SelectMany.Distinct.ToList)** DONE — GREEN. **CORRECTION: earlier AutoGroupData defer assumed AutoGroup was an instance/team cone — it's actually a clean L10n POJO (AutoGroupType[385L]/AutoInstance/etc are NOT referenced by AutoGroup). Lesson: read the target before assuming cone-coupling.** ~5 dataholders remain (NpcData[StaticDataListener]/MotionData[skill cone]/ZoneData[JAXB-marshal]/PlayerInitialData[XmlIDREF]/VortexData[VortexLocation cone]/WalkerData[JAXB-marshal]).
- **model/gameobjects HouseDecoration (Rolandas; extends AionObject implements Persistable→: AionObject, IPersistable) DONE — GREEN** — **IDIOM: Java getName() override on AionObject → C# overrides abstract `Name` property (AionObject.GetName()=>Name bridges; no need to make GetName virtual)**; byte room→sbyte ((byte)→(sbyte) casts); PersistentState.NEW; GetTemplate via DataManager.HOUSE_PARTS_DATA[red-tolerated]. **DEFERRED: WorldZoneTemplate (extends ZoneTemplate — slop re-port task_1903432a; accesses points/zoneType/mapid/flags protected fields) + TrapService (ConcurrentLinkedQueue.removeIf has no ConcurrentQueue analog — needs alt concurrent structure; Trap gameobject) + StaticDataListener (extends JAXB Unmarshaller.Listener — bridge pillar; keeps NpcData blocked) + cron RunnableRunner/CronExpressions/CronExpressionTransformer (Quartz pillar).** Clean-leaf supply very thin now; most remaining work is pillar/cone (JAXB-bridge, AbstractQuestHandler, packets, effect, ZoneTemplate-slop, cron).
- **services/ban HDDBanService (ViAl; singleton SingletonHolder pattern; bannedSerials Dictionary<string,DateTimeOffset>; **java.sql.Timestamp→DateTimeOffset, getTime()→ToUnixTimeMilliseconds, currentTimeMillis()→DateTimeOffset.UtcNow.ToUnixTimeMilliseconds, new Timestamp(long)→FromUnixTimeMilliseconds**; AddBan/RemoveBan/LoadBan/IsBanned) DONE — GREEN.** RED-tolerated: SM_HDDBAN_CONTROL (loginserver serverpackets cone). **CASING NOTE: loginserver serverpackets namespace is ServerPackets (capital P) — distinct from gameserver-aion which is Serverpackets (lowercase p). Verified before commit.** BanAction/LoginServer green. **DEFERRED ChangeLeaderEvent (extends AbstractTeamPlayerEvent<T extends TemporaryPlayerTeam<?>> — team-event wildcard-generic cone).**
- **model/gameobjects/findGroup GroupApplication (final class→sealed; : FindGroupEntry[marker iface, ported]; player/message/groupType/classId/level + lastUpdate; **System.currentTimeMillis()/1000→(int)(DateTimeOffset.UtcNow.ToUnixTimeMilliseconds()/1000)**) DONE — GREEN.** Player class lives in namespace ...Model.GameObjects.Player (subpackage), referenced via `using ...GameObjects.Player`. ResponseRequester still deferred (RequestResponseHandler<? extends Creature> wildcard). **Clean-leaf supply essentially exhausted across mined packages — remaining = pillars (JAXB-bridge/AbstractQuestHandler/packets/effect/cron/ZoneTemplate-slop) + wildcard-generic team-event cone + skill cone + IDREF.**

## PHASE TRANSITION (leaf-porting → pillars)
The standalone clean-leaf supply is **exhausted** across all mined packages (dataholders ~95% done; quest models cone complete; configs/utils/stat-enums/account/itemgroups/siege-data done; QuestTemplate+NpcTemplate+SkillData+SkillTreeData landed). Verified this tick: commons.network packet bases (BaseClientPacket/BaseServerPacket), LoginServerConnection, StaticDataListener(JAXB Unmarshaller.Listener) all unported → everything remaining is **pillar/cone-gated**.

**Remaining unported (~1090) is dominated by convergence pillars (all RED-by-design, name-guardrail-tolerated):**
1. **network/commons packet layer** — BaseClientPacket/BaseServerPacket + ByteBuffer abstraction + connections → unblocks 433 aion packets + loginserver packets + PacketSendUtility. (biggest fan-out)
2. **JAXB→XmlSerializer bridge** (JAXBUtil + Unmarshaller.Listener + @XmlID/@XmlIDREF + StaticData async) → unblocks all dataholder Load()/NpcData/ZoneData/WalkerData/PlayerInitialData + config Load().
3. **AbstractQuestHandler (1290L god-class)** → unblocks 17 template handlers + scripted quests.
4. **skillengine/effect cone (151)**, **dao (57, DB pillar)**, **Quartz cron** (CronExpression/Job), **AIEngine/AbstractAI**, **ZoneTemplate slop re-port (task_1903432a)**, **team-event wildcard-generic cone (AbstractTeamPlayerEvent/TemporaryPlayerTeam<?>)**, **VortexLocation/AutoInstance instance cones**.
These are multi-tick efforts; recommend dedicated focused sessions (or spawned worktree tasks) per pillar rather than 60s-tick slices. Outstanding spawned tasks: task_1903432a (ZoneTemplate), task_1b3c5acb (Passport — delivered), task_3febc765 (StringFunction casing).
- **(addendum to phase transition — 2 standalone utils were actually portable): dataholders/loadingutils/adapters SpaceSeparatedBytesAdapter (Neon; Java extends XmlAdapter<String,byte[]>→plain class w/ Marshal(sbyte[])/Unmarshal(string) [no C# XmlAdapter base; consumers use [XmlIgnore]+Raw-prop idiom delegating here]; **Java signed byte→sbyte, Byte.parseByte→sbyte.Parse**) + network/sequrity NetFlusher (NB4L1; **java.util.Timer/TimerTask scheduleAtFixedRate→System.Threading.Timer per task retained in static List to prevent GC**; Runnable→Action; RuntimeException→Exception; printStackTrace→Console.Error.WriteLine) DONE — GREEN.** Remaining 0-import standalone scan items are all genuinely pillar-coupled (AIEventLog[LinkedBlockingDeque]/HpPhases[NpcAI]/Link[PlayerModel]/CurrentThreadRunnableRunner+RunnableRunner+CronExpressions[Quartz]/Actions+PeriodicActions[effect]/WalkerFormationsCache+WorldWalkerFormations[InstanceWalkerFormations cone]/StringSchemaOutputResolver+XmlValidationHandler[JAXB bridge]).

## PILLAR 1 START: commons network packet layer
- **commons/network/packet BasePacket (Aquanox; opCode + GetPacketName[getClass().getSimpleName()→GetType().Name] + ToFormattedPacketNameString[String.format %0Nd→ToString("D"+N)]; GetOpCodeZeroPadding made virtual [Java protected overridable]) DONE — GREEN** at Commons/Network/Packet/BasePacket.cs. **DISCOVERY: a java.nio ByteBuffer port ALREADY EXISTS at Commons/Nio (Buffer/ByteBuffer/HeapByteBuffer/ByteOrder/Float+ShortBuffer views) but only has Put(byte)/Put(byte[]) — MISSING PutInt/PutShort/PutChar/PutDouble/PutFloat/PutLong (+ Get* counterparts likely) needed by BaseServerPacket.writeD/H/C/DF/F/Q/S.** NEXT PILLAR INCREMENTS: (1) add ByteOrder-aware Put*/Get* primitives to Commons/Nio ByteBuffer+HeapByteBuffer (Java semantics: big-endian default, packets use LITTLE_ENDIAN via order()); (2) port BaseServerPacket (139L; buf field + writeD/H/C/DF/F/Q/S/B); (3) port BaseClientPacket (267L; read counterparts readD/H/C/F/DF/Q/S/B + Runnable); (4) LsServerPacket/LsClientPacket/LoginServerConnection; (5) AionServerPacket/AionClientPacket bases → then the 433-packet cone becomes leaf-portable.
- **PILLAR 1 increment: Commons/Nio ByteBuffer — added Put*/Get* primitives** (PutShort/PutInt/PutLong/PutChar/PutFloat/PutDouble [relative + absolute, ByteOrder-aware] + GetLong/GetDouble/GetChar) matching java.nio semantics (bigEndian default; LITTLE_ENDIAN via Order()). PutFloat→SingleToInt32Bits+PutInt; PutDouble→DoubleToInt64Bits+PutLong; PutChar→PutShort (2-byte). Uses existing Buffer.NextPutIndex/NextGetIndex + abstract Put(index,byte). **Now BaseServerPacket.writeD/H/C/DF/F/Q/S/B + BaseClientPacket.readD/H/C/F/DF/Q/S can bind.** NEXT: port commons BaseServerPacket (buf field→ByteBuffer; writeS putChar UTF-16 + '\000' terminator) then BaseClientPacket.
- **PILLAR 1 increment: commons/network/packet BaseServerPacket (-Nemesiss-; abstract : BasePacket; public ByteBuffer buf + SetBuf; WriteD/H/C(int)/C(byte)/DF/F/Q/S/B over ByteBuffer Put*) DONE — GREEN.** writeS→UTF-16 chars + '\0' terminator. **TOOLING TRAP: typing a null-char literal via Write inserts a raw 0x00 byte (file flagged "binary", Edit can't match it) — fixed by Python byte-replace 0x00→`\0` escape. (Same class of gremlin as PUA chars; author control-char literals via Python.)** NEXT: BaseClientPacket (267L; read side readD/H/C/F/DF/Q/S/B + Run/runImpl + BaseClientPacket<T> generic connection param).
- **PILLAR 1 increment: commons/utils NetworkUtils (KID,-Nemesiss-; FindLocalIPv4[java.net NetworkInterface→System.Net.NetworkInformation, InetAddress→IPAddress, multicast=first octet 224-239] + CheckIPMatching[**Byte.parseByte→sbyte.Parse, preserves Java quirk: octets >127 throw**] + IntToIpString + ToHex(ByteBuffer)/ToText[String.format %04X/%02X→ToString("X4")/("X2")]) DONE — GREEN.** Uses Commons/Nio ByteBuffer (Get(i)/Limit()/Capacity()). Unblocks BaseClientPacket's NetworkUtils.toHex logging dep. **PACKET PILLAR STATUS: write path done (BasePacket+ByteBuffer Put*/Get*+BaseServerPacket+NetworkUtils). BLOCKED on the NIO connection cone (AConnection<T extends BaseServerPacket>/AionConnection/Crypt/Dispatcher/Acceptor/IOServer — large subsystem) which gates BaseClientPacket[`T extends AConnection<?>` wildcard bound] + AionServerPacket[writeImpl(AionConnection)] + all 433 packets. The connection/NIO layer is the next big pillar — recommend dedicated effort.**
- **PILLAR 1 increment: Commons/Nio ByteBuffer.Array()/ArrayOffset() (java.nio backing-array accessors) + network/EncryptionKeyPair (cura; Aion packet cipher, 188L) DONE — GREEN.** EncryptionKeyPair: staticKey="nKO/...?3iI9"[64 chars, i&63]→Encoding.ASCII.GetBytes; **Java signed byte[]→C# byte[] (unsigned; all key/data math &0xff → bit-identical); byte^=int→byte=(byte)(byte^int); System.arraycopy→Array.Copy; currentTimeMillis()→ToUnixTimeMilliseconds; Integer.toHexString→ToString("x"); Decrypt/Encrypt mutate buf.Array() in place at ArrayOffset()+Position(); ValidateClientPacket getShort(0)==~getShort(3) [short→int promotion] && get(2)==staticClientPacketCode**. Self-contained (only ByteBuffer dep). **NEXT: Crypt (92L; delegates to EncryptionKeyPair[now green]; enableKey Rnd.nextInt^0xCD92E4DF+0x3FF2CCCF; encode/decodeOpcode use SM_VERSION_CHECK.INTERNAL_VERSION[red-tolerated]).**
- **PILLAR 1 increment: network/Crypt (hack99,kao,-Nemesiss-; delegates to EncryptionKeyPair[green]) DONE — GREEN.** EnableKey: Rnd.NextInt()→new EncryptionKeyPair; **return (key ^ unchecked((int)0xCD92E4DF)) + 0x3FF2CCCF wrapped in unchecked() [Java int-hex-literal wraps; C# 0xCD92E4DF is uint so cast]**; IllegalStateException→InvalidOperationException; Encrypt skips first packet (SM_KEY); EncodeServerPacketOpcode/DecodeClientPacketOpcode (precedence: `-` before `^`, parens preserved) use SM_VERSION_CHECK.INTERNAL_VERSION[red-tolerated]. staticServerPacketCode=0x44. **PACKET PILLAR write+crypt foundation COMPLETE: BasePacket/ByteBuffer(Put*/Get*/Array)/BaseServerPacket/NetworkUtils/EncryptionKeyPair/Crypt all green. Still gated: AConnection/AionConnection NIO layer (java.nio.channels Selector/SocketChannel — needs Socket-based rewrite, not 1:1) + AionServerPacket(writeImpl(AionConnection)) + BaseClientPacket(T extends AConnection<?>). The NIO channel/selector layer is the remaining hard pillar.**

## AUTONOMOUS-PHASE BOUNDARY (foundation done → big pillars remain)
Clean leaves AND the packet write/crypt foundation are now ported. The remaining ~1085 unported files are large coupled units that are **not well-suited to 60s autonomous slices** — each needs focused, reviewable work:
1. **NIO connection/dispatcher layer** (commons.network AConnection<T extends BaseServerPacket>/Dispatcher/Acceptor/IOServer + AionConnection): deeply tied to java.nio.channels SocketChannel/Selector/SelectionKey — this is a **designed Socket/async rewrite, NOT a 1:1 line port**. Gates AionServerPacket(writeImpl(AionConnection)) + capture observers + BaseClientPacket(T extends AConnection<?> wildcard) + all 433 packets. ⇒ dedicated architectural session.
2. **AbstractQuestHandler (1290L)** — large but genuinely 1:1 (dispatch logic, red deps tolerated). ⇒ SPAWNED task_3f5565d9.
3. **skillengine/effect cone (151)**, **dao (57, DB layer)**, **JAXB→XmlSerializer bridge (JAXBUtil/Unmarshaller.Listener/@XmlID/IDREF)**, **Quartz cron (CronExpression/Job)**, **AIEngine/AbstractAI** — each a dedicated multi-file pillar.
Outstanding spawned tasks: task_1903432a(ZoneTemplate), task_3febc765(StringFunction casing), task_3f5565d9(AbstractQuestHandler). task_1b3c5acb(Passport) delivered.
**Recommendation: the highest-value 1:1 autonomous-tickable remaining target is AbstractQuestHandler (spawned); the NIO layer needs a human-designed rewrite. Packet foundation shipped this phase: BasePacket, ByteBuffer Put*/Get*/Array, BaseServerPacket, NetworkUtils, EncryptionKeyPair, Crypt.**
- **questEngine/handlers AbstractQuestHandler (1290L god-class; MrPoke,vlog,Majka) DONE — GREEN (baseline god-class, no new violation).** Full 1:1 port: register + ~40 virtual OnXEvent handlers + ~50 send*/default*/check*/useQuest* helpers + static spawn* helpers. **MAJOR UNBLOCK: clears the red base of AbstractTemplateQuestHandler → the 17 template handlers (FountainRewards/MonsterHunt/ReportTo/etc) + scripted quests now buildable.** IDIOMS APPLIED: static import DialogAction.*→DialogAction.X (int-const switch cases); **AIEventType→AiEventType (existing PascalCase enum, Ai.Event ns)**; anonymous Runnable(useQuestItem)→ThreadPoolManager.GetInstance().Schedule(ct=>{...;return ValueTask.CompletedTask;}, TimeSpan.FromMilliseconds(3000)) w/ early-return→return ValueTask.CompletedTask; Math.toRadians→deg*Math.PI/180; Stream.of(values()).allMatch→Enum.GetValues<PlayerClass>().All; Collections.empty/unmodifiableSet→new HashSet; instanceof X y→is X y; getObjectId()→ObjectId; QuestStatus? for nullable status; varargs int...→params int[]. **RED-tolerated refs (convergence will fix signatures): QuestEngine/QuestService(StartQuest/FinishQuest/CheckStartConditions[+6-arg overload]/CollectItemCheck/ValidateAndFixRewardGroup/AddOrUpdateQuest), ItemService.AddItem+ItemUpdatePredicate, ItemAddType/ItemUpdateType, SpawnEngine.NewSingleTimeSpawn/SpawnObject/NewSingleTimeSpawn, GeoService.GetZ, DialogPage.X.Id()/GetRewardPageByIndex, EmotionId.Id()/EmotionType.EMOTE, CreatureType.PEACE, TaskId.QUEST_FOLLOW, QuestTasks.NewFollowingToTargetCheckTask, ZoneName, SM_QUEST_ACTION.ActionType/SM_DIALOG_WINDOW/SM_SYSTEM_MESSAGE.STR_*/SM_EMOTION/SM_PLAY_MOVIE/SM_NPC_INFO/SM_ITEM_USAGE_ANIMATION, Player.GetController().UpdateNearbyQuests/AddTask/Die/DeleteIfAliveOrCancelRespawn/Npc.GetAi().OnCreatureEvent/OverrideNpcType, WorldPosition/WorldMapInstance APIs.** NOTE: heading modeled as byte (Java byte; spawn API). **Verification caveat: 1290L transcribed without compiler (red branch); structure/green-dep-calls faithful, red-dep signatures best-effort — needs a compile pass at convergence.**
- **questEngine/handlers/template ReportOnLevelUp (Majka,Bobobear,Pad; : AbstractTemplateQuestHandler[now green]) DONE — GREEN.** First of the 17 template handlers ported atop the live AbstractQuestHandler base. List<Integer>→List<int> ctor; Set.addAll→HashSet.UnionWith; Register→qe.RegisterQuestNpc(id).AddOnTalkEvent + RegisterOnEnterWorld/RegisterOnLevelChanged (red-tolerated QuestEngine methods); OnDialogEvent/OnEnterWorldEvent/OnLevelChangedEvent overrides; SendQuestEndDialog(base, green); QuestService.StartQuest(3-arg)/QuestStateList.HasQuest red-tolerated. **16 template handlers remain (FountainRewards/MonsterHunt/ReportTo/ReportToMany/KillInWorld/KillInZone/ItemCollecting/CraftingRewards/RelicRewards/SkillUse/WorkOrders/ItemOrders/KillSpawned/MentorMonsterHunt/XmlQuest + AbstractTemplateQuestHandler[done]) — all : AbstractTemplateQuestHandler, same idiom set, red-tolerated QuestEngine/QuestService.**
- **questEngine/handlers/template FountainRewards (Wakizashi,vlog,Bobobear,Luzien,Pad; : AbstractTemplateQuestHandler) DONE — GREEN.** Register→AddOnQuestStart+AddOnTalkEvent; OnDialogEvent switch(dialogActionId) USE_OBJECT/SETPRO1 (DialogAction.X) — **SETPRO1 path that doesn't return needs explicit C# `break;` (Java fell through to final return false)**; IsStartable/IsFullSpecialCube; QuestService.InventoryItemCheck/CollectItemCheck/StartQuest/AbandonQuest + PacketSendUtility/SM_SYSTEM_MESSAGE.STR_MSG_FULL_INVENTORY red-tolerated. 15 template handlers remain.
- **questEngine/handlers/template ItemOrders (Altaress,Bobobear,Pad; : AbstractTemplateQuestHandler) DONE — GREEN.** Register→RegisterQuestItem+RegisterQuestNpc.AddOnTalkEvent; OnDialogEvent 3-status branch (startable/START/REWARD) w/ DialogAction switches; **super.onDialogEvent(env)→base.OnDialogEvent(env)** (default case); **HandlerResult.fromBoolean→FromBoolean / HandlerResult.FAILED**; log.warn("Q{}",id)→LogWarning("Q{QuestId}",id); workItems[0].GetItemId(). DataManager.ITEM_DATA/QuestService/PacketSendUtility/SM_SYSTEM_MESSAGE.STR_QUEST_ACQUIRE_ERROR_INVENTORY_ITEM red-tolerated. 14 template handlers remain (CraftingRewards/RelicRewards/MonsterHunt/ReportTo/ReportToMany/KillInWorld/KillInZone/ItemCollecting/SkillUse/WorkOrders/KillSpawned/MentorMonsterHunt/XmlQuest).
- **questEngine/handlers/template RelicRewards (Bobobear,Rolandas,Pad; : AbstractTemplateQuestHandler) DONE — GREEN.** 3-status OnDialogEvent; DialogAction.EXCHANGE_COIN/USE_OBJECT/SELECT1-4/SELECTED_QUEST_NOREWARD switches; isDataDriven?4762:1011 ternary; qs.SetRewardGroup/SetQuestVar/SetStatus; QuestService.CheckAndGetCollectItemQuestRewardCategory(env[,idx]) + player.GetCommonData().GetLevel red-tolerated. 13 template handlers remain.
- **questEngine/handlers/template CraftingRewards (Bobobear,Pad; : AbstractTemplateQuestHandler) DONE — GREEN.** 3-status OnDialogEvent (QUEST_SELECT/SELECT_QUEST_REWARD); CanLearn(400→Expert/500→Master, else IllegalStateException→InvalidOperationException); DataManager/CraftSkillUpdateService.GetInstance()/player.GetSkillList().AddSkill red-tolerated. 12 template handlers remain (ReportTo/ReportToMany/MonsterHunt/KillInWorld/KillInZone/ItemCollecting/SkillUse/WorkOrders/KillSpawned/MentorMonsterHunt/XmlQuest).
- **questEngine/handlers/template ReportTo (MrPoke,Rolandas,Pad; : AbstractTemplateQuestHandler) DONE — GREEN.** Set.addAll→UnionWith; **Set.equals→SetEquals; isEmpty→Count==0**; super.onDialogEvent→base.OnDialogEvent (default case); SendQuestStartDialog(env, workItem) overload; QuestItems[green]; DataManager red-tolerated. 11 template handlers remain (ReportToMany/MonsterHunt/KillInWorld/KillInZone/ItemCollecting/SkillUse/WorkOrders/KillSpawned/MentorMonsterHunt/XmlQuest).
- **questEngine/handlers/template WorkOrders (Mr.Poke,Bobobear,Pad; : AbstractTemplateQuestHandler) DONE — GREEN.** 3-status; DialogPage.ASK_QUEST_ACCEPT_WINDOW/COMBINETASK_WINDOW/SELECT_QUEST_REWARD_WINDOW1.Id(); DialogAction.QUEST_SELECT/QUEST_ACCEPT_1/COMBINE_TASK/USE_OBJECT; List.addAll→AddRange; RecipeService.ValidateNewRecipe/AddRecipe, ItemService.AddItem(4-arg), QuestService.RemoveQuestWorkItems, player.GetRecipeList().DeleteRecipe red-tolerated; CollectItems/CollectItem/QuestItems green. 10 template handlers remain (ReportToMany/MonsterHunt[base for KillSpawned+MentorMonsterHunt]/KillInWorld/KillInZone/ItemCollecting/SkillUse/XmlQuest).
- **questEngine/handlers/template MonsterHunt (MrPoke,vlog,Bobobear,Pad,Majka; : AbstractTemplateQuestHandler) DONE — GREEN.** Base for KillSpawned+MentorMonsterHunt. 3-status OnDialogEvent + OnKillEvent (bit-packed quest-var counters: `total += currentVar << ((varId-var)*6)`, preserved verbatim) + OnAddAggroList/OnEnterWorld/OnEnterZone/OnAtDistance + StartQuest/SearchOpenRift. Set.addAll→UnionWith; isEmpty→Count==0; ZoneName.get(s).name().equalsIgnoreCase("NONE")→`!ZoneName.Get(s).Name().Equals("NONE",OrdinalIgnoreCase)`; Math.max→Math.Max; QuestCategory.FACTION green; Monster model (GetNpcIds/GetStep/GetVar/GetEndVar) green; DataManager/QuestService/RiftService/VortexService/VortexLocation/RiftLocation/AdminConfig.DIALOG_INFO/PacketSendUtility red-tolerated. 9 template handlers remain (ReportToMany/KillInWorld/KillInZone/ItemCollecting/SkillUse/KillSpawned[:MonsterHunt]/MentorMonsterHunt[:MonsterHunt]/XmlQuest).
- **questEngine/handlers/template KillSpawned (vlog,Pad; : AbstractTemplateQuestHandler) + MentorMonsterHunt (MrPoke,Bobobear,Pad; : MonsterHunt) DONE — GREEN.** KillSpawned: spawnerObjectIds from Monster.GetSpawnerNpcId(); Set.addAll→UnionWith, Set.equals→SetEquals, isEmpty→Count==0, Collections.emptyList→new List; USE_OBJECT spawns monster at SpawnSpotTemplate spot via SpawnForFiveMinutes; OnKillEvent increments per-monster var then flips REWARD when all met; DataManager.SPAWNS_DATA.GetFirstSpawnByNpcId(...).GetSpot()/SpawnForFiveMinutes red-tolerated. MentorMonsterHunt: ctor chains base MonsterHunt(...,0,0,null,0,null,0,reward,rewardNextStep); OnKillEvent switch on QuestMentorType.MENTOR/MENTE (group-range gate via PositionUtil.IsInRange/GroupConfig.GROUP_MAX_DISTANCE) → base.OnKillEvent; PlayerGroup/player.IsMentor/IsInGroup red-tolerated. 7 template handlers remain (ReportToMany/KillInWorld/KillInZone/ItemCollecting/SkillUse/XmlQuest — and ReportToMany).
- **questEngine/handlers/template ReportToMany (Hilgert,vlog,Pad,Neon; : AbstractTemplateQuestHandler) DONE — GREEN.** Multi-step report chain via List<NpcInfos>; Set.addAll→UnionWith, isEmpty→Count==0, List.addAll→AddRange, List.get(i)→[i], List.size()→.Count; SETPRO1–12 stacked cases→ChangeQuestStep+GiveQuestItem+CloseDialogWindow; SET_SUCCEED/SELECT_QUEST_REWARD/CHECK_USER_HAS_QUEST_ITEM(_SIMPLE) shared body (rewardStatusFromRewardNpc flag); GetMaxStep/GetDialogId(var*341)/ValidateAndRemoveItems helpers; OnItemUseEvent→HandlerResult.FromBoolean/UNKNOWN; OnLevelChangedEvent→DefaultOnLevelChangedEvent; super.onDialogEvent→base; DataManager/QuestService/NpcInfos(GetNpcIds/GetMovie)/Item red-tolerated. 5 template handlers remain (KillInWorld/KillInZone/ItemCollecting/SkillUse/XmlQuest).
- **questEngine/handlers/template KillInWorld (vlog,bobobear,Pad) + KillInZone (Cheatkiller,Majka,Pad); both : AbstractTemplateQuestHandler DONE — GREEN.** Daily kill-counter quests. Set.addAll→UnionWith, Set.equals→SetEquals, isEmpty→Count==0; null worldIds→iterate DataManager.WORLD_MAPS_DATA (template.GetMapId()); null zones→DataManager.ZONE_DATA.ZoneList (template.GetXmlName()); minRank/levelDiff guards via `((Player)env.GetVisibleObject()).GetAbyssRank().GetRank().GetId()`/GetLevel(); KillInWorld→OnKillInWorldEvent→DefaultOnKillRankedEvent + OnEnterWorld/SearchOpenRift; KillInZone→OnKillInZoneEvent→DefaultOnKillInZoneEvent; super→base; DataManager/QuestService/RiftService/VortexService/WorldMapTemplate/ZoneTemplate red-tolerated. 3 template handlers remain (ItemCollecting/SkillUse/XmlQuest).
- **questEngine/handlers/template ItemCollecting (MrPoke,vlog,Rolandas,Majka,Pad) + SkillUse (vlog,Bobobear,Pad) + XmlQuest (Mr.Poke,Bobobear,Pad); all : AbstractTemplateQuestHandler DONE — GREEN.** ItemCollecting: 3-status; SETPRO1/SELECT1_1/CHECK_USER_HAS_QUEST_ITEM(_SIMPLE)/FINISH_DIALOG/SET_SUCCEED/SETPRO1-4 dialog branches; CheckQuestItems/CheckQuestItemsSimple/DefaultCloseDialog helpers; actionItems looting; QuestCategory.FACTION; ZoneName.Get(s).Name() compare; OnEnterZoneEvent. SkillUse: bit-packed skill-use counters (preserved verbatim), rewardCount==qsd.Count→REWARD; QuestSkillData(GetSkillIds/GetEndVar/GetVarNum); Collections.emptyList→new List. XmlQuest: delegates OnDialogEvent→onTalkEvent.Operate / OnKillEvent→onKillEvent.Operate; OnTalkEvent/OnKillEvent/Monster models red-tolerated. **TEMPLATE-HANDLER VEIN EXHAUSTED — all 17 template handlers ported (AbstractTemplateQuestHandler base + ReportOnLevelUp/ReportTo/ReportToMany/MonsterHunt/MentorMonsterHunt/KillSpawned/KillInWorld/KillInZone/ItemCollecting/ItemOrders/WorkOrders/RelicRewards/CraftingRewards/FountainRewards/SkillUse/XmlQuest).** Next pillar candidates: questEngine/handlers/models/* (Monster/NpcInfos/QuestSkillData/xmlQuest.events.* — the red-tolerated model deps these handlers reference), or AbstractTemplateQuestHandler's own helper god-class convergence, or resume the NIO connection pillar (designed Socket/async rewrite, gates 433 packets).
- **questEngine handler-MODEL vein already complete** (verified: every Java handlers/models/*.java + xmlQuest/{conditions,events,operations}/* has a C# counterpart). Pivoted to questEngine task/analyzer leaves.
- **questEngine/task/checker/CoordinateDestinationChecker + task/FollowingNpcCheckTask + task/QuestTasks + QuestSpawnAnalyzer DONE — GREEN.** CoordinateDestinationChecker : DestinationChecker (PositionUtil.IsInRange(follower,x,y,z,20)). FollowingNpcCheckTask: Runnable→Run(); AIEventType.STOP_FOLLOW_ME→AiEventType; OnSuccess/OnFail→QuestEngine.OnNpcReachTarget/OnNpcLostTarget; TaskId.QUEST_FOLLOW; npc.GetAi().GetName()!="following"→Delete; QuestEngine/Npc.GetAi red-tolerated. QuestTasks: 4 NewFollowingToTargetCheckTask overloads (Npc target / int npcTargetId[ArgumentException] / float x,y,z / ZoneName); Future<?>→IScheduledFuture, scheduleAtFixedRate→ScheduleAtFixedRate(task,1000,1000); ThreadPoolManager red-tolerated. QuestSpawnAnalyzer: Map<Set<Integer>,List<Integer>>→Dictionary<HashSet<int>,List<int>>(HashSet<int>.CreateSetComparer()) for value-key semantics; computeIfAbsent→TryGetValue+init; streams→LINQ (Any/All/OrderBy/string.Join/string.Concat); Files.walk→Directory.EnumerateFiles(...,AllDirectories); regex `\bsp(?:awn)?\(...` preserved; System.currentTimeMillis→DateTimeOffset.UtcNow.ToUnixTimeMilliseconds; AbstractQuestHandler/QuestNpc(FindAllRegisteredQuestIds)/XMLQuest(GetAlternativeNpcs)/config HANDLER_DIRECTORY red-tolerated. **questEngine non-handler leaves now exhausted except QuestEngine.java (945L god-class — the `qe` registration hub every handler calls; next dedicated tick).**
- **questEngine/QuestEngine (945L god-class; MrPoke,Hilgert,vlog,Neon) DONE — GREEN.** THE keystone: the `qe` registration/dispatch hub every quest handler references (RegisterQuestNpc/RegisterOnEnterWorld/RegisterQuestSkill/RegisterCanAct/AddOn* etc.) + all on*Event dispatch (OnDialog/OnKill/OnAttack/OnLevelChanged/OnKillRanked/OnKillInWorld/OnKillInZone/OnEnterZone/OnUseSkill/OnCanAct/OnBonusApply/OnAtDistance/... ~35 dispatchers) + Init/Reload/Clear + AddQuestHandler/AddHandlerSideQuestDrop + AddMessageSendingTask. Idioms: EnumMap<Race/AbyssRankEnum/BonusType>→Dictionary; **computeIfAbsent→private static GetOrCreate<TKey>(map,key) helper** (TryGetValue+init; one small helper replaces ~15 inlined call-sites — sole deliberate non-1:1 sugar, behavior identical); putIfAbsent(k,v)!=null→!TryAdd; Map.get→TryGetValue/GetValueOrDefault; Object...→params object[]; AbyssRankEnum.values()→AbyssRankEnumExtensions.Values(); SingletonHolder→static readonly; logger.error(msg,ex)→LogError(ex,msg). **C# Model/GameEngine is a divergent async redesign (Name/InitAsync/ShutdownAsync) — mirrored Java `: GameEngine` + void Init() faithfully; interface-satisfaction left RED (red-by-design).** Red-tolerated: ScriptManager/AggregatedClassListener/OnClassLoadUnloadListener/QuestHandlerLoader, CronService/Quartz JobDetail, SplitList/DynamicServerPacketBodySplitList, World.ForEachPlayer, SM_QUEST_COMPLETED_LIST/SM_SYSTEM_MESSAGE, player.GetNpcFactions/GetController.UpdateNearbyQuests, HandlerSideDrop. **questEngine package now fully ported (engine + all handlers + all models + tasks + analyzer).** Next pillar candidates: QuestHandlerLoader + the ScriptManager/classlistener scripting infra (gates Init), or the NIO connection pillar (designed Socket rewrite, gates 433 packets), or AbstractQuestHandler helper-signature convergence.
- **commons/scripting classlistener+metadata leaves DONE — GREEN** (Aion.Commons project). ClassListener (interface; Class<?>[]→Type[]; PostLoad/PreUnload), AggregatedClassListener (delegates to List<ClassListener>, preUnload reverse-order), OnClassLoadUnloadListener (reflection: getDeclaredMethods→GetMethods(DeclaredOnly|Static|Instance|Public|NonPublic), Modifier.isStatic→IsStatic, getAnnotation→GetCustomAttribute, invoke(null)→Invoke(null,null), IllegalAccess/InvocationTarget→MethodAccess/TargetInvocationException; setAccessible dropped — unneeded in C#), OnClassLoad+OnClassUnload (@interface @Target(METHOD)@Retention(RUNTIME)→[AttributeUsage(AttributeTargets.Method)] sealed:Attribute).
- **SCOPING NOTE — commons/scripting compiler core is a DESIGNED REWRITE, not 1:1** (like the NIO pillar): ScriptManager/ScriptCompiler/ScriptClassLoader/ScriptContext + impl/javacompiler/* (BinaryClass/ClassFileManager/ErrorListener/JavaSourceFromFile/JavaSourceFromString/ScriptClassLoaderImpl/ScriptCompilerImpl) + url/* all wrap javax.tools.JavaCompiler for RUNTIME .java→.class compilation of quest/ai/instance handlers. C# has no java-source compiler; the equivalent is compiled handlers / Roslyn. These gate QuestEngine.Init's scriptManager.Load(...) (already red-tolerated in QuestEngine). FLAGGED for dedicated human design decision — do not attempt literal port. QuestHandlerLoader (65L, the ClassListener that reflects handlers→qe.AddQuestHandler) IS portable once a handler-discovery strategy is chosen; deferred with the rewrite.
- **Pivoted to single-file quest-referenced leaves (survey: 363-file slop baseline; biggest red clusters network/516, skillengine/176, services/98, model/91, dao/57 are pillars). Ported 2 high-leverage enums my quest ports red-tolerated:**
- **utils/stats/AbyssRankEnum (121L; ATracer,Sarynth,Imaginary) DONE — GREEN.** Directly converges QuestEngine.RegisterOnKillRanked (was calling AbyssRankEnumExtensions.Values()/GetId()). @XmlEnum enum w/ 5 per-instance fields→enum + AbyssRankEnumExtensions: static Dictionary<AbyssRankEnum,RankData(id,pg,pl,ap,gp)>; GetId/GetPointsLost/GetPointsGained/GetRequiredAP/GetRequiredGP/GetGpLossPerDay/GetQuota/GetRankL10n(3 overloads); Values()=Enum.GetValues; GetRankById(IllegalArgument→ArgumentException)/GetRankForPoints; ordinal()→(int)self. RankingConfig.TOP_RANKING_GP_LOSS/QUOTA(GetValueOrDefault)/ChatUtil.L10n/player.GetAbyssRank red-tolerated. NOTE: QuestEngine's `playerRank != null` on this value-type enum compiles (CS0472 always-true warning) — acceptable red-by-design.
- **model/DialogPage (193L; Rolandas,Neon) DONE — GREEN.** Converges WorkOrders handler (DialogPage.X.Id()). enum w/(dialogActionId,id)→enum + DialogPageExtensions: static Dictionary<DialogPage,PageData>; Id()/GetByActionId/GetRewardPageByIndex(Integer→int?)/GetStartPageId/HasAlternativeDialogAfterAscension(Java switch-expr 39-npc-id table→C# `or`-pattern switch)/HasQuestInteraction(stream.anyMatch→.Any). DialogAction.* int consts green; TalkInfo/DialogService/QuestEngine.GetQuestNpc/QuestService.CheckStartConditions/player.GetCommonData.IsDaeva red-tolerated.
- **utils/collections/DynamicServerPacketBodySplitList (32L; Sykra,Neon) + model/vortex/VortexLocation (200L; Source) DONE — GREEN.** DynamicServerPacketBodySplitList<TType> : DynamicElementCountSplitList<TType> (base already ported); Function<Type,Integer>→Func<TType,int>; ctor passes AionServerPacket.MAX_USABLE_PACKET_BODY_SIZE-staticBodyByteSize (gameserver network AionServerPacket red-tolerated). VortexLocation : ZoneHandler — converges MonsterHunt/KillInWorld's VortexService.GetLocationByWorld(...).IsActive(); ~25 template-delegating getters + OnEnterZone/OnLeaveZone; instanceof→is, Map.put/remove→[]=/Remove, Race.equals→==, anonymous Runnable kick-timers→ThreadPoolManager.Schedule(ct=>{...;return ValueTask.CompletedTask;},TimeSpan.FromMilliseconds(10*1000)). DimensionalVortex<VortexLocation>/RVController/InvasionZoneInstance/ZoneInstance/ZoneHandler/Kisk/VortexTemplate/WorldPosition/SM_SYSTEM_MESSAGE red-tolerated.
- **model/challenge cluster: ChallengeQuest (60L) + ChallengeTask (103L) (ViAl) DONE — GREEN.** ChallengeQuest : IPersistable (PersistentState→IPersistable.PersistentState nested enum; NEW→UPDATE_REQUIRED guard preserved); synchronized increaseCompleteCount→lock(this). ChallengeTask: java.sql.Timestamp→DateTimeOffset? (getTime()/1000→ToUnixTimeMilliseconds()/1000; new Timestamp(currentTimeMillis)→FromUnixTimeMilliseconds(UtcNow.ToUnixTimeMilliseconds())); Map→Dictionary, map.get→GetValueOrDefault, synchronized updateCompleteTime→lock(this); 2 ctors (DAO-load / runtime-create). DataManager.CHALLENGE_DATA.GetTaskByTaskId / ChallengeTaskTemplate.GetQuests / ChallengeQuestTemplate(GetId/GetRepeatCount/GetScore) red-tolerated.
- **model/skill cluster: NpcSkillEntry (62L) + NpcSkillList (115L) + NpcSkillTemplateEntry (225L) (ATracer,nrg,Yeats,Neon) DONE — GREEN.** NpcSkillEntry : SkillEntry (abstract, ~18 abstract members; currentTimeMillis→DateTimeOffset.UtcNow.ToUnixTimeMilliseconds). NpcSkillList: Collections.emptyList→new List, Iterator+iter.remove()→backward RemoveAt then Reverse (preserves npcSkills in-place mutation + forward order), prios.sort(reverseOrder)→Sort((a,b)=>b.CompareTo(a)), mapToInt.toArray→ToArray; Rnd.Get(list). NpcSkillTemplateEntry : NpcSkillEntry — Java switch-expr on ConjunctionType(XOR/OR/AND)+NpcSkillCondition(24 cases)→C# switch expr (each instanceof-pattern arm `is Creature tN` with unique binding names per arm; default `_=>false` added for exhaustiveness); HELP_FRIEND lambda→FindObject predicate(knownObject.Get() is Creature target...); Math.toRadians→*Math.PI/180; anonymous Runnable spawn→Schedule(ct=>{...;return ValueTask.CompletedTask;},TimeSpan.FromMilliseconds(delay)); Rnd.chance/nextFloat/get. Red-tolerated: skillengine.effect (AbnormalState/EffectTemplate/SignetBurstEffect/Effect/SkillTemplate), SpawnEngine, GeoService, TribeRelationService, PositionUtil, AbyssNpcType, NpcSkillTemplate(s)/NpcSkillSpawn/NpcSkillCondition(Template).
- **model/craft/Profession (88L) + model/enchants/EnchantmentStone (67L) (Neon) DONE — GREEN.** Both per-instance-field enums→enum+Extensions. Profession: skillId data dict; GetSkillId/IsCrafting/GetUpgradeCost(Integer→int?)/GetMaxUpgradableLevel/GetClientName(2 overloads)/GetSkillGrade(private,7 grade L10n tiers)/GetBySkillId(null→Profession?); DataManager.SKILL_DATA/ChatUtil.L10n red-tolerated. EnchantmentStone: (baseLevel,baseQuality) StoneData dict (EPSILON&OMEGA both 65/MYTHIC); GetBaseLevel/GetBaseQuality/GetByItemId(switch w/ 4 OMEGA fall-through cases + default range cascade 166000001-190; IllegalArgument→ArgumentException); ItemQuality green.
- **model/trade cluster: ExchangeItem (63L) + Exchange (97L) + TradeList (123L) (ATracer,Wakizashi,Neon) DONE — GREEN.** (RepurchaseList/TradeItem/TradePSItem already ported.) ExchangeItem: plain holder, item.SetItemCount. Exchange: Map→Dictionary, items.put→[]=, size>=18 full-check. TradeList: List<TradeItem>+LinkedHashMap<Integer,Long>→Dictionary<int,long>; CalculateBuyListPrice (PricesService.GetBuyPrice*count*modifier/100); CalculateAbyssRewardBuyList (AcquisitionType.equals→==, getType→GetType_(); double-cast AP math `(int)((requiredAp*count*modifier/100.0D)*VendorBuyModifier)/100` preserved verbatim; containsKey/get/put→ContainsKey/[]); SM_SYSTEM_MESSAGE.STR_MSG_NOT_ENOUGH_ABYSSPOINT. Red-tolerated: Item, Player.GetInventory/GetAbyssRank, PricesService, PacketSendUtility, Acquisition/AcquisitionType.
- **model/event ArcadeProgress (65L) + Headhunter (67L) (Estrayl,4.8) + model/broker/BrokerItemMask (208L; kosyachok,Simple,ATracer) DONE — GREEN.** ArcadeProgress: plain holder. Headhunter : IComparable<Headhunter>+IPersistable (compareTo→CompareTo kills-desc-then-lastUpdate; PersistentState→IPersistable.PersistentState; currentTimeMillis→UtcNow.ToUnixTimeMilliseconds). BrokerItemMask: per-instance-field enum w/(typeId,BrokerFilter,parent,childrenExist)→enum + BrokerItemMaskExtensions; static MaskData dict (~110 members, self-referencing parent as BrokerItemMask?); filter ctors (BrokerMinMaxFilter/BrokerContainsFilter/BrokerContainsExtraFilter/BrokerPlayerClassExtraFilter/BrokerRecipeFilter) + PlayerClass red-tolerated; GetId/IsMatches(filter.Accept)/IsChildrenMask(parent-chain walk via p.Value)/GetBrokerMaskById/HasChildren.
- **model/ingameshop/IGItem (88L; xTz) + model/account/PlayerAccountData (121L; Luno) DONE — GREEN.** IGItem: plain holder, signed byte→sbyte (category/subCategory/itemType/gift). PlayerAccountData: char-select holder; java.sql.Timestamp creation/deletionDate→DateTimeOffset? (getTime()/1000→ToUnixTimeMilliseconds()/1000); Collections.emptyList→new List; SetVisibleItems builds from equipment (ItemSlot.GetEquipmentSlotType→sbyte, skip 0); nested `record VisibleItem(byte,int,int,Integer)`→`record VisibleItem(sbyte SlotType,int ItemId,int GodStoneId,int? Color)`. CharacterBanInfo/PlayerCommonData/PlayerAppearance/BoundRadius/ItemSlot/Item red-tolerated. (Account.java 271L deferred to next tick.)
- **model/account/Account (271L; SoulKeeper,cura) DONE — GREEN.** Game-account holder. implements Iterable<PlayerAccountData>→IEnumerable<PlayerAccountData> (GetEnumerator over players.Values + explicit IEnumerable.GetEnumerator); signed byte accessLevel/membership→sbyte; java.sql.Timestamp lastStamp→DateTimeOffset?; Map.put return-old-value→TryGetValue+indexer-assign (addPlayerAccountData decrement-on-replace); race tally switches (ASMODIANS/ELYOS); equals(instanceof→is)/hashCode→Equals/GetHashCode; toString→ToString. Storage/AccountTime/CharacterPasskey/PassportsList/PlayerCommonData red-tolerated. **model/account dir complete (Account+PlayerAccountData; CharacterBanInfo/AccountTime/CharacterPasskey/PassportsList already ported or red-tolerated).**
- **model/actions/PlayerActions (79L; xTz) + model/legionDominion cluster: LegionDominionParticipantInfo (60L) + LegionDominionLocation (132L) (Yeats,Sykra) DONE — GREEN.** PlayerActions: static RIDE/IN_ROLL mode util; Object→object casts (RideInfo/InRoll); synchronized(rideObservers)→lock; Player public fields ride/inRoll + CreatureState/EmotionType/SM_EMOTION/ActionObserver red-tolerated. LegionDominionParticipantInfo: Timestamp→DateTimeOffset? (getTime()/1000→ToUnixTimeMilliseconds()/1000); Legion/LegionService red. LegionDominionLocation: TreeMap→SortedDictionary; stream filter/sorted(comparingInt(getPoints).reversed().thenComparingLong(getDate))→Where/OrderByDescending(Points).ThenBy(Date); groupingBy(getRank)→GroupBy.ToDictionary; Collections.emptyList→new List; synchronized reset→lock; LegionDominionDAO/templates/SM_LEGION_DOMINION_RANK red-tolerated.
- **model/instance/StageType (112L; xTz) DONE — GREEN.** Per-instance-field enum (id,type) for arena/PvP stage messages→enum + StageTypeExtensions (static StageData dict, 90 members; ids legitimately repeat across members e.g. START_STAGE_4_ROUND_1==START_ALTERNATIVE_STAGE_4_ROUND_1==38465, PASS_GROUP_STAGE_7==START_STAGE_7_ROUND_5==13789 — fine in member-keyed dict). GetId/GetType_() (getType→GetType_ avoids Object.GetType clash). Fully self-contained, no red deps.
- **model/instance/instanceposition: DisciplineInstancePosition (80L) + GloryInstancePosition (192L) (xTz) DONE — GREEN.** Both : GeneralInstancePosition (base already ported w/ Teleport(player,x,y,z,sbyte h) + abstract Port). Pure nested position/zone switch→Teleport coordinate tables; (byte)h→(sbyte)h; float literals preserved verbatim. Discipline 4×4, Glory 8×6 (all heading 0). Remaining instanceposition: ChaosInstancePosition (281L), HarmonyInstancePosition (280L) — same pattern, next tick.
- **model/instance/instanceposition: ChaosInstancePosition (281L) + HarmonyInstancePosition (280L) (xTz) DONE — GREEN.** Same coordinate-table pattern; transformed programmatically (Java port-body → C#: teleport→Teleport, (byte)→(sbyte), tab→4-space reindent) to avoid hand-transcription error in ~560 lines; brace-balanced 15/15 each, spot-verified. **instanceposition cluster COMPLETE (General base + Discipline/Glory/Chaos/Harmony + InstancePositionHandler iface).**
- **model/instance/instancescore: PvPArenaScore (250L, base) + HarmonyArenaScore (83L) + LegionDominionScore (123L) (xTz,Yeats) DONE — GREEN.** (DarkPoeta/Normal/PvpInstance/InstanceScore base already ported.) PvPArenaScore : InstanceScore<PvPArenaPlayerReward> — ctor switch (case null,default→C# default-return; sets bonusTime/positionSize/instancePosition per WorldMapType via Discipline/Glory/Harmony/Chaos InstancePosition just ported); Collections.addAll→AddRange; Map<Integer,Boolean>→Dictionary<int,bool> (Boolean.FALSE/TRUE→false/true); List.remove(index)→[i]+RemoveAt; synchronized setRndPosition/clearPosition→lock(this); signed byte buffId→sbyte; switch-expr canReward; getNpcBonusSkill hex literals (0x4E5732 etc) verbatim; currentTimeMillis→UtcNow.ToUnixTimeMilliseconds; IntSummaryStatistics→Select+Max/Min; GetRank/GetTotalPoints/Clear marked virtual for subclass override. HarmonyArenaScore : PvPArenaScore — stream filter.findFirst.orElse→Where.FirstOrDefault, sorted→OrderBy, mapToInt.sum→Sum, override GetRank/GetTotalPoints/Clear; HarmonyGroupReward/AGPlayer red-tolerated. LegionDominionScore : InstanceScore<InstancePlayerReward> — plain 12-field holder. **instancescore cluster COMPLETE.**
- **model/house: HouseBids (147L; Neon) + HouseRegistry (185L; Rolandas) DONE — GREEN.** (House.java 416L + HouseDoorState/PlayerScript still pending; HouseDecoration already ported.) HouseBids: AtomicInteger→Interlocked.Increment(ref static); **non-static inner Bid→nested class holding `outer` HouseBids ref** (GetListIndex/GetHouseObjectId/CalculateSaleRewardKinah via outer); **Java method `bid()` vs inner class `Bid` collide under PascalCase→method renamed `DoBid()`** (callers red-tolerated); synchronized→lock(this); currentTimeMillis→UtcNow.ToUnixTimeMilliseconds; removeAll(list)→RemoveAll(b=>list.Contains(b)); enclosing sets nested-private playerObjectId (internal field). HouseRegistry : IPersistable; LinkedHashMap→Dictionary; putIfAbsent(k,v)!=null→!TryAdd; map.remove(k,v)→TryGetValue+ReferenceEquals+Remove; generic `<T extends AionObject & Persistable>`→`where T:AionObject,IPersistable`; getType→GetType_(); instanceof→is; World.ForEachPlayer lambda. **KNOWN LIMITATION: Java HouseObject<?> wildcard→C# HouseObject<PlaceableHouseObject>** (C# HouseObject<T> invariant, no non-generic base) — red wildcard-erasure, `is UseableHouseObject<PlaceableHouseObject>` won't match other type args under C# invariance; flagged for convergence (a non-generic IHouseObject base would fix it). PartType/PlayerRegisteredItemsDAO/House.GetBuilding red-tolerated.
- **model/house/House (416L; Rolandas) DONE — GREEN.** extends VisibleObject : IPersistable. getName()→override Name property ("HOUSE_"+address.GetId()); getController()→new HouseController cast; getWorldType/getVisibleDistance overrides; java.sql.Timestamp acquiredTime/nextPay→DateTimeOffset?; EnumMap<SpawnType,Npc>→Dictionary; synchronized methods/blocks→lock(this)/lock(spawns); spawns.put return-old→TryGetValue+indexer; **@SuppressWarnings(fallthrough) setPersistentState→explicit `goto default`** (UPDATE_REQUIRED non-NEW falls through to assign); HouseDoorState.get→HouseDoorStates.Get((sbyte))→nullable local + ResetDoorState fallback; getId()<<8 perms; signed byte→sbyte (GetHouseOwnerStates, GetTeleportHeading, HouseDoorState.GetId); **apache DateUtils.round(DAY_OF_MONTH)→private RoundToDay helper (>=12:00 rounds up to midnight)**; TimeUnit.DAYS.toMillis(14)→14L*24*60*60*1000; currentTimeMillis/getTime→UtcNow/ToUnixTimeMilliseconds; Math.max→Math.Max. Red-tolerated: HouseController/HouseAddress/Building/HousingLand/Sale/HouseType/PlayerScripts/HouseOwnerState/SpawnType/HousesDAO/HouseScriptsDAO/PlayerRegisteredItemsDAO/GeoService/AuctionEndTask/TownService/PlayerService/IDFactory/PlayerAwareKnownList. **model/house dir: House+HouseBids+HouseRegistry done; HouseDecoration/HouseDoorState already ported; PlayerScript(43L) still pending.**
- **model/gameobjects/findGroup/ServerWideGroup (76L) DONE — GREEN.** (FindGroupEntry iface + GroupApplication already ported.) final→sealed; currentTimeMillis/1000→(int)(UtcNow.ToUnixTimeMilliseconds()/1000); GetMinLevel=members.Max(level), GetMaxLevel=members.Min(level) (faithful: Java's max-of-reversed-comparator picks lowest); getCurrentTeam() TemporaryPlayerTeam<?>→`var` (sidesteps wildcard).
- **DEFERRED this tick (flagged):** (1) **GroupRecruitment (105L)** — uses `object instanceof TemporaryPlayerTeam<?>` patterns repeatedly; C# TemporaryPlayerTeam<TM> is invariant w/ no non-generic base, so the wildcard match can't be expressed faithfully (chosen-bound would silently miss alliance vs group teams). Needs same non-generic-base refactor as HouseObject<?> — folded into wildcard convergence task. (2) **model/house/PlayerScript (43L)** — Java record w/ static text-block (UTF-16LE) feeding CompressUtil.compress + uncompressedSize=length (security-sensitive Lua sandbox fix); byte-exact reproduction of Java text-block whitespace-stripping needs care — defer until CompressUtil ported + a verified text-block→C# emit (don't risk a wrong byte).
- **model/templates/zone: MaterialZoneTemplate (39L) + WorldZoneTemplate (36L) (Rolandas,Neon) DONE — GREEN.** Both : ZoneTemplate (base ported; protected Java fields→public C# props Mapid/Flags/AreaType/ZoneType/Cylinder/Sphere/Semisphere/Points/XmlName). MaterialZoneTemplate: builds Cylinder/Semisphere/Sphere from geoEngine BoundingBox by name (CYLINDER/CONE/H_COLUME→Cylinder, SEMISPHERE→Semisphere, else Sphere); Math.sqrt→Math.Sqrt; Vector3f.x/y/z→X/Y/Z; AreaType.CYLINDER→AreaType.Cylinder (C# PascalCase enum). WorldZoneTemplate: 4-corner box; **Math.round(float)→(int)Math.Floor(x+0.5f)**; Point2D.x/y→X/Y; ZoneClassName.DUMMY→Dummy; Points.GetPoint() lazy-inits. geoEngine BoundingBox/Vector3f/Spatial + WorldConfig red-tolerated. **templates/zone dir complete.**
- **model/templates/itemgroups/FeedEntries (92L; Rolandas) + model/stats/calc/functions/StatArmorMasteryFunction (57L) + StatWeaponMasteryFunction (58L) (ATracer) DONE — GREEN.** FeedEntries: static container of 16 nested XML marker subclasses of ItemRaceEntry; @XmlType(name)→[XmlType(name)], @XmlAccessorType(FIELD)→dropped (no C# equiv). StatArmorMasteryFunction : StatRateFunction — switch-expr on ItemSlot (TORSO/PANTS/SHOULDER|GLOVES|BOOTS)→C# switch expr; base `value`→Value; truncating int div preserved; override Apply/GetValue. StatWeaponMasteryFunction : StatRateFunction — switch on base `stat`→Stat (MAIN_HAND_POWER/OFF_HAND_POWER/default); ArrayUtils.contains→LINQ Contains; .equals→.Equals; Rnd.get(0,v). **FIX: base StatFunction.GetValue() was non-virtual but Java getValue() is overridden→made `virtual`** (faithful correction). ItemSlot/ItemSubType/ItemGroup/Stat2/Player.GetEquipment red-tolerated.
- **model/stats/calc/functions/PlayerStatFunctions (337L; ATracer) DONE — GREEN.** Multi-class file: public PlayerStatFunctions (static{} FUNCTIONS list→static ctor; GetFunctions/AddPredefinedStatFunctions) + 16 package-private→internal function classes (Physical/MagicalAttack, MaxHp/MaxMp, PDef, Block/Parry/Evasion, Physical Critical/Accuracy, AttackSpeed/BoostCastingTime/PvP/PvE ratio; base DuplicateStatFunction). Each : StatFunction sets base `stat` field→Stat in ctor, overrides Apply(params CalculationType[])/GetPriority. Idioms: instanceof→is; ArrayUtils.contains→LINQ Contains; Rnd.get; stream.max(comparator).get()→MaxBy; functions.forEach→ForEach; Math.abs→Math.Abs; float literals (0.01f/10000f) verbatim. DuplicateStatFunction: main/off-hand+fusioned weapon modifier resolution via StatFunctionProxy.Validate. Red-tolerated: IStatFunction/StatFunctionProxy/Stat2(GetOwner/SetBaseRate/AddToBase/SetBonus/GetBonus/GetBase/AddToBonus)/Player.GetGameStats(~10 dependent-stat getters)/GetEquipment/ItemTemplate.GetModifiers/HasFusionedItem/WorldMapTemplate. **stats/calc/functions cluster COMPLETE.**
- **model/stats/listeners/ItemEquipmentListener (221L; xavier, Wakizashi) DONE — GREEN.** Static equip/unequip stat-listener. Idioms: Consumer<RandomBonusEffect>+varargs→Action+params; **CreatureGameStats<?> wildcard→non-generic CreatureGameStats base** (clean: base exists w/ AddEffect/EndEffect/AddEffectOnly/UpdateArmorMasteryStats); Set<? extends ManaStone>→ISet<ManaStone> (.isEmpty→Count==0); Collections.emptyList→new List; ExtractApplicableWeaponModifiers switch-with-continue (ATTACK_SPEED/PVP_ATTACK_RATIO/BOOST_CASTING_TIME skip) preserved; (int)(0.1f*...) truncation verbatim; ItemGroup ==-compares; currentTimeMillis→UtcNow.ToUnixTimeMilliseconds; buff-skill 15s-cooldown logic. NOTE: cgs.AddEffect param is List<IStatFunction> in ported C# base vs Java List<? extends IStatFunction> — List<StatFunction> passed directly (red-tolerated variance friction; convergence may widen AddEffect to IEnumerable<IStatFunction>). Red-tolerated: Item(~25 getters), IdianStone/ManaStone/RandomBonusEffect/TemperingEffect, ItemTemplate/WeaponStats/ItemSetTemplate/PartBonus/FullBonus, EnchantService/SkillLearnService/SM_SKILL_COOLDOWN, ItemSlot.MAIN_OR_SUB.GetSlotIdMask.
- **model/ingameshop/InGameShopEn (298L; KID, xTz) DONE — GREEN.** Singleton cash-shop service. Map<Byte,List<IGItem>>→Dictionary<sbyte,List<IGItem>> (signed Byte key→sbyte; category/subCategory bytes→sbyte); AtomicInteger lastRequestId→Interlocked.Increment(ref int); ConcurrentHashMap→ConcurrentDictionary (put→[]=, remove→TryRemove); **TreeMap+DescFilter(Comparator)→SortedDictionary<int,int> w/ nested DescFilter:IComparer<int>** (desc -CompareTo); Collections.emptyList→new List; getTopSales cnt<=max(6) 7-entry quirk preserved; equalsIgnoreCase→OrdinalIgnoreCase; new Timestamp(currentTimeMillis)→DateTimeOffset.UtcNow; finishRequest result 1-4 branches (error/not-enough/buy-or-gift/refund); IGRequest public fields accountId/requestId/playerId/itemObjId/gift/receiver/message. Red-tolerated: IGRequest, InGameShop{DAO,LogDAO,Property,Config}/Inventory/PlayerDAO, LoginServer+SM_PREMIUM_CONTROL/SM_ACCOUNT_TOLL_INFO, SM_TOLL_INFO/SM_MAIL_SERVICE/SM_SYSTEM_MESSAGE, ItemService/SystemMailService/PlayerService, player.GetClientConnection().GetAccount(). **model/ingameshop dir complete (IGItem+InGameShopEn; IGRequest red-tolerated).**
- **model/team/common/service/PlayerTeamCommandService (91L; ATracer) DONE — GREEN.** Dispatches TeamCommand→PlayerGroupService/PlayerAllianceService/LeagueService. **TemporaryPlayerTeam<? extends TeamMember<Player>> wildcard→TemporaryPlayerTeam<TeamMember<Player>>** — TRACTABLE here (no instanceof on the wildcard itself; matches Player.GetCurrentTeam()'s C# bound, codebase already invariance-fixed at TeamMember<Player>); contrast GroupRecruitment which needs the deferred non-generic base. switch on TeamCommand (stacked ALLIANCE_CHECKREADY_* cases); team instanceof PlayerAlliance→is PlayerAlliance pa; Objects.requireNonNull(x,supplier)→null-check throw ArgumentNullException; AssignType.PROMOTE/DEMOTE nested enum. PlayerGroup/Alliance/LeagueService + League/LeagueMember/PlayerAlliance red-tolerated.
- **model/team/common/service/PlayerTeamDistributionService (138L; ATracer, nrg) DONE — GREEN.** Group/league kill-reward (XP/DP/AP/drop) distribution. TemporaryPlayerTeam<?>→TemporaryPlayerTeam<TeamMember<Player>>; **nested Consumer<Player> PlayerTeamRewardStats→plain class w/ Accept(Player), passed as Action<Player> method group** to team.ForEach (preserves stateful accumulation across calls); league branch alli.GetLeague().GetMembers().ForEach(a=>a.ForEach(filteredStats.Accept)); instanceof→is PlayerAlliance alli; **Math.round(float)→(int)Math.Floor(x+0.5f); long*=float/int*=float (lossy-conversions)→explicit (long)/(int) narrowing casts**; (int)rewardAp/count cast-precedence preserved; winner.equals(team)→Equals; owner.GetAi().Ask(AIQuestion.REWARD_AP/REWARD_LOOT). Red-tolerated: StatFunctions(CalculateExperienceReward/DPReward/PvEApGained), AbyssPointsService, DropRegistrationService, DamageInfo<Player>, TeamDamageList, Npc.GetAi/GetObjectTemplate, Rates.XP_GROUP_HUNTING. **model/team/common/service dir complete.**
- **model/team/league/LeagueService (163L; ATracer) DONE — GREEN.** Static alliance-league lifecycle (invite/create/add/remove/expel/setLeader/disband/move/rules/kinah). ConcurrentHashMap<Integer,League>→ConcurrentDictionary<int,League> (put→[]=, remove→TryRemove); Objects.requireNonNull(x,msg)→null-check throw ArgumentNullException; IllegalArgumentException→ArgumentException; Collections.unmodifiableCollection(values)→ConcurrentDictionary.Values; .equals→.Equals; LeagueLeftEvent.LeaveReson.LEAVE/EXPEL nested enum (typo "Reson" preserved); CanInvite cascade of SM_SYSTEM_MESSAGE guards. Red-tolerated: League/LeagueMember, PlayerAlliance, ~10 League*Event classes, LootGroupRules/LootRuleType, SM_QUESTION_WINDOW/SM_SYSTEM_MESSAGE, player.GetResponseRequester. **model/team/league dir: LeagueService done; League/LeagueMember already ported.** Remaining team services: PlayerGroupService(224L), PlayerAllianceService(285L).
- **model/team/group/PlayerGroupService (224L; ATracer) DONE — GREEN.** Static group lifecycle (invite/create/add/remove/ban/disband/kinah/leader/mentoring/login-logout/offline-check). ConcurrentHashMap→ConcurrentDictionary; **AtomicBoolean.compareAndSet(false,true)→Interlocked.CompareExchange(ref int offlineCheckStarted,1,0)==0**; Objects.requireNonNull→ArgumentNullException; nested Runnable OfflinePlayerChecker→nested class w/ Run() + ForEachTeamMember(member=>...) lambda (TimeUtil.IsExpired offline-timeout); log.warn{}→LogWarning structured; PlayerLeavedEvent.LeaveReson.BAN/LEAVE_TIMEOUT (typo preserved); TeamKinahDistributionEvent<>→TeamKinahDistributionEvent<PlayerGroup>; TeamType.AUTO_GROUP / GroupEvent.UPDATE_EFFECTS. Red-tolerated: PlayerGroup/PlayerGroupMember, ~12 group event classes, PlayerRestrictions/FindGroupService/TimeUtil/ThreadPoolManager, SM_QUESTION_WINDOW/SM_SYSTEM_MESSAGE. **Remaining team service: PlayerAllianceService(285L).**
- **model/team/alliance/PlayerAllianceService (285L; ATracer) DONE — GREEN.** Static alliance lifecycle (invite/create/add/remove/ban/disband/leader/viceCaptain/memberGroup/checkReady/kinah/login-logout/offline-check) — mirrors PlayerGroupService idioms. ConcurrentHashMap→ConcurrentDictionary; AtomicBoolean.compareAndSet→Interlocked.CompareExchange; Objects.requireNonNull→ArgumentNullException; nested Runnable OfflinePlayerAllianceChecker→class w/ Run+ForEachTeamMember (Vortex invader/defender removal on offence/defence teamType + isAutoTeam 60s kick); TeamKinahDistributionEvent<>→<PlayerAlliance>/<PlayerAllianceGroup>; AssignViceCaptainEvent.AssignType param; TeamType.AUTO_ALLIANCE; LeaveReson.BAN/LEAVE_TIMEOUT. Red-tolerated: PlayerAlliance/PlayerAllianceMember/PlayerAllianceGroup, ~13 alliance/league event classes, VortexService/FindGroupService/PlayerRestrictions/TimeUtil/ThreadPoolManager. **TEAM-SERVICE CLUSTER COMPLETE (PlayerTeamCommandService/PlayerTeamDistributionService/LeagueService/PlayerGroupService/PlayerAllianceService); only model/team/alliance/PlayerAllianceService's GroupRecruitment-style wildcard sibling remains deferred.**
- **model/gameobjects/player/ResponseRequester (68L; Ben) DONE — GREEN + RequestResponseHandler non-generic base added.** Manages SM_QUESTION_WINDOW ask/respond. **Resolved the RequestResponseHandler<? extends Creature> wildcard INLINE** (small, well-understood, unblocks putRequest calls in the 4 team/league services just ported): added non-generic abstract `RequestResponseHandler` base declaring `Handle(Player,int)` (T-independent), made `RequestResponseHandler<T> : RequestResponseHandler` override it — Java has only the single generic class; the base is a C#-invariance accommodation = the wildcard equivalent. ResponseRequester: Map<Integer,RequestResponseHandler<?>>→ConcurrentDictionary<int,RequestResponseHandler> (non-generic); putIfAbsent==null→TryAdd; remove→TryRemove (Respond/Remove); denyAll iterate+Handle+Clear. NOTE: this is the 3rd wildcard-erasure case — HouseObject + TemporaryPlayerTeam still deferred in task_f13a70b1 (larger ripple); RequestResponseHandler done here since Handle is fully T-independent (zero ripple).
- **model/broker/BrokerPlayerCache (96L) + team invite handlers: PlayerGroupInvite (38L) + PlayerAllianceInvite (~70L) + LeagueInviteEvent (41L) (ATracer) DONE — GREEN.** BrokerPlayerCache: plain holder, signed byte→sbyte; BrokerItem red-tolerated. The 3 invite handlers : RequestResponseHandler<Player> — **now tractable thanks to last tick's non-generic RequestResponseHandler base**; acceptRequest/denyRequest→override AcceptRequest/DenyRequest; addAll→AddRange; IllegalArgumentException→ArgumentException; Predicates.Players.AllExcept; create-or-add group/alliance/league flows. PlayerGroupService/PlayerAllianceService/LeagueService/PlayerRestrictions red-tolerated.
- **BLOCKED VEIN (flagged):** the broader team-EVENT hierarchy (AbstractTeamPlayerEvent<T extends TemporaryPlayerTeam<?>> + ~30 PlayerGroup/Alliance/League *Event classes: Entered/Connected/Disconnected/Leaved/Update/Mentoring/ChangeLeader/etc.) is BLOCKED by the **TemporaryPlayerTeam<?> wildcard-in-generic-bound** — C# can't write `where T : TemporaryPlayerTeam<?>` and the concrete bound TemporaryPlayerTeam<TeamMember<Player>> excludes PlayerGroup:TemporaryPlayerTeam<PlayerGroupMember> under invariance. Needs the non-generic TemporaryPlayerTeam base from task_f13a70b1. Only the RequestResponseHandler-based invites (above) were extractable from this dir.
- **model/gameobjects/SummonedHouseNpc (50L; Rolandas) DONE — GREEN.** : SummonedObject<House> (both generic + non-generic SummonedObject base already ported; House just ported). getType(Creature)→**GetTypeValue(Creature)** (C# base name; avoids Object.GetType clash) returning CreatureType.FRIEND; IsEnemy(Creature)/IsEnemyFrom(Npc)/IsEnemyFrom(Player.Player) overrides→false (always friendly); GetCreatorId→GetCreator().GetAddress().GetId() (GetCreator returns House via generic `new T`); ctor sets MasterName(""-coalesced)/PlayerAwareKnownList/EffectController. `Player.Player` qualified (no `using ...Player` to avoid namespace/class ambiguity, matching Creature.cs). NpcController/EffectController/PlayerAwareKnownList red-tolerated.
- **model/gameobjects/NpcObject (56L; Rolandas) DONE — GREEN.** : HouseObject<HousingNpc> (concrete type arg — no wildcard, tractable despite HouseObject<?>-erasure issue elsewhere). synchronized spawn/onDespawn/canExpireNow→lock(this); base.Spawn/OnDespawn; CanExpireNow override (HouseObject declares Expirable default virtual per earlier note); SpawnEngine.NewSingleTimeSpawn→spawn housed npc at owner-house coords; (Npc) cast. OnUse(Player) bare (Player namespace imported). SpawnEngine/HousingNpc/HouseRegistry red-tolerated.
- **model/autogroup: AutoInstance (126L base) + AutoPvPFFAInstance (63L) + AutoPvpInstance (85L) (xTz, Estrayl) DONE — GREEN.** (AutoInstanceHandler/AGPlayer/AGQuestion/EntryRequestType already ported; AutoGroupType/LookingForParty red-tolerated.) AutoInstance : AutoInstanceHandler — Map→ConcurrentDictionary; **InstanceScore<?>→non-generic InstanceScore base** (clean); stream filter/collect→Where/ToList; List.sort(comparingInt(getExpireTime))→Sort((a,b)=>CompareTo); AGPlayer record p.race()→p.Race; values().iterator().next()→Values.First(); handler methods virtual for override; currentTimeMillis→UtcNow.ToUnixTimeMilliseconds; **GetMaxPlayers made virtual** (AutoPvpInstance overrides). AutoPvPFFAInstance: arena difficulty/item-fee (FFA/Solo/Glory)/portToPosition; Map.putAll→foreach add. AutoPvpInstance: **Java `TemporaryPlayerTeam<?> team` (group OR alliance)→C# `AionObject team`** since only GetObjectId() is used (GeneralTeam<M,TM>:AionObject) — sidesteps the wildcard cleanly; List.getFirst()→[0]; group-vs-alliance auto-team creation by race size. PvPArenaScore/AutoGroupService/PlayerGroup+AllianceService/instance handlers red-tolerated. Remaining autogroup: AutoHarmonyInstance(151L), AutoGroupType(385L), LookingForParty(99L).
- **model/autogroup: LookingForParty (99L) + AutoHarmonyInstance (151L) (xTz, Estrayl) DONE — GREEN.** LookingForParty : IComparable — stream map(AGPlayer::new)/collect(toMap)→Select/ToDictionary; Map.of→single-entry Dictionary; ert.ordinal()→(int)ert; compareTo (ert desc, then memberCount, then registrationTime); currentTimeMillis→UtcNow.ToUnixTimeMilliseconds; GetCurrentTeam (concrete bound, no wildcard). AutoHarmonyInstance : AutoInstance — 2 fixed race-groups; **Map.Entry<Integer,List<AGPlayer>>→KeyValuePair<int,List<AGPlayer>>? (nullable for getGroupEntry null-return)**; synchronized→lock(this); List.getFirst→[0]; (byte)7→(sbyte)7; AGPlayer record .race()/.objectId()→Race/ObjectId; HarmonyGroupReward creation/add; CanAddParty 3-per-group + race-match. HarmonyArenaScore/HarmonyGroupReward/PlayerGroupService/AutoGroupService red-tolerated. **autogroup cluster: AutoInstance+3 subclasses+LookingForParty done; only AutoGroupType(385L) remains.**
- **model/autogroup/AutoGroupType (385L; xTz, Estrayl) DONE — GREEN. autogroup cluster COMPLETE.** Java enum w/ per-instance fields + **per-constant abstract newAutoInstance() override + implements L10n**→enum + AutoGroupTypeExtensions. Static Data dict (template/maximumJoinTime/sbyte difficultId) built in static ctor via DataManager.AUTO_GROUP.GetTemplateByInstanceMaskId (eager, mirrors Java class-load; null→LogWarning); 33 members (3-arg difficultId / 2-arg default 0). **Per-constant factory→NewAutoInstance switch** dispatching AutoPvpInstance (dredgions+Kamar/Engulfed/IronWall/Idgel) / AutoHarmonyInstance (Harmony arenas + Haramonious centers) / AutoPvPFFAInstance (default: chaos/discipline/glory+training). 8 is* switch-exprs (`or`-patterns) preserved; static GetAGTByMaskId/GetAutoGroup(×2)/GetAutoGroupByWorld→Values() iteration (null→AutoGroupType?). **NOTE: C# enum can't implement L10n interface — GetL10nId is an extension method only (red polymorphism gap; convergence may need an L10n-bridging wrapper if AutoGroupType is passed as L10n anywhere).** AutoGroup/DataManager red-tolerated. Converges the `agt` field across AutoInstance+3 subclasses.
- **model/gameobjects/StaticDoor (87L; MrPoke, Rolandas) DONE — GREEN.** : StaticObject. EnumSet<StaticDoorState>→HashSet (ISet-compatible); EnumSet.noneOf→new HashSet; StaticDoorState.GetFlag() extension + static StaticDoorStateExtensions.SetStates(int,ISet); getObjectTemplate() narrowing override→`new StaticDoorTemplate` (cast base); SetOpen open/close (CLICKABLE/OPENED state toggles, GeoService.SetDoorState, SM_EMOTION OPEN_DOOR/CLOSE_DOOR, packetState 0x9/0xA verbatim); ChangeState state&0xF. StaticObjectController/GeoService/SM_EMOTION/StaticDoorTemplate red-tolerated.
- **model/gameobjects/UseableItemObject (221L; Rolandas, Neon) DONE — GREEN.** : UseableHouseObject<HousingUseableItem> (concrete type arg). Integer→int? throughout (useCount/requiredItem/cd/checkType/rewardId/finalRewardId/removeCount) with .Value at Java-unbox sites + lifted comparisons; nested PacketWriteHelper subclass UseDataWriter (WriteMe protected override, enclosing-type access); **anonymous Runnable use-delay task→Schedule(ct=>{...;return ValueTask.CompletedTask;},TimeSpan.FromMilliseconds(delayMs))** with early `return ValueTask.CompletedTask;`; requiredItem^removeCount XOR; UseableItemObject.this→this in lambda; **ServerTime.now().with(LocalTime.MAX).toEpochSecond()*1000→server-tz end-of-day (23:59:59) rebuilt from ServerTime.Now() DateTimeOffset** (ServerTime has no .With); currentTimeMillis→UtcNow.ToUnixTimeMilliseconds; full flowerpot/reward/cooldown use flow. Red-tolerated: PacketWriteHelper (unported network base), UseItemAction/HousingUseableItem/LimitType, UseableHouseObject-base increment/occupant/despawn methods, ItemService/SM_USE_OBJECT/SM_OBJECT_USE_UPDATE/SM_SYSTEM_MESSAGE, player cooldowns/equipment/inventory.
- **model/gameobjects/BrokerItem (364L; kosyachok) DONE — GREEN.** implements Comparable+Persistable→IComparable<BrokerItem>+IPersistable. java.sql.Timestamp→DateTimeOffset (**setNanos(0)→FromUnixTimeSeconds(ToUnixTimeSeconds()) truncation**; expireTime immutable so truncated-on-assign vs Java's in-place setNanos); TimeUnit.DAYS.toMillis(N)→N*24L*60*60*1000; @SuppressWarnings(fallthrough) setPersistentState→`goto default` (same pattern as House); **8 anonymous Comparator statics→IComparer<BrokerItem> via Comparer.Create lambdas** (private GetItemName/GetPiecePrice/GetItemLevel accessible from same-class lambdas; NAME_SORT_DESC==ASC preserved as in Java); ComparePossiblyNull<T>:IComparable<T> generic helper; string.compareTo→CompareOrdinal; getComparatoryByType(byte→sbyte, IllegalArgument→ArgumentException). Item/BrokerRace red-tolerated. **converges BrokerPlayerCache's BrokerItem[] dep + InGameShop/BrokerService.**
- **PHASE BOUNDARY: cleanly-portable non-event model leaves EXHAUSTED.** Only remaining model files are wildcard-blocked (GroupRecruitment + ~30 team-events on TemporaryPlayerTeam<?>) or text-block-deferred (PlayerScript). Pivoted to cross-package leaves.
- **restrictions/PlayerRestrictions (406L; lord_rex, Sippolo) DONE — GREEN.** Static can*-checks (UseSkill/InviteToGroup/InviteToAlliance/Attack/Trade/Chat/UseItem/ChangeEquip). instanceof-pattern→is-pattern (Player/Creature/PlayerAlliance/PlayerGroup binds); getType→GetType_(); ChatUtil.l10n→L10n; (byte)levelRestrict→sbyte; Gender/Race nullable→? compares; List.isEmpty→Count==0. **canInviteToTeam's TemporaryPlayerTeam<? extends TeamMember<Player>>→TemporaryPlayerTeam<TeamMember<Player>> (codebase bound); the 2 call-sites (CanInviteToGroup/Alliance passing PlayerGroup/PlayerAlliance) are red under C# invariance — red-tolerated per branch design, fixed by deferred TemporaryPlayerTeam non-generic base (task_f13a70b1).** Converges the PlayerRestrictions.CanInviteTo* calls in the just-ported team invite handlers/group services. Red-tolerated: skillengine (Skill/SkillTemplate/SkillType/TransformType/AbnormalState), GameServer/AutoGroupService/VortexService/ChatBanService/PlayerChatService/AuditLogger, player ~40 state methods, SM_ATTACK_RESPONSE/SM_SYSTEM_MESSAGE, ItemActions/ItemUseLimits/SkillPanel.
- **configs batch: ShutdownConfig (24L) + network/PffConfig (26L) + RankingConfig (36L) + AutoGroupConfig (50L) DONE — GREEN.** Followed established C# config idiom (public static class, fields w/ @Property defaultValue→field initializer, key in XML-doc). CronExpression/CronExpression[] (Quartz.NET, resolves) populated by config loader (no inline default — cron string in doc). @Properties keyPattern maps→Dictionary (PffConfig Dictionary<int,int> w/ GetAllowedMillisBetweenPackets→GetValueOrDefault; RankingConfig Dictionary<AbyssRankEnum,int> TOP_RANKING_QUOTA/GP_LOSS — converges AbyssRankEnum.GetGpLossPerDay/GetQuota). RankingConfig.XFORM_MIN_RANK=AbyssRankEnum.STAR5_OFFICER. AionClientPacket red-tolerated. (Remaining configs: CommandsConfig/HousingConfig/Config/NetworkConfig.)
- **configs: HousingConfig (71L; Rolandas) + administration/CommandsConfig (22L; Neon) DONE — GREEN.** HousingConfig: floats(0.3f/0.1f/0.5f/100f)/bools/ints + CronExpression/int[]/Dictionary<HouseType,int>; AUCTION_REGISTRATION_FEE/SALES_COMMISION converge HouseBids, *_MIN_BID converge House.GetDefaultAuctionPrice. CommandsConfig: Map<String,Byte>→Dictionary<string,sbyte> (signed); File[]→FileInfo[]. HouseType red-tolerated. **configs/main + administration largely done; remaining: Config(104L)/NetworkConfig(135L).**
- **world/zone instance cluster: FlyZoneInstance (40L) + NoFlyZoneInstance (34L) + PvPZoneInstance (33L) (MrPoke) DONE — GREEN.** All : ZoneInstance; synchronized onEnter/onLeave→lock(this)+override; super→base; instanceof(-pattern)→cast/is(-pattern); ZoneType.FLY/NO_FLY/PVP setInsideZoneType/unset/isInside; NoFly does fly-area enter/leave transitions on FLY-zone overlap. **FIX: base ZoneInstance.OnEnter/OnLeave were non-virtual but Java subclasses override→made `virtual`** (faithful, like StatFunction.GetValue earlier). Creature/Player.GetController.OnEnter/LeaveFlyArea red-tolerated.
- **world/WorldMapInstanceFactory (27L) + world/zone/ZoneUpdateService (39L) (ATracer) DONE — GREEN.** WorldMapInstanceFactory: static 2-overload factory; method-ref InstanceEngine.getInstance()::getNewInstanceHandler→method group; Function→Func<WorldMapInstance,InstanceHandler>; WorldMapType.RESHANTA.GetId() extension; RESHANTA→WorldMap3DInstance else WorldMap2DInstance. ZoneUpdateService : AbstractFIFOPeriodicTaskManager<Creature> (base ported) — base(500); override CallTask (RevalidateZones + ZoneLevelService.CheckZoneLevels for Player)/GetCalledMethodName; SingletonHolder→static readonly. InstanceEngine/WorldMap2D/3DInstance/ZoneLevelService red-tolerated.
- **world/zone/handler: QuestZoneHandler (54L; Rolandas) + ZoneHandlerClassListener (54L; MrPoke) DONE — GREEN.** (ZoneHandler iface→IZoneHandler, GeneralZoneHandler, ZoneNameAnnotation already ported.) QuestZoneHandler : GeneralZoneHandler — **getClass().getAnnotation(ZoneNameAnnotation.class)→GetType().GetCustomAttribute<ZoneNameAnnotation>()** (System.Reflection); annotation.questId()→.QuestId; **IncompleteAnnotationException→InvalidOperationException** (no C# equiv); Map→Dictionary; instanceof→is; abstract CreateObserver; AbstractQuestZoneObserver red-tolerated. ZoneHandlerClassListener : ClassListener (commons) — Class<?>[]→Type[]; **ZoneHandler.class.isAssignableFrom→typeof(IZoneHandler).IsAssignableFrom**; Modifier.isAbstract/isInterface/isPublic→Type.IsAbstract/IsInterface/IsPublic; getName→FullName; log.isDebugEnabled→IsEnabled(LogLevel.Debug). **FIX: GeneralZoneHandler.OnEnterZone/OnLeaveZone made virtual** (QuestZoneHandler overrides). ZoneService red-tolerated.
- **taskmanager/AbstractCronTask (120L; Rolandas, Neon) DONE — GREEN.** Base for housing cron tasks. implements Runnable→Run(); Quartz CronExpression.getTimeAfter→GetTimeAfter(.Value); java.util.Date→DateTimeOffset (getTime→ToUnixTimeMilliseconds, new Date()→UtcNow, new Date(ms)→FromUnixTimeMilliseconds); Semaphore→SemaphoreSlim(1,1) (acquireUninterruptibly→Wait, release→Release); SERVER_STOP_MILLIS Long→long? (ServerVariablesDAO.LoadLong); getClass().getSimpleName()→GetType().Name; ThreadPoolManager.ExecuteLongRunning(lambda); findLastPlannedRun binary-search-by-interval preserved. NOTE: shouldRunOnStart dropped Java's `lastPlannedRunBeforeServerStart != null` check (C# DateTimeOffset non-nullable; only read when cron!=null so always set — behavior-equivalent). ServerVariablesDAO/CronService/ThreadPoolManager red-tolerated. CronService.Schedule(this,...) passes `this` (Runnable) — red-tolerated if C# sig takes a delegate.
- **taskmanager/tasks/housing: AuctionEndTask (110L; Neon) + MaintenanceTask (95L; Rolandas,Neon) DONE — GREEN.** Both : AbstractCronTask singletons. AuctionEndTask: TimeUnit.MINUTES.toMillis→*60*1000; ConcurrentHashMap→ConcurrentDictionary; **Map.compute (null-return=remove) → lock(prolongedAuctions)+TryGetValue/indexer/TryRemove** (no atomic C# compute-with-removal); nested ProlongedAuction (Future→IScheduledFuture, task.Cancel(false), anonymous Runnable→Schedule(ct=>{...;ValueTask.CompletedTask;},TimeSpan)); shouldRunOnStart prolongation-window override; converges House.findGraceEndTime's AuctionEndTask.GetNextRunAfter. MaintenanceTask: Date/Timestamp→DateTimeOffset (after→>, before→<, getTime→ToUnixTimeMilliseconds, toInstant().plus(14,DAYS)→AddDays(14)); stream filter/collect→Where/ToList; Collections.emptyList→new List; impound/compensation(90%)/mail flow. HousingService/HousingBidService/MailFormatter/PlayerService red-tolerated.
- **taskmanager: housing/AuctionAutoFillTask (85L; Neon) + tasks/TemporaryTradeTimeTask (63L; Mr.Poke) DONE — GREEN.** AuctionAutoFillTask : AbstractCronTask singleton — Set→HashSet; **Collections.shuffle→in-place Fisher-Yates (static Random)**; groupingBy(getHouseType,counting())→GroupBy.ToDictionary(g=>g.Key,g=>(long)g.Count()); int[]{0} mutable-closure→plain captured int (C# allows); Map.forEach→foreach; removeIf→RemoveAll; iterator.remove→indexed RemoveAt; method-refs→lambdas/method groups. TemporaryTradeTimeTask : AbstractPeriodicTaskManager (override Run) — ConcurrentHashMap→ConcurrentDictionary, entrySet-iterator+remove→snapshot foreach+TryRemove, SingletonHolder→static readonly, currentTimeMillis→UtcNow.ToUnixTimeMilliseconds. HousingService/HousingBidService/Item/World red-tolerated.
- **taskmanager/tasks: TeamStatUpdater (46L; Sarynth) + LegionDominionIntruderUpdateTask (75L) DONE — GREEN.** TeamStatUpdater : AbstractFIFOPeriodicTaskManager<Player> (final→sealed, override CallTask/GetCalledMethodName) — converges PlayerGroupService.UpdateGroup/PlayerAllianceService.UpdateAlliance (GroupEvent/PlayerAllianceEvent.MOVEMENT). LegionDominionIntruderUpdateTask : AbstractPeriodicTaskManager (override Run) — stream filter/collect(groupingBy(getWorldId))→Where.GroupBy.ToDictionary; Map.Entry→KeyValuePair (Java `if(entry==null)` vacuous→dropped); isEmpty→Count==0, isEmpty(str)→Length==0; ZoneName.Get/NONE; getType→GetType_(); forEach→ForEach; conqueror/protector CPType classification. ConquerorAndProtectorService/CPInfo/CPType/LegionDominionService/SM_CONQUEROR_PROTECTOR red-tolerated. **taskmanager tasks vein ~complete (AbstractCronTask+3 housing + AbstractFIFO/Periodic subclasses).**
- **DEFERRED (framework-rewrite-coupled):** utils/xml/XmlValidationHandler + StringSchemaOutputResolver (JAXB ValidationEventHandler/SchemaOutputResolver — part of flagged JAXB→XmlSerializer pillar, no 1:1 C# equiv); utils/cron/ThreadPoolManagerRunnableRunner (extends unported RunnableRunner; Java Runnable model vs C# async-delegate ThreadPoolManager — cron-infra rewrite). Pivoted to dataholders.
- **dataholders/VortexData (51L; Source) DONE — GREEN.** Textbook XML-dataholder: @XmlRootElement(dimensional_vortex)→[XmlRoot]; @XmlElement(vortex_location)→[XmlElement]; @XmlTransient LinkedHashMap<Integer,VortexLocation>→[XmlIgnore] Dictionary built in AfterUnmarshal(object parent) [afterUnmarshal(Unmarshaller,parent)→AfterUnmarshal(object)]; vortex.put→[]=. **Converges VortexLocation** (just ported). VortexTemplate red-tolerated.
- **dataholders/WalkerData (79L; KKnD, Rolandas) DONE — GREEN.** XML-dataholder: @XmlRootElement(npc_walker)→[XmlRoot]; @XmlTransient Map<String,WalkerTemplate>→[XmlIgnore] Dictionary built in AfterUnmarshal (putIfAbsent!=null→!TryAdd dup-warn, walkerlist cleared+nulled); GetWalkerTemplate/AddTemplate/GetTemplates. **saveData JAXB-marshal transcribed LITERALLY** (JAXBContext.NewInstance/Marshaller.CreateMarshaller/SetSchema/SetProperty(JAXB_FORMATTED_OUTPUT)/Marshal + JAXBException all RED-tolerated — most-faithful 1:1; resolved by JAXB→XmlSerializer pillar) + finally cleanup; File→FileInfo; XmlUtil.GetSchema red. WalkerTemplate red-tolerated.
- **dataholders/MotionData (97L; kecimis) DONE — GREEN.** XML-dataholder + animation-time calc. @XmlRootElement(motion_times)→[XmlRoot]; @XmlTransient Map<String,MotionTime>→[XmlIgnore] Dictionary (final→readonly) built in AfterUnmarshal (motionTimes nulled); GetMotionTime(string/Skill overloads); CalculateAnimationTimeUntilFirstHit/AfterLastHit (instanceof ChargeSkill→is; Math.max/min→Math.Max/Min; (int) casts + float motionSpeed*rate math preserved; isHitTimeBoosted/isInRobotMode boost logic); GetAttackRate/CalculateCastSpeedRate; nested record AnimationTimes(int,int). MotionTime/ChargeSkill green; Skill/SkillTemplate/Motion/Times/Stat2 red-tolerated.
- **dataholders/NpcData (110L; Luno) DONE — GREEN.** NpcTemplate container. @XmlRootElement(npc_templates)→[XmlRoot]; @XmlTransient Map<Integer,NpcTemplate>+Set<Integer>→[XmlIgnore] Dictionary/HashSet (final→readonly); **afterUnmarshal→StaticDataListener.RegisterForAsyncExecutionOrRun(parent, Init)** (Java u→C# parent; this::init→Init method group; StaticDataListener red-tolerated/unported); Init derives missing NPC combat stats via NpcStatCalculation.CalculateStat (attack/acc/matk/macc/mres/mdef/mcrit=50/pcrit=10/pdef/parry/spellResist[lvl>=50]/strikeResist[<=700 cap]/stunLike) unless PET/PET_DARK; signed byte level→sbyte; DialogAction.NameOf dialog-validation warn; addAll→UnionWith; npcs nulled. NpcStatCalculation/StatsTemplate/NpcRating/NpcRank red-tolerated.
- **dataholders/ZoneData (120L; ATracer) DONE — GREEN.** @XmlRootElement(zones)/@XmlType("")→[XmlRoot]/[XmlType("")]; @XmlTransient Map<int,List<ZoneInfo>>/Map<ZoneTemplate,int>/count→[XmlIgnore]; afterUnmarshal switch on AreaType (Polygon→PolyArea, Cylinder→CylinderArea, Sphere→SphereArea[R>0], Semisphere→SemisphereArea — all geometry areas green) builds ZoneInfo per zone + weather-zone numbering (ZoneClassName.Weather, per-map reset); Map.get/put→TryGetValue/[]=; getWeatherZoneId Integer-null→GetValueOrDefault(0); saveData JAXB-marshal literal (red-tolerated). ZoneTemplate/ZoneInfo green; XmlUtil/JAXB red-tolerated. **converges ZoneData.ZoneList (referenced by KillInZone handler).**
- **dataholders/PlayerInitialData (162L; Aquanox) DONE — GREEN.** New-player data table w/ nested static classes. @XmlRootElement→[XmlRoot]; @XmlTransient Map<PlayerClass,PlayerCreationData>→[XmlIgnore] Dictionary in AfterUnmarshal; nested PlayerCreationData{ItemsType,ItemType}/LocationData; @XmlAttribute→[XmlAttribute]; signed byte heading→sbyte; Collections.unmodifiableList→AsReadOnly; getSpawnLocation switch (IllegalArgument→ArgumentException); ItemType.toString StringBuilder preserved. NOTE: @XmlElement(required=true) dropped (no C# equiv); **@XmlIDREF on ItemType.template (JAXB cross-ref ItemTemplate by id) has no XmlSerializer equivalent — flagged for JAXB→XmlSerializer pillar.** ItemTemplate red-tolerated. **dataholders/* root largely complete (Vortex/Walker/Motion/Npc/Zone/PlayerInitialData); remaining: loadingutils/StaticDataListener + adapters/NpcEquippedGearAdapter.**
- **dataholders/loadingutils/adapters/NpcEquippedGearAdapter (23L; Luno) DONE — GREEN.** JAXB XmlAdapter<NpcEquipmentList,NpcEquippedGear>→plain class (no C# base, per established SpaceSeparatedBytesAdapter idiom); Marshal(stub null)/Unmarshal(new NpcEquippedGear(v)); throws Exception dropped. NpcEquipmentList(same-package)/NpcEquippedGear red-tolerated.
- **DEFERRED: dataholders/loadingutils/StaticDataListener (37L)** — extends JAXB Unmarshaller.Listener (unmarshaller-lifecycle mechanism: get(Unmarshaller)/registerForAsyncExecutionOrRun); no C# XmlSerializer equivalent → JAXB→XmlSerializer pillar (already red-tolerated by NpcData's AfterUnmarshal). **dataholders package now ~done except StaticDataListener (deferred).**
- **controllers/attack: TeamDamageList (50L) + KillCounter (35L; Neon) DONE — GREEN. (cross-package pivot to controllers.)** TeamDamageList: **TemporaryPlayerTeam<?>→TemporaryPlayerTeam<TeamMember<Player>>** (codebase bound; converges PlayerTeamDistributionService.GetMostDamageByTeam caller + Player.GetCurrentTeam); HashMap.compute(always-non-null)→TryGetValue+assign; computeIfAbsent(DamageInfo::new)→TryGetValue+ctor; team-as-AionObject key (GeneralTeam:AionObject); (DamageInfo<Player>)→(DamageInfo<Player>)(object) cast; stream max/sum→MaxBy/Sum; ctor internal. KillCounter: nested ConcurrentDictionary<int,Dictionary<int,List<long>>>; computeIfAbsent→GetOrAdd (outer) / TryGetValue+init (inner under lock); synchronized→lock; removeIf→RemoveAll; currentTimeMillis→UtcNow.ToUnixTimeMilliseconds. DamageInfo<T>/DamageList/CustomConfig red-tolerated.
- **controllers: TrapController (20L) + PlaceableObjectController (37L; Rolandas) + observer/AbstractQuestZoneObserver (58L; Rolandas) DONE — GREEN.** TrapController : NpcController (override OnDie/OnDelete→TrapService.UnregisterTrap+base). PlaceableObjectController<T:PlaceableHouseObject> : VisibleObjectController<HouseObject<T>> — **Java `instanceof UseableHouseObject<?>`→`is UseableHouseObject<T>`** (class type param sidesteps the HouseObject wildcard cleanly); OnDespawn/OnDialogRequest(talk-range)/NotKnow(releaseOccupant). AbstractQuestZoneObserver : ActionObserver — **AtomicBoolean→int+Interlocked.Exchange** (getAndSet(true)→Exchange(ref,1)==0, set(false)→Exchange(ref,0)); anonymous Runnable move-handler→Execute(ct=>{try{Vector3f distance/center calc;OnMoved}finally{reset};ValueTask}); Vector3f.clone→Clone; currentTimeMillis→UtcNow.ToUnixTimeMilliseconds. TrapService/Vector3f/PositionUtil red-tolerated. **AbstractQuestZoneObserver converges QuestZoneHandler.CreateObserver return type.**

## fad08cb40 — neuralnetwork (Link, PlayerModelLink, PlayerModel, PlayerModelController)
Ported the custom/instance/neuralnetwork backprop net (Jo). Sigmoid/DataSet already existed (auto-commit loop). Idioms: `Double`→`double?` (null-default learnRate .4/momentum .9, `.Value` unbox); varargs `double...`→`params double[]`; `forEach(PlayerModelLink::calculateValue)`→`List.ForEach(a=>a.CalculateValue())`; `calculateGradient(null)`→single `double?` overload; `currentTimeMillis`→`UtcNow.ToUnixTimeMilliseconds`; slf4j named logger "CUSTOM_INSTANCE_LOG"→`NullLoggerFactory.Instance.CreateLogger("CUSTOM_INSTANCE_LOG")` (matches CollectionUtil ILogger convention); `String.format %d`→`string.Format {N}` (behavior-faithful, not literal `%d`); `executeLongRunning(Runnable)`→`ExecuteLongRunning(ct=>{...;return ValueTask.CompletedTask;})`; `Rnd.nextDouble`→`Rnd.NextDouble`. Red-tolerated deps: CustomInstanceService, **PlayerModelEntry (289L, next tick)**. Guardrail green (363/5).
DEFERRED this tick: material-skill-actor observer subtree (AbstractCollisionObserver→AbstractMaterialSkillActor→Zone/TerrainZoneCollisionMaterialActor) — multi-class geoEngine-coupled chain, needs focused multi-tick effort.

## (next) — PlayerModelEntry (completes custom/instance/neuralnetwork)
Ported PlayerModelEntry (289L, Jo) implements Persistable→IPersistable. Idioms: java.sql.Timestamp→DateTimeOffset (`new Timestamp(currentTimeMillis())`→`FromUnixTimeMilliseconds(UtcNow.ToUnixTimeMilliseconds())` ms-precision); `instanceof Player`→`is Player`; chained assignment preserved; `input.stream().mapToDouble(Double::doubleValue).toArray()`→`List<double>.ToArray()`; `skillSet.stream().mapToDouble(skillId->...).toArray()`→`Select(...).ToArray()`; `PersistentState.NEW/UPDATED`→`IPersistable.PersistentState.*`. Red-tolerated: Creature/Player/EffectController/LifeStats, skillengine (Effect/AbnormalState/SkillSubType), PositionUtil. **neuralnetwork package now COMPLETE** (Link, DataSet, Sigmoid, PlayerModelLink, PlayerModel, PlayerModelController, PlayerModelEntry). Guardrail green (363/5). Next vein: custom/instance/* (CustomInstanceService, RoahCustomInstanceHandler, pvpmap/*) or spawnengine leaves.

## (next) — PvpMapService + ChatUtil.Color(Color) overload
Ported custom/pvpmap/PvpMapService (73L, Yeats) singleton. Restored the dropped `ChatUtil.Color(string, java.awt.Color)` overload as `Color(string, System.Drawing.Color?)` (null→White; calls 3-int overload via c.R/G/B) so the call site `ChatUtil.Color(".pvp join", Color.White)` stays literal. Idioms: `PvpMapHandler::new`→`() => new PvpMapHandler()`; `java.awt.Color.WHITE`→`System.Drawing.Color.White`. Verified the three CustomInstanceRank* leaves already exist & are faithful (enum-with-fields + PUA char glyphs + getByRank). Red-tolerated: PvpMapHandler (858L), InstanceService, WorldMapInstance. Guardrail green (363/5). Next: CustomInstanceService (183L) or PvpMapHandler/RoahCustomInstanceHandler (larger handlers).

## (next) — CustomInstanceService (custom/instance singleton)
Ported custom/instance/CustomInstanceService (183L, Jo/Estrayl). Idioms: `Arrays.asList(...)`→List initializer; `ConcurrentHashMap`→`ConcurrentDictionary` (computeIfAbsent→GetOrAdd, remove→TryRemove out-param); **ServerTime.now() ZonedDateTime→DateTimeOffset** — `with(LocalTime.of(RESET_HOUR,0))`→`new DateTimeOffset(now.Year,Month,Day,9,0,0,now.Offset)`, `isBefore`→`<`, `minusDays(1)/minusSeconds(1)`→`AddDays(-1)/AddSeconds(-1)`, `toEpochSecond()*1000`→`ToUnixTimeSeconds()*1000`; `Persistable.NEW` predicate→`IPersistable.New(e)`; `instanceof Creature creature`→`is Creature creature`; `RoahCustomInstanceHandler::new`→`()=>new RoahCustomInstanceHandler()`; Java text block→C# raw string literal (HTML leaderboard; whitespace insignificant, trailing `</tr>` newline preserved via blank line); SingletonHolder idiom kept (nested static class). Red-tolerated: CustomInstanceDAO/CustomInstancePlayerModelEntryDAO (dao pillar), HTMLCache/HTMLService, InstanceService/WorldMapInstance, TeleportService, RoahCustomInstanceHandler (431L). Guardrail green (363/5). Next: RoahCustomInstanceHandler (431L) or PvpMapHandler (858L) — the two remaining custom handlers, or pivot to spawnengine.

## (next) — RoahCustomInstanceHandler (completes custom/instance)
Ported custom/instance/RoahCustomInstanceHandler (431L, Jo/Estrayl) : GeneralInstanceHandler. Idioms: AtomicLong→long+Interlocked (compareAndSet(0,now)→CompareExchange(ref,now,0)==0; get→Interlocked.Read); AtomicBoolean→int (compareAndSet(false,true)→CompareExchange(ref,1,0)==0; get→Volatile.Read==0); Future<?>→ScheduledTask, schedule/scheduleAtFixedRate→Schedule/ScheduleAtFixedRateTask(ct=>{...;return ValueTask.CompletedTask;}, TimeSpan.FromMilliseconds(...)), cancel(true)→Cancel(); Java switch-arrows (multi-label case A,B,C->)→C# case fallthrough+break; nested switch-expression (gender)→C# switch expr w/ `_=>bossID`; setAverageDps switch-expr w/ `or` patterns; Math.round(float)→(int)Math.Floor(x+0.5f); String.format %d/%s/%b/%.3f→string.Format {N}/{N:F3}; Map.get→GetValueOrDefault; instanceof Player effector→is Player effector; getRegisteredObjects().iterator().next()→.First(); static imports→qualified (CustomInstanceService.REWARD_COIN_ID, SM_SYSTEM_MESSAGE.STR_MSG_INSTANCE_START_IDABRE()). Base members instance/mapId/log/spawn/deleteAliveNpcs + many services (Drop/Event/Item/Player/PlayerDAO/SM_* packets/StatSetFunction) red-tolerated. **custom/instance package COMPLETE** (rank leaves, service, neuralnetwork, RoahHandler). Guardrail green (363/5). Next: PvpMapHandler (858L) to finish custom/, or pivot to spawnengine.

## (next) — PvpMapHandler (completes custom/ package)
Ported custom/pvpmap/PvpMapHandler (858L, Yeats) : GeneralInstanceHandler. Largest file this session (mostly literal spawn-coordinate data). Idioms: HashMap→Dictionary; Future<?>→ScheduledTask (cancel(true)→Cancel(), isCancelled()→IsCancelled property); schedule/scheduleAtFixedRate (incl TimeUnit.MINUTES→TimeSpan.FromMinutes)→Schedule/ScheduleAtFixedRateTask(ct=>{...;return ValueTask.CompletedTask;}); **anonymous ItemUseObserver{abort}→private nested sealed PvpMapTeleportObserver:ItemUseObserver** (C# has no anon classes); synchronized methods→lock(this); Math.toRadians(x)→x*Math.PI/180; Float.isNaN→float.IsNaN; Map.get→indexer, Map.remove(k)→Remove(k, out v); switch-expr on string→C# switch expr; enum.name()→ToString(); method ref `::notifyBossSpawn`→lambda; currentTimeMillis→UtcNow.ToUnixTimeMilliseconds; Rnd.get(list/array)→Rnd.Get. Red-tolerated: GeneralInstanceHandler base (instance/mapId/spawn/spawnAndSetRespawn/deleteAliveNpcs/sendMsg), CronService (unported), SpawnEngine/StaticDoorSpawnManager, World/GeoService/SiegeService/PvpService/PlayerReviveService/BindPointTeleportService/TeleportService, SM_* packets, ItemUseObserver/ActionObserver. **custom/ package COMPLETE** (instance + pvpmap). Guardrail green (363/5). Next vein: spawnengine (SpawnEngine, StaticDoorSpawnManager, WalkerFormations, etc.) or instance handlers pillar.

## (next) — spawnengine: StaticDoorSpawnManager, WorldWalkerFormations, WalkerFormationsCache
Pivoted to spawnengine. Ported 3 leaves: StaticDoorSpawnManager (36L, MrPoke; slf4j logger→ILogger NullLoggerFactory; log.info→LogInformation; referenced by the just-ported PvpMapHandler), WorldWalkerFormations (30L) + WalkerFormationsCache (29L) (Rolandas; ConcurrentHashMap→ConcurrentDictionary, Map.get+null-check+put→TryGetValue+set, package-private class→internal, Java protected method→internal for same-namespace cross-class access). Red-tolerated: SpawnEngine, StaticObjectController/StaticDoor/PlayerAwareKnownList, GeoService, DataManager, **InstanceWalkerFormations (161L, next)**. Already-ported in package: SpawnHandlerType, TemporarySpawnEngine, WalkerGroupShift, WalkerGroupType. Guardrail green (363/5). Next spawnengine: InstanceWalkerFormations (161L), WalkerFormator (71L), ClusteredNpc (117L), WalkerGroup (306L), then SpawnEngine (230L)/VisibleObjectSpawner (284L) keystones.

## (next) — InstanceWalkerFormations (spawnengine)
Ported spawnengine/InstanceWalkerFormations (161L, Rolandas) — per-instance walker-group orchestration (the type the two formation caches return). Idioms: slf4j logger→ILogger (warn→LogWarning); HashMap→Dictionary (Map.get→GetValueOrDefault returns null, put→indexer); synchronized methods→lock(this); `stream().collect(groupingBy(positionHash))`→`GroupBy(...).ToDictionary(g=>g.Key, g=>g.ToList())`; Entry iteration→KeyValuePair; `filter(...).collect(toList())`→`Where(...).ToList()`; `List.add` returning bool→Add+`return true`; Map.values()→.Values; Rnd.get→Rnd.Get. Java `protected`/`protected synchronized`→`protected internal` (+lock). Red-tolerated: ClusteredNpc (117L), WalkerGroup (306L), DataManager.WALKER_VERSIONS_DATA. Guardrail green (363/5). Next spawnengine: WalkerFormator (71L), ClusteredNpc (117L), then WalkerGroup (306L)/SpawnEngine (230L)/VisibleObjectSpawner (284L).

## (next) — spawnengine: ClusteredNpc + WalkerFormator
Ported ClusteredNpc (117L, vlog/Rolandas) : WalkerGroupShift — spawn-info holder for walker grouping. Float.floatToIntBits→BitConverter.SingleToInt32Bits (positionHash); getWalkerIndex() Integer→int?; super(0,0)→base(0,0) (WalkerGroupShift float,float ctor). And WalkerFormator (71L) — static walker-group lifecycle facade over WalkerFormationsCache/InstanceWalkerFormations (the protected internal methods ported last tick are reachable same-assembly). slf4j→ILogger; log.warn→LogWarning. Red-tolerated: WalkerGroup (306L, only remaining walker piece), Npc/RouteStep/WalkerTemplate, DataManager.WALKER_DATA. Guardrail green (363/5). Next spawnengine: WalkerGroup (306L) closes the walker subsystem, then SpawnEngine (230L)/VisibleObjectSpawner (284L) keystones.

## (next) — WalkerGroup (closes spawnengine walker subsystem)
Ported spawnengine/WalkerGroup (306L, vlog/Rolandas) — formation geometry (SQUARE line/multi-row layout, getLinePoint trig). Idioms: `Comparator.comparing(getWalkerIndex, nullsLast(naturalOrder())).reversed()`→`OrderBy(x=>x, Comparer.Create(...))` with explicit nulls-last + negated cmp; `IntStream.of(rows).sum()`→`rows.Sum()`; `stream().mapToDouble(...).sum()`→`Sum(cNpc=>(double)...)` then (float) cast; `Math.signum`→`Math.Sign`; `synchronized(members)`→`lock(members)`; **Java `Integer==Integer` (walker indices, small/cached → value-eq in practice)→C# `int?` value equality** (faithful runtime behavior, noted); getType()→GetType_() idiom; slf4j→ILogger. Preserved the Java despawn() quirk (form()+reset inside the per-member foreach). Red-tolerated: AI (NpcAI/AISubState/AILogger/WalkManager), Npc/SpawnTemplate/RouteStep/WalkerTemplate, Point2D. **spawnengine walker subsystem COMPLETE** (WalkerGroup/ClusteredNpc/WalkerFormator/InstanceWalkerFormations/WorldWalkerFormations/WalkerFormationsCache/WalkerGroupShift/WalkerGroupType). Guardrail green (363/5). Next spawnengine: StaticObjectSpawnManager (55L), then keystones SpawnEngine (230L)/VisibleObjectSpawner (284L).

## (next) — StaticObjectSpawnManager (spawnengine)
Ported spawnengine/StaticObjectSpawnManager (55L, ATracer) — straight transcription (pool-spot reserve vs. direct spawn-template loop). Red-tolerated: StaticObject/StaticObjectController, World, DataManager.ITEM_DATA, SpawnGroup/SpawnTemplate, PlayerAwareKnownList, VisibleObjectTemplate. Guardrail green (363/5). spawnengine remaining: **SpawnEngine (230L)** and **VisibleObjectSpawner (284L)** keystones (the two big dispatch classes), then the package is done.

## (next) — SpawnEngine (spawnengine keystone)
Ported spawnengine/SpawnEngine (230L, multi-author) — the NPC spawn-management dispatch keystone (newSpawn factories, spawnObject routing by template subtype, spawnInstance/spawnAll, pool handling). Idioms: instanceof X→`is X` + cast; enum switch-arrows (HandlerType RIFT/STATIC)→switch statement; IllegalArgumentException→ArgumentException; slf4j→ILogger; inner `Consumer<VisibleObject>` StatsCollector→nested class with `Accept` passed as Action method-group to ForEachObject; Java method name `forEachParalllel` (triple-l typo) preserved for parity; `'['+name` char+string concat kept. Wired to the just-ported StaticDoorSpawnManager/StaticObjectSpawnManager/WalkerFormator/TemporarySpawnEngine. Red-tolerated: VisibleObjectSpawner (284L, **last spawnengine file**), World/DataManager/RiftManager/HousingService, Rift/Siege/Vortex spawn templates. Guardrail green (363/5). Next: VisibleObjectSpawner (284L) finishes the spawnengine package.

## (next) — VisibleObjectSpawner (completes spawnengine package)
Ported spawnengine/VisibleObjectSpawner (284L, ATracer) — object-spawn factory (npc/rift/siege/invasion/gatherable/trap/groupgate/kisk/postman/functional/servant/homing/summon/pet). Idioms: slf4j parameterized `log.error("...{}", x)`→`LogError("...{NpcId}", x)`; `log.error(msg, ex)`→`LogError(ex, msg)`; Math.toRadians→*Math.PI/180; `"siege_weapon".equals(x)`→.Equals; protected static→internal static (SpawnEngine package access). Red-tolerated: many controllers/models/knownlists, GeoService.GetClosestCollision, IgnoreProperties/CollisionIntention (geoEngine), RiftService/SummonOwner/SkillTemplate. **spawnengine package COMPLETE** (14 files: SpawnEngine, VisibleObjectSpawner, walker subsystem ×8, Static{Door,Object}SpawnManager, TemporarySpawnEngine, SpawnHandlerType). Guardrail green (363/5). Next vein: instance handlers pillar (InstanceEngine/GeneralInstanceHandler — base of the custom handlers ported earlier), or geoEngine, or services.

## (next) — IInstanceHandler (instance/handlers interface)
Pivoted to the instance-handlers pillar (base of the custom handlers ported earlier). Ported instance/handlers/InstanceHandler (107L, ATracer) as IInstanceHandler (IPersistable I-prefix convention) — 40-method contract. `InstanceScore<?> getInstanceScore()`→non-generic `InstanceScore GetInstanceScore()` (wildcard erasure via the non-generic base, per established resolution). Red-tolerated: StageList/StageType/InstanceScore, ZoneInstance, Effect/Skill. Guardrail green (363/5). Next: **GeneralInstanceHandler (292L)** — the concrete base (implements IInstanceHandler) that RoahCustomInstanceHandler/PvpMapHandler extend; converges those red `: GeneralInstanceHandler` bases + the inherited instance/mapId/spawn/log members. Then InstanceEngine (67L), InstanceHandlerClassListener (52L).

## (next) — GeneralInstanceHandler (instance-handler base) + Roah logger convergence
Ported instance/handlers/GeneralInstanceHandler (292L, ATracer) : IInstanceHandler — the concrete base ALL instance handlers (incl. the custom RoahCustomInstanceHandler/PvpMapHandler ported earlier) extend. This **converges their red `: GeneralInstanceHandler` bases** and inherited members (instance/mapId/log fields; Spawn/SpawnAndSetRespawn/GetNpc/DeleteAliveNpcs/SendMsg methods). All interface methods made `public virtual` (Java overridable). Idioms: slf4j "INSTANCE_LOG"→`protected static readonly ILogger log` (info/warn→LogInformation/LogWarning; parameterized `{}`→named placeholders; `Collectors.joining(", ")`→string.Join); UnsupportedOperationException→NotSupportedException; getClass()→GetType()/typeof; InstanceScore<?>→non-generic InstanceScore; field names kept lowercase (instance/mapId/log) to match handler references. **Convergence fix**: RoahCustomInstanceHandler `log.Info(...)`→`log.LogInformation(...)` + added `using Microsoft.Extensions.Logging;` (base log is now ILogger). Red-tolerated: World/WorldMapInstance, Item/Storage/Inventory, NpcRating, DataManager, SpawnEngine (green now). Guardrail green (363/5). Next: InstanceEngine (67L), InstanceHandlerClassListener (52L) finish the instance package root.

## (next) — InstanceEngine + InstanceHandlerClassListener (completes instance package)
Ported instance/InstanceEngine (67L) + InstanceHandlerClassListener (52L) (ATracer). InstanceEngine: `: GameEngine` (async-interface satisfaction red-tolerated per QuestEngine precedent); Map<Integer,Class<? extends InstanceHandler>>→Dictionary<int,Type>; `getAnnotation(InstanceID.class)`→`GetCustomAttribute<InstanceID>()` (+System.Reflection); `getDeclaredConstructor(WorldMapInstance.class).newInstance(instance)`→`Activator.CreateInstance(handlerClass, instance)` cast IInstanceHandler; ScriptManager/AggregatedClassListener/OnClassLoadUnloadListener wiring; SingletonHolder; addInstanceHandlerClass internal. ClassListener impl: Class<?>[]→Type[]; getName()→FullName; isAssignableFrom→typeof(IInstanceHandler).IsAssignableFrom; Modifier.isAbstract/isInterface/isPublic→Type.IsAbstract/IsInterface/IsPublic; log.isDebugEnabled→IsEnabled(LogLevel.Debug). Red-tolerated: ScriptManager (commons compiler pillar), GameEngine (divergent async). **instance package COMPLETE** (IInstanceHandler, GeneralInstanceHandler, InstanceID, InstanceEngine, InstanceHandlerClassListener). Guardrail green (363/5). Next vein: services pillar, geoEngine, or dao.

## (next) — AccountService (services DAO front-end)
Pivoted to services pillar. Ported services/AccountService (105L, Luno/cura) — DAO front-end retrieving Account objects. Key idiom: Java `Iterator.remove()` during `account.iterator()` traversal → **snapshot `account.GetPlayerAccDataList()` + new `Account.Remove(pad)`** (added a faithful `Remove` to Account.cs that mirrors players.values()-iterator removal: removes from backing map, no side effects). `Date.getTime()`→`DateTimeOffset.Value.ToUnixTimeMilliseconds()`; currentTimeMillis→UtcNow; nested `PlayerAccountData.VisibleItem` preserved. Red-tolerated: dao pillar (PlayerDAO/InventoryDAO/PlayerPunishmentsDAO/PlayerAppearanceDAO/ItemStoneListDAO), GameServer.UpdateRatio, GSConfig. Verified small services (FlyRing/Road/NameRestriction/Debug/BonusPack/etc.) already ported by auto-commit loop. Guardrail green (363/5). Next services candidates (unported): ShieldService (122L), DatabaseCleaningService (142L), TribeRelationService (199L), then PvpService/DuelService/Dialog/Trade/Exchange.

## (next) — ShieldService (services)
Ported services/ShieldService (122L, xavier/Rolandas/SVDNESS) — siege geo-shield registration/attachment. Idioms: `Map.of`/`Set.of`→Dictionary/HashSet initializers; ConcurrentHashMap→ConcurrentDictionary; `computeIfAbsent(k,_->new ArrayList()).add()`→`GetOrAdd(k,_=>new List()).Add()`; Map.get→GetValueOrDefault, Map.remove→TryRemove(out _); `List.remove(idx)`→RemoveAt; `instanceof BoundingBox bb`→`is BoundingBox bb`; switch-arrows multi-label string→switch+break; `stream().anyMatch`→`Any`; slf4j parameterized warn→LogWarning named placeholders; getType()→GetType_(); geoEngine Vector3f public-field access (min.z/min.x) preserved. Red-tolerated: ShieldObserver, Spatial/BoundingBox/Vector3f (geoEngine), SiegeShield/SiegeLocation/FortressLocation/RectangleArea, DataManager.SHIELD_DATA. Guardrail green (363/5). Next services (unported): DatabaseCleaningService (142L), TribeRelationService (199L), DuelService (287L), DialogService (378L).

## (next) — DatabaseCleaningService (services)
Ported services/DatabaseCleaningService (142L, nrg/Neon) — deletes inactive-account players + legion maintenance. Heavily pillar-coupled (JDBC + JAXB + dao), transcribed literally with red tolerance. Idioms: `Thread.threadId()`→`Thread.CurrentThread.ManagedThreadId`; IllegalState/IllegalArgument→InvalidOperation/Argument; `LinkedHashSet`→insertion-ordered List w/ Contains-guard (no C# ordered-set); `System.out.printf("%4.1f%%\r")`→`Console.Write(string.Format("{0,4:F1}%\r"))`; try-with-resources→`using var`; record accessors `player.legionId()`→`player.LegionId()` (red record); `List.getFirst()`→[0]; currentTimeMillis→UtcNow; slf4j parameterized→ILogger named placeholders; File→FileInfo. Red-tolerated: PlayerDAO.PlayerAndLegionInfo record + dao pillar (Legion/LegionMember/Player DAO), DatabaseFactory/Connection/PreparedStatement/ResultSet (commons.database JDBC pillar), JAXBUtil.Deserialize, PlayerExperienceTable. Guardrail green (363/5). Next services: TribeRelationService (199L), DuelService (287L), DialogService (378L), TradeService (388L).

## (next) — TribeRelationService (services)
Ported services/TribeRelationService (199L, Cheatkiller) — tribe-relation predicates (isAggressive/isFriend/isSupport/isNone/isNeutral/isHostile/checkSiegeRelation/canHelpCreature). Pure logic, no logger. Idioms: Java enum switch (bare `case PC:`)→C# enum-qualified labels (`case TribeClass.PC:`); stacked-label fall-through preserved; nested switches; `instanceof X x`→`is X x`; `enum.name().startsWith("GAB1_")`→`ToString().StartsWith`; complex &&/|| precedence in checkSiegeRelation preserved verbatim. Red-tolerated: TribeClass enum, Creature/Npc/Player, DataManager.TRIBE_RELATIONS_DATA, AbyssNpcType, BaseSpawnTemplate. Guardrail green (363/5). Next services: DuelService (287L), DialogService (378L), TradeService (388L), ExchangeService (352L).

## (next) — DuelService (services)
Ported services/DuelService (287L, Simple/Sphinx/xTz) — duel request/accept/withdraw/start/end lifecycle. Idioms: ConcurrentHashMap<Integer,Integer>→ConcurrentDictionary<int,int> (get→TryGetValue/int?, remove→TryRemove out-param); Future<?>→ScheduledTask (cancel(false)→Cancel(false)); **two anonymous RequestResponseHandler<Player> subclasses→nested sealed DuelRequestHandler (DenyRequest/AcceptRequest) + DuelWithdrawHandler (AcceptRequest)**, each capturing the DuelService via ctor (nested types access enclosing private methods); schedule(...,5,MINUTES)→Schedule(ct=>{...;return ValueTask.CompletedTask;}, TimeSpan.FromMinutes(5)); aggroList `stream().map(AggroInfo::getAttacker).filter(...).forEach(remove)`→LINQ Select/Where/ToList+foreach (snapshot before mutating); getAbnormalEffects().forEach→foreach. Red-tolerated: SM_* packets, RequestResponseHandler<T>, Player/AggroInfo/Skill, World/PlayerService. Guardrail green (363/5). Next services: ExchangeService (352L), DialogService (378L), TradeService (388L), or PvpService (284L).

## (next) — PvpService (services)
Ported services/PvpService (284L, Sarynth/Estrayl) — PvP kill reward/AP-loss distribution. Key wildcard resolution: **TemporaryPlayerTeam<?> / <? extends TeamMember<Player>>→TemporaryPlayerTeam<TeamMember<Player>>** (codebase invariance bound — this file *consumes* teams via getCurrentTeam/getCurrentGroup/instanceof, NOT part of the deferred task_f13a70b1 AbstractTeamPlayerEvent hierarchy; resolved like RoahHandler etc.). Other idioms: `Map.putIfAbsent`→lock(this)+TryGetValue (synchronized method); `instanceof X x`→`is X x` (incl. `is TemporaryPlayerTeam<TeamMember<Player>> team`); `!(x is Player winner)` pattern w/ definite-assignment after early-return; Math.round(float)→(int)Math.Floor(+0.5f); removeIf→RemoveAll; stream/map/collect(joining)→string.Join+Where/Select; `Persistable.PersistentState`→IPersistable.PersistentState; enum.name()→ToString(); equalsIgnoreCase→OrdinalIgnoreCase; nested ItemPacketService.ItemUpdatePredicate/ItemAddType/ItemUpdateType. Converges KillCounter (ported earlier). Red-tolerated: many services (Abyss/Event/Conqueror/Item), SM_*, DAO, Rates/StatFunctions/Predicates, bounty templates. Guardrail green (363/5). Next services: ExchangeService (352L), DialogService (378L), TradeService (388L).

## (next) — ExchangeService (services)
Ported services/ExchangeService (352L, ATracer) — player-to-player trade (register/addKinah/addItem/lock/cancel/confirm/performTrade). Idioms: ConcurrentHashMap→ConcurrentDictionary (get→GetValueOrDefault, put→indexer, remove→TryRemove out-param so null-when-absent); Map.get→GetValueOrDefault; getItems().values()→.Values, isEmpty→Count==0; Arrays.asList(x)→new List<Item>{x}; `Player...`→params Player[]; nested ItemPacketService.{ItemDeleteType,ItemUpdateType,ItemAddType} preserved; slf4j→ILogger. Exchange/ExchangeItem (Model/Trade) converged. Red-tolerated: Item/Storage/Inventory, SM_* packets, PlayerRestrictions/AdminService/ItemFactory/TemporaryTradeTimeTask/IDFactory/InventoryDAO. Guardrail green (363/5). Next services: DialogService (378L), TradeService (388L), AtreianPassportService (318L), or other unported services.

## (next) — DialogService (services)
Ported services/DialogService (378L, VladimirZ/Neon) — NPC dialog-action dispatch (huge switch + sub-dialog restriction logic). Idioms: **`import static DialogAction.*`→`using static Aion.GameServer.Model.DialogAction`** (DialogAction is a `static class` of `const int`, ported identically; switch(int) with bare-const case labels valid in C#); cases declaring locals braced (BUY/RECOVERY/AIRLINE_SERVICE/MATCH_MAKER/TRADE_IN/FORT_CAPTURE) to avoid switch-scope collisions; anonymous `RequestResponseHandler<Npc>` (RECOVERY)→nested `RecoveryResponseHandler` capturing expLost/price via ctor; `instanceof PlayerAlliance alliance`→`is`; SummonOwner switch-expression w/ `_=>false`; SubDialogType switch enum-qualified labels; `DialogPage.X.id()`→`.Id()` (extension), `getByActionId`→`DialogPageExtensions.GetByActionId`; `getAutoGroup`→`AutoGroupTypeExtensions.GetAutoGroup`. Converges DialogPage/DialogAction. Red-tolerated: many services (Legion/Craft/Housing/Cube/Warehouse/Teleport/etc.), SM_* packets, templates, QuestEngine. Guardrail green (363/5). Next services: TradeService (388L), AtreianPassportService (318L), or remaining (Account/AutoGroup/Broker/...).

## (next) — TradeService (services)
Ported services/TradeService (388L, ATracer/Rama/Wakizashi/xTz/Neon) — NPC buy/sell/sell-for-AP/trade-in transactions. Idioms: switch on TradeNpcType bare-case→enum-qualified + default break; enum.equals(X)→`== X`; enum.name()→ToString(); Map<Integer,Long>→Dictionary<int,long> (keySet→.Keys, get→indexer); Set.addAll→HashSet.UnionWith, Set.add→Add, .size→Count; `instanceof Npc`→`is Npc`; Math.round(float)→(int)Math.Floor(+0.5f); getType()→GetType_(); nested ItemService.ItemUpdatePredicate + ItemPacketService.{ItemAddType,ItemDeleteType,ItemUpdateType} preserved; (long)/(int)/100D/100F casts kept. Red-tolerated: TradeList/TradeItem/templates, Item/Storage/Inventory, many services (Abyss/Item/Limited/Repurchase/PlayerLimit/Prices), SM_*, DataManager. Guardrail green (363/5). Next services: AtreianPassportService (318L), WeatherService, DialogService done — remaining big ones AutoGroupService/BrokerService/SiegeService/LegionService etc.

## (next) — AtreianPassportService (services)
Ported services/AtreianPassportService (318L, ViAl/Luzien/SVDNESS) — daily attendance passport rewards. **Heaviest java.time mapping yet**: LocalDateTime→DateTime (ServerTime.Now().DateTime); LocalDate→DateOnly (DateOnly.FromDateTime); LocalTime.MAX→TimeOnly.MaxValue.ToTimeSpan(); Instant/Timestamp→DateTimeOffset (toInstant()→DateTimeOffset itself, plusSeconds→AddSeconds, isAfter/isBefore→>/<, Instant.now→DateTimeOffset.UtcNow, truncatedTo(SECONDS)→FromUnixTimeSeconds(ToUnixTimeSeconds)); atTime/plusDays→.Date.Add(timeOfDay).AddDays; minusHours→AddHours(neg). Other idioms: Optional.map(X).orElse(null)→`?.X()` (DateTime? nullable); stream max/min(Comparator.comparing)→MaxBy/MinBy; switch-arrows(AttendType)→switch (ANNIVERSARY braced); PersistentState→IPersistable.PersistentState; Quartz JobDetail→IJobDetail; Map<Integer,Set<Integer>>→Dictionary<int,ISet<int>>; List.of→List initializer. Red-tolerated: CronService (unported), AccountPassportsDAO, PassportsList/Passport/AtreianPassport, ServerTime.AtDate signature. Guardrail green (363/5). Next services: remaining big ones (AutoGroupService/BrokerService/SiegeService/LegionService/WarehouseService/WeatherService/etc.).

## (next) — WorldRaidService (services)
Ported services/WorldRaidService (123L, Whoop/Sykra) — world-raid schedule init + start/stop lifecycle. Idioms: Map→Dictionary/ConcurrentDictionary; Collections.emptyMap→new Dictionary; Map.get→GetValueOrDefault, remove→TryRemove(out); synchronized(this)→lock(this); nested `forEach`→nested foreach; `stream().map().collect(toList())`→Select/ToList; currentTimeMillis→UtcNow.ToUnixTimeMilliseconds; slf4j debug/error→LogDebug/LogError. Red-tolerated: CronService (unported), WorldRaid/WorldRaidLocation/WorldRaidRunnable/WorldRaidSchedules, DataManager.WORLD_RAID_DATA. Guardrail green (363/5). Remaining unported services: WarehouseService (155L), VortexService (176L), UpgradeArcadeService (191L), LegionDominionService (194L), WeatherService (213L), PrivateStoreService (230L), then large (Housing/AutoGroup/HousingBid/Siege/Broker/Quest/Legion).

## (next) — WarehouseService (services)
Ported services/WarehouseService (155L, Simple/Luzien) — warehouse expansion + info packet paging. Idioms: Integer price→int? (.Value after null-check); anonymous RequestResponseHandler<Npc>→nested ExpandResponseHandler (captures price via ctor); StorageType.X.getId()→GetId() (enum extension); `List.subList(from,to)`→`GetRange(from, to-from)`; enum.equals(QuestStatus.COMPLETE)→`== QuestStatus.COMPLETE`; String.valueOf→ToString; SM_WAREHOUSE_INFO(null,...)→null first arg. Red-tolerated: Item/Storage/PlayerCommonData/QuestStateList, StorageExpansionTemplate, SM_* packets, DataManager. Guardrail green (363/5). Remaining unported services: VortexService (176L), UpgradeArcadeService (191L), LegionDominionService (194L), WeatherService (213L), PrivateStoreService (230L), HousingService (326L), AutoGroupService (362L), HousingBidService (405L), SiegeService (716L), BrokerService (733L), QuestService (947L), LegionService (1164L).

## (next) — LegionDominionService (services)
Ported services/LegionDominionService (194L, Yeats) — weekly legion territory-control calculation/rewards/invasion rifts. Idioms: TreeMap→SortedDictionary (key-sorted); new Timestamp(currentTimeMillis())→DateTimeOffset.FromUnixTimeMilliseconds(UtcNow.ToUnixTimeMilliseconds()); ZonedDateTime→DateTimeOffset (getDayOfWeek→DayOfWeek==DayOfWeek.Wednesday, getHour→Hour); Map.get→GetValueOrDefault, values()→.Values, size→Count, 1-based reward keys via indexer; inline LoggerFactory.getLogger().warn→inline NullLoggerFactory.CreateLogger().LogWarning (parameterized→named placeholders); schedule(Runnable, ms)→Schedule(ct=>{...;ValueTask},TimeSpan.FromMilliseconds) keeping Java int-arithmetic (getDuration()*3540*1000). Red-tolerated: LegionDAO/LegionDominionDAO, Legion/LegionDominionLocation/ParticipantInfo, LegionService/RiftService/SystemMailService, SM_* packets. Guardrail green (363/5). Remaining: VortexService (176L), UpgradeArcadeService (191L), WeatherService (213L), PrivateStoreService (230L), Housing/AutoGroup/HousingBid/Siege/Broker/Quest/Legion (large).

## (next) — WeatherService (services)
Ported services/WeatherService (213L, ATracer/Kwazar/Rolandas) — per-zone weather state + random rotation by rank/season/daytime. Idioms: Map→Dictionary (get→GetValueOrDefault, put→indexer, entrySet→KeyValuePair); anonymous Runnable (checkWeathersTime)→Schedule(ct=>{...;return ValueTask.CompletedTask;}, TimeSpan.FromMilliseconds(Rnd.Get(20000,240000))); synchronized(weatherEntries array)→lock(weatherEntries); `stream().filter().findFirst().orElseGet(()->new X)`→`Where().FirstOrDefault() ?? new X`; switch on string (SNOW/SNOW_BEACH); int-division-vs-float chance comparison preserved; GameTime.clone→Clone; broadcastToWorld predicate→lambda; WORLD_MAPS_DATA foreach (IEnumerable). Red-tolerated: WeatherEntry/WeatherTable/GameTime/DayTime, SM_WEATHER, DataManager. Guardrail green (363/5). Remaining services: VortexService (176L), UpgradeArcadeService (191L), PrivateStoreService (230L), HousingService (326L), AutoGroupService (362L), HousingBidService (405L), SiegeService (716L), BrokerService (733L), QuestService (947L), LegionService (1164L).

## (next) — VortexService (services)
Ported services/VortexService (176L, Source) — dimensional-vortex invasion start/stop/spawn lifecycle. Key wildcard resolution: **DimensionalVortex<?>→DimensionalVortex<VortexLocation>** (DimensionalVortex<VL extends VortexLocation>; Invasion extends DimensionalVortex<VortexLocation> — codebase invariance bound, like TemporaryPlayerTeam<TeamMember<Player>>). Idioms: ConcurrentHashMap→ConcurrentDictionary (get→GetValueOrDefault, put→indexer, remove→TryRemove out-param under lock); synchronized(this)→lock(this); CronService.schedule(lambda)→Schedule(()=>...); ThreadPoolManager.schedule(...,HOURS)→Schedule(ct=>{...;ValueTask},TimeSpan.FromHours); enum.equals→==; WorldMapType.X.getId()→GetId(); Map.values()→.Values. Red-tolerated: DimensionalVortex/Invasion (unported), CronService, RiftManager/RiftInformer, VortexLocation/VortexStateType/VortexSpawnTemplate, SpawnEngine (green). Guardrail green (363/5). Remaining services: UpgradeArcadeService (191L), PrivateStoreService (230L), HousingService (326L), AutoGroupService (362L), HousingBidService (405L), SiegeService (716L), BrokerService (733L), QuestService (947L), LegionService (1164L).

## (next) — UpgradeArcadeService (services)
Ported services/UpgradeArcadeService (191L, ginho1/Estrayl/Neon) — upgrade-arcade event progression (try/resume/reward, frenzy mode). Idioms: ConcurrentHashMap→ConcurrentDictionary; `putIfAbsent(k, new X)`→`GetOrAdd(k, new X)` (eager-value overload matches Java eager alloc + return-existing-or-new); 3× anonymous Runnable schedule→Schedule(ct=>{...;return ValueTask.CompletedTask;}, TimeSpan.FromMilliseconds); `stream().filter().findFirst().orElse(null)`→`Where().FirstOrDefault()`; currentTimeMillis→UtcNow.ToUnixTimeMilliseconds; Math.max→Math.Max; Rnd.chance/get. ArcadeProgress (Model/Event) converged. Red-tolerated: Arcade templates (ArcadeRewards/ArcadeRewardItem/ArcadeLevel), SM_UPGRADE_ARCADE, ItemService, DataManager. Guardrail green (363/5). Remaining services: PrivateStoreService (230L), HousingService (326L), AutoGroupService (362L), HousingBidService (405L), SiegeService (716L), BrokerService (733L), QuestService (947L), LegionService (1164L).

## (next) — PrivateStoreService (services)
Ported services/PrivateStoreService (230L, Simple) — player private-shop create/validate/sell/close. Idioms: TradePSItem[] array foreach; `Collection.toArray(new T[size])`→`.ToArray()` (LINQ); Map.values()→.Values, size()→Count, isEmpty()→Count==0; slf4j "EXCHANGE_LOG"→ILogger (info/warn→LogInformation/LogWarning); the index-as-itemId quirk (tradeItem.getItemId() used as store index) preserved verbatim. Red-tolerated: PrivateStore/TradeList/TradeItem/TradePSItem, Item/Inventory, CreatureState/EmotionType/PlayerMode/AbnormalState enums, SM_* packets, ItemService. Guardrail green (363/5). Remaining services: HousingService (326L), AutoGroupService (362L), HousingBidService (405L), SiegeService (716L), BrokerService (733L), QuestService (947L), LegionService (1164L).

## (next) — HousingService (services)
Ported services/HousingService (326L, Rolandas/Neon) — house/studio ownership transfer, spawning, login checks. Idioms: ConcurrentHashMap(DAO-map copy-ctor)→`new ConcurrentDictionary<int,House>(HousesDAO.LoadHouses(...))`; `IntStream.of(int[]).boxed().collect(toSet())`→`new HashSet<int>(arr)`; `Stream.concat(a,b)`→`a.Concat(b)`; `mapToInt(getOwnerId).distinct()`→`Select(...).Distinct()`; `sorted(comparing(getAcquiredTime))`→`OrderBy(...)`; `studios.values().remove(v)`→find-key-by-value + TryRemove; synchronized(house)→lock(house); anyMatch→Any; new Timestamp(currentTimeMillis())→DateTimeOffset.FromUnixTimeMilliseconds(UtcNow…), nullable acquiredTime; getNextPay().getTime()→.Value.ToUnixTimeMilliseconds(); PersistentState→IPersistable.PersistentState; getType()→GetType_(); Collections.singletonList→List initializer. Red-tolerated: House/Building/HouseAddress/Letter, HousesDAO/PlayerDAO, SM_* packets, HousingBidService, SpawnEngine (green). Guardrail green (363/5). Remaining services: AutoGroupService (362L), HousingBidService (405L), SiegeService (716L), BrokerService (733L), QuestService (947L), LegionService (1164L).

## (next) — AutoGroupService (services)
Ported services/AutoGroupService (362L, xTz/Estrayl) — instance matchmaking queue (looking-for-party, quick/group entries, penalties, auto-instance create/destroy). Idioms: ConcurrentHashMap→ConcurrentDictionary; **ConcurrentHashMap.newKeySet()→ConcurrentDictionary<int,byte> as concurrent set** (add→TryAdd(k,0), remove→TryRemove); computeIfAbsent→GetOrAdd; Map.get→GetValueOrDefault, remove(k)→TryRemove(out); synchronized(list)→lock(list); List.sort(null)→Sort(); stream filter/map/count/findFirst.orElse(0)→LINQ (Count(pred), Select/Where/ToList, Keys.Where().FirstOrDefault()); switch-expr(EntryRequestType)→C# switch expr w/ `_=>false`; schedule(...,ms)→Schedule(ct=>{...;ValueTask},TimeSpan.FromMilliseconds); AutoGroupType enum→extension methods (GetAGTByMaskId static). AutoInstance/LookingForParty (Model/Autogroup) converged. Red-tolerated: AutoGroupUtility/AGQuestion/EntryRequestType, InstanceService/PeriodicInstanceManager/PvPArenaService, PlayerGroup/AllianceService, SM_*. Guardrail green (363/5). Remaining services: HousingBidService (405L), SiegeService (716L), BrokerService (733L), QuestService (947L), LegionService (1164L).

## (next) — HousingBidService (services)
Ported services/HousingBidService (405L, Rolandas/Neon) — house auction bid/end/cancel lifecycle. **Key trap: Java `DayOfWeek.getValue()` is ISO (Mon=1..Sun=7) but C# `System.DayOfWeek` is Sun=0..Sat=6** → converted via `now.DayOfWeek == DayOfWeek.Sunday ? 7 : (int)now.DayOfWeek`. Also `HouseBids.bid()`→`DoBid()` (prior method/Bid-nested-class collision rename). Idioms: ConcurrentHashMap→ConcurrentDictionary (putIfAbsent!=null→!TryAdd, remove(k,v)→TryRemove(KeyValuePair), remove(k)→TryRemove(out) incl. assignment-in-condition + local shadowing field via this.bids); stream filter/findAny→Where/FirstOrDefault, anyMatch→Any, `reduce(maxBy(comparing(getTime)))`→`MaxBy(b=>b.GetTime())`; Persistable.PersistentState→IPersistable.PersistentState; ZonedDateTime→DateTimeOffset (getHour→Hour); currentTimeMillis→UtcNow.ToUnixTimeMilliseconds; Integer.parseInt→int.Parse; switch(HouseType); reference-eq (highestBid==initialOffer) preserved. HouseBids/Bid converged. Red-tolerated: House/AuctionResult/HouseType/MailFormatter/AuctionEndTask/HouseBidsDAO/PlayerService. Guardrail green (363/5). Remaining services: SiegeService (716L), BrokerService (733L), QuestService (947L), LegionService (1164L).

## (next) — HouseController (controllers)
Pivoted from the (large) remaining services to controllers root. Ported controllers/HouseController (185L, Rolandas/Neon) : VisibleObjectController<House> — house npc/sign spawn mgmt, visitor kick, door teleport. **HouseObject<?>→HouseObject<PlaceableHouseObject>** (matches the already-ported HouseRegistry.GetSpawnedObjects() bound — resolves a deferred-wildcard consumer cleanly). Idioms: instanceof Player→is Player; SpawnType enum if/else (getType→GetType_); ZoneName.get→ZoneName.Get (ZoneName is a sealed class, not enum); Math.toRadians→*Math.PI/180; **byte heading compound arithmetic (h-=30, h+=ternary)** kept as `byte` (bit-level parity with Java signed byte — same mod-256 result; `(byte)(-60)`≡196); Integer exitMapId→int? with .Value; super→base. Converges the House.GetController() calls from HousingService/HousingBidService (UpdateSign/UpdateAppearance/UpdateHouseSpawns/KickVisitors/SpawnObjects). Red-tolerated: HouseObject/PlaceableHouseObject, House butler/crystal/sign getters, SpawnEngine (green)/VisibleObjectSpawner (green), TeleportService/GeoService/SM_*. Guardrail green (363/5). controllers root now: only RVController (261L) unported. Remaining big services: SiegeService (716L), BrokerService (733L), QuestService (947L), LegionService (1164L).

## (next) — RVController (completes controllers root)
Ported controllers/RVController (261L, ATracer/Source/Sykra) : NpcController — rift/vortex portal dialog controller. Idioms: Map<Integer,Player>→Dictionary; Integer fields→int? (lifted >/>= comparisons in onAccept work for non-null); **two anonymous RequestResponseHandler<Npc>→nested sealed VortexRequestHandler/NormalRequestHandler** capturing the RVController via ctor (access enclosing private OnAccept/SyncPassed/passedPlayers/slaveSpawnTemplate); instanceof PlayerGroup→is; currentTimeMillis/1000→UtcNow.ToUnixTimeMilliseconds()/1000; ++usedEntries preserved; RiftEnum enum→extension methods (IsVortex/GetEntries/etc). Red-tolerated: RiftEnum, RiftInformer/RiftManager/RiftService/VortexService, PlayerGroup/Group/AllianceService, VortexLocation, SM_*, TeleportService. **controllers root COMPLETE** (all root + attack + most observer/movement ported). Guardrail green (363/5). Remaining: movement (NpcMoveController 468L, PlayerMoveController 108L), observer material-actor subtree, big services (Siege 716/Broker 733/Quest 947/Legion 1164), geoEngine, dao.

## (next) — PlayerMoveController (controllers/movement)
Ported controllers/movement/PlayerMoveController (108L, ATracer) : PlayableMoveController<Player> — fall-damage accumulation + last-client-position tracking + random-move-loc-effect timing. Idioms: nested SM_ATTACK_STATUS.TYPE/LOG enums→qualified (SM_ATTACK_STATUS.TYPE.FALL_DAMAGE, SM_ATTACK_STATUS.LOG.REGULAR); currentTimeMillis→UtcNow.ToUnixTimeMilliseconds; super→base; inherited base member `owner` kept lowercase. Red-tolerated: PlayableMoveController base (owner/GetMovementMask/UpdateLastMove), WorldPosition, StatFunctions.CalculateFallDamage, PlayerReviveService, SM_ATTACK_STATUS, Skill. Guardrail green (363/5). Remaining controllers/movement: NpcMoveController (468L). Remaining observer: material-actor subtree (AbstractCollisionObserver→AbstractMaterialSkillActor→Zone/Terrain actors, AttackShieldObserver 244L). Big services: Siege 716/Broker 733/Quest 947/Legion 1164.

## (next) — NpcMoveController (completes controllers/movement)
Ported controllers/movement/NpcMoveController (468L, ATracer) : CreatureMoveController<Npc> — NPC movement/geo-Z pathing, walker routes, back-step recall. **IMPORTANT — the movement/geo subsystem was previously ported in PascalCase (divergent from the SCREAMING_SNAKE convention elsewhere)**; matched it: inherited `Started`(AtomicBoolean: CompareAndSet/Get/Set), `Owner`, `Heading`, `MovementMaskField`, `TargetDestX/Y/Z`, `LastMoveUpdate`, `MOVE_CHECK_OFFSET`, UpdateLastMove/GetMovementMask/SetAndSend{Start,Stop}Move; `SM_MOVE`→`SmMove` (namespace `Network.Aion.ServerPackets`, capital P); `MovementMask.NPC_STARTMOVE`→`MovementMask.NpcStartMove` etc; `AILogger.moveinfo`→`AILogger.Moveinfo`. Other idioms: instanceof Creature→is; LinkedList getLast/removeLast→`.Last.Value`+`RemoveLast()`; synchronized→lock(this); Math.toRadians→*Math.PI/180; currentTimeMillis→UtcNow.ToUnixTimeMilliseconds; Java `final` override→`sealed override`. **Convergence edit**: made base CreatureMoveController.GetTargetX2/Y2/Z2 `virtual` so they can be overridden. Converges WalkerGroup.GetLinePoint/WalkerFormator.ProcessClusteredNpc (ported). Red-tolerated: AI (AIState/AISubState/NpcAI/AILogger/WalkManager/TargetEventHandler), GeoService, Stat2, IgnoreProperties. **controllers/movement COMPLETE** (Player+Npc move controllers + base). Guardrail green (363/5). Remaining: observer material-actor subtree, big services (Siege 716/Broker 733/Quest 947/Legion 1164), geoEngine, dao.

## (next) — material-skill-actor observer subtree (AbstractMaterialSkillActor + Zone/Terrain actors)
AbstractCollisionObserver already existed (auto-commit loop, uses `creature`/`sbyte intentions`/nested CheckType). Ported the 3 missing: AbstractMaterialSkillActor (109L, Yeats/Neon) : AbstractCollisionObserver — AtomicReference<Future<?>>→ScheduledTask field + Interlocked (compareAndSet→CompareExchange==null, getAndSet→Exchange); scheduleAtFixedRate(stateful Runnable)→ScheduleAtFixedRateTask(ct=>{msTask.Run();...}) with nested MaterialSkillTask class capturing outer (preserves skill/secondsElapsed state); synchronized(skills)→lock; volatile fields; getClass().getSimpleName()→GetType().Name; sbyte intentions (matches base); Effect.ForceType.MATERIAL_SKILL. ZoneCollisionMaterialActor (39L) + TerrainZoneCollisionMaterialActor (59L): CollisionIntention.MATERIAL.getId()→(sbyte); collisionResults.size()→Size(); Collections.emptyList→new List; onMoved/moved overrides. **material-skill-actor subtree COMPLETE.** Converges WeatherService.FindWeatherEntry/GameTimeService (ported). Red-tolerated: geoEngine (CollisionResults/Spatial/CollisionIntention), MaterialSkill/MaterialTemplate/MaterialActCondition, SkillEngine/Effect, GeoService. Guardrail green (363/5). Remaining observer: AttackShieldObserver (244L). Big services: Siege 716/Broker 733/Quest 947/Legion 1164.

## (next) — AttackShieldObserver (completes controllers/observer)
Ported controllers/observer/AttackShieldObserver (244L, multi-author) : AttackCalcObserver — shield/reflector/protect/convert-heal damage mechanics (checkShield over attack results). Idioms: 3 overloaded ctors w/ this()-chaining→`: this(...)`; nested SM_ATTACK_STATUS.TYPE/LOG + Effect.ForceType qualified; **HealType→HealType?** (Java passes null for non-convert shields; switch on nullable enum); enum switches (HitType/ShieldType/HealType) w/ explicit default break; instanceof Npc/Summon→is + cast; Rnd.chance→Rnd.Chance; Math.min/max→Math.Min/Max; (int) casts on float/double damage math; shieldType.getId()→GetId(). Red-tolerated: AttackCalcObserver base, Effect/AttackResult/AttackStatus, HitType/ShieldType/HealType enums, EffectTemplate/SkillTemplate, Summon/SummonMode, PositionUtil. **controllers/observer COMPLETE** (collision/material-actor subtree + quest-zone + shield observers all ported). Guardrail green (363/5). **controllers package fully done** (root/attack/movement/observer). Remaining big: services (Siege 716/Broker 733/Quest 947/Legion 1164), geoEngine, dao, network pillar.

## (next) — 10 tiny skillengine effect leaves
Controllers package fully done; pivoted to the skillengine.effect pillar via its trivial leaves. Ported 10 tiny effect subclasses (8-20L each): DeathBlowEffect/SpellAttackInstantEffect (:DamageEffect), AbsoluteSlowEffect/AbsoluteSnareEffect (:BufEffect), ShapeChangeEffect (:TransformEffect), Summon{Binding,House}GateEffect (:SummonGroupGateEffect), Buff{Bind,Silence,Stun}Effect (:Bind/Silence/StunEffect, override Calculate→effect.AddSuccessEffect(this)). JAXB idioms: `@XmlType(name="X")`→`[XmlType("X")]`; `@XmlAccessorType(XmlAccessType.FIELD)`→dropped (no C# equiv). Red-tolerated: all effect bases (skillengine.effect pillar ~176 files unported), Effect model. Guardrail green (363/5). Next: more skillengine effect leaves (BindEffect/SilenceEffect/StunEffect/DamageEffect/BufEffect bases + the many concrete effects), or network/dao/big-services pillars.

## (next) — BindEffect/SilenceEffect/StunEffect (skillengine.effect)
Ported the 3 abnormal-state effect bases (42-45L, ATracer) : EffectTemplate — **converges the BuffBind/Silence/StunEffect leaves committed last tick**. Each overrides applyEffect/calculate/startEffect/endEffect: `super.calculate(effect, StatEnum.X_RESISTANCE, null)`→`base.Calculate(...)`; sets/unsets AbnormalState.BIND/SILENCE/STUN via effected.GetEffectController(); cancels casting skill if matching SkillType (Bind→PHYSICAL, Silence→MAGICAL); Stun also stops gliding/aborts move for Player. Idioms: @XmlType(name)→[XmlType]; getType()→GetType_(); instanceof Player→is Player; AbnormalState same-namespace unqualified. Red-tolerated: EffectTemplate (huge JAXB base, pillar), Effect model, Creature/Player controllers, StatEnum/SkillType. Guardrail green (363/5). Next skillengine.effect: more bases (BufEffect 91L, DamageEffect 81L, TransformEffect 146L) + many concrete effects, or pivot to network/dao/big-services.

## (next) — DamageEffect + BufEffect (skillengine.effect abstract bases)
Ported the 2 abstract effect bases (81L/91L, ATracer) : EffectTemplate — **converges DeathBlow/SpellAttackInstant (:DamageEffect) and AbsoluteSlow/Snare (:BufEffect) leaves**. DamageEffect: @XmlAttribute fields (Func mode=Func.ADD, bool shared)→[XmlAttribute]; applyEffect routes PROVOKED→onAttack(DAMAGE,PROCATKINSTANT) else REGULAR + notifyAttackObservers; nested SM_ATTACK_STATUS.TYPE/LOG qualified; `int *= float` lossy compound preserved; CalculateDamage uses inherited element/CalculateBaseValue; shouldApplyAttackerMovementModifier virtual. BufEffect: **CreatureGameStats<? extends Creature>→non-generic CreatureGameStats** (established wildcard resolution); startEffect adds stat modifiers; getModifiers switch(Func) ADD→StatAddFunction/PERCENT→StatRateFunction/REPLACE→StatSetFunction(.WithConditions); inline LoggerFactory.getLogger().warn→inline NullLoggerFactory ILogger. Red-tolerated: EffectTemplate (position/hopType/element/change/CalculateBaseValue), AttackUtil, Func/Change/Conditions, StatXFunction/IStatFunction, ActivationAttribute/SkillElement. Guardrail green (363/5). Next: TransformEffect (146L, base of ShapeChangeEffect) + more concrete effects, or pivot to network/dao/big-services.

## (next) — TransformEffect (skillengine.effect abstract base)
Ported skillengine/effect/TransformEffect (146L, Sweetkr/kecimis) abstract : EffectTemplate — **converges ShapeChangeEffect leaf**. 11 @XmlAttribute fields (model/type=TransformType.NONE/panelid/banUseSkills/banMovement/res1-6)→[XmlAttribute]; applyEffect removes prior transform on FORM1+panelid; endEffect re-applies any other active TransformEffect via transformModel.Apply else endTransformation; instanceof TransformEffect+double-cast→`is TransformEffect te`. Red-tolerated: EffectTemplate, Effect/Creature, TransformType, transformModel/transformController. Guardrail green (363/5). Next skillengine.effect: many more concrete effects + remaining bases (AbnormalSetEffect, etc.). Or pivot to network/dao/big-services pillars (Siege 716/Broker 733/Quest 947/Legion 1164 services still pending).

## (next) — 10 concrete skillengine effects (EffectTemplate-derived)
Ported 10 more concrete effects (21-27L each): AlwaysHit/AlwaysNoResist/ActivateEnslave/Utility/SupportEvent/AbsoluteEXPPointHealInstant/LimitedReduceDamage (empty applyEffect TODO stubs), FlyoffEffect (@XmlType name="FlyOffEffect"≠class; @XmlAttribute int distance), SignetEffect (applyEffect→AddToEffectedController, calculate→AddSuccessEffect), CloseAerialEffect (applyEffect→removeEffect(8224), super.calculate(effect,null,SpellStatus.CLOSEAERIAL)→base.Calculate). All @XmlType(name)→[XmlType]; verified per-class name (most match class, FlyOff differs). Red-tolerated: EffectTemplate, Effect, SpellStatus. Guardrail green (363/5). skillengine.effect pillar progressing — many concrete effects + bases (AbstractDispelEffect+6 dispel subclasses, AbnormalSetEffect, etc.) remain. Big services still pending (Siege 716/Broker 733/Quest 947/Legion 1164).

## (next) — AbstractDispelEffect + 6 dispel subclasses
Ported the dispel effect cluster (7 files): AbstractDispelEffect (32L, kecimis) abstract : EffectTemplate — `@XmlAttribute(name="dispel_level")`→`[XmlAttribute("dispel_level")]`; 3-arg ApplyEffect(Effect,DispelCategoryType,SkillTargetSlot) helper computing finalPower + removeEffectByDispelCat. 6 concrete subclasses (DispelBuff/DispelDebuff/DispelDebuffMental/DispelDebuffPhysical/DispelNpcBuff/DispelNpcDebuffEffect) each override ApplyEffect(Effect)→base.ApplyEffect with their (DispelCategoryType, SkillTargetSlot) pair. Red-tolerated: EffectTemplate (CalculateBaseValue), Effect, DispelCategoryType/SkillTargetSlot. Guardrail green (363/5). skillengine.effect pillar: ~30+ files ported so far; many concrete effects + bases remain. Big services pending (Siege 716/Broker 733/Quest 947/Legion 1164).

## (next) — 7 more skillengine effects
Ported 7 effects (27-30L): SanctuaryEffect (no @XmlType; set/unset AbnormalState.SANCTUARY, equals→Equals), Return/EscapeEffect (TeleportService.MoveToBindLocation((Player)effector); calculate→addSuccessEffect if spawned), FallEffect (instanceof Player→is; ends fly unless INVULNERABLE_WING), SkillLauncherEffect (@XmlAttribute("skill_id"); SkillEngine.ApplyEffect), DelayedSkillEffect (no @XmlType, @XmlAttribute("skill_id"); endEffect→base+applyEffectsDirectly if IsEndedByTime), SwitchHpMpEffect (**CreatureLifeStats<? extends Creature>→non-generic CreatureLifeStats**; swaps HP/MP). Red-tolerated: EffectTemplate, Effect, TeleportService, SkillEngine, CreatureLifeStats, flyController. Guardrail green (363/5). skillengine.effect: ~37 ported, ~112 remain (heal/buf/abnormal-set/proc/summon families + bases like AbstractOverTimeEffect/SleepEffect/HealEffect). Big services pending (Siege/Broker/Quest/Legion).

## (next) — SleepEffect + AbstractOverTimeEffect (effect bases)
Ported 2 effect bases: SleepEffect (47L, ATracer) : EffectTemplate — **converges BuffSleepEffect**; sets/unsets AbnormalState.SLEEP, cancels skill/gliding/move for Player, setCancelOnDmg(true). AbstractOverTimeEffect (68L, kecimis) abstract : EffectTemplate — base of all over-time (DoT/HoT) effects incl. MpAttackEffect: @XmlAttribute(required=true)→[XmlAttribute] (required dropped per idiom); 2-arg StartEffect(Effect, AbnormalState?) schedules periodic OnPeriodicAction via ScheduleAtFixedRateTask (Future<?>→ScheduledTask); GetValue/GetDuration2 overrides return inherited value/duration2; AbnormalState param→nullable (.Value at use). Red-tolerated: EffectTemplate (value/duration2/position/OnPeriodicAction), Effect, StatEnum, ThreadPoolManager. Guardrail green (363/5). skillengine.effect: ~39 ported. Over-time subclasses (MpAttack/DamageOverTime/HealOverTime/etc.) now have their base. Big services pending.

## (next) — 4 over-time effects (MpAttack/FpAttack/Poison/Bleed)
Ported 4 AbstractOverTimeEffect subclasses: MpAttackEffect/FpAttackEffect (periodic MP/FP drain, percent-aware; FpAttack instanceof Player→is + calculate guard), PoisonEffect/BleedEffect (DoT damage: calculate→base.Calculate(StatEnum.X_RESISTANCE), startEffect computes finalDamage via AttackUtil.CalculateMagicalOverTimeSkillResult + setReserveds(EffectReserved(position,dmg,ResourceType.HP,...)) + base.StartEffect(effect, AbnormalState.X), endEffect→base.EndEffect(effect,AbnormalState.X), onPeriodicAction→OnAttack(DAMAGE, reserved value, LOG.X) + NotifyDotAttackedObservers). Idioms: nested SM_ATTACK_STATUS.TYPE/LOG + EffectReserved.ResourceType qualified; inherited value/percent/position/hopType/CalculateBaseValue. Red-tolerated: AbstractOverTimeEffect (green), EffectReserved, AttackUtil, SM_ATTACK_STATUS. Guardrail green (363/5). Remaining over-time: HealOverTimeEffect (80L), SpellAttackEffect (47L), SpellAtkDrainEffect (40L). skillengine.effect ~43 ported.

## (next) — SpellAttack/SpellAtkDrain/HealOverTime effects (completes over-time family)
Ported the 3 remaining AbstractOverTimeEffect subclasses: SpellAttackEffect (magical DoT, useMagicBoost excludes skill 21110), SpellAtkDrainEffect (@XmlAttribute("hp_percent"/"mp_percent"); drains hp/mp % of damage to effector), HealOverTimeEffect (abstract, implements HealEffectTemplate; HoT: calculate gates on base.Calculate bool, startEffect(Effect,HealType) reserves via ResourceType.Of(healType), onPeriodicAction(Effect,HealType) switch-arrows→switch HP/MP/FP/DP with (Player) cast for FP/DP, IsPercent/AllowHpHealBoost/etc interface impls). Idioms: nested SM_ATTACK_STATUS.TYPE/LOG + EffectReserved.ResourceType qualified; Math.min→Math.Min; switch-expr→switch. **over-time effect family COMPLETE** (base + 7 subclasses). Red-tolerated: HealEffectTemplate interface, EffectReserved.ResourceType.Of, AttackUtil, SM_ATTACK_STATUS, StatEnum. Guardrail green (363/5). skillengine.effect ~46 ported, ~105 remain (heal/buf/proc/summon/spellbook families). Big services pending.

## (next) — heal effect cluster (HealEffectTemplate + HealEffect + AbstractHealEffect)
Ported the heal core (3 files) — **converges HealOverTimeEffect** (which implements HealEffectTemplate). HealEffectTemplate (Neon) interface w/ Java `default calculateHealValue`→**C# default interface method delegating to a `static CalculateHealValueDefault(self,...)`** so overriding classes can invoke the interface-default logic (Java's `HealEffectTemplate.super.calculateHealValue`); Math.round(float)→(int)Math.Floor(+0.5f). HealEffect (kecimis) : HealOverTimeEffect — HP HoT delegating to base 2-arg StartEffect/OnPeriodicAction(HealType.HP). AbstractHealEffect (abstract) : EffectTemplate, HealEffectTemplate — calculate/applyEffect(Effect,HealType) with switch HP/MP/FP/DP, `this instanceof ProcHealInstantEffect`→`is`, ResourceType.of→EffectReserved.ResourceType.Of, calculateHealValue caps at maxStat-currentStat and calls the static default helper; GetCurrentStatValue/GetMaxStatValue left `abstract` (C# requires explicit abstract decl where Java implicitly leaves them). Red-tolerated: EffectTemplate, EffectReserved, Proc*HealInstantEffect (unported), SM_ATTACK_STATUS, StatEnum. Guardrail green (363/5). Next: AbstractHealEffect subclasses (HealInstant/MpHealInstant/FpHealInstant/Proc* + Hp/Mp/Fp/Dp heal-over-time subclasses).

## (next) — 11 heal subclasses (HP/MP/FP/DP instant + over-time + Proc)
Ported 11 AbstractHealEffect/HealOverTimeEffect subclasses (36-41L each): instant — HealInstant/MPHealInstant/FPHealInstant/DPHealInstant + Proc{Heal,MP,DP,FP}HealInstant (calculate/applyEffect delegate to base with HealType.{HP,MP,FP,DP}; ProcHealInstant overrides AllowHpHealBoost→false); over-time — MPHealEffect/DPHealEffect/FPHealEffect (startEffect/onPeriodicAction delegate to base 2-arg). Each overrides GetCurrentStatValue/GetMaxStatValue per resource (HP/MP via lifeStats+gameStats, FP via lifeStats GetCurrentFp/GetMaxFp, DP via (Player) GetCommonData/GetMaxDp). Generated via templated batch. Red-tolerated: Effect, lifeStats/gameStats, Player. Guardrail green (363/5). **Heal family nearly complete** — only CaseHealEffect (83L, has switch logic) remains. skillengine.effect ~60 ported.

## (next) — CaseHealEffect (completes heal effect family)
Ported skillengine/effect/CaseHealEffect (83L, kecimis) : AbstractHealEffect — conditional heal (heals when current ≤ cond% of max). Idioms: @XmlAttribute(name="cond_value")→[XmlAttribute("cond_value")]; getCurrentStatValue/getMaxStatValue Java switch-expr(HealType HP/MP/default)→C# switch expr; **anonymous ActionObserver(ObserverType.HP_CHANGED){hpChanged}→nested CaseHealObserver** capturing the CaseHealEffect+Effect; tryHeal compares current ≤ maxCurValue*condValue/100f, heals HP/MP via calculateHealValue (inherited), endEffect. Red-tolerated: AbstractHealEffect (green), ActionObserver/ObserverType (green), Effect, SM_ATTACK_STATUS. **Heal effect family COMPLETE** (template/interface + AbstractHealEffect + HealOverTimeEffect + 12 subclasses). Guardrail green (363/5). skillengine.effect ~61 ported, ~90 remain (buf/proc/summon/abnormal-set/spellbook families). Big services pending (Siege/Broker/Quest/Legion).

## 2026-06-10 — PolymorphEffect + DeformEffect (TransformEffect subclasses)
Ported PolymorphEffect (sets transformModel tribe from NPC_DATA template on start, clears on end) and DeformEffect (DEFORM abnormal state + DEFORM_RESISTANCE calculate). Both converge on already-ported TransformEffect. Guardrail green (363/5). skillengine.effect: ~63 ported, ~88 remain (summon/shield/proc/abnormal-set families). Big services pending (Siege/Broker/Quest/Legion).

## 2026-06-10 — SummonEffect base + SummonFunctionalNpcEffect + SummonGroupGateEffect
Ported SummonEffect base (createSummon + DESPAWN ScheduledTask, removePetOrderUnSummonEffects) and two subclasses: SummonFunctionalNpcEffect (spawnFunctionalNpc, 300s delete) and SummonGroupGateEffect (newSingleTimeSpawn + spawnGroupGate, time*1000 despawn). Future→ScheduledTask, anonymous Runnable→async delegate. Guardrail green (363/5). skillengine.effect: ~66 ported, ~85 remain (SummonHomingEffect/SummonTrapEffect/SummonServantEffect/SummonSkillAreaEffect/shield/proc families). Big services pending (Siege/Broker/Quest/Legion).

## 2026-06-10 — Summon family complete (Homing/Trap/Servant/SkillArea)
Ported remaining SummonEffect subclasses: SummonHomingEffect (per-npc spawn, anonymous ATTACK observer→nested HomingAttackObserver, 15s despawn), SummonTrapEffect (getClosestCollision firstTargetSelf, TrapService.registerTrap), SummonServantEffect (protected spawnServant w/ INITIAL_SPAWN_DELAY, ArgumentException), SummonSkillAreaEffect (group switch, stateful Runnable→nested SkillAreaTask, scheduleAtFixedRate). **Summon effect family COMPLETE (7 files).** Guardrail green (363/5). skillengine.effect: ~70 ported, ~81 remain (shield/proc/abnormal-set families). Big services pending (Siege/Broker/Quest/Legion).

## 2026-06-10 — ShieldEffect base + Protect/Reflector/MPShield/ConvertHeal
Ported ShieldEffect base (AttackShieldObserver wiring, getType→GetType_() to avoid Object.GetType collision; base Value/Hittype/HitTypeProb/CalculateBaseValue) plus 4 subclasses: ProtectEffect (Summon→nested SummonReleaseObserver else DeathObserver), ReflectorEffect (reflectType→SKILL_REFLECTOR/REFLECTOR), MPShieldEffect (mp_value 8-arg ctor), ConvertHealEffect (HealType, 12-arg ctor). ProvokerEffect (93L) deferred to next tick. Guardrail green (363/5). skillengine.effect: ~75 ported, ~76 remain (Provoker + proc/abnormal-set families). Big services pending (Siege/Broker/Quest/Legion).

## 2026-06-10 — ProvokerEffect (completes shield family)
Ported ProvokerEffect (provoke-on-attack/attacked): anonymous ActionObserver with attack/attacked + private tryApplyEffect→nested ProvokeObserver capturing outer+effector; shouldApply switch-expr (PHHIT/MAHIT/BACKATK), Rnd.Chance>=HitTypeProb; getProvokeTarget switch-expr (ME/OPPONENT, +_=>null for C# completeness); SM_SYSTEM_MESSAGE.STR_SKILL_PROC_EFFECT_OCCURRED. **Shield effect family COMPLETE (6 files).** Guardrail green (363/5). skillengine.effect: ~76 ported, ~75 remain (proc/abnormal-set/spellbook families). Big services pending (Siege/Broker/Quest/Legion).

## 2026-06-10 — BufEffect mastery/boost subfamily (6 files)
Ported BufEffect subclasses: ArmorMasteryEffect (StatArmorMasteryFunction w/ equipped items), ShieldMasteryEffect (StatShieldMasteryFunction), WeaponMasteryEffect (TWO_HAND vs MAIN/OFF_HAND_POWER split), WeaponDualEffect (skillEfficiency/maxDamage/minDamageRatio + static hasDualWieldEffect), BoostSkillCastingTimeEffect (SLOW_RESISTANCE on negative change), BoostSkillCostEffect (anonymous BOOSTSKILLCOST observer→nested). Inherited Value/Delta/SkillEfficiency/MaxDamageChance/MaxDamageDelta from EffectTemplate. Guardrail green (363/5). skillengine.effect: ~82 ported, ~69 remain (more BufEffect: Hide/Slow/Snare/Curse/OneTimeBoost; Always* family; proc/instant families). Big services pending (Siege/Broker/Quest/Legion).

## 2026-06-10 — BufEffect debuff/boost: Slow/Snare/Curse/OneTimeBoostSkillAttack
Ported BufEffect subclasses: SlowEffect/SnareEffect/CurseEffect (calculate→X_RESISTANCE, set/unset AbnormalState), OneTimeBoostSkillAttackEffect (switch PHYSICAL/MAGICAL/ALL, stateful anonymous AttackCalcObserver(boostCount)→nested BoostObserver capturing outer+effect+percent, getBase{Physical,Magical}DamageMultiplier overrides, schedule(100ms) removeEffect). Guardrail green (363/5). skillengine.effect: ~86 ported, ~65 remain (HideEffect + Always* family + proc/instant/abnormal-set). Big services pending (Siege/Broker/Quest/Legion).

## 2026-06-10 — Always* family complete (Block/Dodge/Parry/Resist)
Ported AlwaysBlock/Dodge/Parry/ResistEffect : EffectTemplate. Each: applyEffect→addToEffectedController; anonymous AttackStatusObserver(value, STATUS) overriding checkStatus→nested {Status}Observer capturing effect, --Value<=0→endEffect, matching-status returns true. **Always* family COMPLETE (4 files).** Guardrail green (363/5). skillengine.effect: ~90 ported, ~61 remain (HideEffect, proc/instant DamageEffect family, abnormal-set, Aura/Confuse/Fear/Blind). Big services pending (Siege/Broker/Quest/Legion).

## 2026-06-10 — DamageEffect instant subfamily (5 files) + EffectTemplate virtual converge
Ported DamageEffect subclasses: SkillAttackInstantEffect (rnddmg/cannotmiss, canDodgeOrResist override), ProcAtkInstantEffect (onAttack PROCATKINSTANT, calculateBaseValue provoked override), SkillAtkDrainInstantEffect (1s schedule HP=ABSORBED_HP/MP), SpellAtkDrainInstantEffect (1s schedule HP/MP=ABSORBED_MP), NoReduceSpellATKInstantEffect (percent maxHp + max_damage cap, calculateDamage). Convergence: made EffectTemplate.CalculateBaseValue/CanDodgeOrResist/CalculateDamage virtual (subclasses + existing DamageEffect override require it). using static SM_ATTACK_STATUS for TYPE/LOG. Guardrail green (363/5). skillengine.effect: ~95 ported, ~56 remain (HideEffect, dash/signet/move DamageEffect, abnormal-set, Aura/Confuse/Fear/Blind). Big services pending (Siege/Broker/Quest/Legion).

## 2026-06-10 — DamageEffect movement subfamily (Dash/BackDash/MoveBehind)
Ported DamageEffect calculate-override movers: DashEffect (firstTarget guard, DASH, cos/sin(PI+radian) toward, getClosestCollision), BackDashEffect (BACKDASH, inverseAngle+180, findMovementCollision), MoveBehindEffect (MOVEBEHIND, effected heading, getClosestCollision). All: Math.toRadians→*PI/180, World.updatePosition + skill.setTargetPosition, super.calculate. Guardrail green (363/5). skillengine.effect: ~98 ported, ~53 remain (HideEffect, Signet/CarveSignet/DispelBuffCounterAtk DamageEffect, abnormal-set, Aura/Confuse/Fear/Blind, instant FpAttack/DelayedSpell). Big services pending (Siege/Broker/Quest/Legion).

## 2026-06-10 — Signet DamageEffect subfamily (CarveSignet/SignetBurst)
Ported CarveSignetEffect (Rnd.Chance>=prob gate, getAbnormalEffect/min-max signet level, applyEffect→setCarvedSignet; local Effect `signet`→`signetEffect` to dodge C# field-shadow rule) and SignetBurstEffect (calculateDamage: element!=NONE knowledge mult, SIGNET_DATA_TEMPLATES via Enum.Parse<SignetEnum>, damageMultiplier, setLaunchSubEffect by effectProb; calculate: base 3-arg false→endEffect). int*=float lossy preserved via explicit cast. Guardrail green (363/5). skillengine.effect: ~100 ported, ~51 remain (HideEffect, DispelBuffCounterAtk, DelayedSpellAttackInstant, abnormal-set, Aura/Confuse/Fear/Blind, FpAttack family). Big services pending (Siege/Broker/Quest/Legion).

## 2026-06-10 — DamageEffect leaves: DispelBuffCounterAtk + DelayedSpellAttackInstant
Ported DispelBuffCounterAtkEffect (getCritProbMod2→0 cannot-crit [base GetCritProbMod2 made virtual via converge], dispelBuffCounterAtkEffect, calculateBuffsOrEffectorDebuffsToRemove + valueWithDelta formula, resetDesignatedDispelEffect on end) and DelayedSpellAttackInstantEffect (element knowledge mult int-div, calculateSkillResult ignoreShields=true, delayed onAttack DELAYDAMAGE + notifyAttackObservers via anonymous Runnable→async delegate, empty calculateDamage). Guardrail green (363/5). skillengine.effect: ~102 ported, ~49 remain (HideEffect, abnormal-set family, Aura/Confuse/Fear/Blind, FpAttack/DelayedFpAtk, EffectTemplate leaves). Big services pending (Siege/Broker/Quest/Legion).

## 2026-06-10 — FP attack instant family (FpAttackInstant/DelayedFpAtkInstant)
Ported FpAttackInstantEffect (Player-only, percent→maxFP*Value/100, setReserveds EffectReserved(FP), reduceFp FP_DAMAGE/FPATTACK) and DelayedFpAtkInstantEffect (Player-only calculate, anonymous Runnable→async delegate at delay ms→calculateAndApplyDamage with isEnemy guard + percent maxFP). Both : EffectTemplate directly (PascalCase Value/Position). Guardrail green (363/5). skillengine.effect: ~104 ported, ~47 remain (HideEffect, abnormal-set family, Aura/Confuse/Fear/Blind, Disease/DPTransfer/SwitchHostile, heal-on-attacked/dead). Big services pending (Siege/Broker/Quest/Legion).

## 2026-06-10 — EffectTemplate leaves: Disease/DPTransfer/SwitchHostile
Ported DiseaseEffect (DISEASE_RESISTANCE calculate, set/isAbnormalSet-guarded-unset AbnormalState.DISEASE), DPTransferEffect (reserveds DP transfer effected+/effector-, EffectReserved(DP) on calculate, getCurrentStatValue=effector DP), SwitchHostileEffect (is Player player→summon, swap hate via AggroList stopHating/addHate cross). Guardrail green (363/5). skillengine.effect: ~107 ported, ~44 remain (HideEffect, abnormal-set family, Aura/Confuse/Fear/Blind, HealCastorOnAttacked/OnTargetDead, ChangeHateOnAttacked, resurrect family). Big services pending (Siege/Broker/Quest/Legion).

## 2026-06-10 — On-attacked EffectTemplate family: HealCastorOnAttacked/OnTargetDead/ChangeHateOnAttacked
Ported HealCastorOnAttackedEffect (anonymous ATTACKED observer→nested HealObserver: group-aware heal castor/party in range), HealCastorOnTargetDeadEffect (endEffect: effected dead→heal effector, healparty group-aware), ChangeHateOnAttackedEffect (anonymous ATTACKED→nested HateObserver: Npc attacker→addHate finalValue=value1+value2). var group + is Player p pattern. Guardrail green (363/5). skillengine.effect: ~110 ported, ~41 remain (HideEffect, abnormal-set family, Aura/Confuse/Fear/Blind, resurrect family, pet/dispel-buff leaves). Big services pending (Siege/Broker/Quest/Legion).

## 2026-06-10 — Resurrect effect family (Resurrect base + Base + Positional)
Ported ResurrectEffect base (skill_id, setPlayerResActivate/setResurrectionSkill + SM_RESURRECT, Player&&dead calculate), ResurrectBaseEffect (3-arg calculate, endEffect→scheduleReviveAtBase 2500ms when dead+!dueling), ResurrectPositionalEffect (effector/effected Player casts, setResPosState+setResPos{X,Y,Z} from effector pos). **Resurrect family COMPLETE (3 files).** Guardrail green (363/5). skillengine.effect: ~113 ported, ~38 remain (HideEffect, abnormal-set family, Aura/Confuse/Fear/Blind, pet/dispel-buff/teleport leaves). Big services pending (Siege/Broker/Quest/Legion).

## 2026-06-10 — Target EffectTemplate leaves: TargetTeleport/TargetChange
Ported TargetTeleportEffect (loc==null→teleport in front of effector via getClosestCollision, reflected→originalEffected; else→SKILL_ALIAS_LOCATION_DATA + Rnd.Get random position; TeleportService.teleportTo) and TargetChangeEffect (switch(Delta) case 1→target=effector, setTarget). Math.toRadians→*PI/180. Guardrail green (363/5). skillengine.effect: ~115 ported, ~36 remain (HideEffect, abnormal-set family, Aura/Confuse/Fear/Blind, pet/dispel-buff leaves, signet-related). Big services pending (Siege/Broker/Quest/Legion).

## 2026-06-10 — EffectTemplate leaves batch: PetOrderUnSummon/InvulnerableWing/Rebirth/MpAttackInstant/BuffSleep
Ported PetOrderUnSummonEffect (SummonsService.doMode RELEASE), InvulnerableWingEffect (INVULNERABLE_WING abnormal set/unset), RebirthEffect (resurrect_percent/skill_id getters), MpAttackInstantEffect (percent maxMP, EffectReserved(MP), 4-arg Calculate w/ Element, reduceMp DAMAGE_MP/MPATTACK), BuffSleepEffect (:SleepEffect, cancelCurrentSkill + SLEEP abnormal). Guardrail green (363/5). skillengine.effect: ~120 ported, ~31 remain (HideEffect, Aura/Confuse/Fear/Blind, Paralyze/Petrification/Root family, abnormal-set, recall/search/spin leaves). Big services pending (Siege/Broker/Quest/Legion).

## 2026-06-10 — Abnormal-state EffectTemplate family: Paralyze/Petrification/NoFly
Ported ParalyzeEffect (PARALYZE_RESISTANCE, cancelCurrentSkill+Player glide/move abort, PARALYZE), PetrificationEffect (PERIFICATION_RESISTANCE [Java typo preserved], abortMove+cancelSkill+conditional glide, PETRIFICATION), NoFlyEffect (Player-only NOFLY_RESISTANCE, endFly(true), NOFLY). Standard set/unset abnormal pattern. Guardrail green (363/5). skillengine.effect: ~123 ported, ~28 remain (HideEffect, Aura/Confuse/Fear/Blind, Root/SimpleRoot/Spin, abnormal-set, recall/search/proc-vp-heal leaves). Big services pending (Siege/Broker/Quest/Legion).

## 2026-06-10 — Root/movement-control family: Root/SimpleRoot/Spin
Ported RootEffect (ROOT_RESISTANCE, anonymous ATTACKED observer→nested RootResistObserver: Rnd.Chance>=resistchance→removeEffect), SimpleRootEffect (CANT_MOVE_STATE guard, STAGGER_RESISTANCE+isSubEffect move-back via getClosestCollision/setTargetLoc, World.updatePosition + SM_POSITION for non-Player, SIMPLE_MOVE_BACK), SpinEffect (5-state guard, SPIN_RESISTANCE+SpellStatus.SPIN, removeParalyzeEffects, SPIN). Math.toRadians→*PI/180. Guardrail green (363/5). skillengine.effect: ~126 ported, ~25 remain (HideEffect, Aura/Confuse/Fear/Blind, abnormal-set, recall/search/proc-vp-heal, signet/totem leaves). Big services pending (Siege/Broker/Quest/Legion).

## 2026-06-10 — Control-effect family: Blind/Confuse/Fear
Ported BlindEffect (visualState BLINKING/HIDE10 removeHide, BLIND_RESISTANCE, anonymous AttackStatusObserver(DODGE).checkAttackerStatus→nested BlindObserver Rnd.Chance<Value), ConfuseEffect (CONFUSE_RESISTANCE, Npc emote+AIState.CONFUSE / Player WALK_MODE→RUN, FEAR_ENABLE scheduleAtFixedRate ConfuseTask→nested, random-angle wander, endEffect SM_POSITION+IDLE), FearEffect (FEAR_RESISTANCE, AIState.FEAR, FearTask→nested flee via calculateAngleFrom/findMovementCollision isInRange 40, resistchance<100→nested FearResistObserver). inner Runnable→nested + ScheduledFuture→ScheduledTask + setPeriodicTask(Position). Guardrail green (363/5). skillengine.effect: ~129 ported, ~22 remain (HideEffect, abnormal-set, Aura, recall/search/proc-vp-heal/signet-totem leaves). Big services pending (Siege/Broker/Quest/Legion).

## 2026-06-10 — HideEffect (145L, BufEffect)
Ported HideEffect: set HIDE abnormal + visualState, AttackUtil.cancelCastOn, SM_PLAYER_STATE broadcast, schedule(500ms) removeTargetFrom, onHide/onHideEnd; 4 anonymous observers→nested (HideSkillCastObserver stateful buffNumber w/ ITEM/SHAPECHANGE/selfBuff/buffCount logic, HideAttackObserver, HideItemUseObserver buff-item logic, HideNpcSkillCastObserver); type==0→setCancelOnDmg(true) both Player/Npc branches. Guardrail green (363/5). skillengine.effect: ~130 ported, ~21 remain (abnormal-set, Aura, recall/search/proc-vp-heal/signet-totem, rideRobot/spin-related leaves). Big services pending (Siege/Broker/Quest/Legion).

## 2026-06-10 — Instant/proc EffectTemplate leaves: ProcVPHeal/MagicCounterAtk/SkillCooltimeReset
Ported ProcVPHealInstantEffect (repose energy cap, percent maxReposeEnergy*0.001, addReposeEnergy + SM_STATUPDATE_EXP), MagicCounterAtkEffect (anonymous ENDSKILLCAST→nested CounterObserver: non-ITEM MAGICAL→min(maxdmg, maxHp.base/100*Value) onAttack MAGICCOUNTERATK), SkillCooltimeResetEffect (HashMap→Dictionary, currentTimeMillis→UtcNow.ToUnixTimeMilliseconds, Delta
## 2026-06-10 — Instant/proc EffectTemplate leaves: ProcVPHeal/MagicCounterAtk/SkillCooltimeReset
Ported ProcVPHealInstantEffect (repose energy cap, percent maxReposeEnergy*0.001, addReposeEnergy + SM_STATUPDATE_EXP), MagicCounterAtkEffect (anonymous ENDSKILLCAST -> nested CounterObserver: non-ITEM MAGICAL -> min(maxdmg, maxHp.base over 100 times Value) onAttack MAGICCOUNTERATK), SkillCooltimeResetEffect (HashMap -> Dictionary, currentTimeMillis -> UtcNow.ToUnixTimeMilliseconds, Delta-percent/Value reduction, SM_SKILL_COOLDOWN). Guardrail green (363/5). skillengine.effect: ~133 ported, ~18 remain (abnormal-set, Aura, recall/search/rideRobot/spin-totem/condSkillLauncher leaves). Big services pending (Siege/Broker/Quest/Legion).

## 2026-06-10 — Movement/utility EffectTemplate leaves: Search/ReturnPoint/RideRobot/RandomMoveLoc
Ported SearchEffect (CreatureSeeState set/unset + updateKnownlist + SM_PLAYER_STATE), ReturnPointEffect (RESTING->stand emote, useTeleportScroll via itemTemplate return alias/world, getTargetObjectId helper), RideRobotEffect (setRobotId from mainHand skin, anonymous UNEQUIP->nested UnequipObserver WEAPON->endEffect, endEffect clears robot + rideRobotCondition effects), RandomMoveLocEffect (World.updatePosition, PlayerMoveController flag, DashStatus RANDOMMOVELOC, findMovementCollision direction+180). Guardrail green (363/5). skillengine.effect: ~137 ported, ~14 remain (abnormal-set, Aura, condSkillLauncher, recall-instant, hostileUp, summon-totem, pet-ultraskill, dispelDebuffMental, spin-related). Big services pending (Siege/Broker/Quest/Legion).

## 2026-06-10 — Complex EffectTemplate leaves: HostileUp/CondSkillLauncher/RecallInstant
Ported HostileUpEffect (Npc taunt+temp hate, AtomicReference<DeathObserver> forward-ref -> plain C# locals captured by ref, ScheduledTask temp-hate removal + DeathObserver cancel, StatFunctions.calculateHate), CondSkillLauncherEffect (anonymous HP_CHANGED w/ conditionalEffect+synchronized+onRemoved -> nested CondObserver, lock(this), int? duration PASSIVE 0/null, applyEffectDirectly), RecallInstantEffect (anonymous RequestResponseHandler<Creature> -> nested RecallRequestHandler capturing effect loc, SM_QUESTION_WINDOW summon prompt, deny/accept teleport). Guardrail green (363/5). skillengine.effect: ~140 ported, ~11 remain (abnormal-set family, Aura, summon-totem, pet-ultraskill, dispelDebuffMental, OneTimeBoostCritical, condSkillLauncher-related). Big services pending (Siege/Broker/Quest/Legion).

## 2026-06-10 — Effect leaves: OneTimeBoostSkillCritical/SummonTotem/PetOrderUseUltraSkill
Ported OneTimeBoostSkillCriticalEffect (anonymous AttackerCriticalStatusObserver(CRITICAL) -> nested CritObserver: skill-crit match decrements count or endEffect, AcStatus.SetResult), SummonTotemEffect (:SummonServantEffect, firstTargetSelf collision offset / pointSkill fallback, PR_PROVOKESERVENT 20s / FI_WARFLAG 15s, spawnServant TOTEM), PetOrderUseUltraSkillEffect (PET_SKILL_DATA order skill lookup, inline NullLoggerFactory warn, addSkillOrder + SM_SUMMON_USESKILL). Guardrail green (363/5). skillengine.effect: ~143 ported, ~8 remain (Aura + Stagger/Stumble/Pulled/OpenAerial control family + Effects container [not EffectTemplate]). Big services pending (Siege/Broker/Quest/Legion).

## 2026-06-10 — Forced-move control family: Stagger/Stumble/OpenAerial
Ported StaggerEffect (STAGGER_RESISTANCE+SpellStatus, 4-state guard, 2m backward getClosestCollision, World.updatePosition + Player SM_FORCED_MOVE), StumbleEffect (STUMBLE, also removeStunEffects), OpenAerialEffect (OPENAERIAL, 5-state guard incl SPIN, geoZ via GeoService.getZ + float.IsNaN guard). All: cancelCurrentSkill/removeParalyzeEffects, Player glide+move stop, set/unset abnormal, Math.toRadians->*PI/180. Guardrail green (363/5). skillengine.effect: ~146 ported, ~5 remain (PulledEffect, AuraEffect, abnormal-set base+subs, Effects container [non-EffectTemplate]). Big services pending (Siege/Broker/Quest/Legion).

## 2026-06-10 — PulledEffect + AuraEffect
Ported PulledEffect (PULLED_RESISTANCE, canSee guard, 3-state guard, reflected->originalEffected, pull to 1.5m via getClosestCollision, World.updatePosition + Player SM_FORCED_MOVE) and AuraEffect (mantra: applyEffect double-cast AuditLogger guard, onPeriodicAction Npc-self / Player-team BOOST_MANTRA_RANGE-scaled group range + rangeZ, SM_MANTRA_EFFECT, applyAuraTo->SkillEngine.applyEffect; inner Runnable AuraTask->nested w/ Thread.Yield, scheduleAtFixedRate 0/6500ms setPeriodicTask). Guardrail green (363/5). skillengine.effect: ~148 ported, ~3 remain (abnormal-set base+subclasses, Effects container [non-EffectTemplate]). Big services pending (Siege/Broker/Quest/Legion).

## 2026-06-10 — Effects container (JAXB polymorphic list) — skillengine.effect PILLAR COMPLETE
Ported Effects: ~160 @XmlElements polymorphic entries -> repeated [XmlElement(name, typeof(T))] on  list; @XmlTransient effectTypes -> [XmlIgnore] HashSet<EffectType>; afterUnmarshal -> AfterUnmarshal(object) building set from class simple name (Replace Effect, ToUpper) via Enum.Parse, IllegalArgumentException -> ArgumentException; getEffects; hasAnyEffectType(params). **skillengine.effect pillar 100% file-ported (~149 effect classes + container).** Remaining red references are to not-yet-ported sibling effect subtypes already covered and dataholders; convergence pass will resolve. Guardrail green (363/5). NEXT VEINS: big services (Siege 716L/Broker 733L/Quest 947L/Legion 1164L), dao pillar (57 JDBC), network packet pillar (~516), geoEngine Matrix4f (1829L), rest of skillengine (model/condition/action/properties).

## 2026-06-10 — skillengine.action family COMPLETE (Dp/Hp/Mp/ItemUseAction + Actions container)
Ported the 4 skill-cost use-actions (DpUseAction value check + setDp; HpUseAction value+delta*lvl, ratio maxHp, reduceHp USED_HP; MpUseAction ratio maxMp + boostSkillCost negative-pct reduction, reduceMp USED_MP; ItemUseAction ITEM_DATA + inventory.decreaseByItemId STR_SKILL_NOT_ENOUGH_ITEM) on already-ported Action base, plus Actions JAXB container ([XmlElement] polymorphic list, lazy getActions). **skillengine.action 100% file-ported.** Guardrail green (363/5). Next veins: skillengine condition/properties families, big services (Siege/Broker/Quest/Legion), dao/network pillars.

## 2026-06-10 — skillengine.condition family COMPLETE (Hp/Mp/Form/LeftHand/Target)
Ported the 5 remaining conditions on already-ported Condition base + Conditions container: HpCondition (value+delta*lvl, ratio maxHp, reduceHp USED_HP, getHpValue), MpCondition (ratio maxMp + boostSkillCost reduction, reduceMp), FormCondition (transformModel active+type match else STR_SKILL_CAN_NOT_CAST_IN_THIS_FORM), LeftHandCondition (switch DUAL dual/two-hand assignment-in-condition / SHIELD isShieldEquipped), TargetCondition (NONE/ALL/AREA/firstTarget short-circuits, switch NPC/PC, STR_SKILL_TARGET_IS_NOT_VALID). **skillengine.condition 100% file-ported.** Guardrail green (363/5). Next: skillengine.properties family (FirstTarget/MaxCount/TargetRange/TargetRelation/TargetStatus + Properties), then big services.

## 2026-06-10 — skillengine.properties static helpers: FirstTarget/MaxCount/TargetRelation/TargetStatus
Ported 4 static property classes: FirstTargetProperty (full ME/TARGETORME/TARGET/MYPET/MYMASTER/PASSIVE/TARGET_MYPARTY_NONVISIBLE/POINT switch, auto-retarget + relation gating, isTargetTeamMember w/ TemporaryPlayerTeam<TeamMember<Player>>, Npc heading), MaxCountProperty (nearest-by-distance OrderBy/Take/ToHashSet, PARTY_WITHPET summon re-add, retainAll->RemoveAll), TargetRelationProperty (ENEMY/FRIEND/MYPARTY removeIf->RemoveAll + iterator-keep->RemoveAll predicate, isBuffAllowed SiegeNpc abyss-type deny, isSameAreaType), TargetStatusProperty (RI_PROTECTIONCURTAIN bypass, required abnormal-state filter). instanceof x->is x, streams->LINQ. Properties container + FirstTargetRange/TargetRange helpers pending next tick. Guardrail green (363/5).

## 2026-06-10 — skillengine.properties range helpers: FirstTargetRange/TargetRange
Ported FirstTargetRangeProperty (POINT canSee/obstacle, NPC mid-cast skip, CAST_END pvp revisionDistance, addWeaponRange, in-move covered-distance, CANT_MOVE_STATE + attack-range + geo checks) and TargetRangeProperty (ONLYONE/AREA knownList stream->LINQ Where chain w/ altitude/flight/trap-self/range/geo filters, PARTY+PARTY_WITHPET team rebuild w/ TemporaryPlayerTeam<TeamMember<Player>> + tryAddSummon, POINT knownList; helpers checkCommonRequirements/isInsideDisablePvpZone/checkRange angle-BACK-front-cylinder/checkGeo groundSkill NaN). streams->LINQ, Float.isNaN->float.IsNaN. Only the Properties container (216L, nested CastState+ValidationResult+validate orchestration) remains for skillengine.properties. Guardrail green (363/5).

## 2026-06-10 — Properties container — skillengine.properties SUBTREE COMPLETE
Ported Properties (216L): 17 @XmlAttribute fields (first_target/range/awr/target_relation/type/distance/maxcount/status/revision/effective_*/direction/species/ineffective); validate (FirstTarget+FirstTargetRange+validateEffectedList), endCastValidate (clear+CAST_END), validateEffectedList orchestration chaining TargetRange/TargetRelation/TargetStatus/TargetSpecies/MaxCount; 16 getters; nested CastState enum + ValidationResult (outer sets internal ). **skillengine.properties 100% file-ported** (helpers + range helpers + container). NOTE: optional enum != null guards are red-frontier (C# value-type enums) — convergence will nullable-ify; TargetSpeciesProperty still pending. Guardrail green (363/5). Next: TargetSpeciesProperty, then big services (Siege/Broker/Quest/Legion) or skillengine.model.

## 2026-06-10 — skillengine.model leaves: PenaltySkill/ChargeSkill/MotionTime
Ported PenaltySkill (:Skill, useWithoutPropSkill, skillMethod=PENALTY), ChargeSkill (:Skill, charge ctor carrying hitTime/castStart/castSpeed, useSkill canUseSkill(CAST_END) gate + observer notifies + hitTimeBoost carry currentTimeMillis to UtcNow.ToUnixTimeMilliseconds, updateHitTime/endCast), MotionTime (JAXB motion_time: race/gender Times maps, getTimesFor id..1 walk, afterUnmarshal to AfterUnmarshal building WeaponTypeWrapper maps, parseTimesFrom weapon-string switch incl 2weapon SWORD/MACE dual, computeIfAbsent to local PutInto TryGetValue, IllegalArgument to ArgumentException). skillengine.model now fully file-ported. Guardrail green (363/5). Next: big services (Siege/Broker/Quest/Legion) or SkillEngine/SkillTemplate cores.

## 2026-06-10 — SiegeService (716L) — first big-service god-class ported
Ported services/SiegeService (SoulKeeper/Source/Neon/Estrayl), 3.0 siege orchestration: singleton, SIEGE_LOG logger, activeSieges dict, Map<Integer,Siege<? extends SiegeLocation>>->Siege<SiegeLocation> (wildcard erasure), AtomicBoolean isInitialized.CompareAndSet, Collections.emptyMap->new Dictionary, Date->DateTimeOffset + currentTimeMillis->UtcNow.ToUnixTimeMilliseconds, synchronized startSiege/stopSiege/captureSiege/resetSiegeLocation/updateOutpostSiegeState->lock(this), forEachPlayer lambdas, streams->LINQ, map.compute->TryGetValue, anonymous Consumer->lambda, full getSiegeIdByLocId switch, getPreparationCronString with SIGNED-byte fix (Java byte->C# sbyte for minutes<0 logic), NumberFormat/UnsupportedOperation->Format/NotSupported. CronExpression/Quartz/Siege subclasses/SiegeDAO/locations red-tolerated. Guardrail green (363/5, no new god-class). Remaining big services: Broker 733L, Quest 947L, Legion 1164L.

## 2026-06-10 — BrokerService (733L) — second big-service god-class ported
Ported services/BrokerService (kosyachok, ATracer, Sykra): singleton via nested SingletonHolder, EXCHANGE_LOG logger, 4 race item ConcurrentDictionaries, scheduleAtFixedRate checkExpiredItems; buy/register/cancel/settle/showRequested/showSell/showSettled flows. Idioms: ConcurrentHashMap put/get/remove/values().removeIf -> indexer/GetValueOrDefault/TryRemove/snapshot-TryRemove; synchronized(this)/(map) -> lock; LongSummaryStatistics -> LINQ Min/Max with empty=0; ArrayUtils.removeElement -> List.Remove (first occurrence); subList -> GetRange; Objects::nonNull -> x!=null; mapToLong.sum -> LINQ Sum; currentTimeMillis -> UtcNow.ToUnixTimeMilliseconds; nested BrokerPeriodicTaskManager (AbstractFIFOPeriodicTaskManager override CallTask/GetCalledMethodName), BrokerOpSaveTask (Runnable->Run, DAO stores), SingletonHolder. DAO/BrokerItem/SplitList/packets red-tolerated. Guardrail green (363/5, no new god-class). Remaining big services: Quest 947L, Legion 1164L.

## 2026-06-10 — QuestService (947L) — third big-service god-class ported
Ported services/QuestService (Mr. Poke, vlog, bobobear, xTz, Rolandas): all-static quest lifecycle (finish/start/abandon/event), reward resolution (groups, class-selectable, extended, bonus), start-condition checks, collect/inventory item checks, quest drops (group/alliance/each-member), timers, repeat-date calc. Idioms: using static DialogAction int-consts; aliases ActionType/Status/ItemUpdateType for nested enums; HashMap questDrop->Dictionary getOrDefault/computeIfAbsent->TryGetValue; ZonedDateTime/Timestamp/LocalTime/DayOfWeek->DateTimeOffset + ISO-day helper (Sunday=7) for DayOfWeek.getValue trap; now.with(09:00)->explicit; toEpochSecond*1000->FromUnixTimeMilliseconds; Comparator.comparingInt/streams->LINQ OrderBy/Any/noneMatch->!Any; anonymous Runnable timers->async delegate; Future->ScheduledTask; Rnd.chance->Rnd.Chance; instanceof x->is x; getType->GetType_; log.error(msg,ex)->LogError(ex,msg). QuestTemplate/QuestEngine/QuestState/DAO/enums red-tolerated. Guardrail green (363/5, no new god-class). Remaining big service: LegionService 1164L.

## 2026-06-10 — LegionService (1164L) — FINAL big-service god-class; ALL FOUR big services done
Ported services/LegionService (Simple, cura, Source, Neon): full legion lifecycle (create/disband/recreate/invite/kick/leave), rank/permission/nickname/self-intro/level/emblem/announcement/warehouse/history/rename/dominion. Idioms: singleton SingletonHolder; legionsById/legionMemberById ConcurrentDictionary; 5 anonymous RequestResponseHandler subclasses->nested classes (Disband/Invite/AppointGeneralStart/AppointGeneralConfirm/Recreate) accessing outer privates; inner LegionRestrictions->nested class; computeIfAbsent NO-null-store semantics->TryGetValue+GetOrAdd guard; ByteBuffer emblem chunking->manual byte[] index; LegionRank.values()[id]->Enum.GetValues cast; switch-expr msgId; currentTimeMillis->UtcNow.ToUnixTimeMilliseconds; new Timestamp->FromUnixTimeMilliseconds; removeIf->RemoveAll; equalsIgnoreCase->OrdinalIgnoreCase; Pattern.matcher().matches()->IsMatch; log.error(msg,ex)->LogError(ex,msg). DAO/Legion/packets/RequestResponseHandler red-tolerated. Guardrail green (363/5, no new god-class). **ALL FOUR big services (Siege/Broker/Quest/Legion) now file-ported; top-level services pillar complete.**

## 2026-06-10 — services/reward: BonusService + StarterKitService
Ported BonusService (static quest-bonus reward selection via Chance.selectElement of random matching BonusItemGroup; switch BonusType->ITEM_GROUPS_DATA accessors with commented GATHER/BOSS/ENCHANT preserved; List<? extends ItemRaceEntry>->List<ItemRaceEntry> wildcard erase + Cast; streams->LINQ; getType->GetType_) and StarterKitService (singleton level->RewardItem map, LinkedHashMap->Dictionary [order-independent iteration], onLevelUp sends SystemMailService.sendMail LetterType.EXPRESS per reward; full reward table preserved). Guardrail green (363/5). reward subdir: 2/5 ported; remaining Advent 150L / WebReward 220L / VeteranReward 498L.

## 2026-06-10 — services/reward/AdventService (150L)
Ported AdventService (Solorius advent calendar): singleton, 24-day reward table, onLogin notice / redeemReward (free-slot LINQ count, opposite-race skip, AdventDAO gate) / showTodaysReward (StringBuilder w/ trailing-comma). Idioms: computeIfAbsent->TryGetValue; LocalDate->DateOnly (ServerTime.now().toLocalDate->DateOnly.FromDateTime); Month.DECEMBER->12; java.awt.Color.PINK->System.Drawing.Color.Pink; streams map/filter/count->LINQ Select/Where/Count; iterator hasNext trailing-comma->index test. AdventDAO/ChatProcessor/ChatUtil red-tolerated. Guardrail green (363/5). reward subdir: 3/5; remaining WebReward 220L / VeteranReward 498L.

## 2026-06-10 — services/reward/WebRewardService (220L)
Ported WebRewardService (KID, Neon): WEB_REWARDS_LOG logger, singleton, sendAvailableRewards (unreceived loop, mail-or-action, storeReceived), sendRewardItem (CASH_ITEM_MAIL), executeRewardAction switch; nested MaxLevelReward (CopyOnWriteArraySet->ConcurrentDictionary-as-set pendingAscension: add->TryAdd/remove->TryRemove/contains->ContainsKey; ascension-quest restart flow, findObject is-Npc lambda, daeva max-level gear via two PlayerClass switch blocks weapons+armor). currentTimeMillis->UtcNow.ToUnixTimeMilliseconds; ChatUtil.l10n->L10n; ActionType alias. RewardServiceDAO/QuestState/Teleport red-tolerated. Guardrail green (363/5). reward subdir: 4/5; remaining VeteranReward 498L.

## 2026-06-10 — services/reward/VeteranRewardService (498L) — reward subdir COMPLETE
Ported VeteranRewardService (Neon): monthly veteran rewards (full 60-month table + 30-item random pool for 61+). Singleton (SingletonHolder); Java static{} init->static ctor; tryReward (level-65 gate, char/account age via ChronoUnit.MONTHS.between->MonthsBetween complete-months helper, VeteranRewardDAO received-months tracking, random pick via Rnd.NextInt+RemoveAt down to 4 items, mailbox-overflow guard saving correct month, SystemMailService per item). ServerTime.now/atDate/ofEpochMilli->DateTimeOffset; list.remove(idx)->RemoveAt. VeteranRewardDAO/RewardItem red-tolerated. Guardrail green (363/5). **services/reward subdir 5/5 COMPLETE** (Bonus/StarterKit/Advent/WebReward/Veteran).

## 2026-06-10 — services/teleport/BindPointTeleportService (148L)
Ported BindPointTeleportService (ViAl): hotspot/bind-point teleport with 10-min cooldown. onLogin cooldown notice, teleport (price calc, requirement checks, 10s cast delegate -> decreaseKinah + cooldown + 1s teleport delegate), cancelTeleport, nested Cooldown class. Idioms: HashMap->Dictionary (get->GetValueOrDefault/put->indexer); anonymous Runnables->async delegates (nested schedule); currentTimeMillis->UtcNow.ToUnixTimeMilliseconds; inline LoggerFactory.warn->inline NullLoggerFactory; Math.max/abs->Math.Max/Abs. HotspotTemplate/SM_BIND_POINT_TELEPORT/TeleportService red-tolerated. Guardrail green (363/5). teleport subdir: 1/3; remaining Portal 368L / TeleportService 536L (high-value dependency).

## 2026-06-10 — services/teleport/TeleportService (536L) — high-value dependency
Ported TeleportService (xTz, Neon): all-static teleport hub — teleport (flight vs sendLoc), ~11 teleportTo overloads, dead/prison/npc/bind/instance-exit/event/scroll/channel teleports, obelisk/kisk bind packets, validateTeleporter, HiPass-effect price. Idioms: double[] eventPos statics; FutureTask<Void>(spawnTask,null)->FutureTask<object> (concurrency shim red-tolerated); anonymous RequestResponseHandler->nested TeleportRequestHandler; SpawnTask nested (accesses outer private statics AbortPlayerActions/SpawnOnSameMap); Math.toRadians->*PI/180; Float.isNaN->float.IsNaN; byte-heading bit math preserved; getType->GetType_; ItemUpdateType alias. World/Instance/Conqueror/DAO/packets red-tolerated. Guardrail green (363/5). **teleport subdir 2/3** (BindPoint+Teleport done; Portal 368L remains). This converges TeleportService.* reds across QuestService/WebReward/RecallInstant/TargetTeleport/ReturnPoint/BindPoint.

## 2026-06-10 — services/teleport/PortalService (368L) — teleport subdir COMPLETE
Ported PortalService (ATracer, xTz): instance portal entry — port (maxPlayers 0/1/3/6/alliance/league registration switch via InstanceService, transfer w/ entrance cooltime), requirement checks (mentor/race+siege/rank/title/quests/playerSize/enterLevel/items). GeneralTeam<?,?>->GeneralTeam (wildcard erase to base); DialogPage.X.id()->Id(); getPlayersInside().size()->Count; quest-step var check. InstanceService/WorldMapInstance/PortalPath/DAO red-tolerated. Guardrail green (363/5). **services/teleport subdir 3/3 COMPLETE** (BindPoint/Teleport/Portal).

## 2026-06-10 — services/drop/DropDistributionService (149L)
Ported DropDistributionService (xTz, Sykra): group-loot roll/bid distribution. Singleton; handleRollOrBid (mode 2 roll/3 bid dispatch), handleRoll (Rnd.get max-roll, SM_GROUP_LOOT broadcast + dice msgs), handleBid (kinah/999999999 cap, pay msgs), distributeLoot (winner highest-value tracking, free-for-all fallback when no winner, DropService.requestDropItem/canDistribute, lgr item-distribution chaining). map.get->GetValueOrDefault; synchronized(dropItems)->lock; 0xFFFFFFFF (Java int -1)->unchecked((int)0xFFFFFFFF); getFirst->[0]; isEmpty->Count==0. DropNpc/DropService/SM_GROUP_LOOT/LootGroupRules red-tolerated. Guardrail green (363/5). drop subdir: 1/3; remaining DropRegistration 481L / DropService 534L.

## 2026-06-10 — services/drop/DropRegistrationService (481L) — QuestService dependency
Ported DropRegistrationService (xTz, Aioncool, Bobobear, Neon): singleton; currentDropMap/dropRegistrationMap ConcurrentDictionary; registerDrop (custom+quest+global+event drops, loot-team ROUNDROBIN/FFA/LEADER init, FFA schedule), createDropModifiers (chest detect, boost/reduction rates), 10 global-rule restriction checks, addGlobalDrops/addDropItems (member-limit shuffle distribution), collectDrops (Chance.selectElement cap), rank/rating switch-expr modifiers (+_ default for C# completeness). Collections.shuffle->Fisher-Yates via Rnd; Rnd.chance/get->Rnd.Chance/Get; enum.name().toLowerCase()->ToString().ToLower(); long*=double lossy->explicit cast; Status alias; getMaxDropsPerGroup int? .Value. DAO/GlobalRule/DropNpc/DropService red-tolerated. Guardrail green (363/5). drop subdir: 2/3; remaining DropService 534L. This converges DropRegistrationService.* reds in QuestService/DropDistributionService.

## 2026-06-10 — services/drop/DropService (534L) — drop subdir COMPLETE
Ported DropService (ATracer, xTz): loot lifecycle — scheduleFreeForAll (240s async), unregisterDrop, requestDropList/closeDropList (decay task handoff, FFA looter widening), canDistribute/canAutoLoot (quality rules, roll/bid setup), requestDropItem (kinah equal-split via team.filterMembers, team distribution roll/bid/misc winner selection, winning actions, junk auto-sell via pet), distributeEqually, resend/announce. Idioms: schedule(lambda)->async delegate; map.get/remove->GetValueOrDefault/TryRemove; synchronized(dropItems)->lock; instanceof Npc->is; streams->LINQ Where/Any/ToList; broadcastPacket predicate lambdas + method-group dropNpc.IsAllowedToLoot; nested TempTradeDropPredicate:ItemUpdatePredicate; ScheduledFuture/TimeUnit + getDelay red-tolerated; currentTimeMillis->UtcNow.ToUnixTimeMilliseconds; switch-arrow dist 2/3. DropNpc/LootGroupRules/RespawnService/DAO red-tolerated. Guardrail green (363/5). **services/drop subdir 3/3 COMPLETE** (DropDistribution/DropRegistration/DropService).

## 2026-06-10 — services/instance/InstanceService (240L) — cross-referenced dependency
Ported InstanceService (ATracer): instance lifecycle — getNextAvailableInstance overloads (Function<WorldMapInstance,InstanceHandler>->Func, method-group supplier InstanceEngine.GetInstance().GetNewInstanceHandler, event spawn streams->LINQ Select/Where/ForEach), destroyInstance (temp-spawn unregister, player exit, walker cleanup), get/register/personal/house instance, login/logout/enter/leave instance+zone hooks, instance rate/destroy-delay. scheduleAtFixedRate EmptyInstanceCheckerTask->nested+async delegate; GeneralTeam<?,?>->GeneralTeam; currentTimeMillis->UtcNow.ToUnixTimeMilliseconds; UnsupportedOperation->NotSupported; NullPointer->NullReference; instanceof Player->is; isEmpty->Count==0. World/WorldMapInstance/InstanceHandler/DAO red-tolerated. Guardrail green (363/5). instance subdir: 1/3; remaining PvPArena 83L / PeriodicInstanceManager 140L. Converges InstanceService.* reds in Teleport/Portal/QuestService.

## 2026-06-10 — services/instance: PvPArenaService + PeriodicInstanceManager — instance subdir COMPLETE
Ported PvPArenaService (xTz): arena availability time-windows + entry-item gates; DayOfWeek.getValue ISO trap->IsoDayValue helper (Sunday->7); ServerTime.now->DateTimeOffset getHour->Hour. PeriodicInstanceManager (ViAl, Sykra, Estrayl): singleton scheduling dredgion/kamar/ophidan/iron-wall/idgel registrations via CronService; openRegistration/closeRegistration synchronized->lock; HashSet/HashMap->HashSet/Dictionary; Future->ScheduledTask (isDone/cancel); schedule(lambda,cronExpr)/(lambda,period*60000)->async delegate; forEachPlayer level-range broadcast; using static AutoGroupConfig. CronExpression/AutoGroupType/SM_AUTO_GROUP red-tolerated. Guardrail green (363/5). **services/instance subdir 3/3 COMPLETE** (InstanceService/PvPArena/PeriodicInstanceManager).

## 2026-06-10 — services/craft/RelinquishCraftStatus (120L)
Ported RelinquishCraftStatus (synchro2): static expert/master craft-status relinquish (skill-level range + kinah gate, downgrade, remove recipes above level, delete craft-status quests), removeExcessCraftStatus recursion (master then expert). Private relinquishCraftStatus->RelinquishCraftStatusInternal (C# forbids member same name as enclosing type). PricesService/MasterQuestsList/ExpertQuestsList/RecipeTemplate/CraftSkillUpdateService red-tolerated. Guardrail green (363/5). craft subdir: 1/3; remaining CraftSkillUpdate 181L / CraftService 260L.

## 2026-06-10 — services/craft/CraftSkillUpdateService (181L)
Ported CraftSkillUpdateService (MrPoke, sphinx, Imaginary, Pad): singleton; professionByNpc map (npcId->Profession, both factions); learnSkill (level-10/profession/upgrade-cost gating w/ expert-399/master-499 quest-grant messages, anonymous RequestResponseHandler->nested LearnSkillResponseHandler capturing price/skillId/skillLevel, addSkill on accept), expert/master craft-skill counters (Profession.values()->Values()), can-learn-more expert/master checks. map.put->indexer/get->GetValueOrDefault; Integer price->int? .Value; ItemUpdateType alias. Profession/PlayerSkillList/RequestResponseHandler red-tolerated. Guardrail green (363/5). craft subdir: 2/3; remaining CraftService 260L.

## 2026-06-10 — services/craft/CraftService (260L) — craft subdir COMPLETE
Ported CraftService (MrPoke, sphinx, synchro2, Evil_dnk): CRAFT_LOG logger; finishCrafting (xp formula + crafting-stat boost lossy int*=float, crit combo product, anonymous ItemUpdatePredicate->nested CraftedItemPredicate setting weapon/armor creator, craft cooldown), startCrafting (quality interval cap UNIQUE/EPIC/MYTHIC, CraftingTask interval by skill-diff), checkCraft (target/range/DP/ride-hide/inventory/recipe/cooldown/skill-level/component gates + bonus-item consume), sendCancelCraft, getBonusReqItem skill switch. Map<Integer,Long>->Dictionary; int? nullables .Value; currentTimeMillis->UtcNow.ToUnixTimeMilliseconds; ItemAddType/ItemUpdateType/ItemUpdatePredicate aliases. CraftingTask/RecipeTemplate/DAO red-tolerated. Guardrail green (363/5). **services/craft subdir 3/3 COMPLETE** (Relinquish/CraftSkillUpdate/CraftService).

## 2026-06-10 — services/event/EventService (238L) — cross-referenced dependency
Ported EventService (Rolandas, Neon): singleton; volatile active-events/drop-rules/quests/force-types/theme; start/stop (CronService 5-min check schedule), checkActiveEvents (old-vs-new diff via SetEquals, start/stop transitions, theme broadcast), event hooks (login/team/map/pve-kill/pvp-kill), collectActiveEvents/QuestIds/DropRules, getActiveEventConfigProperties (nullsFirst-by-startDate OrderBy). Quartz JobDetail/Properties red-tolerated; Collections.empty*->new; TemporaryPlayerTeam<? extends TeamMember<Player>>-><TeamMember<Player>>; streams->LINQ Where/Select/SelectMany/Any/UnionWith; Set.equals->SetEquals; ServerTime.now->DateTimeOffset/.DateTime; ForceType alias. EventTemplate/Event/EventDAO red-tolerated. Guardrail green (363/5). event subdir: 1/3; remaining Event 289L / EventBuffHandler 291L. Converges EventService.* reds in Instance/DropRegistration.

## 2026-06-10 — services/event/Event (289L)
Ported Event (Neon): single active-event instance — start (custom-spawn despawn + event spawns, inventory-drop scheduleAtFixedRate task, surveys activate, EventBuffHandler, simulate-login broadcast), stop (event-spawn cleanup w/ count, onEventEndTasks, survey deactivate, buff stop), quest start/maintain (previous-event reset by startDate), hooks delegating to buff handler, despawnNonEventSpawns. AtomicBoolean started; Future->ScheduledTask; List<Runnable> onEventEndTasks; Java int[] count capture-hack->C# mutable captured local; synchronized(this)->lock; ServerTime.atDate().toLocalDateTime->.DateTime; isAfter->>; aliases ActionType/ItemAddType/ItemUpdateType/ItemUpdatePredicate/ForceType. EventTemplate/EventBuffHandler/Spawn/RespawnService/DAO red-tolerated. Guardrail green (363/5). event subdir: 2/3; remaining EventBuffHandler 291L.

## 2026-06-10 — services/event/EventBuffHandler (291L) — event subdir COMPLETE
Ported EventBuffHandler (Neon): per-event buff pools + day-restrictions w/ DB persistence; trigger-driven buff apply (enter-map/team, pve/pvp-kill), restriction checks (allowed-day/map/team-size-percent/instance-level). ConcurrentHashMap->ConcurrentDictionary; Collections.shuffle->generic Fisher-Yates Shuffle<T>; IntStream.rangeClosed.boxed->Enumerable.Range; getOrDefault; containsAll->IsSupersetOf; Consumer<Player>->Action<Player>; TemporaryPlayerTeam wildcards-><TeamMember<Player>>; team.forEach method-group; toLocalDate().lengthOfMonth->DateTime.DaysInMonth; Rnd.chance->Rnd.Chance; buff.isPermanent?0:null->int?; endRestrictedEventBuffs lambda-return-keep->stillValid flag + ToArray snapshot; Buff.TriggerCondition/Trigger/BuffMapType nested; StoredBuffData/ForceType aliases. Buff/EventDAO/SkillEngine red-tolerated. Guardrail green (363/5). **services/event subdir 3/3 COMPLETE** (EventService/Event/EventBuffHandler).

## 2026-06-10
- Ported services/vortex (2/2, completes subdir): DimensionalVortex (abstract base, generic where VL:VortexLocation; AtomicBoolean finished; synchronized start->lock double-start guard; CompareAndSet stop; initRiftGenerator DeathObserver->stopInvasion; NullPointer->NullReference) + Invasion (:DimensionalVortex<VortexLocation>; ConcurrentDictionary invaders/defenders; inv/defAlliance; nested DefenderResponseHandler:RequestResponseHandler<Player>; Arrays.asList->array). Guardrail green (363 slop, 5 god). Commit f145a8c15 as rrfarmer.

## 2026-06-10
- Ported services/findgroup/FindGroupService (cura, MrPoke; 1/2, Hooks file is golden-capture instrumentation, skipped): singleton; ConcurrentDictionary recruitments/applications/instanceGroups; show/add/update/remove tabs, server-wide instance application flow (invite to group/alliance), onJoinedTeam/onLogout cleanup. No-op FindGroupMutationPostTraceCaptureHooks calls omitted (not Aion gameplay). Guardrail green.

## 2026-06-10
- Ported services/rift (RiftEnum + RiftManager + RiftInformer; RiftOpenRunnable already present from auto-commit loop). RiftEnum: first value-carrying Java enum port -> sealed class, static readonly SCREAMING_SNAKE instances + Values(), constructor chaining, Name() via reflection static ctor, getRift/getVortex throw ArgumentException. RiftManager: ConcurrentDictionary riftsPerWorld/riftGroups, CopyOnWriteArrayList->List, computeIfAbsent->GetOrAdd, spawnRift/spawnVortex/spawnInstance, rift.name()->Name(). RiftInformer: SM_RIFT_ANNOUNCE packet building, Java switch on RiftEnum constants -> reference-equality HashSet groups (Vortex/Normal/Volatile), TreeMap->SortedDictionary 12-slot announce data, twin-map ids, anonymous Consumer->lambda. Guardrail green.

## 2026-06-10
- Ported services/transfers PlayerTransfer (xTz; ByteBuffer LITTLE_ENDIAN whole-array puts -> byte[] concatenation via Array.Copy) + PlayerTransferService (KID; cross-server char transfer: startTransfer validations + SM_PTRANSFER_CONTROL info packets, cloneCharacter name-dedupe/slot-check/readInfo/name-change-ticket, onOk/onError, put/getTransfer; LinkedHashMap/HashMap->Dictionary, Remove(out), currentTimeMillis->UtcNow.ToUnixTimeMilliseconds, REUSE_HOURS*3600000 int-overflow parity kept). TransferablePlayer already present (auto-commit loop). CMT_CHARACTER_INFORMATION (399L) remains for next tick. Guardrail green.

## 2026-06-10
- Ported services/transfers/CMT_CHARACTER_INFORMATION (KID; completes transfers subdir, 4/4). BaseClientPacket<AionConnection> deserializer for transferred char blob: common/appearance/position, items (manastones/fusions/godstone, storage+stigma allow-flags), emotes/motions/macros/npc-factions/pets/titles, settings/abyss, skills (restricted-skill filter), recipes, quests. read*/BaseClientPacket red-tolerated; currentTimeMillis->UtcNow.ToUnixTimeMilliseconds; Integer color==-1->int? null; new Timestamp(ms)->DateTimeOffset.FromUnixTimeMilliseconds; enum.valueOf->Enum.Parse; byte->sbyte; protected ctor->public. Guardrail green. transfers subdir DONE.

## 2026-06-10
- Ported services/mail SystemMailService (xTz; sysmail send + recipient mailbox update online/offline), MailService (kosyachok; player-to-player mail: commission/quality math, attached-item disposition unpack + count split, kinah debit, read/getAttachments/delete/login/sendMailList with LINQ sort + SplitList paging), MailFormatter (Rolandas; blackcloud/house-maintenance/house-auction/abyss-reward/guild-dominion/custom-defeat system mails, anonymous MailPart -> 5 nested sealed capturing classes). Completes mail subdir (6/6; AbyssSiegeLevel/AuctionResult/SiegeResult already present). Duration.toDays->ms/86400000; Timestamp->DateTimeOffset; Comparator.reversed->OrderByDescending. Guardrail green.

## 2026-06-10
- Ported services/cron (completes subdir, 6/6; CronServiceException already present): RunnableRunner (abstract Quartz Job, dispatch by long-running flag), CurrentThreadRunnableRunner, CronExpressions (interning cache, computeIfAbsent->GetOrAdd, ParseException->FormatException), CronExpressionTransformer (Neon; PropertyTransformer<CronExpression>), CronService (SoulKeeper/Neon; Quartz scheduler singleton: initSingleton static-synchronized->lock, schedule overloads w/ JobDataMap, cancel by JobDetail/Runnable, findJobDetails/findJobs/getJobTriggers/findNextFireTimes streams->LINQ + Map.compute->TryGetValue/indexer, nested CronScheduleBuilder + MemoryEfficientCronTrigger). Whole Quartz pillar + java.util Properties/TimeZone/Date + Runnable red-tolerated (red-by-design). Guardrail green.

## 2026-06-10
- Ported services/abyss (completes subdir, 6/6; AbyssService already present): AbyssPointsService (ATracer; addAp + onRankChanged), GloryPointsService (ViAl/Sykra; addGp online/offline), AbyssSkillService (ATracer; updateSkills + value-enum AbyssSkills->sealed class w/ params int[] skills + Values/GetSkills), AbyssRankUpdateService (ATracer/Neon; cron scheduleUpdate, performUpdate w/ quota-rank assignment iterator.remove->RemoveAt, daily GP loss; streams min/max->LINQ OrderBy/DefaultIfEmpty), AbyssRankingCache (VladimirZ/Neon; per-race ranking packet cache, toMap->ToDictionary, subList->GetRange paging). Uses now-ported CronService. AbyssRankEnum/AbyssRankDAO records red-tolerated. Guardrail green.

## 2026-06-10
- Ported services/antihack/AntiHackService (Source; canMove abnormal/speed/teleport hack detection + counters, punish move-back/disconnect per SecurityConfig.PUNISH, checkAionBin; Math.rint->Math.Round round-half-to-even, currentTimeMillis->UtcNow, public hack-counter fields keep Java names) and services/autogroup/AutoGroupUtility (Estrayl; auto-group registration gating, checkGroupRequirements member caps + item/cooldown/level/searching, sendSuccessfulRegistration; TemporaryPlayerTeam<?> erase, keySet->Keys). Both single-file subdirs DONE. Guardrail green.

## 2026-06-10
- Ported services/panesterra/PanesterraService (Estrayl, 349L): Panesterra fortress siege + Ahserion raid orchestration. prepare/start/stopFortressSiege (PANESTERRA/ARTIFACT/FACTION_CAMP base transitions, advance corridors w/ rank static ids), createTeams/removeTeams per faction siegeId, start/stopAhserionRaid, onEnterPanesterra (faction assignment/teleport), getSiegeId (WorldMapType switch), teleportToStart/EventLocation, revive. ConcurrentHashMap->ConcurrentDictionary; static-import enum->using static PanesterraFaction; switch-arrow->switch; switch-expr null-case->default; Stream.of/anyMatch/map/findFirst.orElse->LINQ DefaultIfEmpty.First; getType()->GetType_(); getByFortressId->PanesterraFactionExtensions; Rnd.nextBoolean->NextBoolean. Ahserion subdir (PanesterraFaction/Team/AhserionRaid) already present. Guardrail green.

## 2026-06-10
- Ported services/player (3 of remaining): PlayerLimitService (Source/Neon; per-account daily NPC sell limit, cron clear, uses now-ported CronService), PlayerChatService (Source/Neon; flood detection + CHAT_LOG/ADMINAUDIT_LOG routing w/ per-ChatType formatting, String.format->string.Format), MultiClientingService (FULL/SAME_FACTION restriction modes, nested AccountSession synchronized->lock + Identifiers record, values().removeIf->ToArray()+TryRemove, computeIfAbsent->GetOrAdd, LinkedList, Duration.ofMinutes.toMillis->*60000). PlayerMailboxState/SecurityTokenService already present. Remaining player: PlayerService(359), PlayerEnterWorld(543), PlayerLeaveWorld(153), PlayerReviveService(261). Guardrail green.

## 2026-06-10
- Ported services/player PlayerLeaveWorldService (ATracer/Neon; leaveWorldDelayed DESPAWN task + full leaveWorld logout sequence: safe-position fallback, all service onLogout hooks, dead->revive, persist effects/cooldowns/lifestats/commondata, release summon/pet/postman, quest onLogout, chatserver logout) and PlayerReviveService (Jego/xTz; duel/skill/rebirth/bind/kisk/instance/itemSelf revives + shared revive() w/ no-resurrect-penalty handling + scheduleReviveAtBase). Future->ScheduledTask, schedule(Runnable,ms)->Schedule(ct-lambda), DimensionalVortex<?>-><VortexLocation>, method-ref->lambda, new Timestamp(currentTimeMillis)->DateTimeOffset.FromUnixTimeMilliseconds. Remaining player: PlayerService(359), PlayerEnterWorldService(543). Guardrail green.

## 2026-06-10
- Ported services/player/PlayerService (SoulKeeper/Saelya/Cura, 359L): isNameUsedOrReserved, storeNewPlayer, storePlayer (full DAO persistence), getPlayer (complete load pipeline: legion/macros/skills/friend/block/title/settings/abyss/factions/motions/effects/cooldowns/inventory/warehouse/petbags/cabinets/equipment stats/punishments/lifestats/emotions), newPlayer (spawn loc, starting skills + auto-equip creation items, mailbox), getOrLoadPlayerCommonData, cancel/deletePlayer (deletion timer), deletePlayerFromDB, storeCreationTime, add/removeMacro, getPlayerName. Timestamp->DateTimeOffset (getTime->ToUnixTimeMilliseconds, new Timestamp(ms)->FromUnixTimeMilliseconds); currentTimeMillis->UtcNow; nested-type aliases (LocationData/PlayerCreationData/ItemType/PunishmentType/PersistentState). Only PlayerEnterWorldService(543) remains in player. Guardrail green.

## 2026-06-10
- Ported services/player/PlayerEnterWorldService (ATracer/Neon, 543L; completes player subdir 9/9): full enter-world handshake (validation: account/pcd/online/reentry/ban/passkey/dupe, multi-client gate) + retail login packet sequence (skills/quests/titles/UI/inventory/warehouse/abyss/legion/group/mail/housing/etc.), energy-of-repose calc, passive skill activation, fortress/vortex zone relocation, expirable registration, periodic GeneralUpdateTask/ItemUpdateTask (Runnable classes w/ Run()). ConcurrentLinkedQueue dedupe->ConcurrentDictionary set; stream findAny.map.orElse->LINQ; Timestamp->DateTimeOffset; Throwable->Exception; IllegalState->InvalidOperation; Math.round->(long)Floor(x+0.5); lossy long*=float->cast; static-import enum->using static; nested aliases (Msg/Status/ConnectType/PunishmentType/PersistentState). player subdir DONE. Guardrail green.

## 2026-06-10
- Ported services/summons/TrapService (Sykra; per-owner trap limit, register/unregister; Queue->ConcurrentQueue, removeIf->drain+re-enqueue, values().removeIf(isEmpty)->key iterate+TryRemove) and services/worldraid/WorldRaid (Whoop/Sykra; timed prep sequence flag/vortex/markers/boss via fixed-rate stateful Runnable->nested PreparationRunnable capturing outer, boss DeathObserver->stop, 1h despawn timer; AtomicBoolean gates, Future->ScheduledTask, schedule(...,1,HOURS)->TimeSpan.FromHours(1)). summons (2/2) + worldraid (2/2; WorldRaidRunnable already present) subdirs DONE. Guardrail green.

## 2026-06-10
- Ported services/conquerorAndProtectorSystem CPInfo (Source; per-player CP state, getType->GetType_) + ConquerorAndProtectorService (Source/Dtem/ginho/Yeats/Neon; 4.8 conqueror/protector: init handled-worlds + kill-decay fixed-rate task, per-map CPInfo create/lookup, enter/leave map+zone+legion hooks, onKill victim accrual + rank transitions + buff, intruder scan w/ 180s cooldown, canSee rank vis). ConcurrentHashMap->ConcurrentDictionary, HashMap->Dictionary; computeIfAbsent->GetOrAdd; values().removeIf->ToArray+TryRemove; Stream.concat->Concat; scheduleAtFixedRate(lambda)->ScheduleAtFixedRateTask(ct-lambda); handledWorlds Race lookup->TryGetValue; CPType? nullable; enum.name->ToString; equalsIgnoreCase->OrdinalIgnoreCase. Completes subdir (3/3; CPBuff already present). Guardrail green.

## 2026-06-10
- Ported services/item (3 of remaining): HouseObjectFactory (Rolandas; instantiate HouseObject subclass from placeable template + inventory->registry transfer w/ expiry; instanceof->is, requireNonNull->null-check+NullReference, HouseObject<?>-><PlaceableHouseObject> invariance bound), ItemActionService (Estrayl; identifyItem 5s cast w/ abort observer->nested IdentifyItemObserver + roll sockets/bonus/enchant/tune, applyTuneResult; anonymous Runnable->Schedule ct-lambda closure), ItemRestrictionService (ATracer; from/to storage move restrictions, canRemoveItem; switch-on-enum). Remaining item: ItemChargeService(182), ItemMoveService(127), ItemPurificationService(138), ItemRemodelService(124), ItemSocketService(209), ItemSplitService(147). Guardrail green.

## 2026-06-10
- Ported services/item ItemSplitService (ATracer; split stack to slot/storage incl kinah cube<->acc-wh checksum move, mergeStacks) + ItemMoveService (ATracer; moveItem same-storage reslot/cross-storage move w/ restriction+trade+shutdown checks + stackable merge + WH history, switchItemsInStorages swap). static-import ItemPacketService.*->using static; nested ItemUpdateType/ItemDeleteType aliases; String.format->string.Format; switch-on-StorageType blocks; GameServer.isShuttingDownSoon red-tolerated. Remaining item: ItemChargeService(182), ItemPurificationService(138), ItemRemodelService(124), ItemSocketService(209). Guardrail green.

## 2026-06-10
- Ported services/item/ItemSocketService (ATracer/Sykra, 209L): addManaStone (next/specific slot w/ special+normal slot accounting), copyFusionStones, removeManastone (kinah cost + DAO store), removeAllManastone, socketGodstone (move-cancel listener + 2s cast). Set->ISet/HashSet; stream.filter.findFirst.orElse->LINQ FirstOrDefault; Collections.singleton->new HashSet; anonymous StartMovingListener->nested GodstoneMoveListener (super.moved->base.Moved); anonymous Runnable->Schedule ct-lambda. Remaining item: ItemChargeService(182), ItemPurificationService(138), ItemRemodelService(124). Guardrail green.

## 2026-06-10
- Ported services/item ItemRemodelService (Sarynth/Wakizashi; skin remodel w/ level/gender/kinah/type checks, Pattern Reshaper revert, dye transfer), ItemPurificationService (Ranastic/Estrayl; isPurificationAllowed validations, decreaseMaterials, upgradeItem carrying over sockets/fusion/stones/godstone/bonus/color), ItemChargeService (ATracer; equipment charging chargeWay 1=kinah/2=AP, confirm window anonymous RequestResponseHandler->nested ChargeRequestHandler, pay-amount ratio math, switch-expr payment). Math.round->Floor(x+0.5), Math.ceil->Ceiling, switch-expr, stream->LINQ. **item subdir DONE (12/12).** Guardrail green.

## 2026-06-10
- Ported services/siege base classes: Siege<SL extends SiegeLocation> (SoulKeeper/Source; abstract base - AtomicBoolean finished, synchronized start->lock double-start guard, CompareAndSet stop + balaur auto-assault, siege counter/boss, initSiegeBoss, spawn/despawn/broadcast helpers, sendRewardsToParticipants GP/kinah/item mail) and Assault<SiegeType extends Siege<?>> (Luzien/Estrayl; AtomicBoolean isStarted, startAssault schedule, finishAssault cancel tasks, spawnAssaulter trig placement). Siege<?>->Siege<SiegeLocation> invariance bound; currentTimeMillis->UtcNow; log.error(msg,ex)->LogError(ex,msg); Math.round(float)->(long)Floor(x+0.5f); Math.toRadians->x*PI/180; getClass().getSimpleName()->GetType().Name; Future->ScheduledTask. Remaining siege: ArtifactSiege/OutpostSiege/AgentSiege/FortressSiege, ArtifactAssault/FortressAssault, MercenaryLocation, BalaurAssaultService. Guardrail green.

## 2026-06-10
- Ported services/siege ArtifactSiege (SoulKeeper; endless artifact siege - boss init, capture winner race/legion + system messages, restart) + OutpostSiege (SoulKeeper/Estrayl; field-abyss agent boss - vulnerable/spawn, agent-defeated top-damager announce + winner buff, reward distribution, AP accrual). getWinnerLegionId Integer->int?(??0); keySet().iterator().next()->Keys.First(); forEachPlayer lambda; Map->Dictionary. Remaining siege: AgentSiege(154), FortressSiege(414), ArtifactAssault(55), FortressAssault(178), MercenaryLocation(138), BalaurAssaultService(146). Guardrail green.

## 2026-06-10
- Ported services/siege ArtifactAssault (Luzien/Whoop; extends Assault<ArtifactSiege>, level-based assaulter spawn near boss) + AgentSiege (Estrayl/Sykra; Levinshor Mastarius/Veille - recursive 60s delayStart ramp to spawn, zone-gated quest distribution, BALAUR/SIEGE boss init, capture base 6113 + rewards on finish). schedule(Runnable,ms)->Schedule ct-lambda (return->return CompletedTask); SiegeRace winner->SiegeRace? nullable; switch-on-SiegeRace; Math.toRadians->x*PI/180; ZoneName.get->Get. Remaining siege: FortressSiege(414), FortressAssault(178), MercenaryLocation(138), BalaurAssaultService(146). Guardrail green.

## 2026-06-10
- Ported services/siege MercenaryLocation (Whoop; purchasable merc spawn zone - spawn/despawn, cooldown+50%-alive request validity, zone resolution by siege/race) + BalaurAssaultService (synchro2/Luzien/Estrayl; schedules fortress 1-15min/artifact 3-48h Balaur assaults, calculateFortressAssault influence chance + per-map limits, spawnDredgion carrier broadcast). ConcurrentHashMap->ConcurrentDictionary; map.remove(k).call->TryRemove(out)+call; Siege<?>-><SiegeLocation>; instanceof X x->is X x; Race.getRaceByString(name)->ToString. Remaining siege: FortressSiege(414), FortressAssault(178). Guardrail green.

## 2026-06-10
- Ported services/siege FortressAssault (Luzien/Estrayl; Balaur fortress assault - wave scheduling, budgeted assaulter spawn by type stake/cost, commander chance, difficulty from faction balance+influence, dredgion commander kill drain) + FortressSiege (SoulKeeper, 414L; full fortress siege - start vulnerable/spawn/boss/merc-zones/balance-buffs/faction-troop+balaur assault, finish capture/defend, player+legion reward distribution, world buffs, faction-balance adjust, quest onKill). EnumMap->Dictionary; AssaulterType.values()->Values(); schedule(Runnable,..)->ct-lambda; Integer legionId->int?(??0); Math.round->(int/long)Floor(x+0.5f); retainAll->IntersectWith; switch-on-locationId/race. **siege subdir DONE (14/14).** Guardrail green.

## 2026-06-10
- Ported services/trade/PricesService (Sarynth/wakizashi; influence-scaled global prices/taxes, vendor buy/sell modifiers, ordered round-down multiply chains for getPriceForService/getBuyPrice, getSellReward). Math.round(float)->(int)Floor(x+0.5f); switch-on-Race + IllegalArgument->Argument; /100D round-down casts preserved. Converges PricesService.getPriceForService used by many ported services (mail/item/siege). **trade subdir DONE (1/1).** Guardrail green.

## 2026-06-10
- Ported services/panesterra/ahserion PanesterraTeam (Yeats/Estrayl; faction origin/start positions, member roster, teleports; List<Integer>.remove(int) index-pitfall preserved as RemoveAt) + AhserionRaid (Yeats/Neon/Estrayl, 308L; raid orchestration - 30s fixed-rate staged progress via nested ProgressRunnable, spawnRaid/spawnStage, corridor-shield team elimination + consolation reward, boss-killed winner handling). Converges PanesterraService deps (PanesterraTeam/AhserionRaid). AtomicBoolean; Future->ScheduledTask; values()->Enum.GetValues; ordinal()->(int); instanceof X x->is X x; method-ref->ct-lambda. ahserion subdir DONE (3/3). Guardrail green.
